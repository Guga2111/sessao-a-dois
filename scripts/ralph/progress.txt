# Ralph Progress Log - Descobrir: Busca Avancada, Filtros e Ordenacao

## Codebase Patterns
- `com.app.media`: novo record publico `MediaPage(results, page, totalResults, totalPages)` e o contrato de paginacao padrao; reutilize-o em qualquer endpoint paginado (trending, discover, search) em vez de criar DTOs de pagina paralelos.
- TMDB `/trending/all/week` devolve itens no mesmo formato de `/search/multi` (mesmos campos `media_type`, `title`/`name`, `poster_path`, etc.) — reaproveite `TmdbMultiSearchItem` para o mapeamento por item; so foi necessario um record novo (`TmdbTrendingResponse`) para os campos extras `page`/`total_pages`/`total_results`.
- Sandbox deste ambiente Ralph NAO tem JDK/Maven nem acesso a rede (Maven Central bloqueado), entao `./mvnw test` nao pode ser executado aqui — build so pode ser verificado onde JDK 21 + Maven Central estao disponiveis (ver nota em api/CLAUDE.md). Revisar o codigo manualmente com cuidado antes de commitar.

- `MediaPage`'s compact constructor clamps `totalPages`/`totalResults` to the TMDB cap (500 pages, 10000 results) — applies automatically to every construction site, so new paginated endpoints (search, discover) don't need to reimplement the clamp.
- TMDB `/discover/movie` and `/discover/tv` item shapes differ from `/search/multi`/`/trending/all/week` (no `media_type`, but include `popularity`) — use the dedicated `TmdbDiscoverItem`/`TmdbDiscoverResponse` records (not `TmdbMultiSearchItem`) for anything backed by `/discover/*`, and pass the known `MediaType` explicitly (it's not on the item) when mapping to `MediaSearchResult`.
- `npx shadcn add <component>` does not work in this sandbox (no network route to ui.shadcn.com). Any missing shadcn primitive (Select, Slider, ToggleGroup, Pagination, ...) must be hand-written as a thin wrapper over the already-installed `@base-ui/react/<primitive>` package, following the existing `client/src/components/ui/*.tsx` files as templates (`data-slot` attributes, `cn(...)` merging, theme tokens as defaults, screens override via `className`). See `client/CLAUDE.md`.
- To test the query params/path a service builds for a TMDB call, capture the `Function<UriBuilder, URI>` passed to `.uri(...)` with an `ArgumentCaptor<Function<UriBuilder, URI>>` (`ArgumentCaptor.forClass(Function.class)` + `@SuppressWarnings("unchecked")`), then apply it to `UriComponentsBuilder.newInstance()` and inspect `UriComponentsBuilder.fromUri(uri).build().getQueryParams()` — this is the pattern for any future "assert the exact TMDB URL built" test.
- `@base-ui/react/toggle` and `@base-ui/react/toggle-group` export plain function components (`Toggle`, `ToggleGroup`, not `.Root`-namespaced like `Select`/`Popover`/`Collapsible`). `ToggleGroup`'s `multiple` prop + `value`/`onValueChange: (string[], eventDetails) => void` is the pattern for multi-select chip groups (US-009's Data/Classificacao/Generos chips); active state via the `data-pressed` attribute.
- A helper function passed as a `useEffect` dependency (e.g. a shared `runFetch`) must itself be wrapped in `useCallback` with a stable (often empty, if it only closes over `setState` setters/refs) dep array — otherwise it's a new reference every render and either re-triggers the effect every render or forces an `eslint-disable` for `react-hooks/exhaustive-deps`. Prefer the `useCallback` route.

---

## 2026-07-29 - US-015
- Refatorados os 5 pontos de SearchTab (MatchScreen.tsx) que populavam `results` (trending no mount, busca debounced, handleSortChange, handleApplyFilters, handlePageChange) para um unico helper `runFetch(url, params, context, isCancelled?)` (`useCallback`, dep array vazio - so fecha sobre setters de estado e refs, ambos estaveis), eliminando a duplicacao de `.then/.catch/.finally` que existia em cada um. `runFetch` agora tambem seta um novo estado `fetchError` (`false` no sucesso, `true` no catch), alem do que ja fazia (`results`/`totalResults`/`totalPages`/`page`/`resultsContext`/`searched`).
- Novo `lastAttemptRef` (grava `{url, params, context}` de TODA tentativa, sucesso ou falha - diferente de `lastFetchRef`, que so grava em sucesso e sem `page`, usado pela paginacao) alimenta o novo `handleRetry`, que rechama exatamente a ultima tentativa. Isso cobre inclusive a falha da carga inicial de trending (que nunca populou `lastFetchRef` antes, entao um retry generico baseado nele nao funcionaria para esse caso).
- Novo card de erro (borda/fundo vermelho translucido, icone `TriangleAlert`, botao "Tentar novamente" com `RefreshCw`, renderizado so quando `searched && !searching && fetchError`) distinto do card de vazio legitimo existente (que ganhou o guard extra `!fetchError`) e do header de contagem (idem, ganhou `!fetchError`). Mensagem varia: "Nao foi possivel carregar os titulos em alta agora." quando a falha e do trending no mount (`resultsContext === "trending" && !hasQuery`), senao "Algo deu errado ao buscar no TMDB." (busca/discover).
- Arquivos alterados: client/src/screens/MatchScreen.tsx, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- Verificacao: `npx tsc --noEmit`, `npx eslint src/screens/MatchScreen.tsx` e `npx vite build` todos passaram sem erros/warnings. Sem browser neste sandbox (ver client/CLAUDE.md) - verificacao visual real (forcar uma falha de rede e ver o card vermelho + botao funcionando) fica pendente.
- **Learnings for future iterations:**
  - Esta era a ultima story do PRD (US-001..US-015) - todas com `passes: true` apos este commit.
  - Padrao `runFetch` + `lastAttemptRef`/`lastFetchRef` documentado no topo deste arquivo (Codebase Patterns) para qualquer refatoracao futura que precise adicionar mais um "modo" de busca ao SearchTab.
  - Sem browser/Chromium neste sandbox (confirmado novamente) - verificacao ficou em tsc/eslint/vite build.
---

## 2026-07-28 - US-014
- Novo client/src/components/ui/pagination.tsx (Pagination/PaginationContent/PaginationItem/PaginationLink/PaginationPrevious/PaginationNext/PaginationEllipsis): botoes simples estilizados com buttonVariants (button.tsx), sem wrapping de @base-ui/react (shadcn's Pagination nao usa nenhum primitivo headless, so nav/ul/li/button) - unico primitivo de UI ate agora que nao precisou do tratamento "hand-roll contra @base-ui" documentado para Select/Slider/ToggleGroup.
- MatchScreen.tsx (SearchTab): novo estado `page`/`totalPages` (lidos de MediaPage.page/totalPages, que ja existiam no tipo desde US-002/US-003 mas nunca eram consumidos) e `lastFetchRef` (ref com `{url, params, context}` do ultimo fetch bem-sucedido que populou `results`, guardando os params SEM `page`). Os 4 pontos que populam `results` (trending no mount, busca debounced, handleSortChange, handleApplyFilters) agora pedem `page: 1` explicitamente (garantindo reinicio de paginacao a cada busca/filtro/ordenacao, conforme a AC) e gravam `lastFetchRef.current` apos sucesso.
- Novo `handlePageChange(nextPage)`: reusa `lastFetchRef` para re-chamar exatamente o mesmo endpoint com os mesmos params, so trocando `page` (clamped entre 1 e `Math.min(totalPages, 500)` - teto do TMDB), sem tocar em nenhum outro estado de busca/filtro. Novo paginador (`Pagination` de shadcn) renderizado abaixo do grid quando `totalPages > 1`: botoes Anterior/Proxima (desabilitados nas pontas) + paginas numericas com janela `current-1..current+1` + primeira/ultima pagina + ellipsis (`paginationRange(current, total)`, nova funcao helper), pagina atual destacada em ambar (`#ffcb2b`). Mensagem "Voces chegaram ao fim dos resultados." quando `page === min(totalPages, 500)`.
- Arquivos alterados: client/src/components/ui/pagination.tsx (novo), client/src/screens/MatchScreen.tsx, client/CLAUDE.md, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- Verificacao: `npx tsc --noEmit`, `npx eslint src/screens/MatchScreen.tsx src/components/ui/pagination.tsx` e `npx vite build` todos passaram sem erros/warnings. Sem browser neste sandbox (ver client/CLAUDE.md) - verificacao visual real (clicar nas paginas, ver a janela de numeros deslizar, ellipsis aparecendo com muitas paginas) fica pendente.
- **Learnings for future iterations:**
  - `lastFetchRef` e o novo padrao central de US-014: qualquer fetch futuro que popule `results` (ex.: se um novo modo de busca for adicionado) deve gravar `{url, params, context}` (sem `page`) nele apos sucesso, senao a paginacao desse modo simplesmente nao vai funcionar (handlePageChange faz early-return quando `lastFetchRef.current` e null).
  - `MediaPage.page`/`totalPages` ja existiam no tipo TS (client/src/types/media.ts) desde US-002/US-003 mas nunca tinham sido lidos ate esta story - vale sempre checar se um campo do DTO ja existe no tipo antes de assumir que precisa adicionar.
  - shadcn Pagination e o unico primitivo de UI hand-rolled ate agora que NAO precisou de `@base-ui/react/*` (e so botoes/nav/lista) - diferente do padrao "sandbox gotcha" documentado para Select/Slider/ToggleGroup/Collapsible/Popover.
  - US-015 (estados vazios/erro) e a ultima story do PRD; deve diferenciar erro real do TMDB (5xx) de resultado vazio legitimo - hoje ambos caem no mesmo bloco (`results.length === 0` generico), sem um campo de erro dedicado no estado (mencionado tambem no learnings de US-012).
  - Sem browser/Chromium neste sandbox (confirmado novamente) - verificacao ficou em tsc/eslint/vite build.
---

## 2026-07-28 - US-013
- MatchScreen.tsx (SearchTab): o grid de resultados ja tinha (de US-008..US-012) o badge de tipo top-left, o botao de curtir com todos os estados (idle/loading/liked/matched/error) reutilizando POST /api/match/like e GET /api/tracking, e a celebracao de match e global (MatchCelebrationModal em App.tsx, ouvindo useMatchStore/WebSocket independente do SearchTab) - nada disso precisou mudar. So faltava o badge de nota TMDB: adicionada uma pill no canto superior direito do poster (mesmo estilo visual do badge de tipo: `bg-[rgba(9,9,10,.6)] backdrop-blur-md`), com um ponto verde `#01b47f` (`size-1.5 rounded-full`) + `result.voteAverage.toFixed(1)`, renderizada condicionalmente quando `voteAverage != null`. Verificado contra docs/design/prints/matchpage-search-with-filters.jpeg (screenshot mostra exatamente as duas pills lado a lado no topo do poster).
- Arquivos alterados: client/src/screens/MatchScreen.tsx, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- Verificacao: `npx tsc --noEmit`, `npx eslint src/screens/MatchScreen.tsx` e `npx vite build` todos passaram sem erros/warnings. Sem browser neste sandbox (ver client/CLAUDE.md) - verificacao visual real fica pendente.
- **Learnings for future iterations:**
  - `MediaSearchResult.voteAverage` (client/src/types/media.ts) ja existia desde antes desta story (espelha o campo do backend) - nao foi necessario mudar nenhum tipo, so consumir o campo que ja chegava em `results`.
  - Boa parte da AC de US-013 (badge de tipo, botao curtir com estados, deteccao de ja-rastreado, celebracao de match) ja tinha sido implementada incidentalmente em stories anteriores (US-008/US-012) enquanto o grid de cards era construido - vale sempre conferir o que ja existe no arquivo antes de reimplementar do zero, a AC pode estar parcialmente satisfeita.
  - Sem browser/Chromium neste sandbox (confirmado novamente) - verificacao ficou em tsc/eslint/vite build; checagem visual real (badge posicionado corretamente sobre o poster, contraste do ponto verde) fica pendente para quando houver ambiente com browser disponivel.
---

## 2026-07-28 - US-001
- Implementado GET /api/media/trending (MediaController) consumindo GET /trending/all/week do TMDB via MediaSearchService.trending(page); filtra itens media_type=person; mapeia para MediaSearchResult reaproveitando TmdbMultiSearchItem.
- Criado com.app.media.MediaPage (record publico results/page/totalResults/totalPages) e com.app.media.TmdbTrendingResponse (record package-private com os campos extras da resposta de trending).
- Testes adicionados: MediaSearchServiceTest (trending_returnsMixedMovieAndTvResultsFilteringOutPeople, trending_returnsEmptyPageWhenTmdbResponseBodyIsNull, trending_throwsTmdbUnavailableWhenTmdbIsDown) e MediaControllerTest (trending_returnsPagedResultsForAuthenticatedUser, trending_passesRequestedPageToService, trending_deniesAccessWithoutAuthentication, trending_returnsBadGatewayWhenTmdbIsUnavailable).
- Arquivos alterados: api/src/main/java/com/app/media/{MediaController.java,MediaSearchService.java,MediaPage.java(novo),TmdbTrendingResponse.java(novo)}, api/src/test/java/com/app/media/{MediaControllerTest.java,MediaSearchServiceTest.java}, api/CLAUDE.md, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- **Learnings for future iterations:**
  - MediaPage ja existe e cobre a AC de US-002 (o record em si); US-002 provavelmente so precisara adicionar testes dedicados de mapeamento e, se necessario, o teto de totalResults do TMDB (~10000, 500 paginas) - ainda nao aplicado em nenhum lugar, so retornamos o total_results cru do TMDB.
  - Nao foi possivel rodar `./mvnw -q -pl api test` neste ambiente (sem JDK, sem Maven, sem acesso a repo.maven.apache.org). Codigo foi revisado manualmente linha a linha seguindo os padroes de MediaSearchServiceTest/MediaControllerTest existentes. Proximas iteracoes devem tentar rodar os testes se o ambiente permitir, e reportar aqui se conseguirem.
---

## 2026-07-28 - US-002
- MediaPage ja existia desde US-001 (record publico results/page/totalResults/totalPages). Adicionado o unico requisito que faltava: um compact constructor que clampa totalPages a 500 e totalResults a 10000 quando o TMDB reporta valores maiores (teto documentado nas Consideracoes Tecnicas do PRD: Discover limita a ~500 paginas).
- Criado MediaPageTest (com.app.media) cobrindo: valores dentro do limite ficam inalterados, totalPages e totalResults sao clampados separadamente quando o TMDB reporta acima do teto.
- Adicionado MediaSearchServiceTest.trending_clampsTotalsToTmdbCapWhenTmdbReportsMore para garantir que o clamp se aplica de ponta a ponta atraves de MediaSearchService.trending(page).
- Arquivos alterados: api/src/main/java/com/app/media/MediaPage.java, api/src/test/java/com/app/media/MediaPageTest.java (novo), api/src/test/java/com/app/media/MediaSearchServiceTest.java, api/CLAUDE.md, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- **Learnings for future iterations:**
  - O clamp fica centralizado no compact constructor do record `MediaPage`, entao qualquer endpoint futuro que monte um MediaPage (US-003 search paginado, US-004 discover) ja herda o comportamento automaticamente, sem precisar chamar nada explicitamente.
  - Mesma limitacao de ambiente do Ralph que em US-001: sem JDK/Maven/rede aqui, entao `./mvnw -q -pl api test` nao pode ser executado neste sandbox. Codigo revisado manualmente com atencao a sintaxe de record compact constructor (reatribuicao dos parametros implicitos e permitida em Java).
---

## 2026-07-28 - US-003
- GET /api/media/search migrado para resposta paginada: MediaController.search agora aceita @RequestParam(defaultValue = "1") int page alem de q (continua obrigatorio/nao vazio via InvalidSearchQueryException); MediaSearchService.search(String query, int page) repassa page ao TMDB (queryParam "page") e retorna MediaPage em vez de List<MediaSearchResult>, reaproveitando o clamp automatico do MediaPage.totalPages/totalResults.
- TmdbMultiSearchResponse ganhou os campos page/total_pages/total_results (antes so tinha results), no mesmo padrao ja usado por TmdbTrendingResponse.
- Testes atualizados: MediaSearchServiceTest (todos os asserts de search agora leem page.results(), + search_clampsTotalsToTmdbCapWhenTmdbReportsMore novo) e MediaControllerTest (search_passesRequestedPageToService novo, demais testes de search adaptados ao contrato MediaPage).
- Arquivos alterados: api/src/main/java/com/app/media/{MediaController.java,MediaSearchService.java,TmdbMultiSearchResponse.java}, api/src/test/java/com/app/media/{MediaControllerTest.java,MediaSearchServiceTest.java}, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- **Learnings for future iterations:**
  - Essas mudancas ja estavam presentes no working tree (nao commitadas) no inicio desta iteracao — provavelmente de uma tentativa anterior que nao chegou a commitar/marcar o PRD. Sempre vale checar `git diff`/`git status` no inicio da iteracao antes de reimplementar algo do zero.
  - US-004 (discover) pode seguir o mesmo padrao: reaproveitar MediaPage e o parsing de items ja existente (toMediaSearchResult), so muda o endpoint/params do TMDB e a logica de merge ao combinar /discover/movie + /discover/tv.
  - Mesma limitacao de ambiente: sem JDK/Maven/rede neste sandbox, `./mvnw -q -pl api test` nao pode ser executado aqui; codigo revisado manualmente linha a linha contra os padroes existentes de MediaSearchServiceTest/MediaControllerTest.
---

## 2026-07-28 - US-004
- Implementado GET /api/media/discover (MediaController) + MediaSearchService.discover(DiscoverFilters). Novos tipos: DiscoverFilters (record com os 9 filtros opcionais + page; mediaType nulo = ambos), DiscoverSortBy (enum popularity/vote_average/release_date x desc/asc, com fromValueOrDefault(String) e tmdbValue(MediaType) que resolve release_date.* para primary_release_date.*/first_air_date.* conforme o tipo), TmdbDiscoverItem/TmdbDiscoverResponse (novos records, pois o shape do /discover difere do /search/multi: sem media_type, com popularity), InvalidSortByException (400, tratado em MediaExceptionHandler). MediaType ganhou fromQueryValueOrNull(String) (blank/ausente -> null == "ambos", reaproveitando fromPathValue para validar).
- Decadas (releaseDecades, CSV de anos-decada) viram um unico intervalo min-max: menor valor informado como .gte (yyyy-01-01) e maior valor + 9 como .lte (yyyy-12-31) - decadas do meio de um intervalo nao contiguo entram no resultado, por decisao do PRD.
- Modo "ambos" (mediaType ausente): chama /discover/movie e /discover/tv na mesma pagina solicitada, combina os itens (com o MediaType de origem, ja que o item do discover nao carrega media_type), ordena pela mesma comparacao usada no sort_by pedido (popularity/vote_average/data, asc ou desc) e corta em 20; totalResults = soma dos dois totalResults do TMDB, totalPages = ceil(totalResults/20).
- certifications e aceito no endpoint/DiscoverFilters (contrato da US-004 no prd.json) mas ainda NAO e mapeado para certification_country/certification no TMDB - isso e explicitamente US-005 (proxima story).
- Arquivos alterados: api/src/main/java/com/app/media/{MediaController.java, MediaSearchService.java, MediaType.java, MediaExceptionHandler.java, DiscoverFilters.java(novo), DiscoverSortBy.java(novo), InvalidSortByException.java(novo), TmdbDiscoverItem.java(novo), TmdbDiscoverResponse.java(novo)}, api/src/test/java/com/app/media/{MediaSearchServiceTest.java, MediaControllerTest.java}, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- **Learnings for future iterations:**
  - Ver padroes consolidados no topo deste arquivo sobre TmdbDiscoverItem/TmdbDiscoverResponse e o padrao de teste de URL via ArgumentCaptor<Function<UriBuilder,URI>>.
  - US-005 (certificacao BR) deve estender `callDiscover`/`DiscoverFilters.certifications()` no MediaSearchService: adicionar certification_country=BR + certification.lte (maior classificacao marcada como teto) apenas quando mediaType inclui MOVIE (TV ignora, TMDB nao suporta certification em /discover/tv) - o parametro certifications ja existe no contrato, so falta a logica de mapeamento e o dicionario de chip->certificacao TMDB (Livre->L, 10->10, 12->12, 14->14, 16->16, 18->18).
  - US-006 (generos) pode reaproveitar TmdbGenre (ja existe, usado em MediaDetails) para os records de resposta de /genre/movie/list e /genre/tv/list.
  - Mesma limitacao de ambiente do Ralph: sem JDK/Maven/rede neste sandbox, `./mvnw -q -pl api test` nao pode ser executado aqui; codigo revisado manualmente linha a linha, com atencao especial a ordem posicional dos campos dos records (DiscoverFilters e TmdbDiscoverItem tem varios campos do mesmo tipo em sequencia, facil trocar a ordem por engano nos testes).
---

## 2026-07-28 - US-005
- Implementado o mapeamento de classificacao indicativa BR em GET /api/media/discover: MediaSearchService.certificationCeilingFor(String certifications) parseia o CSV de chips (Livre->L case-insensitive, demais chips passam como estao), ignora silenciosamente qualquer chip que nao esteja em CERTIFICATION_ORDER = [L,10,12,14,16,18] e retorna a maior classificacao valida marcada (a mais restritiva) como teto.
- callDiscover ganhou um bloco `if (mediaType == MediaType.MOVIE)` que so adiciona certification_country=BR + certification.lte=<teto> quando ha um teto valido; para TV esse bloco nunca executa, entao /discover/tv nunca recebe certification_country/certification (TMDB nao suporta certification em /discover/tv).
- DiscoverFilters.certifications() e o mesmo campo CSV introduzido em US-004 (endpoint/contrato inalterados); so o javadoc do record foi atualizado para descrever o comportamento agora implementado em vez de "ainda nao mapeado".
- Testes adicionados em MediaSearchServiceTest: discover_movieOnly_withCertifications_usesHighestSelectedAsCeiling (Livre,12,16 -> teto 16), discover_movieOnly_withOnlyLivreChip_usesLAsCeiling (Livre -> teto L), discover_movieOnly_withoutCertifications_omitsCertificationParams (sem certifications -> nenhum param enviado), discover_tvOnly_ignoresCertificationsEvenWhenProvided (TV com Livre,18 -> nenhum param enviado, endpoint continua /discover/tv).
- Arquivos alterados: api/src/main/java/com/app/media/{MediaSearchService.java, DiscoverFilters.java}, api/src/test/java/com/app/media/MediaSearchServiceTest.java, api/CLAUDE.md, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- **Learnings for future iterations:**
  - Padrao consolidado no topo deste arquivo e em api/CLAUDE.md: qualquer parametro TMDB que so se aplique a filme (ou so a serie) deve entrar em um bloco `if (mediaType == MediaType.MOVIE)`/`== MediaType.TV` dedicado dentro de `callDiscover`, do mesmo jeito que o bloco de certification agora existe ao lado do bloco de dateRange.
  - US-006 (generos) e a proxima story de backend pendente (GET /api/media/genres, reaproveitando TmdbGenre); a partir de US-007 as stories sao todas de frontend e devem usar a skill frontend-design antes de implementar, conforme CLAUDE.md do repo.
  - Mesma limitacao de ambiente do Ralph: sem JDK/Maven/rede neste sandbox (confirmado novamente: `mvn`/`java`/`javac` nao existem no PATH, e nao ha JDK em /usr/lib/jvm), `./mvnw -q -pl api test` nao pode ser executado aqui; codigo revisado manualmente linha a linha contra os padroes existentes de MediaSearchServiceTest (reuso do ArgumentCaptor<Function<UriBuilder,URI>> ja documentado).
---

## 2026-07-28 - US-006
- Implementado GET /api/media/genres (MediaController) + MediaSearchService.genres(). Novo record TmdbGenreListResponse(List<TmdbGenre> genres) mapeia a resposta de /genre/movie/list e /genre/tv/list (reutilizando o TmdbGenre existente, ja usado por MediaDetails). genres() chama os dois endpoints, mescla os resultados e deduplica por name() com LinkedHashMap.putIfAbsent (filmes inseridos primeiro, entao um genero repetido nos dois - ex.: "Acao" em movie e "Action & Adventure"/mesma string em tv - fica com o id do filme). Novo record publico MediaGenre(id, name) e o unico DTO exposto pelo endpoint; TmdbGenreListResponse/TmdbGenre continuam package-private (nunca vazam a forma crua do TMDB).
- Falhas de qualquer um dos dois GETs (/genre/movie/list ou /genre/tv/list) sao capturadas com o mesmo catch (RestClientResponseException | ResourceAccessException) -> TmdbUnavailableException dos demais endpoints de com.app.media, tratado por MediaExceptionHandler (502).
- Testes adicionados: MediaSearchServiceTest (genres_mergesMovieAndTvGenresDeduplicatingByName, genres_returnsEmptyListWhenTmdbResponseBodyIsNull, genres_throwsTmdbUnavailableWhenTmdbIsDown) e MediaControllerTest (genres_returnsMergedGenresForAuthenticatedUser, genres_deniesAccessWithoutAuthentication, genres_returnsBadGatewayWhenTmdbIsUnavailable).
- Arquivos alterados: api/src/main/java/com/app/media/{MediaController.java, MediaSearchService.java, MediaGenre.java(novo), TmdbGenreListResponse.java(novo)}, api/src/test/java/com/app/media/{MediaSearchServiceTest.java, MediaControllerTest.java}, api/CLAUDE.md, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- **Learnings for future iterations:**
  - Padrao de "mesclar duas listas TMDB do mesmo shape, deduplicando por chave" (LinkedHashMap.putIfAbsent) agora documentado em api/CLAUDE.md; diferente do padrao discoverBoth (merge por comparator de sort_by + slice de 20 para paginacao) usado em US-004 - use o que for adequado ao caso (deduplicacao simples vs. paginacao ordenada).
  - US-006 era a ultima story de backend do PRD; a partir de US-007 todas as stories restantes sao de frontend (client/) e devem usar a skill frontend-design antes de implementar, conforme CLAUDE.md do repo. US-007 (abas Descobrir/Sugestoes + cabecalho) e a proxima.
  - Mesma limitacao de ambiente do Ralph: sem JDK/Maven/rede neste sandbox, `./mvnw -q -pl api test` nao pode ser executado aqui; codigo revisado manualmente linha a linha (RestClient.get().uri(String) com deep stubs mockado com a string exata do endpoint, sem uri variables, seguindo o mesmo padrao das chamadas existentes que usam Function<UriBuilder,URI>).
---

## 2026-07-28 - US-008
- Novo client/src/components/ui/select.tsx (Select/SelectTrigger/SelectValue/SelectContent/SelectItem), escrito a mao sobre @base-ui/react/select seguindo o padrao de popover.tsx/button.tsx (data-slot, cn, tokens do tema como default, overrides via className nos usos), ja que este sandbox nao tem rede para `npx shadcn add select` (EHOSTUNREACH para ui.shadcn.com - documentado em client/CLAUDE.md).
- MatchScreen.tsx (SearchTab): adicionado o bloco Filtros+Ordenacao entre o input de busca e a area de resultados, dentro de um Collapsible (ja existente) cujo painel por enquanto e so um placeholder textual (paineis reais de chips/sliders sao US-009/US-010/US-011). Botao Filtros: icone SlidersHorizontal, badge ambar circular com activeFilterCount (constante 0 por enquanto, ja que nenhum filtro real existe ainda - sera substituida por estado derivado dos filtros quando US-009/US-010 os introduzirem), chevron up quando aberto / down quando fechado, estado visual ambar quando aberto/badge>0. Dropdown de ordenacao usa o novo Select com os 6 valores de DiscoverSortBy do backend (popularity.desc/asc, vote_average.desc/asc, release_date.desc/asc) via prop `items` (Select.Root aceita items:[{value,label}] e SelectValue usa o label automaticamente sem precisar de children customizado).
- Ambos (botao Filtros e Select) ficam disabled quando ha texto na busca (hasQuery = query.trim().length > 0), ja que digitar busca usa /search (mutuamente exclusivo com /discover, que sera reforcado de verdade em US-011 - por ora so o disabled visual/funcional dos controles).
- handleSortChange(nextSortBy) chama GET /api/media/discover?sortBy=...&page=1 (ignorado se hasQuery) e substitui `results` com response.data.results, reaproveitando o grid de cards existente.
- Bug-fix de passagem: o fetch de /api/media/search ainda tratava a resposta como MediaSearchResult[] (`api.get<MediaSearchResult[]>`), mas o backend retorna MediaPage desde US-003 (679d179/f24a554) - a seguir `results.map` sobre um objeto MediaPage quebraria em runtime. Corrigido para `api.get<MediaPage>` + `response.data.results`. Novo tipo MediaPage adicionado em client/src/types/media.ts espelhando o record Java (results/page/totalResults/totalPages).
- Arquivos alterados: client/src/components/ui/select.tsx (novo), client/src/screens/MatchScreen.tsx, client/src/types/media.ts, client/CLAUDE.md, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- Verificacao: `npx tsc --noEmit`, `npx eslint src/screens/MatchScreen.tsx src/components/ui/select.tsx src/types/media.ts` e `npx vite build` todos passaram sem erros/warnings. Sem browser neste sandbox (ver client/CLAUDE.md) - verificacao visual real fica pendente.
- **Learnings for future iterations:**
  - `npx shadcn add <component>` NAO funciona neste sandbox (sem rota de rede para ui.shadcn.com, `EHOSTUNREACH`) - qualquer primitivo shadcn que falte (Slider, ToggleGroup, Pagination - necessarios em US-009/US-010/US-014) tera que ser escrito a mao contra `@base-ui/react/<primitivo>` (os pacotes NPM em si estao instalados e disponiveis em node_modules, so o registry HTTP do shadcn e que esta bloqueado). Ver o novo guia em client/CLAUDE.md ("Sandbox gotcha: no network access for `npx shadcn add`") com o padrao a seguir.
  - `@base-ui/react/select`'s `Select.Root` aceita uma prop `items` (array de `{value, label}` ou `Record<string, ReactNode>`); quando o shape e `{value, label}`, `<Select.Value>` usa o label automaticamente sem precisar de children customizado - mais simples que passar uma funcao de formatacao manualmente.
  - O painel de filtros (US-009/US-010) deve entrar dentro do `CollapsibleContent` que ja existe em MatchScreen.tsx (SearchTab), substituindo o texto placeholder atual; `activeFilterCount` (hoje uma constante 0) devera virar estado derivado da selecao de chips/sliders quando essas stories forem implementadas.
  - `/api/media/discover` so deve ser chamado quando `!hasQuery` (modo busca textual e modo descoberta sao mutuamente exclusivos, US-011 vai reforcar isso de forma mais completa incluindo o carregamento inicial via trending/US-012).
---

## 2026-07-28 - US-007
- MatchScreen.tsx ja tinha (de implementacoes anteriores da tela) o cabecalho central (eyebrow "Match a Dois" + titulo "Descubram o proximo juntos") e o input de busca com icone Search (lucide) + placeholder "Ex.: Coracao de Vidro, Fronteira Norte..." dentro de SearchTab, ambos ja seguindo os tokens do design system (bg #09090a, card #161513, primario #ffcb2b, texto #f6f4ec, muted #a6a39a, font-display/font-auth-body). Comparado contra o mock de referencia docs/design/prints/matchpage-search-with-filters.jpeg (screen "Match" full state, com filtros), so 2 ajustes eram necessarios para esta story: (1) subtitulo trocado para "Filtrem o catalogo do TMDB e curtam. Quando os dois curtirem o mesmo, vira um match." (igual ao mock); (2) a aba antes rotulada "Buscar" (activeTab === "search") foi renomeada para "Descobrir" e reordenada para aparecer primeiro (antes de "Sugestoes"), igual a ordem visual do mock. A aba Sugestoes (SuggestionsTab) nao foi tocada.
- Arquivos alterados: client/src/screens/MatchScreen.tsx, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- **Learnings for future iterations:**
  - O mock de referencia completo para todo o epico US-007..US-015 esta em docs/design/prints/matchpage-search-with-filters.jpeg (screenshot, nao html) - mostra o layout final: header, abas Descobrir/Sugestoes, busca + botao Filtros com badge + dropdown de ordenacao, painel expansivel com chips de Data/Classificacao/Generos + sliders de Nota/Duracao + Limpar tudo/Aplicar filtros, e a area "Em alta esta semana" com grid de cards (badge tipo + nota). Usar essa imagem como fonte de verdade visual para US-008 em diante, em vez de docs/design/claude-design-project/Sessao a Dois.dc.html (prototipo antigo, tela "Match" la dentro é so a versao swipe antiga, sem filtros).
  - `activeTab === "search"` continua sendo o nome interno do estado que representa a aba "Descobrir" (nao foi renomeado no codigo, so o rotulo visivel) - proximas stories (US-008 em diante) devem continuar usando "search" como o valor do estado, so a label na UI que e "Descobrir".
  - Sem browser/Chromium neste sandbox (confirmado, ver client/CLAUDE.md) - verificacao foi so por tsc --noEmit, eslint no arquivo alterado e vite build (todos passaram); checagem visual no browser real fica pendente para quando houver ambiente com browser disponivel.
---

## 2026-07-28 - US-009
- Novo client/src/components/ui/toggle-group.tsx (ToggleGroup/ToggleGroupItem), escrito a mao sobre @base-ui/react/toggle-group e @base-ui/react/toggle (mesmo motivo dos demais primitivos hand-rolled: sem rede para `npx shadcn add toggle-group` neste sandbox, ja documentado em client/CLAUDE.md). Diferente de select.tsx/collapsible.tsx, `Toggle`/`ToggleGroup` sao exportados como componentes de funcao simples (nao namespaced como `.Root`/`.Trigger`).
- MatchScreen.tsx (SearchTab): o `CollapsibleContent` do painel de filtros (ate entao um texto placeholder de US-008) agora renderiza tres secoes de chips, reproduzindo docs/design/prints/matchpage-search-with-filters.jpeg: "Data de lancamento" (Anos 2020/2010/2000/1990/Anterior) e "Classificacao indicativa" (Livre/10/12/14/16/18) lado a lado em grid de 2 colunas (`sm:grid-cols-2`), e "Generos" abaixo em largura total (separado por `border-t`), carregado de GET /api/media/genres (novo tipo `MediaGenre` em types/media.ts, fetch em useEffect proprio). Cada secao e um `ToggleGroup multiple` com estado local proprio (`selectedDecades`/`selectedCertifications`/`selectedGenres`, todos `string[]`).
- `activeFilterCount` (badge do botao Filtros, ate entao uma constante 0 fixada em US-008) agora e `selectedDecades.length + selectedCertifications.length + selectedGenres.length` - primeira vez que o badge reflete selecao real do usuario.
- Nenhuma chamada a /api/media/discover e disparada ao (des)marcar um chip - isso continua sendo responsabilidade explicita de US-011 ("Aplicar filtros"); por ora e so estado local, conforme a AC da story.
- Valores dos chips de decada usam os anos-decada que `MediaSearchService.dateRangeFromDecades` (backend, api/CLAUDE.md) espera no CSV `releaseDecades` ("2020","2010","2000","1990"); o chip "Anterior" usa "1900" como piso (menor decada suportada, cobre tudo anterior a 1990 quando vira o `min()` do intervalo).
- Arquivos alterados: client/src/components/ui/toggle-group.tsx (novo), client/src/screens/MatchScreen.tsx, client/src/types/media.ts, client/CLAUDE.md, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- Verificacao: `npx tsc --noEmit`, `npx eslint src/screens/MatchScreen.tsx src/components/ui/toggle-group.tsx src/types/media.ts` e `npx vite build` todos passaram sem erros/warnings. Sem browser neste sandbox (ver client/CLAUDE.md) - verificacao visual real fica pendente.
- **Learnings for future iterations:**
  - Padrao de `Toggle`/`ToggleGroup` (`@base-ui/react/toggle` + `@base-ui/react/toggle-group`, exportados como funcao simples, `data-pressed` para estado ativo) consolidado no topo deste arquivo e em client/CLAUDE.md.
  - US-010 (sliders de Nota TMDB/Duracao) deve entrar no mesmo `CollapsibleContent`, provavelmente logo apos a secao Generos, usando um `@base-ui/react/slider` hand-rolled do mesmo jeito (ainda nao existe `client/src/components/ui/slider.tsx`); os valores tambem devem ficar como estado local ate US-011.
  - US-011 ("Limpar tudo"/"Aplicar filtros") e quem vai de fato montar o objeto de filtros (decades/certifications/genres + os sliders de US-010) e chamar GET /api/media/discover com page=1 - so entao o modo descoberta com filtros fica funcional de ponta a ponta; tambem e quem deve reforcar a exclusividade busca-texto vs. filtros de forma mais completa.
  - Sem browser/Chromium neste sandbox (confirmado novamente) - verificacao ficou em tsc/eslint/vite build; checagem visual real (chips clicaveis, grid responsivo em telas pequenas) fica pendente para quando houver ambiente com browser disponivel.
---

## 2026-07-28 - US-010
- Novo client/src/components/ui/slider.tsx (Slider/SliderControl/SliderTrack/SliderIndicator/SliderThumb), escrito a mao sobre @base-ui/react/slider (`Slider.Root/Control/Track/Indicator/Thumb`, namespace import `Slider as SliderPrimitive`, mesmo padrao de select.tsx: data-slot, cn, tokens do tema/hex fixo como default) ja que este sandbox nao tem rede para `npx shadcn add slider` (mesmo gotcha documentado em client/CLAUDE.md).
- MatchScreen.tsx (SearchTab): o `CollapsibleContent` do painel de filtros ganhou uma nova secao (grid `sm:grid-cols-2`, separada por `border-t`) logo apos "Generos", com dois sliders de intervalo (dois thumbs cada): "Nota TMDB" (min 0, max 10, step 0.5, `minStepsBetweenValues={1}`, label dinamico `formatVoteRangeLabel` -> "X,X - Y" com 1 casa decimal e virgula, exceto o maximo quando == 10 que mostra so "10" sem decimal, batendo com a referencia "7,0 - 10" em docs/design/prints/matchpage-search-with-filters.jpeg) e "Duracao" (min 0, max 240, step 5, label dinamico `formatRuntimeRangeLabel` -> "Ate Xh"/"Ate X min" quando o minimo esta em 0 (caso default), renderizado com `className="uppercase"` para bater com "ATE 3H" da referencia). Limites fixos "0"/"10" e "0 min"/"240 min" abaixo de cada trilha.
- Novo estado local `voteRange`/`runtimeRange` (`[number, number]`, default `[0,10]`/`[0,240]`, constantes `DEFAULT_VOTE_RANGE`/`DEFAULT_RUNTIME_RANGE`); `activeFilterCount` agora soma +1 por slider quando ele sai do padrao (alem dos 3 arrays de chips ja existentes de US-009).
- Nenhuma chamada a /api/media/discover e disparada ao arrastar os sliders - e so estado local, mesma exclusividade de US-009/US-011 ("Aplicar filtros" e quem de fato monta o request com esses valores).
- Arquivos alterados: client/src/components/ui/slider.tsx (novo), client/src/screens/MatchScreen.tsx, client/CLAUDE.md, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- Verificacao: `npx tsc --noEmit`, `npx eslint src/screens/MatchScreen.tsx src/components/ui/slider.tsx` e `npx vite build` todos passaram sem erros/warnings. Sem browser neste sandbox (ver client/CLAUDE.md) - verificacao visual real fica pendente.
- **Learnings for future iterations:**
  - Padrao de range slider (`Slider.Root` com `value`/`onValueChange` tipados como `[number, number]`, dois `<SliderThumb index={0|1}/>` dentro de um `<SliderControl>`, `minStepsBetweenValues={1}` para impedir os thumbs se cruzarem) consolidado no topo deste arquivo e em client/CLAUDE.md.
  - US-011 ("Limpar tudo"/"Aplicar filtros") e quem finalmente monta o objeto completo de filtros (decades/certifications/genres de US-009 + voteRange/runtimeRange de US-010) e chama GET /api/media/discover com voteAverageMin/voteAverageMax/runtimeMin/runtimeMax (nomes de parametros do backend, ver US-004/api/CLAUDE.md) + page=1; "Limpar tudo" deve resetar voteRange/runtimeRange para DEFAULT_VOTE_RANGE/DEFAULT_RUNTIME_RANGE junto com os arrays de chips.
  - Sem browser/Chromium neste sandbox (confirmado novamente) - verificacao ficou em tsc/eslint/vite build; checagem visual real (arrastar os thumbs, ver a trilha preenchida em ambar) fica pendente para quando houver ambiente com browser disponivel.
---

## 2026-07-28 - US-011
- MatchScreen.tsx (SearchTab): adicionado o rodape de acoes do painel de filtros (`CollapsibleContent`, apos a secao de sliders de US-010, separado por `border-t`) com dois botoes: "Limpar tudo" (text button, `disabled={activeFilterCount === 0}`) e "Aplicar filtros" (`Button` primario ambar, `disabled={hasQuery || searching}`).
- `handleClearFilters` reseta `selectedDecades`/`selectedCertifications`/`selectedGenres` para `[]` e `voteRange`/`runtimeRange` para `DEFAULT_VOTE_RANGE`/`DEFAULT_RUNTIME_RANGE` — `activeFilterCount` zera sozinho por ser derivado desse estado, nao precisa de logica propria de "zerar badge".
- `handleApplyFilters` monta um objeto `params` para `GET /api/media/discover` a partir da selecao atual: `sortBy` e `page: 1` sempre presentes; `releaseDecades`/`certifications`/`genres` como CSV (`array.join(",")`) so quando o array correspondente nao esta vazio; `voteAverageMin`/`voteAverageMax`/`runtimeMin`/`runtimeMax` so quando o valor difere do default (evita mandar `voteAverageMin=0&voteAverageMax=10` sempre, que seria redundante com "sem filtro"). Reaproveita o mesmo padrao de loading/erro de `handleSortChange` (US-008): `setSearching(true)` -> `api.get<MediaPage>` -> `setResults(response.data.results)` + `setSearched(true)` no sucesso, `setResults([])` + `setSearched(true)` no erro, `setSearching(false)` no finally.
- A exclusividade busca-texto vs. painel de filtros ja estava garantida desde US-008 (`CollapsibleTrigger`/`Select` `disabled` quando `hasQuery`); esta story so reforca com um early-return em `handleApplyFilters` quando `hasQuery` (defesa extra contra uma race entre o clique e a resposta, ja que o botao tambem fica `disabled` nesse caso).
- Arquivos alterados: client/src/screens/MatchScreen.tsx, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- Verificacao: `npx tsc --noEmit`, `npx eslint src/screens/MatchScreen.tsx` e `npx vite build` todos passaram sem erros/warnings. Sem browser neste sandbox (ver client/CLAUDE.md) - verificacao visual real fica pendente.
- **Learnings for future iterations:**
  - US-012 (header dinamico + contador de resultados) e US-013/US-014 (grid com badges/curtir + paginacao) sao as proximas; `handleApplyFilters`/`handleSortChange` ja substituem `results` mas nao guardam `page`/`totalResults`/`totalPages` da resposta em estado algum ainda - US-012 provavelmente precisa desses campos de `MediaPage` (alem de `.results`) para o contador e o header dinamico, e US-014 vai precisar de um estado de pagina atual reaproveitado por busca/discover/sort/filtros (reiniciando para 1 em qualquer mudanca desses, conforme a AC de US-014).
  - Nenhum estado novo de "modo atual" (search vs discover vs trending) foi introduzido explicitamente ainda - o codigo infere isso via `hasQuery` (verdadeiro = /search) e chamadas diretas a /api/media/discover nos outros casos. US-012 pode precisar formalizar isso um pouco mais para decidir o texto do header ("Em alta esta semana" vs "Resultados para ..." vs "Filtros aplicados").
  - Sem browser/Chromium neste sandbox (confirmado novamente) - verificacao ficou em tsc/eslint/vite build; checagem visual real (clicar em Aplicar filtros e ver o grid atualizar, Limpar tudo zerando o badge) fica pendente para quando houver ambiente com browser disponivel.
---

## 2026-07-28 - US-012
- SearchTab (MatchScreen.tsx): novo useEffect de montagem que chama GET /api/media/trending?page=1 assim que a aba Descobrir abre (antes desta story nenhum fetch inicial existia - o grid ficava vazio ate o usuario digitar ou aplicar filtros, o que nao dava nenhum estado real para o header/contador desta story refletirem). Usa o mesmo padrao de deferimento ja estabelecido no debounce de busca (`setTimeout(fn, 0)` + `cancelled` flag limpo no cleanup) para evitar o lint `react-hooks/set-state-in-effect` ao chamar `setSearching(true)` de forma sincrona dentro do efeito - documentado como novo gotcha em client/CLAUDE.md.
- Novo estado `resultsContext` (`"trending" | "search" | "discover"`) e `totalResults` (number), setados nos 4 pontos que hoje populam `results`: fetch de trending no mount ("trending"), busca textual debounced ("search"), `handleSortChange` e `handleApplyFilters` (ambos "discover", ja que os dois chamam `/api/media/discover` - trocar so a ordenacao sem filtro nenhum tambem conta como "discover" para fins de header, nao ha um terceiro texto para esse caso na PRD).
- Novo header dinamico entre o painel de filtros (`Collapsible`) e os estados vazios/grid, visivel só quando `searched === true`: `hasQuery` -> `Resultados para "{query}"`; senao `resultsContext === "discover"` -> `"Filtros aplicados"`; senao (`"trending"`) -> `"Em alta esta semana"` + subtitulo `"Resultados dinamicos do TMDB - refine com o painel de filtros."` (unico caso com subtitulo, batendo com docs/design/prints/matchpage-search-with-filters.jpeg). Contador alinhado a direita via `formatResultsCount(totalResults)` (pluraliza "titulo"/"titulos").
- Guard extra `!searching` adicionado a condicao do placeholder "Comecem digitando..." para nao piscar por um instante enquanto o fetch inicial de trending ainda esta em voo (antes ele so checava `!searched && !query.trim()`).
- Arquivos alterados: client/src/screens/MatchScreen.tsx, client/CLAUDE.md, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- Verificacao: `npx tsc --noEmit`, `npx eslint src/screens/MatchScreen.tsx` e `npx vite build` todos passaram sem erros/warnings. Sem browser neste sandbox (ver client/CLAUDE.md) - verificacao visual real fica pendente.
- **Learnings for future iterations:**
  - `resultsContext` agora existe como a fonte de verdade de "que tipo de fetch populou `results` por ultimo" - US-013/US-014 (grid de cards, paginacao) e US-015 (estados vazios/erro) devem reaproveitar esse estado (junto com `totalResults`/`page` do `MediaPage`, que so `totalResults` foi guardado ate agora) em vez de inferir o modo so por `hasQuery`.
  - US-014 (paginacao) vai precisar guardar tambem `page`/`totalPages` de `MediaPage` (so `totalResults` foi adicionado nesta story) e resetar `page` para 1 sempre que busca/filtros/ordenacao mudam - os 4 pontos de fetch (trending mount, busca debounced, handleSortChange, handleApplyFilters) sao os mesmos lugares que precisarao ganhar o parametro `page` variavel.
  - US-015 (estados vazios/erro) deve diferenciar "trending indisponivel no load inicial" (o novo catch do useEffect de mount, hoje so faz `setResults([]); setTotalResults(0); setResultsContext("trending"); setSearched(true)`, sem nenhuma mensagem de erro dedicada - cai no mesmo bloco generico de "Nada encontrado com os filtros atuais") de uma falha real do TMDB (5xx) vs. resultado vazio legitimo; hoje os dois casos sao indistinguiveis no estado local (nao ha um campo tipo `fetchError`).
  - Sem browser/Chromium neste sandbox (confirmado novamente) - verificacao ficou em tsc/eslint/vite build; checagem visual real (header trocando de "Em alta esta semana" para "Resultados para X"/"Filtros aplicados", grid populado por trending no load) fica pendente para quando houver ambiente com browser disponivel.
---
