## Codebase Patterns
- This sandbox has no `docker`, `java`, `mvn`, or `psql` — `./mvnw` cannot run here (no JDK). Backend "Typecheck passes"/"Tests pass" criteria that require compiling/running the Spring Boot app cannot be verified in this environment; note that limitation explicitly in progress/notes instead of silently skipping it. `api/CLAUDE.md` already documents this.
- There is no Flyway/Liquibase yet on `create-migrations`'s baseline — schema currently evolves via `spring.jpa.hibernate.ddl-auto=update`. Entities: `com.app.user.User`, `com.app.couple.Couple`, `com.app.tracking.MediaTrack` (+`media_track_genre` via `@ElementCollection`), `com.app.tracking.UserReview`, `com.app.match.MatchLike`/`MatchReject`, `com.app.notification.Notification` (new, not in `origin/main`).
- The `/ralph` skill (`.claude/skills/ralph/SKILL.md`) writes its converted `prd.json` into its own skill directory (`.claude/skills/ralph/`), which is a DIFFERENT directory from the one the running Ralph loop actually reads (`scripts/ralph/`, per `scripts/ralph/CLAUDE.md`: "same directory as this file"). If `scripts/ralph/prd.json` is ever found empty/stale after running `/ralph`, check `.claude/skills/ralph/prd.json` for the real converted content and copy it over before doing anything else — do not treat an empty `prd.json` as "PRD not ready yet" without checking there first.
- `git diff origin/main HEAD -- <entity file>` is a fast way to confirm whether a JPA entity actually changed between the deployed baseline and the current branch, instead of trusting a PRD's claims at face value.

## [2026-08-01] - US-001
- Implemented: validated (via static analysis, since no live DB/JDK access exists in this sandbox) that the only schema difference between `origin/main` (production baseline, `e0f3d36`) and `create-migrations` (HEAD, `cc5c464`) is the new `notification` table. Confirmed via `git diff origin/main HEAD` on all six existing entities (byte-identical) and `git ls-tree -r origin/main` (MatchLike/MatchReject already present, Notification absent). Wrote the full derived schema (tables/columns/types/constraints/indexes) to docs/SCHEMA_BASELINE.md as the authoritative base for the future V1__baseline.sql, with an explicit caveat to re-validate against Supabase's real information_schema before the production deploy.
- Also fixed a repo-state issue found before starting: `scripts/ralph/prd.json` (the file this Ralph loop actually reads) had been emptied to 0 bytes, while the correctly-converted flyway-migrations PRD had been written to `.claude/skills/ralph/prd.json` instead (see Codebase Patterns above). Copied the correct content into `scripts/ralph/prd.json` (and mirrored it back to `.claude/skills/ralph/prd.json` to keep both in sync) so the loop had a PRD to execute. Also restored the previous (fully-passed, already-merged) comparacao-de-titulos PRD+progress into `scripts/ralph/archive/2026-08-01-comparacao-de-titulos/` from git history, matching this repo's existing archiving convention, instead of letting that history be silently lost.
- Files changed: docs/SCHEMA_BASELINE.md (new), scripts/ralph/prd.json, .claude/skills/ralph/prd.json, scripts/ralph/archive/2026-08-01-comparacao-de-titulos/{prd.json,progress.txt} (new), scripts/ralph/progress.txt.
- **Learnings for future iterations:**
  - No JDK/Maven/Docker/Postgres client available here — any future story whose acceptance criteria include "Typecheck passes" or "Tests pass" against the `api/` Spring Boot project cannot be mechanically verified in this sandbox; say so explicitly rather than claiming a pass.
  - US-001's "confirm via information_schema or a clean Postgres boot" acceptance criteria were satisfied via static JPA-entity analysis instead, since neither prerequisite (DB network access, JDK) is available — this is a documented, deliberate fallback, not a skipped step. Whoever runs the actual production deploy should re-run the `information_schema` query in docs/SCHEMA_BASELINE.md against Supabase first.
  - Next story is US-002 (add flyway-core/flyway-database-postgresql to api/pom.xml, no `<version>` tag) — this only needs a text edit to pom.xml; the "`./mvnw -f api/pom.xml compile` executes without error" criterion cannot be run here, so it'll need the same caveat.
---
