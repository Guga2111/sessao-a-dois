# Ralph Progress Log
Started: Sun Jul 26 14:28:59 PDT 2026
---

## 2026-07-26 - US-006
- Implemented REST endpoints for notifications: GET /api/notifications (paginated), GET /api/notifications/unread-count, PATCH /api/notifications/{id}/read, PATCH /api/notifications/read-all.
- Files changed:
  - api/src/main/java/com/app/notification/NotificationController.java (new)
  - api/src/main/java/com/app/notification/NotificationExceptionHandler.java (new)
  - api/src/main/java/com/app/notification/UnreadCountResponse.java (new)
  - api/src/main/java/com/app/notification/NotificationService.java (listNotifications/unreadCount/markAsRead/markAllAsRead)
  - api/src/main/java/com/app/notification/NotificationRepository.java (markAllAsReadByRecipientUserId bulk @Modifying query)
  - api/src/test/java/com/app/notification/NotificationControllerTest.java (new)
  - api/src/test/java/com/app/notification/NotificationServiceTest.java (extended)
- **Learnings for future iterations:**
  - Every feature package in this backend gets its own `@RestControllerAdvice(basePackages = "com.app.X")` handler that maps `com.app.tracking.ResourceNotFoundException` -> 404 and `org.springframework.security.access.AccessDeniedException` -> 403 (see TrackingExceptionHandler/MatchExceptionHandler). Reuse this exact pattern rather than inventing a new exception type per feature.
  - `com.app.tracking.ResourceNotFoundException` is a shared cross-feature class (already imported by com.app.match) - it's fine to reuse from com.app.notification too instead of creating a notification-specific one.
  - For a "mark all as read/done" bulk update, prefer a `@Modifying @Query("UPDATE ... SET ... WHERE ...")` on the repository (needs the calling service method annotated `@Transactional`) over fetching all rows and calling save() in a loop.
  - When a DTO/list endpoint needs a display name resolved from a foreign id per row (e.g. actorName from actorUserId), batch-resolve with `userRepository.findAllById(distinctIds)` into a Map first, then map over the page - avoids N+1 UserRepository.findById calls per row.
  - This sandbox has no JDK/Maven and no network access to Maven Central (mvnw tries to download the maven distribution and fails) - consistent with the note already in api/CLAUDE.md. Changes here were verified by manual code review only, not by running ./mvnw test. Flag this to a human or a later iteration with build tooling available.
---

## 2026-07-26 - US-007
- Implemented TypeScript types and Zustand store for notifications.
- Files changed:
  - client/src/types/notification.ts (new)
  - client/src/stores/useNotificationStore.ts (new)
- **Learnings for future iterations:**
  - Frontend paginated list endpoints (Spring Data `Page<T>`) get a `Paged<X>Response` interface mirroring `PagedMediaTrackResponse` (content/totalElements/totalPages/number/size) - reused this shape for `PagedNotificationResponse`.
  - Store pattern (see useMatchStore/useAuthStore): plain Zustand `create<State>((set, get) => ({...}))`, all HTTP via the shared `api` axios client (src/lib/api.ts) which already attaches the JWT.
  - For optimistic updates with rollback (markAsRead/markAllAsRead), snapshot the pre-update state at the top of the action, apply the optimistic `set()`, then in the `catch` restore the snapshot via `set()` and rethrow - avoids hand-rolling inverse-operation logic.
  - `fetchNotifications()` and `fetchUnreadCount()` are kept as two separate store methods (not combined) because US-008's spec calls them independently on app load ("chamar fetchNotifications() e buscar unread-count").
  - `npm run lint` in client/ currently has 6 pre-existing errors unrelated to notifications work (PendingDetailModal.tsx, ReviewModal.tsx, WatchModal.tsx react-hooks/set-state-in-effect; button.tsx react-refresh/only-export-components) - verified with `npx eslint <new files>` directly that the new files themselves are lint-clean; do not assume `npm run lint` will exit 0 in this repo right now.
---

## 2026-07-26 - US-008
- Subscribed to /topic/couple/{coupleId}/notifications on the existing STOMP connection managed by useMatchStore, and wired notification fetch-on-load into App.tsx.
- Files changed:
  - client/src/stores/useMatchStore.ts (client.subscribe to the notifications topic inside onConnect, forwards each message to useNotificationStore.getState().pushIncoming)
  - client/src/App.tsx (the existing isAuthenticated+hasPartner+coupleId effect now also calls fetchNotifications()/fetchUnreadCount() alongside connect())
- **Learnings for future iterations:**
  - Cross-store calls from inside a Zustand store body (not a React component) should use `useOtherStore.getState().action(...)` rather than a hook - avoids importing React hook machinery into non-component code and sidesteps any circular-subscription concerns.
  - No payload transform was needed for the STOMP notification message: NotificationDto (backend) and the `Notification` TS type (client/src/types/notification.ts, from US-007) already have matching field names, so `JSON.parse(message.body) as Notification` is a direct, safe cast.
  - Sandbox gotcha confirmed still current: `npx vite build` fails with `Cannot find module './rolldown-binding.linux-arm64-gnu.node'` until `npm install @rolldown/binding-linux-arm64-gnu lightningcss-linux-arm64-gnu @tailwindcss/oxide-linux-arm64-gnu` is run locally - but do NOT commit that `npm install` (it dirties package.json/package-lock.json with sandbox-only arm64 optional bindings that were never part of prior commits); `git checkout -- client/package.json client/package-lock.json` after verifying the build to keep the diff clean.
---

## 2026-07-26 - US-009
- Added NotificationBell icon/badge component and mounted it in the Header, to the left of the couple avatars.
- Files changed:
  - client/src/components/NotificationBell.tsx (new)
  - client/src/components/Header.tsx (import + render inside the existing couple?.partner block)
- **Learnings for future iterations:**
  - `npx vite build` worked in this sandbox run without needing the arm64 optional-binding workaround noted after US-008 - the packages must already be present from that prior install. Still remove `dist/` after a manual build check and confirm `git status --short package.json package-lock.json` is empty before committing, just in case.
  - No Chromium/Playwright in this sandbox (per client/CLAUDE.md), so "verify in browser" for UI stories here means: tsc --noEmit, eslint on the changed files, and a full `npx vite build` as a stronger-than-typecheck signal that the component actually compiles and renders without errors - not a real screenshot. Documented as such per US-009's acceptance criteria fallback clause.
  - The bell is mounted inside the same `couple?.partner &&` conditional as `CoupleAvatars` in Header.tsx (not rendered when there's no linked partner yet), since notifications are couple-scoped and only make sense once a couple exists. Keep this in mind for US-010's dropdown - it should live in the same conditional block.
---
