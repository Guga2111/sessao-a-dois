# Ralph Progress Log
Epico 9 - Ciclo de Vida de Conta e Casal
Started: 2026-08-06
(o log do Epico 8 esta em scripts/ralph/archive/2026-08-06-epico8-cicd-infraestrutura/)
---

## Codebase Patterns
- Backend: `./mvnw test` roda de verdade no sandbox (~40s, 431 testes) e ja inclui o
  gate do JaCoCo (`check` na fase `test`, minimo 90% de linha). Nunca dar uma story de
  backend como pronta sem rodar. `./mvnw -q test` esconde o resumo e mostra so
  stacktrace de log esperado — usar `./mvnw test | grep -E "Tests run:|BUILD"`.
- `@WebMvcTest` so prova alguma coisa sobre autenticacao se a `SecurityConfig` do app
  entrar no slice. Conjunto padrao de import:
  `@Import({ SecurityConfig.class, ClientIpResolver.class, RateLimitService.class,
  RateLimitProperties.class, SecurityAuditLogger.class })` + `@MockitoBean JwtService`.
  Filtros (`RateLimitFilter`, `CorrelationIdFilter`, `JwtAuthenticationFilter`) sao
  auto-incluidos por implementarem `jakarta.servlet.Filter` — nao importar, so prover
  as dependencias deles.
- Para uma rota publica, `verifyNoInteractions(jwtService)` e a assercao que realmente
  prova "nao exige autenticacao"; um 200 sozinho passaria mesmo se a rota virasse
  autenticada-com-anonimo.
- NAO ha `docker` nem `psql` neste sandbox. Criterio de aceite que depende de
  `docker build`/`docker ps`/Postgres real fica pendente do lado humano — registrar
  aqui em vez de travar a story.
- Mudanca de infraestrutura (variavel de ambiente, compose, workflow) NAO e coberta por
  `./mvnw test`. Verificar de outro jeito: parsear o YAML (`bunx --bun js-yaml <arquivo>`),
  rodar o step de verdade quando for shell, e registrar o que so pode ser conferido do
  lado humano.
- Imports do Spring Boot 4.1 mudaram de pacote: `@WebMvcTest` vem de
  `org.springframework.boot.webmvc.test.autoconfigure.WebMvcTest`; `@DataJpaTest` de
  `org.springframework.boot.data.jpa.test.autoconfigure.DataJpaTest`. Copiar o import
  de um teste vizinho em vez de escrever de memoria.
- Entidade com `@GeneratedValue` nao tem setter de id nem de campo interno. Para montar
  um estado que o dominio nao deixa montar (ex.: linha legada dissolvida com
  `invite_code` preenchido), usar
  `ReflectionTestUtils.setField(entity, "campo", valor)` — ja e o padrao do projeto
  para `id`.
- `@DataJpaTest` NAO passa pelas migrations: `spring.flyway.enabled=false` +
  `ddl-auto=create-drop` no `src/test/resources/application.properties`, schema gerado
  das entidades JPA. O unico teste que aplica V1..Vn de verdade e
  `com.app.migration.FlywayMigrationTest`, e a lista de versoes dele e literal — toda
  migration nova precisa da string correspondente adicionada la, senao o teste passa
  sem provar nada sobre o arquivo novo.
- Consulta derivada nao expressa `IS NULL` combinado com `OR` de forma legivel; para
  filtros de dominio (ex.: "casal ativo") usar `@Query` JPQL com `@Param`, que tambem
  deixa o nome do metodo livre para dizer a intencao (`findActiveByUserId`) em vez da
  mecanica (`findByUser1IdOrUser2IdAndDissolvedAtIsNull`).

---

## 2026-08-06 - Setup do Epico 9
- O `prd.json` ainda continha o Epico 8 (todas as stories `passes: true`) enquanto o
  branch atual (`epic9/couple-lifecycle`) e o commit `eeb7e82` ("chore: prd for
  epico9") ja apontavam para o Epico 9, cujo PRD estava so em markdown
  (`tasks/prd-epico-9-ciclo-de-vida-de-conta-e-casal.md`, 611 linhas).
- Arquivado o Epico 8 em `scripts/ralph/archive/2026-08-06-epico8-cicd-infraestrutura/`
  (prd.json + progress.txt), `prd.json` reescrito com as 14 stories do Epico 9 e
  `.last-branch` atualizado para `epic9/couple-lifecycle` — sem isso o `ralph.sh`
  arquivaria o prd.json NOVO dentro da pasta do epico ANTIGO na proxima execucao (ele
  compara `.last-branch` com `branchName` do prd.json) e ainda zeraria este progress.txt.
- **Learnings for future iterations:**
  - Quando o `prd.json` estiver de um epico anterior e existir um PRD novo em `tasks/`,
    a conversao (arquivar + reescrever + `.last-branch`) e parte do trabalho, nao um
    bloqueio. Os tres passos andam juntos.
---

## 2026-08-06 - US-001 (Migration V7 e o conceito de casal dissolvido)
- `api/src/main/resources/db/migration/V7__add_couple_dissolved_at.sql` (novo):
  `ALTER TABLE couples ADD COLUMN IF NOT EXISTS dissolved_at TIMESTAMP WITH TIME ZONE;`
  — aditivo, idempotente, sem `DROP`/`TRUNCATE`. O cabecalho registra a D13 (dissolver,
  nao apagar) e a consequencia de cardinalidade da E9.20.
- `Couple`: campo `dissolvedAt` (`Instant`, nullable), `getDissolvedAt()`, `isActive()`
  e o metodo de dominio `dissolve()` — grava o instante, chama o `clearInviteCode()` que
  ja existia (E9.17) e lanca `CoupleAlreadyDissolvedException` (nova, em `com.app.couple`)
  quando o casal ja esta dissolvido (E9.7).
- `CoupleAlreadyDissolvedException extends IllegalStateException`, DELIBERADAMENTE sem
  entrada no `CoupleExceptionHandler`: nenhum fluxo de usuario chega la (casal dissolvido
  ja nao e "ativo" e vira 404 antes), entao se ela aparecer em producao e bug interno e
  500 e a resposta certa. Se uma story futura precisar mapear, mapear conscientemente.
- `CoupleRepository`: nascem `findActiveByUserId(UUID)` e `findActiveByInviteCode(String)`
  (`@Query` JPQL com `dissolved_at is null`); `findByInviteCode` foi **removido** e o
  unico caller (`CoupleService.joinCouple`) migrado. `findByUser1IdOrUser2Id` continua
  vivo de proposito — a remocao dele e da US-002, que migra os quatro callers de uma vez.
- Testes: `CoupleTest` (novo, 4 testes de dominio: casal novo e ativo, dissolve grava o
  instante, dissolve limpa o convite, dissolve duas vezes lanca e preserva o primeiro
  instante); `CoupleRepositoryTest` ganhou 3 testes (dissolvido nao e retornado por
  usuario; usuario com um dissolvido + um ativo devolve so o ativo — o cenario que a
  E9.20 protege; linha legada dissolvida com `invite_code` preenchido a mao nao e
  retornada por `findActiveByInviteCode`). `CoupleServiceTest` teve os 11 stubs de
  `findByInviteCode` trocados. `FlywayMigrationTest` passou a exigir a versao `"7"`.
- `docs/FLYWAY.md`: a lista de migrations do corpo ganhou as linhas da **V6** (que estava
  faltando) e da **V7**, e a ressalva do pooler do Supabase (5432 direta ou pooler em modo
  Session, nunca Transaction/6543) passou a citar as duas explicitamente.
- `api/CLAUDE.md`: secao nova sobre o ciclo de vida do casal dissolvido.
- Arquivos alterados: `V7__add_couple_dissolved_at.sql` (novo),
  `CoupleAlreadyDissolvedException.java` (novo), `CoupleTest.java` (novo), `Couple.java`,
  `CoupleRepository.java`, `CoupleService.java`, `CoupleRepositoryTest.java`,
  `CoupleServiceTest.java`, `FlywayMigrationTest.java`, `docs/FLYWAY.md`, `api/CLAUDE.md`,
  `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Verificacao: `./mvnw test` -> **431 testes, 0 falhas, 0 erros**; JaCoCo `check` verde
  ("All coverage checks have been met").
  **PENDENTE DO LADO HUMANO:** o `FlywayMigrationTest` rodou contra **H2 em
  MODE=PostgreSQL**, nao contra Postgres real — `DB_URL` nao existe neste sandbox e o
  arranjo cai para H2 em silencio. Quem exerce a V7 contra `postgres:17-alpine` de
  verdade e o CI (`.github/workflows/ci.yml`). E, no deploy que carregar a V7: connection
  string direta (5432) ou pooler em modo Session, mais o snapshot manual do Supabase
  (D15: nao ha backup gerenciado).
- **Learnings for future iterations:**
  - `dissolve()` faz DUAS coisas (marca o instante e limpa o convite) porque os dois
    caminhos de resolucao de casal sao independentes: `findActiveByUserId` parte do
    usuario, `findActiveByInviteCode` parte do codigo. Limpar sozinho nao cobre linha
    legada; filtrar sozinho nao cobre um `dissolve()` futuro fora do servico. Cinto e
    suspensorio, os dois de proposito.
  - Remover um metodo de repositorio e mais barato do que parece: o compilador aponta o
    caller de producao na hora, e os testes quebram por `when(...)` de um metodo que nao
    existe mais. Foi assim com `findByInviteCode` (1 caller de producao, 11 stubs de
    teste) e sera assim com `findByUser1IdOrUser2Id` na US-002 (4 callers).
  - `CoupleServiceTest` nao precisou de nenhum teste novo nesta story porque
    `joinCouple` so trocou de consulta, sem mudar de comportamento — a cobertura do
    convite de casal dissolvido no nivel de servico/controller e AC da US-004, junto com
    o endpoint que dissolve.
---

## 2026-08-06 - US-002 (Porta CoupleFacade - so UUID cruza a fronteira)
- `CoupleFacade` (novo, `com.app.couple`): `findActiveCoupleId`, `requireActiveCoupleId`
  (404 via `com.app.common.ResourceNotFoundException`, E9.16), `memberIds(coupleId)` e
  `dissolveIfActive(userId)`. Fachada FINA sobre o `CoupleService` — todo metodo delega,
  nenhum reimplementa consulta. Nenhuma assinatura aceita/devolve `Couple` (E9.4).
- `Couple.memberIds()` (novo metodo de dominio): user1 + user2, pulando o `user2Id` nulo.
  A semantica que hoje vive em `MediaTrackMapper.memberIds(Couple)` desceu para a
  entidade, que e quem sabe "quem sao os dois" — a US-003 apaga a estatica do mapper.
- `CoupleService`: `memberIds(UUID coupleId)` (por `findById`, SEM filtro de dissolucao —
  ver learning abaixo) e `dissolveIfActive(UUID) -> Optional<UUID>` (id do casal
  dissolvido, vazio se nao havia; o `Optional` e para a US-004/US-007 auditarem).
  Os 4 callers de `findByUser1IdOrUser2Id` (`createCouple`, `getCurrentCouple`,
  `joinCouple`, `regenerateInviteCode`) migrados para `findActiveByUserId`.
- `CoupleRepository`: **`findByUser1IdOrUser2Id` removido**. `grep -rn
  "findByUser1IdOrUser2Id" api/src` -> VAZIO (inclusive nos comentarios: o javadoc do
  `getCurrentCouple` cita "a derivada sem filtro" em vez do nome, senao o grep de aceite
  nao fecha).
- Testes: `CoupleFacadeTest` (novo, 10 testes — casal ativo, sem casal, dissolvido,
  require nos dois caminhos, memberIds com/sem parceiro, memberIds de coupleId
  inexistente, dissolveIfActive dissolvendo e o no-op). Monta o `CoupleService` REAL
  sobre `CoupleRepository` mockado, nao um mock de servico: o que precisa ser provado e
  que a porta HERDA o filtro, e mock de servico provaria so o proprio stub.
  `CoupleDestinationAuthorizationManagerTest` ganhou
  `deniesExMemberSubscribingToTheTopicOfADissolvedCouple` com o mesmo arranjo (servico
  real + repositorio mockado) — o componente nao mudou uma linha (E9.19).
  `CoupleServiceTest` (16 stubs) e `CoupleRepositoryTest` (1 teste) migrados de consulta.
- `api/CLAUDE.md`: secao nova sobre a porta `CoupleFacade`.
- Arquivos alterados: `CoupleFacade.java` (novo), `CoupleFacadeTest.java` (novo),
  `Couple.java`, `CoupleService.java`, `CoupleRepository.java`, `CoupleServiceTest.java`,
  `CoupleRepositoryTest.java`, `CoupleDestinationAuthorizationManagerTest.java`,
  `api/CLAUDE.md`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Verificacao: `./mvnw test` -> **442 testes, 0 falhas, 0 erros**; JaCoCo `check` verde
  ("All coverage checks have been met"). Os dois greps objetivos do AC fecham.
- **Learnings for future iterations:**
  - `memberIds(coupleId)` NAO filtra casal dissolvido, e isso e a decisao, nao um
    esquecimento: ele parte do `couple_id` de um track/notificacao, que sobrevive a
    dissolucao (D13), e o historico precisa continuar mostrando os nomes dos dois. O
    filtro mora no caminho que parte do USUARIO (`findActiveCoupleId`), que e quem
    autoriza a operacao. Se a US-003 filtrasse aqui, o historico ficaria sem nomes.
  - Quando um AC pede `grep X` VAZIO, o comentario/javadoc conta. Reescrever a mencao em
    vez de deixar o nome literal — ou o criterio de aceite falha por documentacao.
  - Para provar que um componente "herda" um filtro sem ganhar codigo, mock de servico
    nao serve (o stub prova a si mesmo). O arranjo que prova e servico REAL + repositorio
    mockado: a cadeia `getCurrentCouple -> findActiveByUserId` fica exercitada de
    verdade, e o `CoupleRepositoryTest` (@DataJpaTest) e quem prova o filtro no SQL.
  - `dissolveIfActive` devolve `Optional<UUID>` no servico e `void` na porta de proposito:
    o AC exige `void` na porta, mas a US-004 vai querer o id do casal para o
    `SecurityAuditLogger`. O log de auditoria de dissolucao foi deixado para a US-004,
    junto com o endpoint — nao esquecer la.
---
