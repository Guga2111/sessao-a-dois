# Ralph Progress Log
Started: Wed Aug  5 17:17:20 PDT 2026
---

## Codebase Patterns
- `client/`: typecheck script must use `tsc -b` (project references), not `tsc --noEmit` (see client/CLAUDE.md) — already correctly configured, don't "fix" it.
- `client/`: when extracting a hook/component that's used from more than its original call sites, move it verbatim first (no behavior tweaks) and update client/CLAUDE.md's existing description of it afterward — the compare-selection pattern doc in client/CLAUDE.md said "module-scope in the same file, not its own file — only 2 call sites"; that comment is now stale once HubScreen becomes a third consumer in US-003, needs updating then.
- `client/`: `eslint-plugin-react-refresh`'s `react-refresh/only-export-components` rule forbids a single `.tsx` file from exporting both React components and plain functions/constants. When splitting a screen into a `screens/<name>/` directory, put shared components in one file (e.g. `compareUi.tsx`) and shared plain helpers/constants in a separate `.ts` file (e.g. `helpers.ts`) — a single combined `shared.tsx` fails lint.

## 2026-08-05 - US-001
- Extracted `useCompareSelection<T>` out of `client/src/screens/MatchScreen.tsx` into its own file `client/src/lib/useCompareSelection.ts`, exported, and imported back into MatchScreen.tsx for both call sites (SuggestionsTab with PendingMatch, SearchTab with MediaSearchResult).
- Files changed: client/src/lib/useCompareSelection.ts (new), client/src/screens/MatchScreen.tsx (removed the hook body, added one import line).
- Verified pure refactor: `git diff client/src/screens/MatchScreen.tsx` shows only 1 added line (the import) and 104 removed lines (the hook definition) — no className/style values touched.
- bun run typecheck / lint / build all pass.
- **Learnings for future iterations:**
  - The hook's internal setTimeout(...,0)-wrapped fetch effect and the Escape-key listener were copied byte-for-byte — do not "clean up" this pattern, it's a documented lint workaround (react-hooks/set-state-in-effect) per client/CLAUDE.md.
  - No browser available in this sandbox (no Chromium/Playwright, no sudo) — verification for pure-refactor stories is typecheck+lint+build+git diff review, not a rendered screenshot.
  - client/CLAUDE.md's "Selection mode on an existing list/card screen" section still describes useCompareSelection as living inline in MatchScreen.tsx with "only 2 call sites" — this is now inaccurate now that it's in lib/, and will be more inaccurate once HubScreen (US-003) becomes a 3rd consumer. Left as-is for US-003 to update, since that story is the one that actually adds the 3rd call site.
---

## 2026-08-05 - US-002
- Created `client/src/lib/comparisonItem.ts` exporting a single `buildComparisonItem(mediaType, tmdbId, overrides?: Partial<ComparisonItem>)` that fetches `/api/media/{type}/{id}` and spreads `overrides` last (so overrides win over the endpoint response, matching the old SearchTab preference behavior).
- Replaced the three copies:
  - `MatchScreen.tsx`'s `buildComparisonItemFromDetails` deleted outright; its only caller (`buildComparisonItemFromPending`) also deleted — the `SuggestionsTab` call site now passes an inline `(item) => buildComparisonItem(item.mediaType, item.tmdbId)` directly to `useCompareSelection`.
  - `MatchScreen.tsx`'s `buildComparisonItemFromSearchResult` now delegates to `buildComparisonItem(result.mediaType, result.tmdbId, { title, year, posterUrl, overview, voteAverage })` — deliberately omits `genres`/`watchProviders` from overrides so the endpoint's values still win for those two fields, same as before.
  - `HubScreen.tsx`'s `buildComparisonItem(track)` now computes `coupleRating` from `track.reviews` first, then calls the shared `buildComparisonItemBase(track.mediaType, track.tmdbId, { coupleRating })` (imported under an alias to avoid shadowing the local wrapper of the same name).
- Files changed: `client/src/lib/comparisonItem.ts` (new), `client/src/screens/MatchScreen.tsx`, `client/src/screens/HubScreen.tsx`.
- Removed now-unused imports: `MediaDetails` type from `MatchScreen.tsx` and from `HubScreen.tsx` (both screens' own `api.get(/api/media/{type}/{id})` call moved into the shared file). `HubScreen.tsx` still imports `api` because it separately calls `/api/tracking` for section pages — that import stayed.
- Verified: `grep -rn "/api/media/" client/src` before/after has the identical set of endpoint call sites (same 5 distinct string literals: `/api/media/${...}/${...}` for details, `/api/media/genres`, `/api/media/search`, `/api/media/trending`, `/api/media/discover`) — the two duplicate detail-fetch literals in MatchScreen.tsx and the one in HubScreen.tsx collapsed into the single one now in `comparisonItem.ts`, nothing added or removed.
- `git diff` of both screens has zero `className`/`style` value changes — pure refactor confirmed by grep.
- bun run typecheck / lint / build all pass.
- **Learnings for future iterations:**
  - When a shared helper duplicates a local function name a screen already has (here: both `HubScreen.tsx` and the new `lib/comparisonItem.ts` want to export something named `buildComparisonItem`), import the shared one under an alias (`import { buildComparisonItem as buildComparisonItemBase } from "@/lib/comparisonItem"`) rather than renaming the screen's existing wrapper — keeps the screen's own call sites (`useCompareSelection<MediaTrackResponse>(buildComparisonItem, ...)` etc., touched in US-003) unchanged.
  - `overrides` spread *after* the endpoint-derived object is the whole trick for "search result values win when present, endpoint wins when absent" — since `Object.assign`/spread semantics mean an `overrides` key of `undefined` would still overwrite a real value, callers must only include keys they actually want to win (e.g. `buildComparisonItemFromSearchResult` deliberately does NOT pass `genres`/`watchProviders` in its overrides object, rather than passing them as `undefined`).
---

## 2026-08-05 - US-003
- `HubScreen.tsx` now calls `useCompareSelection<MediaTrackResponse>(buildComparisonItem, (track) => track.id)` from `@/lib/useCompareSelection` instead of declaring its own 6 comparison useState values (`compareMode`, `selectedForCompare`, `compareLeft`, `compareRight`, `compareLoading`, `compareError`) and the two effects that backed them (the `Promise.all` detail-fetch effect and the Escape-key listener) — both now live inside the shared hook.
- The local `buildComparisonItem(track: MediaTrackResponse): Promise<ComparisonItem>` wrapper (computing `coupleRating` from `track.reviews`, then delegating to `@/lib/comparisonItem`'s `buildComparisonItem`, imported there as `buildComparisonItemBase`) was kept as-is — its signature already matched the hook's `buildItem` param, so it's passed directly.
- Removed the screen-local `exitCompareMode`/`clearCompareSelection`/`retryCompareFetch`/`toggleCompareSelection` callbacks; JSX now calls `compare.exitCompareMode`/`compare.clearSelection`/`compare.retry`/`compare.toggle` (the hook's returned names differ slightly from the old local ones — `clearCompareSelection` -> `clearSelection`, `retryCompareFetch` -> `retry`, `toggleCompareSelection` -> `toggle`, and there's a new `compare.enterCompareMode` where the screen used to inline `setCompareMode(true)`).
- Files changed: `client/src/screens/HubScreen.tsx` (only file with behavior-affecting changes), `client/CLAUDE.md` (updated the stale "only 2 call sites" note left from US-001/US-002 to reflect HubScreen as the 3rd `useCompareSelection` consumer, and fixed a leftover `selectedForCompare`/`setSelectedForCompare` reference in the retry-pattern paragraph to the hook's actual `selected`/`setSelected`/`retry` names).
- Verified: `grep -c` on `useState(` in `HubScreen.tsx` returns 7 (13 before the epic, minus the 6 comparison ones) as required. `git diff` review + `grep -iE "className|style="` on the diff hunks returns nothing — pure refactor, no visual value touched. `MediaCard` still receives the same 4 compare props (`compareMode`/`compareSelected`/`compareOrder`/`onCompareToggle`) with identical semantics, just sourced from `compare.*` instead of local state.
- `bun run typecheck` / `bun run lint` / `bun run build` all pass.
- **Learnings for future iterations:**
  - `CompareToggleButton`/`CompareSelectionChip` are still local (unexported) function components inside `MatchScreen.tsx` (lines ~173, ~210) — they were NOT extracted to a shared location in this story. `HubScreen.tsx` keeps its own inline JSX for the toggle button and the bottom-sheet selection chip; only the *data* (compare mode flag, selection, error, dialog state) is shared via the hook now, not the JSX. US-005 (`screens/match/` decomposition) is the story that's supposed to move those two components out of `MatchScreen.tsx` into a shared location — when that happens, HubScreen's inline JSX could then be swapped for the shared components too, but that's out of scope for what US-005's acceptance criteria currently describe (it only says "moved... and consumed by the two tabs" of MatchScreen, not HubScreen) — worth flagging to the user if HubScreen's duplicate JSX for the same chip/button should also be collapsed at that point.
  - The hook's returned method names don't 1:1 match the old ad-hoc names screens invented before extraction (`clearSelection` not `clearCompareSelection`, `retry` not `retryCompareFetch`, `toggle` not `toggleCompareSelection`, plus a new explicit `enterCompareMode` instead of inlining `setCompareMode(true)`) — when wiring a new consumer to `useCompareSelection`, don't assume the old MatchScreen/HubScreen local names, use the hook's actual return shape (`compareMode, enterCompareMode, exitCompareMode, clearSelection, retry, toggle, selected, left, right, error, dialogOpen`).
---

## 2026-08-05 - US-004
- Created `client/src/screens/match/useDiscoverSearch.ts`: a `useReducer`-backed hook that owns 17 of `SearchTab`'s 19 `useState` values (everything except `trackedKeys`/`likeStates`, which stay local to `SearchTab` per the PRD's decision E7 — they're match/like state, not search state). Single flat `State` object with fields grouped by comment into query, filtros (`sortBy`, `filtersOpen`, `genres`, `selectedDecades`, `selectedCertifications`, `selectedGenres`, `voteRange`, `runtimeRange`), resultado (`results`, `totalResults`, `resultsContext`, `searched`), paginacao (`page`, `totalPages`) and status de rede (`searching`, `fetchError`); a discriminated `Action` union + `reducer` covers all transitions (`QUERY_CHANGED` replicates the old inline "clear results+searched when query goes empty" behavior that used to live in the `<input onChange>`, `FETCH_STARTED`/`FETCH_SUCCEEDED`/`FETCH_FAILED`/`FETCH_SETTLED` replace the four separate `setX` calls `runFetch`'s promise chain used to make).
- `SORT_OPTIONS` and `TMDB_MAX_PAGE` (both needed by `MatchScreen.tsx`'s JSX, not just the hook's internal logic) are now exported from the hook file instead of duplicated; `DEFAULT_VOTE_RANGE`/`DEFAULT_RUNTIME_RANGE` moved in as hook-private since nothing outside the hook referenced them directly.
- `lastFetchRef`/`lastAttemptRef` (pagination replay pattern) and the `setTimeout(...,0)`-wrapped debounced fetch effect were preserved literally inside the hook, same as the initial genres-load effect (moved in since `genres` is part of the filtros slice) — `trackedKeys`' own fetch effect stayed behind in `SearchTab` since that state didn't move.
- `MatchScreen.tsx`'s `SearchTab` now calls `const search = useDiscoverSearch()` and destructures it; local state is down to just `trackedKeys`/`likeStates`. `runFetch`/`FetchAttempt` type/`handleSortChange`/`handleApplyFilters`/`handlePageChange`/`handleClearFilters`/`handleRetry`/`activeFilterCount`/`hasQuery` all deleted from the screen, now sourced from the hook.
- Files changed: `client/src/screens/match/useDiscoverSearch.ts` (new), `client/src/screens/MatchScreen.tsx` (removed ~200 lines of state/effects/handlers, added the hook import + destructure + 1 leftover effect).
- Verified: `bun run typecheck`/`lint`/`build` all pass. `grep -c "useState(" MatchScreen.tsx` shows only the pre-existing `SuggestionsTab` (4) + `SearchTab`'s 2 remaining (`trackedKeys`, `likeStates`) + `MatchScreen`'s own `activeTab` = same call sites as before, just fewer per-component. `grep -rn "/api/media/" client/src` shows the identical set of endpoint literals as before (relocated into the hook file, none added/removed). `git diff` on `MatchScreen.tsx` has zero className/style value changes.
- **Learnings for future iterations:**
  - This story does NOT move any component to a new file (that's US-005/US-006) — `useDiscoverSearch.ts` is the only new file, and it lives in `screens/match/` (not `lib/`) per decision E5 since it has exactly one consumer.
  - The old `<input onChange>` for the search box used to call `setQuery` AND conditionally `setResults([])`/`setSearched(false)` inline as two separate state updates — when that state moved into the reducer, this collapsed into the `QUERY_CHANGED` action itself doing the conditional reset, so the JSX callback is now just `onChange={(event) => setQuery(event.target.value)}`. When grouping ad-hoc multi-`setState` UI event handlers into a reducer, check for this pattern (one DOM event driving 2+ independent `setState` calls) and fold them into a single action rather than keeping them split.
  - `useDiscoverSearch()` returns a flat object (not nested per-slice) even though the state groups into 4 conceptual slices — this was a deliberate simplification since the acceptance criteria's minimum required shape (`{query, results, page, totalPages, totalResults, searching, searched, fetchError, resultsContext, runFetch}`) is flat, and a flat destructure at the `SearchTab` call site keeps the diff mechanical (`const [query, setQuery] = useState(...)` → `const { query, setQuery } = search` for pretty much every line).
---

## 2026-08-05 - US-005
- Created `client/src/screens/match/` with the shell + `SuggestionsTab` (this story is part 1 of 2 of the PRD's US-005 — prd.json splits it, part 2 is prd.json's US-006 which decomposes `SearchTab`): `MatchScreen.tsx` (72 lines — tab-switcher shell, moved from `MatchScreen.tsx:1115-1175` verbatim, only `export` added), `SuggestionsTab.tsx` (316 lines, 4 `useState`, moved from `MatchScreen.tsx:237-533` verbatim), `helpers.ts` (`trackKey`, `paginationRange`, `formatResultsCount`, `formatVoteRangeLabel`, `formatMinutes`, `formatRuntimeRangeLabel`, `keyOfCompareItem`, `TYPE_LABEL` — moved verbatim from `MatchScreen.tsx:67-142`), and `compareUi.tsx` (`CompareToggleButton`/`CompareSelectionChip` — moved verbatim from `MatchScreen.tsx:156-235`).
- `SearchTab` was NOT moved to `screens/match/` in this story (that's US-006/prd.json's part 2) — it stays in the old root file `client/src/screens/MatchScreen.tsx`, which now only exports `SearchTab` (679 lines, down from 1175; imports the shared helpers/components from `./match/helpers` and `./match/compareUi`) instead of the whole screen. The new shell imports it via `import { SearchTab } from "../MatchScreen"`.
- `client/src/App.tsx` now imports `MatchScreen` from `@/screens/match/MatchScreen` instead of `@/screens/MatchScreen`.
- Files changed: `client/src/screens/match/MatchScreen.tsx` (new), `client/src/screens/match/SuggestionsTab.tsx` (new), `client/src/screens/match/helpers.ts` (new), `client/src/screens/match/compareUi.tsx` (new), `client/src/screens/MatchScreen.tsx` (trimmed to just `SearchTab` + its own imports/types/constants), `client/src/App.tsx` (import path), `client/CLAUDE.md` (documented the interim split and the `SearchTab` still being in the old root file, to be finished by US-006).
- Verified: every moved block diffed byte-for-byte against the pre-story file content (via `git show HEAD:client/src/screens/MatchScreen.tsx`) with only `export` keywords added — zero JSX/className/style changes. `grep -rn "/api/media/"` shows the identical set of endpoint literals as before (none added/removed, none in the moved files since API calls live in `useDiscoverSearch.ts`/`comparisonItem.ts`, untouched). `bun run typecheck`/`lint`/`build` all pass.
- **Learnings for future iterations:**
  - First attempt combined helpers + `CompareToggleButton`/`CompareSelectionChip` into one `shared.tsx` file — failed lint with 8 `react-refresh/only-export-components` errors (one per non-component export). Split into `helpers.ts` (plain functions/consts) and `compareUi.tsx` (components only) fixed it immediately. Added to Codebase Patterns at the top of this file since this will recur in US-006 (`FiltersPanel.tsx`/`SearchResultCard.tsx` will need the same helpers) and US-007 (`screens/hub/`).
  - `MatchScreen.tsx` (old root file) is now a **misleading name** — it only contains `SearchTab`. This is intentional and temporary per prd.json's US-005 acceptance criteria ("SearchTab continua funcionando - nesta story ele pode permanecer temporariamente no arquivo antigo"); US-006 is the story that deletes this file for good after moving `SearchTab` (further split into `SearchTab.tsx`/`FiltersPanel.tsx`/`SearchResultCard.tsx`) into `screens/match/`.
  - Verifying "pure move" without a browser: `git show HEAD:<path> > /tmp/orig.tsx` then `diff <(sed -n 'START,ENDp' /tmp/orig.tsx) <(sed -n '/^export function Name/,/^}/p' newfile.tsx)` catches accidental content changes during a move — the only diff line should be the added `export` keyword. Faster and more precise than eyeballing a large paste.
---

## 2026-08-05 - US-006
- Decomposed `SearchTab` out of the interim root file `client/src/screens/MatchScreen.tsx` (679 lines, US-005 leftover) into `client/src/screens/match/`: `SearchTab.tsx` (354 lines, orchestration — `useDiscoverSearch`, `useCompareSelection`, local `trackedKeys`/`likeStates` state, `handleLike`, the derived `status` switch), `FiltersPanel.tsx` (301 lines, presentational — the search box/sort-select/filters `Collapsible`, all state passed as props), `SearchResultCard.tsx` (156 lines, presentational — one result card). The old root file is deleted; `client/src/screens/MatchScreen.tsx` no longer exists.
- Converted the 4 mutually-exclusive message conditionals (idle/"no skeleton yet", loading skeleton, error, empty) into a derived `status: 'idle' | 'loading' | 'error' | 'empty' | 'ready'` rendered via a `switch` in `SearchTab.tsx`. Kept the "Resultados para..." header block and the results grid **outside** the switch, unconditionally rendered with their original guards (`searched && !fetchError` for the header; `cn(..., showSearchSkeleton && "hidden")` for the grid) — they were never part of the original 4-block conditional chain, only the message region between them was. `ready` renders `null` in the switch since its "content" is the always-mounted grid below, matching pre-refactor behavior exactly (today's code has no explicit "ready" block either).
- `LikeState` type moved from a local declaration in the old root file into `client/src/screens/match/helpers.ts` (plain `.ts`, safe to export types+functions together) since both `SearchTab.tsx` and `SearchResultCard.tsx` need it now.
- Files changed: `client/src/screens/match/SearchTab.tsx` (new), `client/src/screens/match/FiltersPanel.tsx` (new), `client/src/screens/match/SearchResultCard.tsx` (new), `client/src/screens/match/helpers.ts` (added `LikeState` export), `client/src/screens/match/MatchScreen.tsx` (import path `../MatchScreen` -> `./SearchTab`), `client/src/screens/MatchScreen.tsx` (deleted), `client/CLAUDE.md` (replaced the "in progress" note with the finished-state description + the status-switch gotcha).
- Verified pure refactor without a browser: `git show HEAD:client/src/screens/MatchScreen.tsx` diffed against the three new files' concatenated content — `grep -oE 'className=...'` sorted-diff and `grep -oE 'style={{...}}'` sorted-diff both exit 0 (identical sets), and all 4 status message strings (`grep -n` for each) match verbatim between old and new. `bun run typecheck`/`lint`/`build` all pass. Line counts: all `screens/match/` files under 400 lines (largest is `useDiscoverSearch.ts` at 380, followed by `SearchTab.tsx` at 354). `useState` counts: `SearchTab.tsx` has 2 (`trackedKeys`, `likeStates`), `FiltersPanel.tsx`/`SearchResultCard.tsx` have 0 (presentational) — all well under the 8 cap.
- **Learnings for future iterations:**
  - When converting a conditional-JSX chain to a `status` + `switch`, first identify precisely which blocks were part of the original mutually-exclusive chain (here: 4 message blocks with disjoint boolean guards) vs. adjacent blocks that only *look* related but have independent guards (here: the results header and the grid, which render based on different conditions and were never mutually exclusive with the message chain — e.g. the header can render at the same time as the loading skeleton, since `searched` stays `true` across a page-change refetch). Folding an independent block into the switch changes behavior even though it "reads" like it belongs there.
  - `grep -oE 'className=\{?"[^"]*"|className="[^"]*"'` + sort + diff is a fast, precise way to verify zero className drift across a multi-file move, faster than eyeballing — reusable for `style={{...}}` too with a matching regex. Works well as a final gate before declaring a decomposition story "pixel-identical."
---

## 2026-08-05 - US-007
- Decomposed `client/src/screens/HubScreen.tsx` (523 lines, 7 `useState`, one collapsible section repeated 3x inline) into `client/src/screens/hub/`: `HubScreen.tsx` (336 lines — shell: all 7 `useState`, the `fetchSectionPage`/`reloadAllFirstPages`/`handleLoadMore` callbacks, the 5 modals, the compare bottom bar, `ComparisonDialog`), `TrackSection.tsx` (147 lines, 0 `useState` — one section's header/count/chevron/skeleton/empty/grid/load-more, fully presentational, takes the section's `SectionState` slice + every callback as props), `LoadMoreSentinel.tsx` (46 lines, moved verbatim — the IntersectionObserver sentinel), `helpers.ts` (68 lines — `SECTIONS`, `Section`, `PAGE_SIZE`, `SectionState`, `emptySectionState`/`emptySections`, `buildComparisonItem`, `COMPARE_TOOLTIP`).
- The one intentional non-pure change (per the story's own acceptance criteria): `fetchSectionPage`'s `.catch()` in `HubScreen.tsx` now does `console.error(`Falha ao carregar a seção ${status} (página ${page})`, error)` before clearing the loading flags, instead of silently swallowing the error. Visual failure behavior (skeleton stops, whatever already loaded stays) is unchanged.
- Files changed: `client/src/screens/hub/HubScreen.tsx` (new), `client/src/screens/hub/TrackSection.tsx` (new), `client/src/screens/hub/LoadMoreSentinel.tsx` (new), `client/src/screens/hub/helpers.ts` (new), `client/src/screens/HubScreen.tsx` (deleted), `client/src/App.tsx` (import path `@/screens/HubScreen` -> `@/screens/hub/HubScreen`), `client/CLAUDE.md` (new `screens/hub/` section documenting the split, mirroring the existing `screens/match/` section).
- Verified pure refactor without a browser: `diff` of sorted `className=...` extractions and sorted `style={{...}}` extractions between `git show HEAD:client/src/screens/HubScreen.tsx` and the concatenated new files both exit 0 (identical sets, aside from the one intentional `console.error` line which isn't a className/style). `grep -oE '"/api/[a-zA-Z/]*"'` shows the identical single endpoint (`/api/tracking`) before and after. `bun run typecheck`/`lint`/`build` all pass. Line counts: all 4 files under 400 (largest is `HubScreen.tsx` at 336). `useState` counts: `HubScreen.tsx` has 7 (`sections`, `modalOpen`, `detailTrack`, `watchTrack`, `deleteTrack`, `reviewTrack`, `openSections`), `TrackSection.tsx`/`LoadMoreSentinel.tsx` have 0 — all well under the 8 cap.
- **Learnings for future iterations:**
  - `grep -c "useState("` (with a literal open-paren) undercounts when a component has typed `useState<T>(...)` calls — `useState<Record<...>>(...)` doesn't match `useState(` because of the `<...>` in between. Use `grep -c "useState"` (no paren) or `grep -n "useState<\|useState("` to get an accurate per-component count; this bit both the verification step here and would bite any future story that gates on the "8 useState max" acceptance criterion.
  - `git show HEAD:<path>` resolves the pathspec relative to the **repo root**, not the shell's `cwd` — even though bash `cwd` was already `client/` (persisted from an earlier command in the same tool-call session), `git show HEAD:src/screens/HubScreen.tsx` failed with "path exists, but not..." and suggested the repo-root-relative form (`git show HEAD:client/src/screens/HubScreen.tsx`). When diffing against `HEAD` from inside a subdirectory, always prefix the path from repo root, don't assume `git`'s pathspec resolution follows the shell's `cd`.
  - Passing the raw `compareSelected: T[]` array as a prop (rather than precomputing a boolean/index once in the parent) keeps a child component's per-item lookup logic (`.some(...)`/`.findIndex(...)`) byte-identical to what it replaced inline — cheaper to verify as a "pure move" than restructuring the lookup into a different shape at the same time.
---
