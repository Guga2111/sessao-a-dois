## Codebase Patterns
- Client is React + TS + Vite (Tailwind v4, CSS-based config, no tailwind.config.*), run checks with `cd client && npx tsc --noEmit` and `npx eslint <files>`.
- Dark theme colors/spacing are inlined via Tailwind arbitrary values (e.g. `bg-[#09090a]`, `text-[#ffcb2b]`) — match this pattern, don't introduce new tokens. `docs/design/claude-design-project/Sessao a Dois.dc.html` is the source of truth for exact colors/spacing.
- The project's eslint config uses `eslint-plugin-react-hooks` recommended (React Compiler-era) rules: calling `setState` synchronously inside a `useEffect` body is a lint ERROR (`react-hooks/set-state-in-effect`), and reading/writing a ref's `.current` during render is also a lint ERROR (`react-hooks/refs`). To reset a subtree's local state on route change (e.g. closing a mobile menu on navigation), extract the stateful part into its own subcomponent and mount it with `key={location.pathname}` from the parent — this remounts the subcomponent and resets its `useState` without touching effects or refs.
- No browser MCP/testing tool is available in this sandbox (checked via ToolSearch) — UI verification for these stories is limited to `tsc`/`eslint` plus manually inspecting the dev server output; flag "manual browser verification needed" in reports.

## 2026-07-23 - US-007
- Implemented: mobile responsive audit + fix for DashboardScreen. Only real gap found: the KPI card grid used `grid-cols-[repeat(auto-fit,minmax(230px,1fr))]`, which never overflows horizontally but starts showing 2 columns from ~476px wide, below the project's 768px mobile breakpoint — didn't match the AC's literal "single column below 768px" wording. Changed to `grid-cols-1 md:grid-cols-2 lg:grid-cols-4` (using `md`=768px, consistent with the project's existing mobile threshold from `useIsMobile`). Everything else already passed inspection: the charts row (`grid-cols-1 lg:grid-cols-[1.6fr_1fr]`) and `GenreBarsChart` were already single-column/full-width below `lg`; `MonthlyBarsChart`'s flex bar row and `MovieTvDonutChart`'s fixed 130px donut + legend were checked by hand against a 375px viewport (accounting for `main`'s `px-5` and `ChartCard`'s `p-6` padding) and don't cause horizontal overflow.
- Files changed: `client/src/screens/DashboardScreen.tsx`.
- **Learnings for future iterations:**
  - `auto-fit`/`minmax(Npx,1fr)` grids can satisfy "no horizontal overflow" while still not matching a literal "single column below Xpx" acceptance criterion if `N` is small enough relative to the container — check the actual column-count-per-width math, not just whether it overflows, when an AC specifies an exact breakpoint behavior.
  - `tsc --noEmit` and `eslint` both pass clean; `npm run dev` boots fine (smoke-tested). No browser tooling available in this sandbox — visual verification at 375px viewport is manual-only.
---

## 2026-07-23 - US-006
- Implemented: mobile responsive audit + fix for MatchScreen. Inspected `SuggestionsTab`, `SearchTab`, and the outer `MatchScreen` layout for fixed-width/overflow risks at 375px. Only one element didn't already satisfy the acceptance criteria as literally stated: the suggestion swipe-card wrapper had a fixed `max-w-[280px]`, which never overflows but doesn't match the AC's explicit `calc(100vw - 32px)` formula — changed to `w-full max-w-[calc(100vw-32px)] sm:max-w-[280px]` (same calc pattern used for modals in US-004). Everything else already passed inspection without changes: action buttons (`Passar`/`Curtir`, `size-14` = 56px circles with `gap-6` = 24px) are already touch-adequate and fit well inside a 335px content area (main uses `px-5 sm:px-8`, unchanged since AC didn't call it out like it did for HubScreen in US-005); the search results grid (`grid-cols-[repeat(auto-fill,minmax(250px,1fr))]`) and search input already collapse to a single column with no horizontal scroll at 375px.
- Files changed: `client/src/screens/MatchScreen.tsx`.
- **Learnings for future iterations:**
  - Not every "responsive" story requires code changes across the whole screen — inspect first, then fix only what's actually broken or doesn't match the AC's literal wording. Most of MatchScreen's layout (flex/grid with `auto-fill`/`minmax`, `w-fit`, `clamp()` for the heading) was already responsive by construction from how it was originally built.
  - `tsc --noEmit` and `eslint` both pass clean. No browser tooling available in this sandbox — visual verification at 375px viewport is manual-only.
---

## 2026-07-23 - US-005
- Implemented: mobile padding/sizing fixes on the HubScreen header row and FAB. `<main>` padding changed `px-5` -> `px-4` on mobile (`sm:px-8` untouched). "Adicionar Título" `Button` is now `w-full sm:w-auto` with `justify-center` added so its icon+label stay centered when full-width. FAB (`+` button, `fixed bottom-8`) mobile right offset changed `right-5` -> `right-4` (`sm:right-11` untouched). Added `min-w-0` to the title/subtitle wrapper `<div>` inside the `flex flex-wrap` header row so it can shrink instead of forcing overflow at 375px (the row already wraps via `flex-wrap`, this was a defensive fix for the flex `min-width:auto` default).
- Files changed: `client/src/screens/HubScreen.tsx`.
- **Learnings for future iterations:**
  - This story was a small, targeted Tailwind-class change on an already-established screen/design system (dark theme, `#ffcb2b` accent, existing HubScreen layout from US-002/US-003) — no new visual surface was introduced, so the `frontend-design` skill's brainstorm output was intentionally kept minimal and changes just matched existing conventions exactly rather than introducing new tokens/patterns.
  - `tsc --noEmit` and `eslint` both pass clean. No browser tooling available in this sandbox — visual verification at 375px viewport is manual-only.
---

## 2026-07-23 - US-003
- Implemented: auto-pagination for the mobile Hub carousel from US-002. Added `LoadMoreSentinel` component in `HubScreen.tsx` — a 1px-wide invisible flex child rendered as the last item of a section's carousel, only when `useIsMobile()` is true and that section `hasMore`. It uses a callback ref to create one `IntersectionObserver` (threshold 0.5) per mount, disconnecting/recreating only if `status` or `onLoadMore` identity changes (both stable). The observer callback reads `hasMore`/`loadingMore` off a ref that's kept in sync via a plain `useEffect` (no deps array, no setState) — this avoids recreating the observer on every data change while still seeing fresh flags, and avoids the `react-hooks/refs` lint error (writing `ref.current` happens in an effect, never during render). Added an extra `SkeletonCard` at the end of the items list while `loadingMore` (works for both the mobile auto-load path and the desktop button path). The existing "Carregar mais" button is now `hidden md:flex` (was unconditional `flex`) so it only shows on desktop.
- Also had to make `handleLoadMore` a stable `useCallback`: previously it read `sections[status]` directly from the render closure, so a new function was created every render, which would have torn down and recreated the sentinel's IntersectionObserver on every HubScreen re-render. Added a `sectionsRef` synced via `useEffect(() => { sectionsRef.current = sections }, [sections])` and made `handleLoadMore` read `sectionsRef.current[status]` instead, with `[fetchSectionPage]` as its only dependency.
- Files changed: `client/src/screens/HubScreen.tsx`.
- **Learnings for future iterations:**
  - "Latest ref" pattern (a ref that mirrors fast-changing state/props for use inside a stable callback or observer) must sync the ref inside a `useEffect`, never inline in the render body — writing `ref.current` during render is a lint error here (`react-hooks/refs`), same as the earlier `set-state-in-effect` gotcha for setState. The effect itself has no dependency array (runs after every render) and is fine since it never calls setState.
  - When wiring an `IntersectionObserver` (or any DOM observer) to a list item that needs access to frequently-changing flags, prefer a small dedicated child component (like `LoadMoreSentinel` here) over inlining the hook logic in a `.map()` — hooks can't be called per-iteration inside a loop/map body directly.
  - `tsc --noEmit` and `eslint` both pass clean; `npm run dev` boots fine (smoke-tested). No browser tooling available in this sandbox — visual/interaction verification of the auto-load-on-scroll behavior is manual-only.
---

## 2026-07-23 - US-002
- Implemented: mobile horizontal carousel for the three Hub sections (Assistindo Atualmente, Queremos Ver, Já Vimos). The items container in `HubScreen.tsx` uses one Tailwind class list toggled at the `md:` breakpoint instead of separate mobile/desktop branches: base (mobile) = `flex gap-5.5 overflow-x-auto pr-[20vw] [overscroll-behavior-x:contain] [scroll-snap-type:x_mandatory] no-scrollbar`; `md:` overrides restore the original `grid grid-cols-[repeat(auto-fill,minmax(250px,1fr))]` with scroll/snap disabled. Each card is now wrapped in a `<div>` (MediaCard has no `className` prop) with `min-w-[72vw] max-w-[72vw] shrink-0 [scroll-snap-align:start]`, neutralized at `md:`. Added a `.no-scrollbar` utility (`scrollbar-width: none` + `::-webkit-scrollbar{display:none}`) to `client/src/index.css` since Tailwind v4 has no built-in scrollbar-hiding utility. `SkeletonCard` got the same mobile sizing classes so the loading state lines up horizontally too. Collapsible open/close and the empty-state block were untouched (they wrap the carousel container, not the cards) so both keep working as before.
- Files changed: `client/src/screens/HubScreen.tsx`, `client/src/index.css`.
- **Learnings for future iterations:**
  - Tailwind v4 (this project) has no scrollbar-hiding utility class built in — added `.no-scrollbar` to `index.css`, reuse it for any other future horizontal-scroll UI instead of duplicating the CSS.
  - `MediaCard` (`client/src/components/MediaCard.tsx`) does not accept a `className` prop — to control a card's outer sizing (e.g. carousel width) wrap it in a plain `<div>` rather than trying to pass classes through.
  - Arbitrary Tailwind properties not covered by named utilities (e.g. `scroll-snap-align`, `overscroll-behavior-x`) work fine as bracket syntax like `[scroll-snap-align:start]` and can be breakpoint-prefixed (`md:[scroll-snap-align:none]`) same as any other utility.
  - US-003 (auto-pagination) will build directly on this carousel: the sentinel/IntersectionObserver needs to be appended as the last child inside the same `flex`/`grid` container introduced here, gated by `useIsMobile()` from `src/lib/useIsMobile.ts` (already created in US-001).
  - No browser tooling available in this sandbox (confirmed again) — `npm run dev` starts fine and was smoke-tested to boot cleanly, but visual verification is manual-only; noted here rather than skipped silently.
---

## 2026-07-23 - US-001
- Implemented: `src/lib/useIsMobile.ts` hook (matchMedia `(max-width: 767px)`, sync init via `useState(getIsMobile)`, `change` listener via `useEffect`). Updated `client/src/components/Header.tsx`: inline nav now `hidden md:flex`; added a `MobileNav` subcomponent (hamburger button with lucide `Menu`/`X` icon, `md:hidden`) rendered as `<MobileNav key={location.pathname} />` so its internal `menuOpen` state resets automatically on route change (avoids the react-hooks/set-state-in-effect and react-hooks/refs lint errors that a `useEffect`/`useRef` approach triggered). Dropdown is `absolute z-50`, right-aligned, lists the 3 NAV_ITEMS with the active item highlighted `#ffcb2b`, closes on item click via `onClick={() => setMenuOpen(false)}`. Name/day text block already had `hidden ... sm:block`, satisfying "hide on very small screens". Avatars remain visible.
- Files changed: `client/src/lib/useIsMobile.ts` (new), `client/src/components/Header.tsx`.
- `useIsMobile` hook was created per the acceptance criteria but Header itself doesn't need to consume it — the responsive switch is done purely with Tailwind `hidden md:flex` / `md:hidden` classes. Future stories (US-002, US-003) that need JS-level mobile detection (e.g. gating an IntersectionObserver) should import `useIsMobile` from `@/lib/useIsMobile`.
- `tsc --noEmit` and `eslint` both pass clean.
- **Learnings for future iterations:**
  - Don't use `useEffect(() => setState(...), [dep])` to reset UI state on prop/route change — the repo's eslint config forbids it. Prefer the `key`-remount pattern (extract to subcomponent, key by the changing value) or derive the value without state.
  - No browser tooling available in this sandbox — noted per story as "manual browser verification needed" rather than skipped silently.
---

## 2026-07-23 - US-004
- Implemented: mobile-responsive sizing for the 6 modals/dialogs. Two structural patterns exist in this codebase:
  1. shadcn/base-ui `Dialog`/`DialogContent` (only `TitleModal.tsx`) — width controlled via `className` passed to `DialogContent`, which already centers via `fixed top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2`.
  2. Custom `fixed inset-0` backdrop `<div>` with a plain inner `<div>` (all the others: `MediaDetailModal`, `PendingDetailModal`, `ReviewModal`, `WatchModal`, `DeleteTrackDialog`) — width controlled directly on that inner div's `className`.
  For both, changed the mobile-default width from a fixed `max-w-[Npx]` to `w-[calc(100vw-32px)] max-w-[calc(100vw-32px)]`, restoring the original fixed max-width at `sm:` (`sm:w-full sm:max-w-[520px|420px|900px]`). `MediaDetailModal`/`PendingDetailModal` already capped height via inline `style={{maxHeight, overflowY:"auto"}}` (changed `90dvh` → `90svh` to match the acceptance criteria's unit); `ReviewModal`/`WatchModal`/`DeleteTrackDialog` had no height cap at all previously, so added `max-h-[90svh] overflow-y-auto` to their inner div's className. `TitleModal`'s `DialogContent` kept `overflow-visible` (not `overflow-y-auto`) — it has a search-results dropdown absolutely positioned right under the search input, and clipping the outer container would risk cutting that dropdown off; the modal's actual scrolling already happens on an inner body wrapper (`max-h-[calc(90vh-280px)] overflow-y-auto`) below the (non-scrolling) header/search area, so capping+scrolling the outer DialogContent too was redundant on top of being risky for the dropdown. Still added `max-h-[90svh]` to the outer DialogContent as a hard ceiling.
- Files changed: `client/src/components/TitleModal.tsx`, `client/src/components/MediaDetailModal.tsx`, `client/src/components/PendingDetailModal.tsx`, `client/src/components/ReviewModal.tsx`, `client/src/components/WatchModal.tsx`, `client/src/components/DeleteTrackDialog.tsx`.
- **Learnings for future iterations:**
  - Not all "modals" in this codebase use the shadcn `Dialog` component — most (5 of 6) are hand-rolled `fixed inset-0` backdrop divs with their own click-outside/Escape-key handling. Check which pattern a given modal uses before assuming `DialogContent` className overrides apply.
  - `git stash` + re-run eslint is a fast way to confirm whether a lint failure is pre-existing vs. introduced by your change, without needing to remember/diff error output manually. Used here: `react-hooks/set-state-in-effect` errors in `ReviewModal.tsx`, `WatchModal.tsx`, `PendingDetailModal.tsx` (calling multiple `setState`s synchronously in a `useEffect` body when a prop goes null) are pre-existing on `main`/prior commits, not caused by this story's changes — confirmed via `git stash && npx eslint ... && git stash pop`. Left unfixed since it's out of scope for a responsiveness story; flagging here in case a future story wants to clean these up using the same `key`-remount pattern noted in US-001's learnings (Codebase Patterns section above).
  - `tsc --noEmit` passes clean; `npm run dev` boots fine (smoke-tested). No browser tooling available in this sandbox — visual verification of the 375px viewport behavior is manual-only.
---
