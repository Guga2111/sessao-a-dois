## Codebase Patterns
- Client is React + TS + Vite (Tailwind v4, CSS-based config, no tailwind.config.*), run checks with `cd client && npx tsc --noEmit` and `npx eslint <files>`.
- Dark theme colors/spacing are inlined via Tailwind arbitrary values (e.g. `bg-[#09090a]`, `text-[#ffcb2b]`) — match this pattern, don't introduce new tokens. `docs/design/claude-design-project/Sessao a Dois.dc.html` is the source of truth for exact colors/spacing.
- The project's eslint config uses `eslint-plugin-react-hooks` recommended (React Compiler-era) rules: calling `setState` synchronously inside a `useEffect` body is a lint ERROR (`react-hooks/set-state-in-effect`), and reading/writing a ref's `.current` during render is also a lint ERROR (`react-hooks/refs`). To reset a subtree's local state on route change (e.g. closing a mobile menu on navigation), extract the stateful part into its own subcomponent and mount it with `key={location.pathname}` from the parent — this remounts the subcomponent and resets its `useState` without touching effects or refs.
- No browser MCP/testing tool is available in this sandbox (checked via ToolSearch) — UI verification for these stories is limited to `tsc`/`eslint` plus manually inspecting the dev server output; flag "manual browser verification needed" in reports.

## 2026-07-23 - US-001
- Implemented: `src/lib/useIsMobile.ts` hook (matchMedia `(max-width: 767px)`, sync init via `useState(getIsMobile)`, `change` listener via `useEffect`). Updated `client/src/components/Header.tsx`: inline nav now `hidden md:flex`; added a `MobileNav` subcomponent (hamburger button with lucide `Menu`/`X` icon, `md:hidden`) rendered as `<MobileNav key={location.pathname} />` so its internal `menuOpen` state resets automatically on route change (avoids the react-hooks/set-state-in-effect and react-hooks/refs lint errors that a `useEffect`/`useRef` approach triggered). Dropdown is `absolute z-50`, right-aligned, lists the 3 NAV_ITEMS with the active item highlighted `#ffcb2b`, closes on item click via `onClick={() => setMenuOpen(false)}`. Name/day text block already had `hidden ... sm:block`, satisfying "hide on very small screens". Avatars remain visible.
- Files changed: `client/src/lib/useIsMobile.ts` (new), `client/src/components/Header.tsx`.
- `useIsMobile` hook was created per the acceptance criteria but Header itself doesn't need to consume it — the responsive switch is done purely with Tailwind `hidden md:flex` / `md:hidden` classes. Future stories (US-002, US-003) that need JS-level mobile detection (e.g. gating an IntersectionObserver) should import `useIsMobile` from `@/lib/useIsMobile`.
- `tsc --noEmit` and `eslint` both pass clean.
- **Learnings for future iterations:**
  - Don't use `useEffect(() => setState(...), [dep])` to reset UI state on prop/route change — the repo's eslint config forbids it. Prefer the `key`-remount pattern (extract to subcomponent, key by the changing value) or derive the value without state.
  - No browser tooling available in this sandbox — noted per story as "manual browser verification needed" rather than skipped silently.
---
