# Ralph Progress Log
# Feature: Landing Page Publica - Sessao a Dois (ralph/landing-page)
# Started: 2026-07-24


## 2026-07-24 - US-001
- Implemented: installed `@fontsource/bricolage-grotesque` and `@fontsource/dm-sans` (self-hosted) and imported the specific weight CSS files needed (Bricolage 600/700/800, DM Sans 400/500/600/700) in `client/src/index.css` right after the existing `@fontsource-variable/instrument-sans` import. `.font-display`/`.font-auth-body` in index.css already referenced "Bricolage Grotesque"/"DM Sans" by name with Instrument Sans as fallback, so no CSS class changes were needed once the actual font files were imported.
- Also removed a pre-existing Google Fonts `<link>` block (2x preconnect + 1x stylesheet) from `client/index.html` that was already remotely loading these same two fonts — the AC explicitly requires no Google Fonts link / self-hosted only, so this stale remote link had to be deleted, not just left alongside the new self-hosted import.
- Files changed: `client/package.json`, `client/package-lock.json`, `client/src/index.css`, `client/index.html`.
- **Learnings for future iterations:**
  - Codebase Patterns note: no browser tooling (dev-browser skill/MCP) is available in this sandbox — verification is `tsc --noEmit` + `eslint` only, plus manual code inspection. `npm run dev` / `vite build` currently fail in this sandbox with the pre-existing "Cannot find native binding" rolldown error (documented in client/CLAUDE.md) — confirmed via `git stash` that this failure exists independent of my changes, so it's not a regression, just an unverifiable-here build step.
  - `npm install <new pkg>` in `client/` can print "removed N packages" referring to optional rolldown platform bindings churn — check `git diff package.json`/`package-lock.json` stay scoped to the intended new deps; this is npm's known optional-deps bug (https://github.com/npm/cli/issues/4828), not something to "fix" here.
  - This PRD's actual state lives at `.claude/skills/ralph/prd.json` / `.claude/skills/ralph/progress.txt` (14 landing-page stories, US-002 next), not `scripts/ralph/prd.json` (found empty at session start, pre-existing/unrelated to this work).
- Next: US-002 (migrate Hub route from `/` to `/hub`).
---

## 2026-07-24 - US-002
- Implemented: migrated the Hub route from `path="/"` to `path="/hub"` in `client/src/App.tsx` (kept `ProtectedRoute` + `RequireCouple` guards unchanged), and updated the catch-all `*` route to redirect to `/hub` instead of `/`. Updated every other reference in the codebase that treated `/` as the Hub: `Header.tsx` `NAV_ITEMS` first entry + the `end={item.to === "/"}` check in both the desktop `<nav>` and `MobileNav` (now `=== "/hub"`), `guards.tsx` `PublicOnlyRoute` (authenticated+partnered redirect target) and `RedirectIfCoupled`, `LoginPage.tsx` and `JoinPage.tsx` post-success `navigate(...)` calls, and `MatchCelebrationModal.tsx`'s "Ver na lista" button handler.
- Files changed: `client/src/App.tsx`, `client/src/components/Header.tsx`, `client/src/components/MatchCelebrationModal.tsx`, `client/src/routes/auth/JoinPage.tsx`, `client/src/routes/auth/LoginPage.tsx`, `client/src/routes/guards.tsx`, plus this progress log and `prd.json` (`scripts/ralph/` — see Codebase Patterns note below).
- **Learnings for future iterations:**
  - Also fixed a setup bug found at session start: `scripts/ralph/prd.json`/`progress.txt` were empty on disk even though `scripts/ralph/CLAUDE.md` (the file that defines "same directory as this file") is what's actually loaded for this task. The real landing-page PRD data had ended up at `.claude/skills/ralph/prd.json`/`progress.txt` instead (likely written there by mistake during the PRD→JSON conversion step). Copied that content into `scripts/ralph/` so the working files match what `ralph.sh`/this task's CLAUDE.md expect. If `scripts/ralph/prd.json` is ever empty again at the start of a run, check `.claude/skills/ralph/` for a stray copy before re-deriving the PRD from `tasks/prd-landing-page.md`.
  - Dev server (`npm run dev`) is still broken in this sandbox with the pre-existing rolldown "Cannot find native binding" error — reinstalled `node_modules` (rm -rf + npm install, no lockfile changes) per the documented workaround and it still fails. Despite `client/CLAUDE.md` saying it worked as of 2026-07-23, it does not currently — don't spend much time retrying this, just note it in the report and rely on `tsc --noEmit` + `eslint` + a manual grep audit of all route references instead.
  - Useful grep pattern for future route-rename stories: `grep -rn 'to="/X"\|navigate("/X"\|Navigate to="/X"' client/src` to find every place a given path is referenced as a nav target (covers `<Link>`/`<NavLink>` `to=`, `useNavigate()` calls, and guard `<Navigate>` redirects) — used this after the edits to confirm zero remaining references to `/` as the Hub.
- Next: US-003 (public `/` route with LandingScreen + session guard).
---

## 2026-07-24 - US-003
- Implemented: added a public `path="/"` route in `client/src/App.tsx` rendering a new minimal `client/src/screens/LandingScreen.tsx` (sticky header with brand + Entrar/Criar conta links, hero headline/subtitle/CTAs, radial-gradient bg over `#09090a`, no API calls). Reused the existing `PublicOnlyRoute` guard from `routes/guards.tsx` to wrap the route instead of writing new session logic — it already redirects authenticated+partnered users to `/hub`, authenticated-without-couple to `/join`, and renders children for anonymous visitors, which is exactly the AC's required behavior. Invalid/expired JWT handling was NOT reinvented: the existing `api.ts` response interceptor (401 + had-auth-header -> `clearAuthToken()` + hard redirect to `/login`) combined with `App.tsx`'s mount-time `loadCurrentUser()` call already covers "JWT present but invalid -> session cleared -> /login", satisfying the AC as-is.
- Files changed: `client/src/App.tsx` (new `/` route + import), `client/src/screens/LandingScreen.tsx` (new file).
- **Learnings for future iterations:**
  - `PublicOnlyRoute` (in `routes/guards.tsx`) is reusable as-is for any public route that needs "anonymous sees content, authenticated gets redirected to /hub or /join" — no need to write a bespoke guard per public route, just wrap the route element with it like `/login`/`/register` already do.
  - `LandingScreen.tsx` here is intentionally a minimal placeholder (header + single hero block, inline Tailwind, no `components/landing/` subfolder yet) — US-004 explicitly asks to refactor this into a composition of isolated section components under `client/src/components/landing/` (starting with `LandingHeader`). Future iterations should replace/extend this file rather than pile more sections into it directly.
  - Sandbox's `npm run dev` / vite still fails with the same pre-existing rolldown "Cannot find native binding" error as of this run (re-confirmed, not a regression) — browser verification continues to rely on `tsc --noEmit` + `eslint` + manual review only.
- Next: US-004 (scaffold LandingScreen composition + LandingHeader as isolated components under `client/src/components/landing/`).
---

## 2026-07-24 - US-004
- Implemented: refactored LandingScreen.tsx from an inline placeholder into a composition of isolated section components under client/src/components/landing/: LandingHeader.tsx (sticky, translucent+blur, bottom border, logo link to "/", anchor nav to #como-funciona/#recursos/#faq hidden below md, Entrar+Criar conta buttons always visible) and LandingHero.tsx (moved verbatim from the old inline hero, no visual changes).
- Files changed: `client/src/components/landing/LandingHeader.tsx` (new), `client/src/components/landing/LandingHero.tsx` (new), `client/src/screens/LandingScreen.tsx` (rewritten to compose the two).
- **Learnings for future iterations:**
  - Anchor links in LandingHeader point to ids (#como-funciona, #recursos, #faq) that don't exist as real sections yet - US-006 (HowItWorks), US-007 (FeatureGrid) and US-011 (FaqSection) need to add matching `id="como-funciona"` / `id="recursos"` / `id="faq"` on their outer `<section>` elements so the anchors actually scroll somewhere.
  - Mobile collapse pattern used: `hidden md:flex` on the anchor `<nav>`, CTA buttons stay in a separate flex-none container that's always rendered - this satisfies "anchors collapse but CTA stays reachable at ~360px" without any JS/media-query state, just Tailwind responsive classes.
  - `client/CLAUDE.md` sandbox note now says `npm run dev` works again (fixed 2026-07-23) but this session didn't try starting it - relied on `tsc --noEmit` + `eslint` only, per prior sessions' precedent; both clean for the new/changed landing files. The eslint run on the whole `client/` shows 6 pre-existing errors (ReviewModal.tsx, WatchModal.tsx set-state-in-effect x2 each = 4, button.tsx react-refresh/only-export-components x1... actually totals 6 as counted by eslint) that are unrelated to this change and pre-date it.
- Next: US-005 (LandingHero section - note: US-004 already produced a LandingHero.tsx as a straight extraction of the existing minimal hero; US-005 asks for a richer hero with eyebrow/CTA styling already mostly present, but also asks for a "mock/preview visual reaproveitando o estilo dos cards de midia" which is NOT yet implemented - that's the main remaining gap for US-005).
---

## 2026-07-24 - US-005
- Implemented: enriched LandingHero.tsx with the required "mock/preview visual" reusing the app's media-card design language. Restructured the hero into a responsive two-column grid (text+CTAs left, preview mock right on md+, single centered column below md). The preview mock is a #161513 panel with duo-avatar + "Lista de vocês dois" label + "Queremos Ver" status pill header, and a 2-up grid of mini title cards mirroring the real HubScreen card anatomy (poster gradient placeholder + genre badge, title/year, star rating, type pill) sourced from docs/design/claude-design-project/Sessao a Dois.dc.html lines 87-113.
- Files changed: `client/src/components/landing/LandingHero.tsx`.
- **Learnings for future iterations:**
  - The prototype HTML's media-card anatomy (docs/design/claude-design-project/Sessao a Dois.dc.html ~lines 87-113) is the reusable reference for any future landing section that needs to mimic a real title card (FeatureGrid in US-007, MatchShowcase in US-008 will likely want the same poster-gradient + genre-badge + type-pill pattern) - just scale it down proportionally like this hero mock does.
  - Sandbox `npm run dev` is broken again with the same pre-existing rolldown "Cannot find native binding" error (client/CLAUDE.md's 2026-07-23 "fixed" note did not hold this session) - don't spend time debugging this again, it's a known-flaky sandbox issue; rely on `tsc --noEmit` + `eslint` (both clean for this change) + manual code review against the prototype reference instead.
- Next: US-006 (HowItWorks 3-step section, needs `id="como-funciona"` to match LandingHeader's anchor link).
---
