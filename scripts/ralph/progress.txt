# Ralph Progress Log
Started: 2026-08-03 — Epico 3 (Resiliencia e Latencia do TMDB), branch epico3/tmdb-latency
---

## Codebase Patterns
- `./mvnw test` RODA neste devcontainer (JDK 21 + Temurin, Maven 3.9.16). Nunca declarar uma story de backend concluida sem executar. Se falhar com erro de rede, rodar `sudo /usr/local/bin/init-firewall.sh` e tentar de novo antes de concluir qualquer coisa.
- `api/src/test/resources/application.properties` SUBSTITUI o principal nos testes de contexto completo, nao soma. Toda propriedade nova exigida por bean instanciado ansiosamente (TmdbConfig, JwtService) precisa ser declarada nos DOIS arquivos.
- Migrations: `IF NOT EXISTS` sempre, nunca `DROP`/`TRUNCATE`, nenhum default no lado do banco para coluna que a app gera, sintaxe compativel com H2 em modo PostgreSQL. `com.app.migration.FlywayMigrationTest` e o UNICO teste que aplica as migrations de verdade (os slices usam `create-drop`) — se a migration nova nao aparece la, ela nao foi validada.
- `spring.jpa.open-in-view=false` desde o Epico 2: qualquer metodo que devolva entidade e depois toque associacao LAZY precisa ser `@Transactional`, senao e `LazyInitializationException`, nao query extra silenciosa.
- `MediaTrackMapper` e o unico tradutor `MediaTrack` -> `MediaTrackResponse`. Ele NAO injeta dependencia: recebe o `Map<UUID,String>` de nomes ja resolvido. Resolver nomes uma vez por request (`findAllById`), nunca por track.
- Falha do TMDB nunca pode falhar persistencia: o padrao e `catch (RuntimeException)` em volta de `mediaDetailsService.getDetails(...)` com fallback vazio (ver `MediaTrackService.fetchGenreIds` e `RatingRequestService`).
- Sem codegen entre back e front: todo campo novo em DTO de resposta tem que ser espelhado a mao em `client/src/types/*.ts`.
- Sem browser neste sandbox (sem Chromium/Playwright, sem sudo para as libs de sistema) e sem `docker`/`psql`. Verificacao de UI so por `tsc`/`eslint` + revisao estatica; sintaxe Postgres-only so e validada no CI (postgres:17-alpine).
- Story de UI: invocar a skill `frontend-design` ANTES de implementar. Refatoracao pixel-identica esta dispensada (regra do CLAUDE.md raiz, decisao D11).
---

## 2026-08-03 - US-001
- Adicionados connect/read timeout ao `tmdbRestClient` (TmdbConfig): `SimpleClientHttpRequestFactory` com `setConnectTimeout(Duration)`/`setReadTimeout(Duration)` (overloads Duration existem desde Spring Framework 6.1, confirmado via javap contra o jar 7.0.8 usado aqui), passado ao builder via `.requestFactory(...)`.
- Duas propriedades novas, `tmdb.connect-timeout` (default 3s) e `tmdb.read-timeout` (default 5s), injetadas via `@Value` com Duration parseavel (`3s`, `5s`) direto no default do `@Value` e em `application.properties` (main e test), seguindo o padrao de `tmdb.base-url`/`tmdb.api-key`.
- Nenhum tratamento de erro novo: timeout vira `ResourceAccessException`, ja capturada em `MediaSearchService`/`MediaDetailsService` como `RestClientResponseException | ResourceAccessException` -> `TmdbUnavailableException` -> 502.
- Arquivos alterados: `api/src/main/java/com/app/media/TmdbConfig.java`, `api/src/main/resources/application.properties`, `api/src/test/resources/application.properties`, `api/src/test/java/com/app/media/TmdbConfigTest.java`.
- Testes: `TmdbConfigTest` ja existia (3 casos de fail-fast/api-key) e ganhou constructor de 4 args + 2 casos novos (defaults e customizados), verificados por reflection nos campos privados `connectTimeout`/`readTimeout` (em millis, int) do `SimpleClientHttpRequestFactory` acessado via campo `clientRequestFactory` de `DefaultRestClient` (`ReflectionTestUtils.getField`, ambos os nomes de campo confirmados via `javap -p` antes de escrever o teste). `./mvnw test`: 311/311, BUILD SUCCESS.
- **Learnings for future iterations:**
  - `RestClient.builder().requestFactory(...)` aceita qualquer `ClientHttpRequestFactory`; para inspecionar a factory de um `RestClient` ja construido em teste (sem subir contexto Spring), `ReflectionTestUtils.getField(restClient, "clientRequestFactory")` funciona porque `DefaultRestClient` guarda a factory original nesse campo privado antes de qualquer wrapping para interceptors (`interceptingRequestFactory` e um campo separado, resolvido lazy).
  - Quando a duvida for "esse setter aceita Duration ou so int/long", confirmar com `javap -p` no `.class` dentro do jar em `~/.m2` antes de assumir a API de uma versao do Spring Framework/Boot mais antiga - Spring Boot 4.1.0 aqui usa Spring Framework 7.0.8, que ja tem os overloads `Duration` em `SimpleClientHttpRequestFactory` desde o 6.1.
---

## 2026-08-03 - US-003
- `MediaTrackService.addTrack`: renomeado o antigo `fetchGenreIds` (retornava so `List<Integer>`) para `applyTmdbMetadata` (void, recebe o `MediaTrack` e seta `genreIds`/`title`/`posterUrl`/`releaseYear` a partir da MESMA `MediaDetails` ja buscada) - continua UMA chamada a `mediaDetailsService.getDetails`, protegida pelo mesmo `catch (RuntimeException)`: falha do TMDB deixa os 4 campos vazios/nulos e nao impede a criacao do track.
- `MatchService.createMatch`: as 3 linhas `track.setTitle/setPosterUrl/setReleaseYear(details....)` foram acrescentadas logo apos `track.setGenreIds(details.genreIds())` - reaproveita a `MediaDetails` que o metodo ja buscava (sem try/catch aqui, igual antes: `createMatch` sempre exigiu TMDB disponivel para o `MatchEvent`/notificacao).
- `LikeRequest` ganhou `title`/`posterUrl`/`releaseYear` (sem `@NotNull`) MAIS um construtor de 2 args (`tmdbId, mediaType`) que delega pro canonico com os 3 novos como `null` - mesmo padrao do `Notification` 7-arg -> 8-arg citado em `api/CLAUDE.md`; sem isso as ~11 chamadas `new LikeRequest(603L, MediaType.MOVIE)” espalhadas em `MatchServiceTest` quebrariam a compilacao.
- `MatchService.like`: ao criar o `MatchLike` (branch "ainda nao curtiu"), seta os 3 campos a partir do `request` antes do `save` - quando ausentes (request de 2 args, ou reject que reusa `LikeRequest`) os setters recebem `null`, sem chamada nova ao TMDB (`verify(mediaDetailsService, never()).getDetails(any(), anyLong())` cobre isso).
- Testes ajustados: `MediaTrackServiceTest` (addTrack com sucesso agora tambem verifica `title`/`releaseYear` no track capturado + `verify(times(1))`; addTrack com falha do TMDB verifica os 3 campos nulos), `MatchServiceTest` (createMatch verifica `title`/`releaseYear` + `verify(times(1))`; teste novo `like_persistsProvidedMetadataOnMatchLikeWithoutCallingTmdb` prova que title/posterUrl/releaseYear do request chegam no `MatchLike` salvo sem tocar o TMDB).
- Arquivos alterados: `api/src/main/java/com/app/tracking/MediaTrackService.java`, `api/src/main/java/com/app/match/MatchService.java`, `api/src/main/java/com/app/match/LikeRequest.java`, `api/src/test/java/com/app/tracking/MediaTrackServiceTest.java`, `api/src/test/java/com/app/match/MatchServiceTest.java`.
- Testes: `./mvnw test`: 312/312, BUILD SUCCESS (311 + 1 teste novo).
- **Learnings for future iterations:**
  - Quando um record de request ganha campos opcionais novos e ha dezenas de `new XRequest(a, b)` espalhados em testes existentes, adicionar um construtor extra que delega pro canonico com `null` nos campos novos evita reescrever todos os call sites - so vale a pena escrever os testes novos que exercitam os campos novos explicitamente.
  - US-004 (proxima) e quem vai consumir estes campos na LEITURA (`MatchService.getPending`, `MediaTrackMapper`/`MediaTrackResponse`) - `getPending` ainda chama `mediaDetailsService.getDetails` incondicionalmente hoje (nao mudou nesta story, so a escrita mudou); a leitura so para de chamar o TMDB quando US-004 trocar isso para ler `ml.getTitle()`/etc quando nao nulos.
---

## 2026-08-03 - US-002
- Nova migration `api/src/main/resources/db/migration/V4__denormalize_media_metadata.sql`: `ADD COLUMN IF NOT EXISTS title VARCHAR(255)`, `poster_url VARCHAR(500)`, `release_year INTEGER` em `media_track` e `match_like`, todas nullable, sem default no banco.
- `MediaTrack` e `MatchLike` ganharam os tres campos com getters/setters (nenhum construtor mudou de assinatura - campos entram por setter, como pedia o AC).
- `FlywayMigrationTest` atualizado para exigir a versao "4" na lista de migrations aplicadas (era so "1","2","3").
- `docs/SCHEMA_BASELINE.md`: as tres colunas novas documentadas nas tabelas `media_track` e `match_like` (marcadas "(V4)").
- `docs/FLYWAY.md`: V4 registrada na lista de migrations, e a ressalva do pooler Supabase (usar porta 5432 direta ou pooler em modo Session, nunca modo Transaction/6543) agora tambem menciona V4 explicitamente.
- Esta story so cria schema/campos - ninguem ainda escreve title/posterUrl/releaseYear (isso e US-003) nem le (US-004), entao nenhum teste novo de service/controller foi necessario alem do FlywayMigrationTest.
- Arquivos alterados: `api/src/main/resources/db/migration/V4__denormalize_media_metadata.sql` (novo), `api/src/main/java/com/app/tracking/MediaTrack.java`, `api/src/main/java/com/app/match/MatchLike.java`, `api/src/test/java/com/app/migration/FlywayMigrationTest.java`, `docs/SCHEMA_BASELINE.md`, `docs/FLYWAY.md`.
- Testes: `./mvnw test`: 311/311, BUILD SUCCESS (mesma contagem de antes - story so adiciona campos/schema, sem comportamento novo a testar isoladamente ainda).
- **Learnings for future iterations:**
  - Para adicionar uma coluna nullable a uma entidade existente sem quebrar nada: `ADD COLUMN IF NOT EXISTS` na migration (nunca default no banco para campo que a app popula), campo Java simples com getter/setter (sem entrar no construtor), e atualizar a lista de versoes esperadas em `FlywayMigrationTest` (`.contains("1","2","3",...)`) - esse e o UNICO teste que realmente aplica a migration nova contra o schema das entidades.
  - US-003 (proxima) e quem vai popular estes campos em `MediaTrackService.addTrack`/`MatchService.createMatch`/`MatchService.like`, reaproveitando a chamada a `mediaDetailsService.getDetails(...)` que ja existe hoje - conferir o `catch (RuntimeException)` ja existente em `MediaTrackService.fetchGenreIds` antes de mexer, e nao introduzir chamada nova ao TMDB.
---
