# Ralph Progress Log
Started: Thu Aug  6 10:59:06 PDT 2026
---

## Codebase Patterns
- Backend: `./mvnw test` roda de verdade no sandbox (~21s, 424+ testes) e ja
  inclui o gate do JaCoCo (`check` na fase `test`, minimo 90% de linha). Nunca
  dar uma story de backend como pronta sem rodar.
- `@WebMvcTest` so prova alguma coisa sobre autenticacao se a `SecurityConfig`
  do app entrar no slice. Conjunto padrao de import:
  `@Import({ SecurityConfig.class, ClientIpResolver.class, RateLimitService.class,
  RateLimitProperties.class, SecurityAuditLogger.class })` + `@MockitoBean JwtService`.
  Filtros (`RateLimitFilter`, `CorrelationIdFilter`, `JwtAuthenticationFilter`)
  sao auto-incluidos por implementarem `jakarta.servlet.Filter` — nao importar,
  so prover as dependencias deles.
- Para uma rota publica, `verifyNoInteractions(jwtService)` e a assercao que
  realmente prova "nao exige autenticacao"; um 200 sozinho passaria mesmo se a
  rota virasse autenticada-com-anonimo.
- NAO ha `docker` nem `psql` neste sandbox. Criterio de aceite que depende de
  `docker build`/`docker ps` fica pendente do lado humano — registrar aqui em
  vez de travar a story.
- Imports do Spring Boot 4.1 mudaram de pacote: `@WebMvcTest` vem de
  `org.springframework.boot.webmvc.test.autoconfigure.WebMvcTest`, nao do
  pacote `org.springframework.boot.test.autoconfigure.web.servlet` do Boot 3.x.
  Copiar o import de um teste vizinho em vez de escrever de memoria.

---

## 2026-08-06 - US-001 (Teste do HealthController)
- Criado `api/src/test/java/com/app/HealthControllerTest.java` com
  `@WebMvcTest(HealthController.class)`, cobrindo a ultima classe da tabela da
  T8.1 que estava sem teste proprio.
  - `healthReturnsUp`: `GET /api/health` -> 200, `Content-Type` JSON e corpo
    exatamente `{"status":"UP"}` (`content().json(..., true)` — strict, entao um
    campo novo no corpo quebra o teste, que e o ponto: o probe do Docker e o
    smoke test do CD dependem dessa forma).
  - `healthIsPublicAndNeverConsultsTheAccessToken`: request anonimo -> 200 e
    `verifyNoInteractions(jwtService)`.
- Adicionadas duas entradas em `api/CLAUDE.md`: quem consome o `/api/health`
  (HEALTHCHECK do container + smoke test do CD + cookie XSRF da SPA), e o
  conjunto padrao de `@Import` para um slice `@WebMvcTest` que precisa da
  seguranca real.
- Arquivos alterados: `api/src/test/java/com/app/HealthControllerTest.java` (novo),
  `api/CLAUDE.md`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Verificacao: `./mvnw test` -> 424 testes, 0 falhas; JaCoCo `check` verde
  ("All coverage checks have been met").
- **Learnings for future iterations:**
  - `HealthController` vive em `com.app` (raiz), nao em `com.app.common` nem
    numa feature — o teste tem que ficar em `src/test/java/com/app/`.
  - `SecurityConfigTest` ja tinha um `healthRespondsOkWithoutAuthorizationHeader`,
    mas ele e `@SpringBootTest` (contexto inteiro): o valor do teste novo e ser
    slice, rodar em ~2s e falhar por um motivo so.
  - A `SecurityConfig` trata `/api/health` em DOIS lugares: `permitAll` no
    `authorizeHttpRequests` e `ignoringRequestMatchers` do CSRF. Mexer num sem o
    outro deixa a rota meio publica.
  - Este commit tambem carrega mudancas de infraestrutura que ja estavam na
    working tree antes desta iteracao (T8.3, feita em 2026-08-06): `deploy.yml`
    novo, `ci.yml`/`deploy.sh`/`docs/DEPLOY.md` alterados e o PRD do epico em
    `tasks/`. Elas nao foram produzidas por esta story — estao aqui porque a
    instrucao do Ralph e commitar tudo.
---

## 2026-08-06 - US-002 (Container roda como nao-root e expoe healthcheck)
- `api/Dockerfile`, estagio de runtime:
  - `RUN addgroup -g 10001 -S app && adduser -u 10001 -S app -G app` + `USER app`
    (UID/GID FIXOS, nao os que o adduser escolheria — o bind mount da VPS
    depende do numero).
  - `COPY --from=build --chown=app:app /app/target/*.jar app.jar`.
  - `HEALTHCHECK --interval=30s --timeout=3s --start-period=40s CMD
    wget -qO- http://localhost:8080/api/health || exit 1` (porta INTERNA 8080).
  - Ordem do multi-stage preservada (`.mvn/`, `mvnw`, `pom.xml`,
    `dependency:go-offline` continuam antes de `COPY src/`); mesma imagem base,
    mesmo `EXPOSE`, mesmo `ENTRYPOINT`; nenhum pacote novo instalado (wget do
    BusyBox ja vem na alpine).
- `docs/DEPLOY.md` secao 3.4 ganhou o
  `chown -R 10001:10001 /var/lib/sessao-a-dois/security-audit-logs` como passo
  obrigatorio ANTES do primeiro deploy com esta mudanca, com o porque escrito:
  sem ele o `RollingFileAppender` do `security.audit` falha por permissao e o
  Logback NAO derruba o app — a API sobe, o site funciona e a trilha some em
  silencio. Adicionado tambem o comando de verificacao pos-deploy e uma
  referencia cruzada na secao "Trilha de auditoria de seguranca".
- `api/CLAUDE.md`: duas entradas novas na secao Docker (UID fixo 10001 + a
  regra de que todo caminho gravavel novo dentro do container tem a mesma
  restricao; e o healthcheck na 8080 com wget do BusyBox).
- Arquivos alterados: `api/Dockerfile`, `docs/DEPLOY.md`, `api/CLAUDE.md`,
  `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Verificacao: `./mvnw test` -> 424 testes, 0 falhas, JaCoCo verde.
  **PENDENTE DO LADO HUMANO** (nao ha `docker` neste sandbox): `docker build`,
  `docker run --rm --entrypoint sh sessao-api:latest -c whoami` != root,
  `docker ps` reportando `(healthy)` apos o start-period, e a comparacao de
  tamanho da imagem (<= +5MB). E, na VPS, o `chown` antes do proximo deploy.
- **Learnings for future iterations:**
  - Trocar o processo para nao-root e barato no Dockerfile; o risco real esta
    fora dele, em todo caminho gravavel montado de fora (hoje so o bind mount
    da trilha de auditoria). Antes de adicionar qualquer volume novo ao
    `docker-compose-prod.yml`, conferir o dono no host.
  - `HEALTHCHECK` usa a porta de dentro do container (8080). O 8085 do compose
    e so `ports:` do host — usa-lo deixaria o container eternamente unhealthy.
  - `docs/DEPLOY.md` tem a mesma informacao em dois lugares (secao 3.4 de setup
    e secao "Trilha de auditoria de seguranca"); mudanca em uma pede referencia
    cruzada na outra.
---

## 2026-08-06 - US-003 (Dependabot para Maven, npm e GitHub Actions)
- Criado `.github/dependabot.yml` (arquivo novo, unico arquivo de codigo da story):
  - `version: 2` e tres entradas em `updates`: `maven` em `/api`, `npm` em
    `/client` e `github-actions` em `/`. Os dois projetos vivem em subpastas —
    `directory: /` estaria errado para maven e npm.
  - `schedule.interval: weekly` e `open-pull-requests-limit: 5` nas tres.
  - Um `groups` por ecossistema (`maven-minor-patch`, `npm-minor-patch`,
    `actions-minor-patch`) com `update-types: [minor, patch]`. Major fica fora do
    grupo por construcao — o Dependabot abre PR proprio para o que o grupo nao
    captura, entao um salto de major do Spring Boot/React vem isolado (E8.6).
  - `target-branch: dev` nas tres. Isto e o item de risco da story: o
    `deploy.yml` dispara no push da `main`, entao um PR do Dependabot contra a
    `main` viraria deploy de producao a partir de um bump nao revisado.
  - O cabecalho do arquivo carrega o porque de `target-branch` e de `groups`, para
    quem editar depois nao "simplificar" removendo.
- Verificacao feita aqui: YAML parseado com `bunx js-yaml` (o `python3` do
  sandbox NAO tem o modulo `yaml`) + assercoes de schema sobre o objeto
  resultante (version, os tres ecossistemas/diretorios, target-branch, cadencia,
  limite <= 5, update-types de cada grupo). Todas passaram.
- Confirmado que a branch `dev` existe no remoto (`git branch -r` -> `origin/dev`);
  um `target-branch` inexistente e erro de configuracao silencioso no Dependabot.
- NAO rodado `./mvnw test`: a story nao toca nenhum arquivo de `api/` nem de
  `client/`.
- PENDENTE DO LADO HUMANO: a validacao real do arquivo so acontece no GitHub —
  aba **Insights > Dependency graph > Dependabot**, que deve listar os tres
  ecossistemas sem erro de parsing. Nao ha schema check local.
- Arquivos alterados: `.github/dependabot.yml` (novo), `scripts/ralph/prd.json`,
  `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - Nao existe `python3 -c "import yaml"` neste sandbox. Para validar YAML use
    `bunx --bun js-yaml <arquivo>` — imprime o JSON equivalente e falha com
    codigo != 0 se o YAML for invalido.
  - O repo tem `origin/dev` alem da `main`, e o fluxo do CD e `dev` -> `main`
    (push na `main` = deploy). Qualquer automacao que abra PR tem que mirar `dev`.
  - Este epico nao tem CLAUDE.md em `.github/`; a decisao de `target-branch`
    ficou como comentario dentro do proprio `dependabot.yml`, que e onde alguem
    editando o arquivo vai olhar.
---

## 2026-08-06 - US-004 (Limpar o que der de vulnerabilidade e triar o resto)
- `bun update` (SEM `--latest`) em `client/`: 15 dependencias diretas subiram de
  patch/minor (`react`/`react-dom` 19.2.7 -> 19.2.8, `vite` 8.1.5 -> 8.2.1,
  `eslint` 10.7.0 -> 10.8.0, `axios` 1.18.1 -> 1.19.0, `shadcn` 4.13.1 -> 4.16.2,
  `react-router-dom` 7.18.1 -> 7.18.2, entre outras). `package.json` mudou so nos
  ranges (o `bun update` reescreve o piso do `^`, ex.: `"vite": "^8"` ->
  `"^8.2.1"`); NENHUM pacote novo, nenhuma remocao, nenhum arquivo de `src/`.
- `bun run typecheck && bun run lint && bun run build` -> os tres verdes depois do
  update (build em ~350ms, mesmo aviso pre-existente de chunk > 500kB).
- **`bun audit` depois do update: 15 advisories, 6 high, 9 moderate, 0 critical**
  — exatamente o mesmo numero de antes. O update de dependencia direta nao move
  transitiva (ver learnings), e todos os 6 highs sao transitivos.
- Triagem dos 6 highs escrita em `client/CLAUDE.md` (secao "Triagem de
  vulnerabilidades (`bun audit`) — Epico 8, US-004"), com pacote, GHSA, caminho
  de dependencia, tipo, se ha versao corrigida e a decisao:
  - `brace-expansion` 5.0.7 x2 (`GHSA-mh99-v99m-4gvg`, `GHSA-rgw5-rvv9-x895`) —
    via `eslint`/`typescript-eslint`/`shadcn`, build-time. Ignorar.
  - `fast-uri` 3.1.4 (`GHSA-7p8r-x3mc-p8w7`) — via `ajv` do eslint/MCP. Ignorar.
  - `ip-address` 10.2.0 (`GHSA-mwp4-54f8-5fhr`) — via `express-rate-limit` do
    servidor MCP embutido na CLI do shadcn. Ignorar.
  - `undici` 7.28.0 (`GHSA-4cwx-7wf7-3272`) — via `@dotenvx/dotenvx` do shadcn.
    Ignorar.
  - `react-router` 7.18.2 (`GHSA-qwww-vcr4-c8h2`) — UNICO de producao. RSC Mode
    CSRF bypass; esta app e SPA em Vite sem React Server Components, o vetor nao
    existe. Correcao so no major 8.3.0, fora do epico (vem por PR do Dependabot).
    Ignorar com justificativa.
  Esses 6 GHSA sao exatamente a lista de `--ignore` que a US-005 tem que colocar
  no step do `ci.yml`.
- Arquivos alterados: `client/package.json`, `client/bun.lock`,
  `client/CLAUDE.md`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
  Nenhum componente, nenhum pixel — a skill `frontend-design` nao se aplica.
- **Learnings for future iterations:**
  - `bun update` sem argumento so atualiza dependencias **diretas**. Transitiva
    fica congelada na resolucao do `bun.lock` mesmo quando o range do pai ja
    aceitaria a versao corrigida (ex.: `minimatch` pede `brace-expansion ^5.0.5`
    e o 5.0.9 existe, mas o lock continuou no 5.0.7).
  - **ARMADILHA:** `bun update <pacote-transitivo>` NAO atualiza a transitiva —
    ele ADICIONA o pacote como dependencia direta no `package.json`. Testado
    aqui: `bun update brace-expansion fast-uri ip-address undici postcss hono
    @hono/node-server` acrescentou os 7 ao bloco `dependencies` (e puxou majors:
    `undici` 8, `fast-uri` 4). Revertido com `git checkout package.json bun.lock`
    + `bun install --frozen-lockfile`. Para forcar transitiva o mecanismo e
    `overrides`/`resolutions` no `package.json`, nao `bun update`.
  - `bun audit --json` devolve um objeto `{"<pacote>": [advisory, ...]}` — NAO o
    formato `{"advisories": {...}}` do `npm audit`. Um `jq '.advisories'` volta
    vazio silenciosamente. Para listar os highs:
    `bun audit --json | bunx --bun jq -r 'to_entries[] | .key as $m | .value[] |
    select(.severity=="high") | "\($m) \(.url)"'`.
  - `shadcn` esta em `dependencies` no `package.json`, mas e uma CLI: nenhum
    arquivo de `src/` a importa e ela nao entra no bundle. 4 dos 6 highs vem
    debaixo dela. Movida para `devDependencies` seria mais honesto (nada em
    producao instala essas deps — o deploy manda o `dist/` pronto por `scp`),
    mas isso ficou FORA desta story de proposito: e mudanca de topologia de
    dependencia, decisao do mantenedor. Registrado em `client/CLAUDE.md`.
  - A tabela de `client/CLAUDE.md` e a lista de `--ignore` do `ci.yml` (US-005)
    sao duas copias da mesma decisao. Mexer numa sem a outra cria ponto cego —
    a propria secao do CLAUDE.md avisa isso no final.
---

## 2026-08-06 - US-005 (Ligar o gate do bun audit no CI)
- `.github/workflows/ci.yml`, job `frontend`: novo step `Audit dependencies`
  entre `Install dependencies` e `Typecheck` (portanto DEPOIS do `bun install` e
  ANTES do `build`). Roda `bun audit --audit-level=high "${IGNORES[@]}"` com os 6
  advisories triados na US-004. **Sem `continue-on-error`, sem `|| true`** — se e
  gate, quebra o job; e como o `deploy.yml` reusa o `ci.yml` via `workflow_call`,
  quebra o deploy junto.
- Os `--ignore` estao num array bash (`IGNORES=(...)` / `IGNORES+=(...)`), uma
  linha por advisory, cada uma precedida de comentario com: pacote, por que nao
  alcanca o usuario, e o que faria revisitar a decisao. Sao exatamente os 6
  `Ignorar` da tabela de `client/CLAUDE.md`: `GHSA-mh99-v99m-4gvg`,
  `GHSA-rgw5-rvv9-x895` (brace-expansion), `GHSA-7p8r-x3mc-p8w7` (fast-uri),
  `GHSA-mwp4-54f8-5fhr` (ip-address), `GHSA-4cwx-7wf7-3272` (undici),
  `GHSA-qwww-vcr4-c8h2` (react-router).
- Verificacao REAL (nao presumida), rodando o proprio texto do step extraido do
  YAML e executado com `bash -e` em `client/`:
  - com os 6 ignores: **exit 0** (os 9 moderate continuam no relatorio e nao
    quebram nada — `--audit-level=high`);
  - sem `--ignore=GHSA-qwww-vcr4-c8h2`: **exit 1**, `1 vulnerabilities (1 high)`,
    react-router. Ou seja, o gate morde. Ignore restaurado em seguida.
  - `bun audit --audit-level=high` sozinho (sem ignore nenhum): exit 1, 6 high.
- `client/CLAUDE.md`: a secao de triagem da US-004 agora aponta o nome exato do
  step, diz que ele e gate desde hoje e registra que `--ignore` aceita id GHSA.
- Arquivos alterados: `.github/workflows/ci.yml`, `client/CLAUDE.md`,
  `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`. Nenhum arquivo de
  `client/src/`, nenhum pixel — a skill `frontend-design` nao se aplica.
- **Learnings for future iterations:**
  - **`bun audit --ignore` aceita GHSA** (`GHSA-xxxx-xxxx-xxxx`), o mesmo id que o
    comando imprime — a duvida "sera que so aceita CVE?" registrada na story esta
    respondida: aceita GHSA, nao precisa traduzir.
  - `bun audit` (1.3.14) so tem 3 flags: `--json`, `--audit-level`, `--ignore`.
    Nao existe `--prod`. Separar producao de build-time e trabalho de triagem
    manual, nao de flag.
  - Comentario por argumento dentro de um comando quebrado com `\` nao da: o
    truque de `` `# ...` `` (backtick com comentario) funciona mas e fragil com
    acento/parenteses/aspas. **Array bash (`IGNORES+=(...)`) e o jeito limpo** —
    cada item vira uma linha propria e o `#` acima e comentario de shell normal.
    O default shell do Actions em ubuntu e bash, entao array e seguro.
  - Para testar um step de workflow sem Actions: extrair o bloco `run:` do YAML e
    rodar com `bash -e`. Nao ha `pyyaml` neste sandbox (nem `pip`), entao a
    extracao foi por fatiamento de linhas mesmo.
---

## 2026-08-06 - US-006 (Resolver o pin manual do jjwt)
- **Investigacao (feita ANTES de escolher o caminho, decisao E8.3):**
  - `./mvnw dependency:tree -Dincludes=io.jsonwebtoken` -> `jjwt-api:0.12.6:compile`,
    `jjwt-impl:0.12.6:runtime`, `jjwt-jackson:0.12.6:runtime`.
  - `./mvnw help:effective-pom -Doutput=/tmp/effective-pom.xml`: o bloco
    `<dependencyManagement>` vai das linhas 244 a 9901; as 3 unicas ocorrencias de
    `io.jsonwebtoken` no arquivo estao nas linhas 9970/9976/9982, ou seja **fora**
    do dependencyManagement — sao as declaracoes do proprio pom, com a versao que
    nos escrevemos.
  - Confirmacao direta no BOM: `grep -c jsonwebtoken
    ~/.m2/.../spring-boot-dependencies-4.1.0.pom` -> **0**.
  - **Resposta: o BOM do Spring Boot NAO gerencia io.jsonwebtoken.** Logo, caminho
    2 do criterio de aceite (property unica), nao remocao das versoes.
- `api/pom.xml`: nova property `<jjwt.version>0.12.6</jjwt.version>` com comentario
  explicando que o jjwt esta fora do BOM, que o bump e manual e que o Dependabot da
  US-003 (ecossistema maven em `/api`) e quem passa a avisar. As 3 tags
  `<version>0.12.6</version>` viraram `<version>${jjwt.version}</version>`.
- **Sem regressao de versao:** `dependency:tree -Dincludes=io.jsonwebtoken` rodado
  DEPOIS da mudanca devolve exatamente `0.12.6` nos tres artefatos — mesma versao de
  antes, so que resolvida via property.
- `./mvnw test` -> 424 testes, 0 falhas; JaCoCo `check` verde.
- `api/CLAUDE.md`: sub-bullet novo em `com.app.security` (JWT) registrando a
  investigacao fechada, para ninguem repeti-la.
- Arquivos alterados: `api/pom.xml`, `api/CLAUDE.md`, `scripts/ralph/prd.json`,
  `scripts/ralph/progress.txt`. Nenhum arquivo de `client/` — sem pixel, a skill
  `frontend-design` nao se aplica.
- **Learnings for future iterations:**
  - Para responder "o BOM gerencia X?", o teste decisivo NAO e olhar o
    `dependency:tree` (ele mostra a versao resolvida, seja de onde for). E ver se o
    artefato aparece dentro do `<dependencyManagement>` do effective-pom, ou
    simplesmente `grep` no proprio `spring-boot-dependencies-<versao>.pom` do `~/.m2`
    — este ultimo e o mais rapido e o menos ambiguo.
  - `help:effective-pom` com `-q` nao imprime nada no stdout quando se usa
    `-Doutput=` — o arquivo e escrito, e so ler dele.
  - Os 3 artefatos do jjwt (`api`/`impl`/`jackson`) precisam da MESMA versao: o
    `impl` e o `jackson` sao `runtime`, entao uma divergencia entre eles nao aparece
    na compilacao, so num `NoSuchMethodError` em producao. Property unica existe
    exatamente para tornar isso impossivel de errar num bump.
  - `jjwt-jackson` e quem traz o Jackson 2 (`com.fasterxml`) transitivamente no
    runtime — ja registrado em `api/CLAUDE.md` na nota do `RateLimitFilter`. Um bump
    de jjwt pode mexer nisso; nao e so "a lib que assina token".
---
