## Codebase Patterns
- Backend package base is `com.app` (package-by-feature): user, couple, media, tracking, match, notification, security, websocket.
- Notifications: `NotificationService.notifyCouple(...)` is the single entry point today; it persists one Notification per couple member and pushes STOMP to `/topic/couple/{coupleId}/notifications` via `TransactionSynchronizationManager.afterCommit()`. Reuse the persist-then-push-after-commit pattern.
- Enums on entities are always `@Enumerated(EnumType.STRING)`, so adding an enum constant needs no data migration.
- `api/src/test/resources/application.properties` REPLACES the main one for `@SpringBootTest`; any new required property must be mirrored there.
- **This sandbox has no JDK and no Maven Central access** (`java` not found, `./mvnw` cannot download Maven). Backend compile/test acceptance criteria cannot be verified here — keep changes conservative and flag verification as pending in the progress log.
- Frontend rule (CLAUDE.md): any change under `client/` must invoke the `frontend-design` skill first.

---

## 2026-07-27 - US-001
- Added `RATING_REQUEST` to `NotificationType` (now MATCH, NO_MATCH, RATING_REQUEST). `Notification.type` is already `@Enumerated(EnumType.STRING)`, so the constant persists as the literal string and existing rows are untouched (no migration needed).
- Added `NotificationRepositoryTest.ratingRequestTypeIsPersistedAsItsEnumName` asserting the raw `type` column stores `'RATING_REQUEST'` and reads back as the enum.
- Files changed:
  - `api/src/main/java/com/app/notification/NotificationType.java`
  - `api/src/test/java/com/app/notification/NotificationRepositoryTest.java`
- **Learnings for future iterations:**
  - No `switch` over `NotificationType` exists in main code (only `notifyCouple` passing it through and Mockito `any(NotificationType.class)` matchers in tests), so adding a constant does not break exhaustiveness anywhere.
  - `./mvnw -DskipTests package` could NOT be run in this environment (no JDK, Maven wrapper cannot fetch the distribution). Build/test verification is pending on a machine with JDK 21 + network.
  - `NotificationRepositoryTest` is a `@DataJpaTest` with an autowired `EntityManager`; native queries against the `notification` table are already used there (see the createdAt-backdating test) — reuse that for column-level assertions.
---

## 2026-07-27 - US-002
- `Notification` ganhou coluna nullable `media_track_id` (UUID) + getter e um segundo construtor de 8 args com `mediaTrackId`; o construtor antigo de 7 args delega passando `null`, entao MATCH/NO_MATCH seguem identicos sem tocar em `MatchService`.
- `NotificationDto` ganhou `UUID mediaTrackId` como ultimo componente do record; `NotificationService.toDto` preenche a partir da entidade (vale tanto para REST quanto para o push STOMP, que serializa o mesmo DTO).
- Testes: `NotificationServiceTest` (mapeamento do DTO com mediaTrackId preenchido em RATING_REQUEST e null em MATCH), `NotificationRepositoryTest.mediaTrackIdIsPersistedForRatingRequestAndNullForMatch`, e `NotificationControllerTest.dto(...)` ajustado para o novo arity.
- Arquivos alterados:
  - `api/src/main/java/com/app/notification/Notification.java`
  - `api/src/main/java/com/app/notification/NotificationDto.java`
  - `api/src/main/java/com/app/notification/NotificationService.java`
  - `api/src/test/java/com/app/notification/NotificationServiceTest.java`
  - `api/src/test/java/com/app/notification/NotificationRepositoryTest.java`
  - `api/src/test/java/com/app/notification/NotificationControllerTest.java`
- **Learnings for future iterations:**
  - `NotificationDto` e um record: adicionar um componente quebra TODA construcao posicional. Hoje o unico ponto fora do service e `NotificationControllerTest.dto(...)` — grepar por `new NotificationDto(` antes de mexer.
  - Ao adicionar campo opcional a uma entidade com construtor publico usado por outra feature (`MatchService`), preferir sobrecarga: construtor novo completo + o antigo delegando com `null`. Evita tocar em call sites de match.
  - `ddl-auto=update` (application.properties) cria a coluna nova automaticamente em dev/prod; nao ha Flyway/Liquibase no projeto, entao "migracao" e sempre so adicionar coluna nullable.
  - Continua valendo: sem JDK/Maven Central neste sandbox — `./mvnw -DskipTests package` falha no download do proprio Maven. Compilacao/testes do backend seguem pendentes de verificacao em maquina com JDK 21.
---

## 2026-07-27 - US-003
- `NotificationService.notifyRatingRequest(couple, recipientUserId, actorUserId, tmdbId, mediaType, title, mediaTrackId)`: persiste exatamente 1 `Notification` do tipo `RATING_REQUEST` para o parceiro e retorna `Optional<NotificationDto>`. Reusa `scheduleBroadcast(...)` (mesmo `afterCommit` de `notifyCouple`) publicando em `/topic/couple/{coupleId}/notifications`.
- `recipientUserId == null` (casal sem parceiro): retorna `Optional.empty()` sem salvar nada e sem push — guard antes de qualquer acesso a repositorio.
- Testes novos em `NotificationServiceTest`: cria 1 notificacao com destinatario/mediaTrackId/actorName corretos; push so acontece depois do `afterCommit` (usando `TransactionSynchronizationManager.initSynchronization()` + disparo manual das synchronizations); caso destinatario nulo e no-op total.
- Arquivos alterados:
  - `api/src/main/java/com/app/notification/NotificationService.java`
  - `api/src/test/java/com/app/notification/NotificationServiceTest.java`
  - `api/CLAUDE.md`
- **Learnings for future iterations:**
  - Para testar o `afterCommit` sem Spring: `TransactionSynchronizationManager.initSynchronization()`, chamar o metodo, verificar que `messagingTemplate` NAO foi usado, depois `getSynchronizations().forEach(TransactionSynchronization::afterCommit)` e `clearSynchronization()` num `finally`. Sem `initSynchronization()` o service publica imediatamente — por isso os testes antigos de `notifyCouple` veem o send na hora.
  - `notificationRepository.save(...)` mockado deve devolver a propria entidade com id setado via `ReflectionTestUtils.setField(n, "id", ...)`; devolver uma entidade fabricada a parte (como `savedNotification`) apaga os campos que o teste quer assertar (mediaTrackId, recipient).
  - Nova convencao: eventos de destinatario unico usam `Optional<NotificationDto>`; `notifyCouple` (varios destinatarios) continua com `List<NotificationDto>`.
  - Segue valendo: sem JDK/Maven Central neste sandbox — `./mvnw -DskipTests package` falha ao baixar o proprio Maven. Compilacao/testes pendentes de verificacao em maquina com JDK 21.
---

## 2026-07-27 - US-004
- Novo `RatingRequestService` (com.app.tracking) com o orquestrador unico `onRatingRegistered(MediaTrack track, UUID actorUserId)`: resolve o parceiro pelo `Couple`, checa as 4 condicoes (ator tem rating; parceiro existe; parceiro sem rating; sem RATING_REQUEST nao lida) e delega para `NotificationService.notifyRatingRequest(...)`.
- Chamado nos 3 caminhos: `MediaTrackService.markAsWatched` (sempre), `MediaTrackService.addTrack` (so quando o track nasce `WATCHED`) e `UserReviewService.upsertReview`. As tres passaram a ser `@Transactional` para que a notificacao e o push after-commit fiquem na mesma transacao da escrita da nota.
- Idempotencia: `NotificationRepository.existsByRecipientUserIdAndCoupleIdAndTmdbIdAndTypeAndReadFalse(...)` — derived query escopada por casal (nunca cruza casais que rastreiam o mesmo tmdbId).
- Titulo via `MediaDetailsService.getDetails(...).title()` em `try/catch (RuntimeException)` com fallback `RatingRequestService.FALLBACK_TITLE` ("seu titulo"); TMDB fora do ar nao derruba o registro da nota.
- Testes: `RatingRequestServiceTest` (7 casos: notifica com ator=user1 e ator=user2, parceiro ja avaliou, casal sem parceiro, ator sem nota, pedido nao lido ja existente, fallback de titulo), tests de call site nos 3 caminhos (`MediaTrackServiceTest` x3, `UserReviewServiceTest` x1) e 2 casos de repositorio para a nova query.
- Arquivos alterados:
  - `api/src/main/java/com/app/tracking/RatingRequestService.java` (novo)
  - `api/src/main/java/com/app/tracking/MediaTrackService.java`
  - `api/src/main/java/com/app/tracking/UserReviewService.java`
  - `api/src/main/java/com/app/notification/NotificationRepository.java`
  - `api/src/test/java/com/app/tracking/RatingRequestServiceTest.java` (novo)
  - `api/src/test/java/com/app/tracking/MediaTrackServiceTest.java`
  - `api/src/test/java/com/app/tracking/UserReviewServiceTest.java`
  - `api/src/test/java/com/app/notification/NotificationRepositoryTest.java`
  - `api/CLAUDE.md`
- **Learnings for future iterations:**
  - Adicionar uma dependencia ao construtor de `MediaTrackService`/`UserReviewService` quebra os testes unitarios (`new MediaTrackService(...)` no `@BeforeEach`) — nao ha `@InjectMocks` nesses testes, a construcao e manual. Grepar por `new MediaTrackService(`/`new UserReviewService(` antes de mexer no construtor.
  - `MediaTrackService.markAsWatched`/`addTrack` e `UserReviewService.upsertReview` NAO eram `@Transactional` antes desta story; sem transacao ativa o `scheduleBroadcast` do `NotificationService` publica imediatamente (antes do commit). Qualquer novo efeito colateral pos-escrita precisa que o metodo seja transacional.
  - Em `upsertReview`, a `UserReview` vinda de `userReviewRepository.findByMediaTrackIdAndUserId` e a mesma instancia presente em `track.getReviews()` dentro do mesmo persistence context — por isso o orquestrador pode ler a nota do ator direto de `track.getReviews()` nos 3 caminhos, sem consultar o repositorio de reviews.
  - `US-005` (resolver o pedido do proprio ator) e `US-006` (limpar ao deletar o track) devem entrar no mesmo `RatingRequestService`/`NotificationRepository`, nao em novos servicos.
  - Segue valendo: sem JDK/Maven Central neste sandbox — `./mvnw -DskipTests package` falha ao baixar o proprio Maven. Compilacao/testes pendentes de verificacao em maquina com JDK 21.
---

## 2026-07-27 - US-005
- `RatingRequestService.onRatingRegistered` agora tambem resolve os pedidos do proprio ator: logo apos confirmar que o ator tem nota (e antes de decidir sobre notificar o parceiro), marca como lidas todas as RATING_REQUEST nao lidas de `(actorUserId, couple, tmdbId)` via `resolvePendingRequestsFor(...)`. Como o orquestrador ja era chamado em `markAsWatched`, `addTrack` (WATCHED) e `upsertReview`, os 3 caminhos ficam cobertos sem tocar nos call sites.
- Nova derived query `NotificationRepository.findByRecipientUserIdAndCoupleIdAndTmdbIdAndTypeAndReadFalse(...)` (escopada ao casal, espelhando a query de idempotencia `exists...`), seguida de `saveAll` das entidades com `setRead(true)`. Lista vazia = no-op total (nem `saveAll`).
- Testes: `RatingRequestServiceTest` +3 (resolve o pedido pendente do ator; no-op quando nao ha pendente; nada acontece quando o ator nao deu nota) e `NotificationRepositoryTest` +2 (a query filtra por destinatario/casal/tmdbId/tipo/nao-lida; vazio quando nao ha pendente).
- Arquivos alterados:
  - `api/src/main/java/com/app/tracking/RatingRequestService.java`
  - `api/src/main/java/com/app/notification/NotificationRepository.java`
  - `api/src/test/java/com/app/tracking/RatingRequestServiceTest.java`
  - `api/src/test/java/com/app/notification/NotificationRepositoryTest.java`
  - `api/CLAUDE.md`
- **Learnings for future iterations:**
  - A resolucao tem que vir ANTES do `return` do "parceiro ja avaliou": o caso mais comum (ator e o parceiro pendente respondendo ao pedido) e justamente aquele em que o outro membro ja tem nota, entao colocar a resolucao depois dessa guarda nunca resolveria nada.
  - Em teste Mockito, metodo de repositorio que retorna `List` devolve lista vazia por padrao — os testes antigos que agora passam pelo novo `find...` continuam verdes sem stub adicional. Nao vale para retorno `Optional`/objeto.
  - US-006 (limpar RATING_REQUEST ao deletar o track) deve reusar `NotificationRepository` com uma query de delecao escopada por casal e ser chamada de `MediaTrackService.deleteTrack`, que precisa ser `@Transactional`.
  - Segue valendo: sem JDK/Maven Central neste sandbox — `./mvnw -DskipTests package` nao roda. Compilacao/testes pendentes de verificacao em maquina com JDK 21.
---

## 2026-07-27 - US-006
- `NotificationRepository.deleteByCoupleIdAndTmdbIdAndType(coupleId, tmdbId, type)` (derived delete query, escopada por casal e tipo) + `RatingRequestService.onTrackDeleted(track)`, que chama essa query com `NotificationType.RATING_REQUEST` — nunca toca MATCH/NO_MATCH nem outro casal que rastreie o mesmo tmdbId.
- `MediaTrackService.deleteTrack` passou a ser `@Transactional` e chama `ratingRequestService.onTrackDeleted(track)` antes de `mediaTrackRepository.delete(track)`, na mesma transacao.
- Testes: `NotificationRepositoryTest` +2 (apaga so as RATING_REQUEST daquele casal+titulo, ignorando MATCH/outro titulo/outro casal; no-op quando nao ha nenhuma), `RatingRequestServiceTest` +1 (delega para o repositorio com os ids certos, sem tocar em `notificationService`), `MediaTrackServiceTest` +2 (`verify(ratingRequestService).onTrackDeleted(track)` no caminho feliz; `verify(ratingRequestService, never())...` quando o track nao existe).
- Arquivos alterados:
  - `api/src/main/java/com/app/notification/NotificationRepository.java`
  - `api/src/main/java/com/app/tracking/MediaTrackService.java`
  - `api/src/main/java/com/app/tracking/RatingRequestService.java`
  - `api/src/test/java/com/app/notification/NotificationRepositoryTest.java`
  - `api/src/test/java/com/app/tracking/MediaTrackServiceTest.java`
  - `api/src/test/java/com/app/tracking/RatingRequestServiceTest.java`
- **Learnings for future iterations:**
  - Este era o ultimo item backend do epico; US-007 em diante e tudo `client/` — invocar a skill `frontend-design` antes de qualquer implementacao ali, por regra do `CLAUDE.md` da raiz.
  - `deleteByCoupleIdAndTmdbIdAndType` roda ANTES de `mediaTrackRepository.delete(track)` em `deleteTrack`, nao depois — a limpeza de notificacoes nao depende do track ainda existir (usa `couple.getId()`/`track.getTmdbId()`, nao o id do track), mas manter a ordem "notificacoes primeiro" segue o mesmo padrao dos outros orquestradores do `RatingRequestService`.
  - Segue valendo: sem JDK/Maven Central neste sandbox — `./mvnw -DskipTests package` nao roda. Compilacao/testes pendentes de verificacao em maquina com JDK 21.
---

## 2026-07-27 - US-007
- `client/src/types/notification.ts`: `NotificationType` ganhou `"RATING_REQUEST"` e a interface `Notification` ganhou `mediaTrackId: string | null`, espelhando o `NotificationDto` do backend (US-002).
- Nao ha nenhuma construcao de objeto `Notification` por literal no client (so `import type` e uso via API/store), entao nenhum outro arquivo precisou mudar.
- Arquivos alterados:
  - `client/src/types/notification.ts`
- **Learnings for future iterations:**
  - `client` roda com `npm` neste sandbox (nao ha `bun` instalado, apesar do `docs/ARCHITECTURE.md` mencionar Bun como package manager do projeto); `npm run typecheck` (`tsc --noEmit`) funciona e passou limpo aqui.
  - Story puramente de tipos, sem skill `frontend-design` necessaria (nao ha UI/componente/pagina visivel envolvido) — a regra do CLAUDE.md da raiz sobre `frontend-design` se aplica a partir de US-008 (Dialog) em diante.
  - `docs/` (regra obrigatoria do CLAUDE.md raiz) foi conferido: `ARCHITECTURE.md` tem contexto de arquitetura relevante; `support.js`/`Sessao a Dois.dc.html` sao artefatos gerados de um runtime de design (dc-runtime), sem instrucoes de projeto — so codigo de renderizacao de prototipo.
---

## 2026-07-27 - US-008
- Novo `client/src/components/RatingRequestDialog.tsx`: usa o `Dialog`/`DialogContent` shadcn (`components/ui/dialog.tsx`, primitiva `@base-ui/react/dialog`) em vez do backdrop manual do `ReviewModal`, mas reaproveita 1:1 os tokens visuais inline dele (bg `#161513`, texto `#f6f4ec`, estrelas `#ffb443`/`rgba(255,255,255,.18)`, textarea `#201e18` com foco `#ffcb2b`, botao salvar com borda/bg/texto amarelos, erro em vermelho `#ff6b6b`). Controlado via `mediaTrackId: string | null` (dialog aberto quando nao-null), nao por um trigger interno — pensado para ser aberto de fora (US-009 vai controla-lo a partir do `NotificationDropdown`).
- Estrelas 1-5 e opiniao opcionais (mesma logica de toggle do `ReviewModal`: clicar na estrela atual zera a nota). Submete `PUT /api/tracking/{mediaTrackId}/review` com `{ rating: rating > 0 ? rating : null, opinion: opinion.trim() || null }`; em sucesso chama `onSuccess(track)` (quem monta decide o que fazer, ex. fechar + marcar notificacao como lida).
- Erro 404 tratado explicitamente (`caught.response?.status === 404`) com a mensagem fixa "Este título não está mais na sua lista."; qualquer outro erro cai no fallback generico (`caught.response?.data?.message` ou mensagem padrao), igual ao `ReviewModal`. O Dialog nao fecha sozinho no erro, so limpa `saving`.
- Arquivos alterados:
  - `client/src/components/RatingRequestDialog.tsx` (novo)
- **Learnings for future iterations:**
  - Confirmar `npm run lint` ANTES de mexer em componentes de modal: ha 6 erros pre-existentes de baseline (`react-hooks/set-state-in-effect` em `ReviewModal.tsx`, `WatchModal.tsx`, `PendingDetailModal.tsx` + `react-refresh/only-export-components` em `ui/button.tsx`), nao relacionados a esta story. Testado isolando o arquivo novo (`mv` para fora do `src/`, rodar lint, `mv` de volta) — o count sobe de 6 para 7 so por seguir o MESMO padrao de `useEffect` + `setState` do `ReviewModal` (inevitavel ao espelhar o padrao existente). `npm run typecheck` e `npm run build` sao a barra de qualidade real aqui; lint ja estava vermelho antes desta mudanca.
  - `DialogContent` (shadcn) ja centraliza e anima o popup sozinho; para replicar o visual "hand-rolled" do `ReviewModal` dentro dele, passar `showCloseButton={false}` e colocar as classes de largura/cor direto no `className` do `DialogContent` (ele aceita className livremente, ver `client/CLAUDE.md` secao "Modal patterns" — os dois padroes de modal podem ser misturados: primitiva shadcn por fora, estilo hand-rolled por dentro).
  - Sem browser no sandbox (ver `client/CLAUDE.md`) — verificacao visual real fica pendente para quem tiver Chromium disponivel; so build/typecheck confirmados aqui.
  - US-009 precisa importar este componente no `NotificationDropdown.tsx` e controlar `mediaTrackId` a partir do estado local (ex. `useState<Notification | null>`), chamando `onSuccess` para fechar E marcar a notificacao como lida no `useNotificationStore`.
---
