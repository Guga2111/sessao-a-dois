## Codebase Patterns
- `client/`: dark theme prototype colors are historically inlined via Tailwind arbitrary values (`bg-[#161513]`, etc.) — see `client/CLAUDE.md`. As of Epico 13/US-001, `client/src/index.css` now carries the *real* brand palette as semantic tokens (`.dark` block), so future color-migration stories (US-003+) should map hex -> token per the list in that file's comment, not invent new arbitrary values.
- The app is dark-only in practice (D17, `POST-MVP-TASK.md`): the `ThemeProvider`/`d`-key toggle to light mode still exists in code but produces a broken UI, since every screen hardcodes dark hex regardless of theme. Removing that toggle is a separate future story (T13.3) — don't touch `:root` (light) tokens for color-migration work, only `.dark`.
- Hex/oklch round-trip check: convert with the standard sRGB<->OKLCH matrices (D65, CSS Color 4), round L/C/H to 3 decimals, then convert back and diff per-channel — 1 unit of rounding tolerance is expected and acceptable.
- `bun run typecheck`/`lint`/`test:run`/`build` are all run from `client/` (that's where `package.json` lives), not the repo root.
- Per-file color migration mechanical rules (established US-003, confirmed US-004/US-005/US-006): (1) `bg-[rgba(R,G,B,X)]`/`border-[rgba(R,G,B,X)]` whose R,G,B exactly equals a known token's hex (e.g. `rgba(255,203,43,X)`=`#ffcb2b`=primary, `rgba(255,107,107,X)`=`#ff6b6b`=destructive, `rgba(9,9,10,X)`=`#09090a`=background — confirmed in US-006) converts to `token-name/X` for *any* alpha X — the Tailwind opacity modifier handles the alpha, no exact-alpha match needed. (2) Reusing a *named* semantic token for a *different* CSS property (e.g. `--border` for a `bg-` utility) DOES require an exact alpha match — `rgba(255,255,255,.1)` → `border-border` is valid (exact `--border` value), but `bg-[rgba(255,255,255,.05)]`/`.06`/`.07`/`.08`/`.15` has no token yet and must stay untouched, don't round it into `--border`. (3) A hex value that lives outside a Tailwind className (a plain JS object field, or a `style={{ background: "#hex" }}` inline style) that matches a known token still needs migrating — swap it for `"var(--token-name)"` (same prop/structure), don't leave it just because it's not in a `className`.
- Chart/series color exception (US-007, `DashboardScreen.tsx`): unlike the default "no token → leave hex, note it" fallback, a story's AC can explicitly require chart/graph series colors with no semantic match to use `--chart-1`..`--chart-5` (present in `index.css` but, as of US-007, still unused by any `.tsx` and still the raw shadcn grayscale preset — no brand mapping documented) instead of staying hex — this intentionally breaks pixel-identity for that one series and is scoped only to literal chart/graph fills (donut segments, bar fills), not to icon/label accents that happen to share the same hex. Always check for a story-specific AC bullet like this before applying the generic fallback; `check-color-literals.mjs`'s `HEX_RE`/`ARBITRARY_COLOR_RE` never flag a bare `rgba(...)` outside a Tailwind arbitrary-utility bracket, so those can stay untouched in a gradient string even when a token would technically match — only literal `#hex` and `token-[...]` bracket usages are gated.
- **Superseded by US-039 — `ARBITRARY_COLOR_RE` now DOES check bracket content.** (Old note, kept for history: US-008 found the original regex matched `text-[13px]` identically to `text-[#a6a39a]` because it only checked for the prefix + `-[`.) As of US-039, `check-color-literals.mjs` only flags `bg|text|border|ring|from|to|via|shadow|fill|stroke-[...]` when the bracket content itself looks like a color (hex, `rgb()`/`rgba()`/`hsl()`/`hsla()`, `gradient`, or named `white`/`black`) — a font-size arbitrary is never a gate violation anymore. This mattered because US-039 also removed the per-file allowlist entirely (the gate now runs repo-wide with no allowlist file), and the old imprecise regex would have failed CI on hundreds of legitimate `text-[13px]`-style utilities once there was no allowlist left to absorb them.
- **Color gate has no allowlist as of US-039** — `color-migration-allowlist.txt` was deleted. The only way a raw color survives `check-color-literals.mjs` now is a `color-ok: <reason>` comment on the *same line* as the offense or the line *immediately before* it (checked literally by substring, not by any special syntax) — reserved for genuinely decorative/illustration/external-brand color, never as a stand-in for a missing token. Comment placement is strict: two lines above (e.g. above an opening JSX tag when the color is on the `className=` line below it) does **not** suppress the violation — put the comment directly above the exact line with the color, or between JSX attributes with a bare `//` (valid there, unlike JSX children which need `{/* */}`).
- **Token set as of US-039** (extends the original 8 from US-001): `--series`, `--coral`/`--coral-foreground`/`--coral-chip`, `--success`/`--success-foreground`, `--rating`, `--pill-foreground`, `--label-foreground`, `--tertiary-foreground`, `--accent-strong`, `--destructive-soft`, `--surface-secondary`, `--on-primary`/`--on-success`/`--on-destructive`, `--want-to-see`, `--opinion-foreground`, `--synopsis-foreground`, `--caption-foreground`, `--scrollbar-thumb`, `--warning-foreground`, `--backdrop`, `--nav-surface` — plus a `--shadow-glow-*`/`--shadow-elevation-*` bank (consume via `shadow-[var(--shadow-name)]`) and 6 named `.bg-*` CSS classes in `index.css` for two-stop gradients that can't be expressed as a single token + opacity modifier (`bg-coral-fade-h`, `bg-primary-fade-h`, `bg-primary-fade-h-reverse`, `bg-coral-card-wash`, `bg-coral-divider-v`, `bg-coral-fade-v`). `rgba(255,255,255,X)`/`rgba(8,7,11,X)` convert to Tailwind's own `white/X`/`backdrop/X` respectively, not a new token, for the first one (X% opacity on a *named* Tailwind color needs no CSS variable at all).
- `border-white/10`/`hover:bg-white/[0.06]`/`hover:text-white` (Tailwind's built-in named `white` color, with a plain `/N` opacity modifier or a bracketed *opacity* value) are never flagged by either regex — `ARBITRARY_COLOR_RE` requires the literal sequence `-[` right after the utility prefix, and `white` isn't inside brackets, only the opacity number is. These are common in modal/input borders (`TitleModal.tsx`, confirmed US-008) and are correctly out of migration scope even though `border-white/10` is visually identical to `border-border` (`--border: oklch(1 0 0 / 10%)`) — don't "fix" them into `border-border` on your own initiative, the AC only requires migrating what the gate actually flags.

## 2026-08-13 - US-001
- Implemented: `client/src/index.css`'s `.dark` block now declares the app's real brand palette as semantic tokens, replacing 6 of the generic shadcn neutral defaults and adding 1 new token, so future migration stories can replace hardcoded hex/arbitrary-value classNames with token utilities instead of copy-pasted hex.
  - `--background` -> `#09090a` (oklch(0.14 0.002 286.109))
  - `--foreground` -> `#f6f4ec` (oklch(0.966 0.011 95.163))
  - `--card` -> `#161513` (oklch(0.196 0.004 84.591))
  - `--primary` -> `#ffcb2b` (oklch(0.864 0.167 88.652))
  - `--muted-foreground` -> `#a6a39a` (oklch(0.715 0.013 91.563))
  - `--destructive` -> `#ff6b6b` (oklch(0.712 0.181 22.839))
  - `--accent` (reused, was unused by any component/primitive) -> `#ffe08a` (oklch(0.914 0.111 90.281)) — "destaque claro" (light accent: hover state of the primary yellow, highlighted links/text)
  - `--destructive-foreground` (NEW token, also added to the `@theme inline` block as `--color-destructive-foreground`) -> `#ffb3b3` (oklch(0.839 0.089 19.107)) — light-red text used over translucent destructive-tinted surfaces (error banners, "Tentar novamente" chips). None of the 8 reusable semantics (`--background`/`--card`/`--foreground`/`--muted-foreground`/`--primary`/`--destructive`/`--accent`/`--popover`) represent "light text on a destructive wash", so a new token was the only option — named `--destructive-foreground` to match the existing shadcn `--primary`/`--primary-foreground` pairing convention.
  - `:root` (light theme) intentionally left untouched — see Codebase Patterns above (D17).
  - `--card-foreground`, `--popover`, `--popover-foreground`, `--accent-foreground`, `--primary-foreground` left untouched: not among the 8 required hexes, no evidence they need to change for this story's scope, and touching them would only add risk without an AC requiring it.
- Files changed: `client/src/index.css` only (no `.tsx` touched, per AC).
- Verification: `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`.
- Hex->oklch round-trip (AC requirement): computed with a standalone Node script using the CSS Color 4 sRGB<->OKLCH matrices; every one of the 8 tokens round-trips within 1 unit per channel (7 of 8 exact, `#ffcb2b` round-trips to `#ffcb2c`, 1 unit off on the blue channel).
- **Known nuance surfaced during implementation (documented for the human gate, not blocking):** the AC's premise "nenhum componente usa os tokens ainda, entao redefinir token nao move pixel" is true for every `.tsx` screen file (100% of ~70 `<Button>` usages found across `screens/`/`components/`/`routes/` were checked and each one overrides background/text color via an arbitrary hex className, which `cn()`/`twMerge` resolves in favor of the arbitrary value over the token-driven variant class) — but it is **not strictly true** for a few `components/ui/` primitives that already consume `--primary`/`--destructive`/`--foreground` directly: `button.tsx`'s `default`/`link`/`destructive` variants (`bg-primary`, `text-primary-foreground`, `text-primary`, `bg-destructive/10`, focus-visible/aria-invalid destructive states) and `outline` variant's `hover:text-foreground`/`aria-expanded:text-foreground`. These only render their *un-overridden* pixels in interaction states (hover/focus-visible/aria-expanded/aria-invalid) that a static screenshot wouldn't normally catch, since every actual usage site overrides the base background/text color in its own `className`. No default-static-render pixel diff was found. Flagging this explicitly per D19 (gate humano, `POST-MVP-TASK.md`) since visual verification isn't possible in this sandbox (no browser) — a human should spot-check hover/focus states on a `Button` with `variant="destructive"` (e.g. `SuggestionsTab.tsx`'s reject button) and any `variant="link"`/default-variant button before merge.
- Registrado conforme US-001 AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep + calculo/roundtrip oklch + typecheck/lint/test/build).
- **Learnings for future iterations:**
  - Next priority story is US-002 (CI color-literal gate with allowlist) — it should reference the same 8 tokens (plus `--destructive-foreground`) established here when writing its "use token X instead" error message suggestions.
  - The `.dark`-only edit decision matters for US-002 too: the CI gate/migration stories should keep assuming `.dark` is the only theme that matters; don't "fix" `:root` as a side effect of an unrelated story.
  - `bun audit`/CI concerns are unrelated to this story (no dependency changes) — see `client/CLAUDE.md` for the existing audit-ignore table if a future story touches `package.json`.
---

## 2026-08-13 - US-002
- Implemented: CI gate against new raw color literals outside `client/src/components/ui/`.
  - `client/scripts/check-color-literals.mjs`: walks `src/**/*.{ts,tsx}` (skipping `components/ui/` — permanently exempt — and test files: `*.test.tsx`/`*.spec.tsx`/anything under a `test/` dir), flags `#rrggbb` hex literals and arbitrary Tailwind color utilities (`bg-[...]`/`text-[...]`/`border-[...]`/`ring-[...]`/`from-[...]`/`to-[...]`/`via-[...]`/`shadow-[...]`/`fill-[...]`/`stroke-[...]`). Any hit in a file NOT listed in `color-migration-allowlist.txt` fails with exit 1 and a per-hit message naming the semantic token to use instead (built from the 8 tokens established in US-001: `--background`/`--foreground`/`--card`/`--primary`/`--muted-foreground`/`--accent`/`--destructive`/`--destructive-foreground`); unknown hex values get a generic "no known token, add one to index.css" message instead of silently allowing a new arbitrary value. `--list-offenders` flag prints the raw file list with no allowlist/exit-code enforcement, for regenerating the allowlist.
  - `client/color-migration-allowlist.txt`: the 54 files that currently have hex/arbitrary color (generated via `--list-offenders`, not hand-written), top comment declaring it's temporary debt/the migration scoreboard, and that the PR removing the last line closes the migration.
  - `package.json`: new `check:colors` script (`node scripts/check-color-literals.mjs`).
  - `.github/workflows/ci.yml`: new `Check for raw color literals` step in the `frontend` job, between `Lint` and `Test` — same "gate, no continue-on-error" posture as `Audit dependencies`/`Test`.
- Files changed: `client/scripts/check-color-literals.mjs` (new), `client/color-migration-allowlist.txt` (new), `client/package.json`, `.github/workflows/ci.yml`, `client/CLAUDE.md`, `scripts/ralph/prd.json`.
- Verification: manually confirmed the gate fails (exit 1, correct token suggestion) when a new `#ffcb2b` is added to a currently-clean `screens/` file (`screens/match/helpers.ts`, reverted after the check), and stays green (exit 0) when the same probe hex is added inside `components/ui/button.tsx` (reverted after the check) — matches the two AC test cases verbatim. `bun run typecheck`/`bun run lint`/`bun run test:run` (119 tests, 14 files)/`bun run build` all pass from `client/`.
- **Discrepancy noted (documented in the story's `notes` field too):** the PRD description states the allowlist should have "exactly 53" files; the actual grep/script-verified count is **54**. Trusted the real, script-generated count over the stale PRD number per the AC's own instruction ("gerada por script/grep... conferida contra a contagem real") — the PRD's count was likely computed at a slightly different point in the same day given how fast the hex/arbitrary counts were moving (see the epic description's 21%/33% growth note).
- **Learnings for future iterations:**
  - Regenerate the allowlist with `node scripts/check-color-literals.mjs --list-offenders` (from `client/`) rather than hand-editing `color-migration-allowlist.txt` — this is also how the next per-file migration story (US-003+) should remove its own file's line: confirm with `--list-offenders` that the file no longer appears, then delete its line.
  - The regex only matches 6-digit hex (`#rrggbb`), not 3-digit shorthand or `rgba(...)`/`hsl(...)` — matches the AC's literal wording and this codebase's actual usage (spot-checked: no 3-digit hex found in any offender file). If a future story finds a 3-digit hex slipping through, the regex in `check-color-literals.mjs` is the one place to widen.
  - `components/ui/*` is a **permanent** exemption (design-system primitives are allowed to define raw colors — that's their job), not part of the migration debt/allowlist. Don't add any `components/ui/*.tsx` file to `color-migration-allowlist.txt` even if it has hex — the gate already ignores that whole directory unconditionally.
  - Next priority story is US-003 (migrate `ComparisonDialog.tsx`, 110 occurrences) — when done, remove `components/ComparisonDialog.tsx` from `color-migration-allowlist.txt` in the same commit (per its own AC) and re-run `check:colors` to confirm the file dropped off `--list-offenders`.
---

## 2026-08-13 - US-003
- Implemented: mechanically migrated every hex/arbitrary-color literal in `client/src/components/ComparisonDialog.tsx` that maps to one of the 8 US-001 tokens, to the token utility — `text-[#ffcb2b]`→`text-primary`, `text-[#f6f4ec]`→`text-foreground`, `text-[#a6a39a]`→`text-muted-foreground`, `bg-[#161513]`(incl. `/95` opacity variant)→`bg-card`, `border-[#161513]`→`border-card`, `bg-[#ffcb2b]`→`bg-primary`, `hover:text-[#ffe08a]`→`hover:text-accent`, `border-[rgba(255,203,43,.25)]`→`border-primary/25`, `bg-[rgba(255,203,43,.12)]`→`bg-primary/12`, `border-[rgba(255,255,255,.1)]`→`border-border` (matches the existing `--border: oklch(1 0 0 / 10%)` token exactly — white/10%, already the Tailwind default border color via the `@layer base` rule, so this is a real semantic match, not an approximation). Two inline `style={{ background: ... }}` hex ternaries (the watch-provider "shared" dot) became `style={{ background: isShared ? "var(--primary)" : "var(--muted-foreground)" }}` — same prop, same structure, just the literal value swapped for the CSS var, since neither side of that ternary is a Tailwind className.
- **Files changed:** `client/src/components/ComparisonDialog.tsx` (className/style-value strings only — zero JSX structure/prop/handler/import/logic changes, confirmed by reading the full diff), `scripts/ralph/prd.json` (US-003 → `passes: true`, `notes` documents the leftover colors and the allowlist decision below).
- **Deliberate AC deviation, documented per the story's own notes field:** `components/ComparisonDialog.tsx` was **NOT** removed from `client/color-migration-allowlist.txt`, even though that's a literal AC bullet. Reason: after the mechanical migration above, 16 occurrences across 11 distinct hex/rgba values remain that have **no corresponding token** in US-001's 8-token set — `#ffb443` (TMDB-rating gold star, distinct from primary yellow), `#ffdd7a` ×3 (shared-badge text), `#3ddc97` (TMDB green dot), `rgba(255,255,255,.05)` ×2 (unshared-pill background), `#d6d2c8` ×2 (unshared-pill text), `#c4bfb4` (synopsis paragraph text), `shadow-[...rgba(0,0,0,.6)]` (dialog shadow — no `--shadow-*` token exists anywhere in `index.css`), `#2b2920` (scrollbar thumb), `border-[rgba(255,255,255,.06)]` (sticky-header border, alpha `.06` ≠ the `.10` in `--border`), `#111` ×2 (VS-badge text), `shadow-[...rgba(255,203,43,.4)]` (VS-badge colored shadow). The AC's own fallback clause ("se algum valor de cor NAO tiver token correspondente: deixar como esta e registrar em notes") explicitly anticipates this — but removing the file from the allowlist anyway would make `check:colors` (the CI gate from US-002) fail on the next run, since those 16 raw-color hits would no longer be covered by the allowlist. Kept the file in the allowlist to keep CI green, which the Ralph instructions treat as a harder constraint than a single AC bullet whose own text already concedes partial completion is expected. Confirmed `node scripts/check-color-literals.mjs` still reports "ok" after the edit.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` (no `--list-offenders`) passes with the file still allowlisted.
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmação do gate `check:colors`).
- **Learnings for future iterations:**
  - **Big one:** every subsequent per-file migration story (US-004 onward) should expect the same shape of problem — files almost certainly have hex/rgba values beyond the 8 tokens US-001 established (US-001 only tokenized the 8 *most frequent* hexes app-wide, not every color any single file happens to use). When that happens: migrate what maps, leave what doesn't (don't invent), note the leftover values in the story's `notes` field — but do **NOT** remove the file from `color-migration-allowlist.txt` if any raw color remains, or `check:colors` in CI breaks. Only pull a file off the allowlist once `node scripts/check-color-literals.mjs --list-offenders` (from `client/`) no longer lists it.
  - `rgba(255,255,255,.1)` (10% white) already has an exact semantic match: the `--border` token (`oklch(1 0 0 / 10%)` in `.dark`) — use `border-border`, not an arbitrary value, whenever you see that exact alpha. `.05` and `.06` white/black rgba values are *not* the same token (different alpha) — don't round them into `--border` just because they're visually close; that would move a pixel to save a token.
  - Inline `style={{ background: "#hex" }}` (not a Tailwind className) can still trip `check-color-literals.mjs` — its `HEX_RE` matches any `#rrggbb` in the line, class or not. If the hex matches a known token, swap it for `style={{ background: "var(--token-name)" }}` (same prop, same structure) rather than trying to force it into a className.
  - A recurring color family worth flagging for a future US-001 follow-up (not done here, per "don't invent tokens" rule): `#ffdd7a`/`rgba(255,255,255,.05)`/`#d6d2c8` form a "shared vs. not-shared pill" pattern that shows up identically in both the genres and watch-providers sections of `ComparisonDialog.tsx` — likely reused verbatim in other match/comparison components too, so a real token pair for it (e.g. something like `--pill-shared`/`--pill-neutral`) would clean up several files at once instead of one at a time.
  - Next priority story is US-004 (`MediaDetailModal.tsx`, 103 occurrences) — same mechanical approach; check first whether it shares any of the untokenized colors found here (`#ffb443`, `#3ddc97`, `#d6d2c8`, `#c4bfb4`, `#2b2920`, `#111` are all plausible prototype-wide constants, not one-off local values).
---

## 2026-08-13 - US-004
- Implemented: mechanically migrated `client/src/components/MediaDetailModal.tsx`'s hex/arbitrary-color literals to the US-001 tokens — `text-[#f6f4ec]`/`hover:text-[#f6f4ec]`→`text-foreground`/`hover:text-foreground`, `text-[#a6a39a]`(incl. `/40` opacity variant)→`text-muted-foreground`, `bg-[#161513]/90`→`bg-card/90`, inline gradient/style hex `#161513`→`var(--card)`, `text-[#ffb3b3]`→`text-destructive-foreground`, `border-[rgba(255,107,107,.35|.4)]`→`border-destructive/35|40`, `bg-[rgba(255,107,107,.08|.12)]`→`bg-destructive/8|12`, `border-[rgba(255,255,255,.1)]`(exact `--border` alpha, 3 occurrences)→`border-border`, `text-[#ffcb2b]`→`text-primary`, `bg-[#ffcb2b]`/`hover:bg-[#ffe08a]`→`bg-primary`/`hover:bg-accent`, `text-[#09090a]`(incl. hover variant)→`text-background`. Two hex values that live outside any Tailwind className (`STATUS_BADGE.WATCHING.text` plain object field, and the watch-provider dot's `style={{ background: "#ffcb2b" }}`) became `"var(--primary)"` — same non-className CSS-var substitution pattern US-003 used for `ComparisonDialog.tsx`'s shared-dot ternary.
- **Files changed:** `client/src/components/MediaDetailModal.tsx` (className/style-value strings only — full diff read to confirm zero JSX structure/prop/handler/import/logic changes), `scripts/ralph/prd.json` (US-004 → `passes: true`, `notes` documents the 6 leftover untokenized hex values and the allowlist decision below).
- **Deliberate AC deviation (same shape as US-003, expected per that story's own learning note):** `components/MediaDetailModal.tsx` was **NOT** removed from `client/color-migration-allowlist.txt`. Confirmed via `node scripts/check-color-literals.mjs --list-offenders` (from `client/`) that the file still has raw color after the mechanical migration: 6 hex values with no US-001 token — `#c49dff` (WANT_TO_SEE badge text, purple, unique to this file), `#3ddc97` (WATCHED badge text + TMDB-score dot, green — same hex flagged as leftover in US-003), `#ffb443` (`Stars` component's filled-star gold), `#d6d2c8` ×2 (genre/provider pill text — same hex flagged as leftover in US-003), `#c4bfb4` (overview paragraph text), `#d9d4e6` (opinion italic text) — plus several rgba arbitrary utilities with no exact-alpha token match (`border-[rgba(255,255,255,.08)]`, `border-[rgba(255,255,255,.06)]`, `bg-[rgba(255,255,255,.05)]` ×3) and one `shadow-[...rgba(255,203,43,.35)]` (no `--shadow-*` token exists, same as US-003's VS-badge shadow). Pulling the file off the allowlist with any of these remaining would break the `check:colors` CI gate (US-002) — kept it allowlisted, matching the precedent and the AC's own fallback clause.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` (no `--list-offenders`) passes — the file's remaining raw color is still covered by the allowlist entry.
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao do gate `check:colors`).
- **Learnings for future iterations:**
  - `#3ddc97` and `#d6d2c8` are now confirmed leftovers in **two** files (`ComparisonDialog.tsx` and `MediaDetailModal.tsx`) — reinforces US-003's note that a "TMDB green dot" / "shared vs. neutral pill text" token pair is worth a future US-001 follow-up once enough files surface the same untokenized colors, rather than guessing early.
  - The `rgba(255,107,107,X)` family is `#ff6b6b` (the `--destructive` token) at various alphas — same mechanical rule as `rgba(255,203,43,X)` → `--primary`/X established in US-003: any `bg-[rgba(R,G,B,X)]`/`border-[rgba(R,G,B,X)]` whose R,G,B exactly matches a known token's hex converts to `token-name/X` (Tailwind opacity modifier), regardless of which alpha X is — the modifier handles the opacity, no need for an exact-alpha token match (that requirement only applies when reusing a *named* semantic token like `--border` for a *different* property, e.g. `border-[rgba(255,255,255,.1)]` → `border-border`).
  - `bg-[rgba(255,255,255,.05)]` (as opposed to the `.1`-alpha `border-[rgba(255,255,255,.1)]` → `border-border` case) has no token yet in this codebase, on either file checked so far — don't reuse `--border` for a `bg-` utility even at a different alpha; `--border` is a border-color-role token, not a generic "white translucent" token, and no `bg-` equivalent exists. Leave `bg-[rgba(255,255,255,.05)]`/`.06`/`.08` untouched until/unless a future US-001 follow-up adds one.
  - Next priority story is US-005 (`PendingDetailModal.tsx`, 98 occurrences) — per `client/CLAUDE.md`'s "Loading-state pattern" section, this file shares the `detailsError`/`fetchDetails`/`showSkeleton` three-way render pattern with `MediaDetailModal.tsx` (both born in Epico 7 US-007), so expect a similar/overlapping leftover-hex list (`#3ddc97`, `#d6d2c8`, `#c4bfb4` are good first guesses to check for) and the same `bg-[rgba(255,255,255,.05)]`/`border-[rgba(255,255,255,.1)]` split.
---

## 2026-08-13 - US-005
- Implemented: mechanically migrated `client/src/components/PendingDetailModal.tsx`'s hex/arbitrary-color literals to the US-001 tokens — `text-[#f6f4ec]`→`text-foreground`, `bg-[#161513]`/`bg-[#161513]/90`→`bg-card`/`bg-card/90` (including the same hex embedded inside an inline `radial-gradient(...)` CSS string, not just a className, which still trips `HEX_RE` since it matches anywhere on the line — swapped for `var(--card)`, same non-className substitution pattern as US-003/US-004), `text-[#a6a39a]` (8 occurrences, incl. the `text-[#a6a39a]/40` separator)→`text-muted-foreground`, `border-[rgba(255,203,43,.4)]`/`bg-[rgba(255,203,43,.18)]`/`text-[#ffcb2b]`→`border-primary/40`/`bg-primary/18`/`text-primary` (Sugestao badge), `border-[rgba(255,107,107,X)]`/`bg-[rgba(255,107,107,X)]`/`text-[#ff6b6b]`→`border-destructive/X`/`bg-destructive/X`/`text-destructive` (Passar button + both error/retry blocks), `text-[#ffb3b3]`→`text-destructive-foreground` (4 occurrences), `border-[rgba(255,255,255,.1)]` (exact `--border` alpha, close button + both genre/provider pills)→`border-border`, and the non-shared watch-provider dot's `style={{ background: "#ffcb2b" }}`→`style={{ background: "var(--primary)" }}`.
- **Files changed:** `client/src/components/PendingDetailModal.tsx` (className/style-value strings only — full diff read to confirm zero JSX structure/prop/handler/import/logic changes), `scripts/ralph/prd.json` (US-005 → `passes: true`, `notes` documents the leftover untokenized values and the allowlist decision below).
- **Deliberate AC deviation (same shape as US-003/US-004, expected per those stories' own learning notes):** `components/PendingDetailModal.tsx` was **NOT** removed from `client/color-migration-allowlist.txt`. Confirmed via `node scripts/check-color-literals.mjs --list-offenders` (from `client/`) that the file still has raw color after the mechanical migration: `rgba(255,255,255,.05)`/`.06`/`.08` (5 occurrences, no `bg-`/`border-` token exists at those alphas — only `.1` matches `--border`), `#3ddc97` (TMDB-score dot + the entire Curtir/Like button, including its `rgba(61,220,151,X)` family, which *is* this same hex — a green with no token, same leftover flagged in US-003/US-004), `#d6d2c8` (genre/provider pill text, 2 occurrences), `#c4bfb4` (synopsis paragraph). Pulling the file off the allowlist with any of these remaining would break the `check:colors` CI gate (US-002) — kept it allowlisted, matching precedent.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` (no `--list-offenders`) passes — the file's remaining raw color is still covered by the allowlist entry.
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao do gate `check:colors`).
- **Learnings for future iterations:**
  - **New pattern found here, worth generalizing:** a token-matching hex can live inside a composite CSS value string, not just as a standalone `style={{ background: "#hex" }}` — here it was the *second* layer of a two-layer `background: "radial-gradient(...), #161513"` string. `HEX_RE` in `check-color-literals.mjs` matches `#rrggbb` anywhere on the line regardless of surrounding syntax, so this still counts as a violation and still gets swapped for `var(--token-name)` in place, leaving the rest of the CSS value string (the gradient function, its stops) untouched — confirmed CSS custom properties are valid as a fallback/final layer in a multi-layer `background` shorthand.
  - `rgba(61,220,151,X)` (`#3ddc97`, the TMDB-green) is now confirmed as a leftover in a **third** file (previously `ComparisonDialog.tsx`, `MediaDetailModal.tsx`) — same reinforcement noted in US-004: a real token for this green (and for `#d6d2c8`/`#c4bfb4`, also now 3-for-3 or 2-for-2 across files) is worth a future US-001 follow-up once the pattern is this consistent, rather than guessing early.
  - `PendingDetailModal.tsx` and `MediaDetailModal.tsx` (US-004) share near-identical markup for the close button and the genre/watch-provider pills (both born in Epico 7 US-007's shared detail-modal pattern per `client/CLAUDE.md`) — confirmed the exact same mechanical substitution (`border-border`, `bg-[rgba(255,255,255,.05)]` left alone, `hover:bg-[rgba(255,255,255,.1)]` left alone since no `bg-` equivalent of `--border` exists) applies verbatim to both files. Any future modal sharing this structure should expect the same result without re-deriving it.
  - Non-color arbitrary utilities (`text-[13px]`/`[11px]`/`[12px]`/`[14px]`/`[17px]`/`[clamp(...)]`, font sizes) and `bg-white/[0.06]` (Tailwind's named `white` color + arbitrary opacity, not an arbitrary color utility in the `bg-[...]` sense) are **not** matched by `check-color-literals.mjs`'s regexes and are correctly out of scope for these migration stories — confirmed by grepping `MediaDetailModal.tsx` (already migrated, `passes: true`) and finding the exact same patterns still present there untouched.
  - Next priority story is US-006 (`MediaCard.tsx`, 98 occurrences) — given how consistently `#3ddc97`/`#d6d2c8`/`#c4bfb4`/`rgba(255,255,255,.05|.06|.08)` keep showing up as leftovers across every file migrated so far, expect the same set here too.
---

## 2026-08-13 - US-006
- Implemented: mechanically migrated `client/src/components/MediaCard.tsx`'s hex/arbitrary-color literals to the US-001 tokens — `hover:border-[rgba(255,203,43,.5)]`→`hover:border-primary/50` (the `STATUS_BORDER_HOVER` map's `WATCHING`/`WANT_TO_SEE` entries), `bg-[#161513]`/`text-[#f6f4ec]`→`bg-card`/`text-foreground` (card root), `border-[#ffcb2b]`→`border-primary` (compare-selected outline), `bg-[rgba(9,9,10,.6)]`→`bg-background/60` (type badge + delete button — **new pattern this story**: `rgba(9,9,10,X)` matches `--background`'s hex exactly, same "reuse a token for its natural property at any alpha" rule already established for primary/destructive, just applied to `--background` for the first time), `border-[#161513]`/`bg-[#ffcb2b]`→`border-card`/`bg-primary` (compare-order badge), `bg-[rgba(255,107,107,.85)]`/`outline-[#ff6b6b]`→`bg-destructive/85`/`outline-destructive` (delete-button hover/focus), `text-[#a6a39a]`→`text-muted-foreground` (×3: year, couple-avg, watched date), `border-[rgba(255,203,43,.25)]`/`bg-[rgba(255,203,43,.12)]`→`border-primary/25`/`bg-primary/12` (type pill), `border-[rgba(255,255,255,.1)]`→`border-border` (tooltip, exact alpha match), `text-[#f6f4ec]`→`text-foreground` (tooltip + 3 outline buttons), `border-[rgba(255,203,43,.18)]`/`bg-[rgba(255,203,43,.07)]`/`text-[#ffcb2b]`→`border-primary/18`/`bg-primary/7`/`text-primary` (opinion box + quote mark), `text-[#ffb3b3]`→`text-destructive-foreground` (start-watching error), `border-[rgba(255,203,43,.35)]`/`bg-[rgba(255,203,43,.1)]`/`text-[#ffcb2b]`/`hover:bg-[rgba(255,203,43,.16)]`/`hover:text-[#ffcb2b]`→`border-primary/35`/`bg-primary/10`/`text-primary`/`hover:bg-primary/16`/`hover:text-primary` (Avaliar button).
- **Files changed:** `client/src/components/MediaCard.tsx` (className strings only — full diff read to confirm zero JSX structure/prop/handler/import/logic changes), `scripts/ralph/prd.json` (US-006 → `passes: true`, `notes` documents leftovers and the allowlist decision below).
- **Deliberate AC deviation (same shape as US-003 through US-005, expected per those stories' learning notes):** `components/MediaCard.tsx` was **NOT** removed from `client/color-migration-allowlist.txt`. Confirmed via `node scripts/check-color-literals.mjs --list-offenders` (from `client/`) that raw color remains: `hover:border-[rgba(61,220,151,.4)]`/`bg-[rgba(61,220,151,.9)]` (the `#3ddc97` green family, WATCHED status — leftover in every file so far), `text-[#ffb443]` (`Stars` component gold, same as `MediaDetailModal.tsx`), `shadow-[...rgba(255,203,43,.18)]` (no `--shadow-*` token, same as the VS-badge/dialog shadows in US-003/004), `border-[rgba(255,255,255,.07)]`/`.06`/`.15` (alphas with no exact `--border` match — `.1` is the only alpha that qualifies), `text-[#07130d]`/`text-[#1a0808]` (unique dark text-on-colored-surface values, no token), `text-[#111]` (compare-order badge text, same leftover as the `ComparisonDialog.tsx` VS badge in US-003), `text-[#d6d2c8]`/`text-[#ffdd7a]`/`text-[#d9d4e6]` (recurring leftovers, 4th+ file each), `bg-[#201e18]` (tooltip surface, not one of the 8 tokens). Pulling the file off the allowlist with any of these remaining would break the `check:colors` CI gate (US-002) — kept it allowlisted, matching precedent.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` (no `--list-offenders`) passes — the file's remaining raw color is still covered by the allowlist entry.
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao do gate `check:colors`).
- **Learnings for future iterations:**
  - **New pattern, worth generalizing to the Codebase Patterns section:** `rgba(9,9,10,X)` (any alpha) is `#09090a` = `--background`, exactly the same "reuse a token for its own property at any alpha" rule already used for `--primary`/`--destructive` — converts to `bg-background/X`. This is the first file where `--background` itself (not just `--card`/`--primary`/`--destructive`) showed up as a `bg-[...]` arbitrary value; expect it again in files with translucent badges/pills over a poster image (`SearchResultCard.tsx`, `SuggestionsTab.tsx` both already have the identical `bg-[rgba(9,9,10,.6)]` badge pattern, confirmed by grep — likely convert the same way when their stories come up).
  - `#111` (near-black badge/text) is now a leftover in a **second** file (`ComparisonDialog.tsx`'s VS badge, now `MediaCard.tsx`'s compare-order badge) — same non-token status, no new evidence it needs one.
  - Next priority story is US-007 (`DashboardScreen.tsx`, 95 occurrences) — per its own AC, chart/series colors without a semantic-token match should use the existing `--chart-1`..`--chart-5` tokens instead of being left as hex, which is a different rule than the "leave it, note it" fallback used for every story so far — read that AC bullet carefully before starting.
---

## 2026-08-13 - US-007
- Implemented: mechanically migrated `client/src/screens/DashboardScreen.tsx`'s hex/arbitrary-color literals to the US-001 tokens — `bg-[#161513]`→`bg-card` (ChartCard, KpiCard, donut center circle), `text-[#a6a39a]`→`text-muted-foreground` (15 occurrences across all chart/KPI labels), `bg-[#ffcb2b]`→`bg-primary` (monthly bar fill, donut "Filmes" legend swatch, genre bar fill, KPI movie/tv split bar's movie segment), `text-[#f6f4ec]` (root container)→`text-foreground`, the `#09090a` hex embedded inside the root's composite `background` gradient string (not a className — same "hex inside a non-className CSS value" pattern as US-005's `radial-gradient(...), #161513`)→`var(--background)`, and `iconColor="#ffcb2b"` (KPI "Tempo juntos", a plain string prop consumed via `style={{ color: iconColor }}`, same non-className substitution pattern as every prior story)→`var(--primary)`.
- **New pattern this story, driven by its own distinct AC bullet (not the standard "leave it, note it" fallback the other stories used):** the AC explicitly requires chart/series colors without a semantic-token match to use the existing (previously unused-by-any-`.tsx`) `--chart-1`..`--chart-5` tokens instead of staying raw hex. `#ff9e2c` — the "Séries/TV" series color in the actual chart renders (donut `conic-gradient` second segment, donut legend swatch, and the "Filmes vs Séries" KPI's progress-bar second segment) — has no semantic-token match, so it became `var(--chart-2)` (inline style) / `bg-chart-2` (className) in those 3 spots, while the "Filmes" series kept `--primary` (already tokenized, unchanged). This is a **deliberate exception to the "pixel-identical" rule** every other migration story followed: `--chart-2` resolves to the shadcn preset gray (`oklch(0.556 0 0)`), not `#ff9e2c`'s orange, so the second chart segment visibly changes color (orange → gray) — the AC itself mandates this trade (zero hex in charts over pixel fidelity), unlike the general "leave what doesn't map" instruction used for every other file so far.
- **Leftovers deliberately left untouched (same fallback rule as every other file, since these are icon/label accents, not chart-series pixels):** `border-[rgba(255,255,255,.07)]` (ChartCard/KpiCard border, alpha doesn't match `--border`'s `.1`, 2 occurrences), `#ff9e2c` in the "Filmes vs Séries" KPI's `iconColor` prop and the "Estatísticas do Casal" eyebrow label text (both decorative, not literal chart-series fills, so NOT redirected to `--chart-2` — only the actual bar/donut fill pixels were, per the AC's own scoping to "graficos (barras, donut, breakdown de genero)"), `#3ddc97` (KPI "Tempo juntos" up-indicator text + "Total assistido" `iconColor` — same recurring green from every prior story), `#ffb443` (Heart `iconColor`, "Gênero favorito" KPI — same recurring gold).
- **Files changed:** `client/src/screens/DashboardScreen.tsx` (className/style-value strings only — full diff read to confirm zero JSX structure/prop/handler/import/logic changes), `scripts/ralph/prd.json` (US-007 → `passes: true`, `notes` documents the chart-token decision and remaining leftovers).
- **Deliberate AC deviation (same shape as US-003 through US-006):** `screens/DashboardScreen.tsx` was **NOT** removed from `client/color-migration-allowlist.txt` — confirmed via `node scripts/check-color-literals.mjs --list-offenders` (from `client/`) that raw color remains (the leftovers listed above). Removing it would break the `check:colors` CI gate (US-002); kept it allowlisted, matching precedent.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` (no `--list-offenders`) passes — the file's remaining raw color is still covered by the allowlist entry.
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao do gate `check:colors`). Atencao extra do gate humano: o segundo segmento do donut e da barra "Filmes vs Séries" mudou de cor (laranja `#ff9e2c` → cinza `--chart-2`) de proposito, unico pixel alterado nesta story — vale confirmar visualmente se esse cinza e aceitavel ou se `--chart-2` merece uma cor de marca numa story futura.
- **Learnings for future iterations:**
  - **Chart-series color rule, worth remembering for any future chart/graph story:** when an AC explicitly calls out chart/series colors, check whether it overrides the default "leave untouched, note in `notes`" fallback — here it did, redirecting to `--chart-1`..`--chart-5` instead. Don't assume every story follows the same fallback; read each AC's chart-specific bullets before applying the mechanical pattern blindly.
  - `--chart-1`..`--chart-5` are still the raw shadcn grayscale preset (`oklch(0.87 0 0)` down to `oklch(0.269 0 0)`) — nothing in `index.css`'s `.dark` block comment documents an intended brand mapping for them (unlike the 8 US-001 tokens, which have inline comments explaining their real hex). If a future story recolors them to match the brand, expect a real (intentional) pixel change on every consumer, including this one.
  - The same "hex embedded inside a non-className CSS value string" pattern from US-005 (there: inside a `radial-gradient(...)` in `background`) recurred here in `DashboardScreen.tsx`'s own root-level `background` gradient string — same fix (swap only the trailing hex for `var(--token)`, leave the `rgba(...)` gradient stops alone since they're not hex and not flagged by the gate's regexes).
  - Confirmed via `check-color-literals.mjs`'s own regexes (`HEX_RE`/`ARBITRARY_COLOR_RE`) that `rgba(...)` values that aren't wrapped in a Tailwind arbitrary utility (`bg-[...]`/`border-[...]`/etc.) are **not** flagged at all, even when they're a token's RGB at some alpha embedded in a plain CSS string — e.g. this file's `rgba(255,203,43,.16)`/`rgba(255,158,44,.10)` inside the root gradient's `style` string were left untouched, correctly, since only the `#09090a` in that same string was an actual gate violation.
  - Next priority story is US-008 (`TitleModal.tsx`, 89 occurrences) — no chart-specific AC bullet expected there (it's a form modal, not a data-viz screen), so the standard "leave it, note it" fallback from US-003–006 should apply again; don't assume `--chart-*` tokens are relevant outside dashboard/stats-style screens.
---

## 2026-08-13 - US-008
- Implemented: mechanically migrated `client/src/components/TitleModal.tsx`'s hex/arbitrary-color literals to the US-001 tokens — `bg-[#161513]`→`bg-card` + `text-[#f6f4ec]`→`text-foreground` (`DialogContent` root), `text-[#f6f4ec]`→`text-foreground` (title, both text inputs, textarea, Cancelar button), `text-[#a6a39a]`→`text-muted-foreground` (subtitle, close button, search icon, result-count span, "Selecionado:" row, "trocar" link, opinion label's "(opcional)"), `border-[#ffcb2b]`/`bg-[#ffcb2b]`→`border-primary`/`bg-primary` (active status pill, all 3 inputs' `focus:border-[#ffcb2b]`, Salvar Título button), `border-[#ff6b6b]/30`+`bg-[#ff6b6b]/10`+`text-[#ffb3b3]`→`border-destructive/30`+`bg-destructive/10`+`text-destructive-foreground` (error block).
- **Files changed:** `client/src/components/TitleModal.tsx` (className strings only — full diff read to confirm zero JSX structure/prop/handler/import/logic changes), `scripts/ralph/prd.json` (US-008 → `passes: true`, `notes` documents leftovers and the allowlist decision).
- **Deliberate AC deviation (same shape as US-003 through US-007):** `components/TitleModal.tsx` was **NOT** removed from `client/color-migration-allowlist.txt`. Confirmed via `node scripts/check-color-literals.mjs --list-offenders` (from `client/`) that raw color remains: `bg-[#201e18]` (5×, input/dropdown surface — same hex as the "tooltip surface" leftover from US-006, here reused for form inputs), `text-[#d8d3c5]` (6×, label text — a near-neighbor of the `#d6d2c8` leftover seen in other files but a *distinct* hex, no exact match), `text-[#ffdd7a]` (selected-title highlight, recurring "shared badge text" leftover), `text-[#111]` (2×, dark text over the yellow status pill / Salvar button — same leftover as `ComparisonDialog.tsx`'s VS badge and `MediaCard.tsx`'s compare-order badge), `#ffb443` inline in the star-rating `style={{ color: ... }}` ternary (TMDB/rating gold, recurring leftover, still caught by `HEX_RE` even outside a className), and 4 `shadow-[...]` arbitrary values (dialog shadow, 2 focus-ring/dropdown shadows with `rgba(255,203,43,X)`, Salvar-button shadow — no `--shadow-*` token exists, same as every prior story's shadow leftovers). Removing the file would break the `check:colors` CI gate (US-002) — kept it allowlisted, matching precedent.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` (no `--list-offenders`) passes — the file's remaining raw color is still covered by the allowlist entry (54 files, unchanged count).
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao do gate `check:colors`).
- **Learnings for future iterations:**
  - See the new Codebase Patterns bullets above: `ARBITRARY_COLOR_RE` matches font-size arbitrary utilities (`text-[13px]`) exactly like color ones (a prior note calling these "unmatched" was wrong, just usually inconsequential), and `border-white/10`-style utilities (Tailwind's named `white` + plain opacity modifier, not a bracket) are never flagged at all — don't migrate those on your own initiative even when they're visually equivalent to a token.
  - Next priority story is US-009 (`screens/account/DeleteAccountSection.tsx`, 81 occurrences) — first `screens/account/` file in this migration sequence; per `client/CLAUDE.md`'s "O tratamento destrutivo do design system" section, this file is where the app's distinct destructive palette (`#ff5c47`/`rgba(255,92,71,.28)`/`rgba(255,92,71,.07)`/`#ffb3a5`/`#ff8f7c`) originates — none of those hexes are US-001 tokens (`--destructive` is `#ff6b6b`, a different red), so expect a large leftover block there specifically, not a sign of a mapping error.
---

## 2026-08-13 - US-009
- Implemented: mechanically migrated `client/src/screens/account/DeleteAccountSection.tsx`'s hex/arbitrary-color literals that map to a US-001 token — `text-[#a6a39a]`→`text-muted-foreground` (`FIELD_LABEL_CLASS`, "Fica onde está" list, dialog subtitle, "done" farewell message), `text-[#f6f4ec]`→`text-foreground` ("Sai com você" list, `DialogContent` root, "Manter minha conta" button), `text-[#ffcb2b]`/`bg-[#ffcb2b]`/`border-[#ffcb2b]/30`/`bg-[#ffcb2b]/10`→`text-primary`/`bg-primary`/`border-primary/30`/`bg-primary/10` (the "Fica onde está" label, its bullet dot, and the `Latch` open-state pill), `bg-[#161513]`→`bg-card` (`DialogContent` root) — 8 substitutions total, same mechanical rule as US-003–US-008 (only exact-hex matches convert, `rgba(R,G,B,X)` converts to `token/X` at any alpha only when R,G,B exactly equals a token's RGB).
- **Confirmed prediction from US-008's learning note:** this file's own destructive palette (`#ff5c47`, `#ff8f7c`, `#ffb3a5`, `rgba(255,92,71,X)` at several alphas: `.08`/`.1`/`.12`/`.15`/`.18`/`.28`/`.35`) is genuinely distinct from the app's `--destructive` token (`#ff6b6b`) — confirmed via `client/CLAUDE.md`'s own "screens/account/" section, which documents this coral as a *deliberate* design choice ("vizinho de matiz do `#ff9e2c` já usado, para ler como 'o mesmo mundo ficando hostil', não como um vermelho de sistema"), not an oversight. None of these converted; per the AC's fallback clause they're left exactly as they were.
- **Other leftovers (same "no token, leave it" fallback):** `#6f6c62` (tertiary/muted text, 3 occurrences, distinct from `--muted-foreground`'s `#a6a39a`), `bg-[rgba(8,7,11,.72)]` (dialog backdrop — `rgb(8,7,11)` is close to but **not** exactly `--background`'s `rgb(9,9,10)`, so per the "exact RGB match only" rule it does not convert to `bg-background/X`), `bg-[linear-gradient(180deg,rgba(255,92,71,.5),rgba(255,203,43,.5))]` (the Inventory divider rule — a single `bg-[...]` arbitrary value containing two color stops, one of which *is* `--primary` at `.5` but the utility itself can't be split into two tokens without inventing a new gradient utility, so left whole), `shadow-[0_30px_80px_rgba(0,0,0,.6)]`/`shadow-[0_6px_20px_rgba(255,92,71,.15)]` (no `--shadow-*` token, same as every prior story). `border-white/[0.07]`/`[0.08]`/`10` and `bg-white/[0.04]`/`hover:bg-white/[0.06]` (Tailwind's named `white` + opacity, not `-[...]` immediately after the prefix) are confirmed — again — not matched by `ARBITRARY_COLOR_RE` at all (per US-008's finding), so correctly untouched and out of scope.
- **Files changed:** `client/src/screens/account/DeleteAccountSection.tsx` (className strings only — full diff read to confirm zero JSX structure/prop/handler/import/logic changes), `scripts/ralph/prd.json` (US-009 → `passes: true`, `notes` documents the leftovers and the allowlist decision below).
- **Deliberate AC deviation (same shape as US-003 through US-008):** `screens/account/DeleteAccountSection.tsx` was **NOT** removed from `client/color-migration-allowlist.txt`. Confirmed via `node scripts/check-color-literals.mjs --list-offenders` (from `client/`) that the file still has raw color after the mechanical migration (the leftovers listed above). Removing it would break the `check:colors` CI gate (US-002) — kept it allowlisted, matching precedent.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` (no `--list-offenders`) passes — the file's remaining raw color is still covered by the allowlist entry (54 files, unchanged count).
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao do gate `check:colors`).
- **Learnings for future iterations:**
  - `screens/account/` files (this is the first one migrated) carry **two** color families to keep straight: the app-wide US-001 tokens (which do convert) and this section's own local destructive coral (`#ff5c47`/`#ff8f7c`/`#ffb3a5`/`rgba(255,92,71,X)`), which is a deliberate design decision documented in `client/CLAUDE.md`, not leftover debt to "fix" by mapping it onto `--destructive`. Any future story touching `AccountSection.tsx`/`ProfileSection.tsx`/`PasswordSection.tsx`/`CoupleSection.tsx` (same `screens/account/` family) should expect this same coral family as a leftover, not a bug.
  - New leftover shape confirmed here: a single Tailwind arbitrary utility can bundle a CSS function (`linear-gradient(...)`) with *multiple* embedded colors, one of which happens to match a token (`rgba(255,203,43,.5)` = `--primary` at 50%) and one of which doesn't (`rgba(255,92,71,.5)`, the local coral). Since `bg-[...]` is a single indivisible utility, there's no way to convert "half" of it — the whole thing stays arbitrary, same as any other no-match leftover. Don't try to hand-split a multi-stop gradient into token calc() expressions to "partially" migrate it.
  - `rgba(R,G,B,X)` needs an **exact** R,G,B match to a token's RGB to convert — `rgba(8,7,11,X)` (this file's dialog backdrop) is visually near-black like `--background` (`rgb(9,9,10)`) but is a genuinely different value, not a rounding artifact of the same design decision. Don't round a "close enough" rgba into a token; only bit-for-bit matches from the US-001 8-hex table qualify.
  - Next priority story is US-010 (`screens/account/CoupleSection.tsx`, 78 occurrences) — same `screens/account/` family as this one, so expect the same reach-color pattern (`"you"` amber vs `"both"` coral per `client/CLAUDE.md`) and possibly a third color family beyond the 8 tokens and the destructive coral, given it's the "both" (couple-dissolving) section. Its own AC also has a special note: the dissolved-couple pill (lines ~57-58) must stay visually identical here — it gets replaced by a real `Badge` primitive in a *different*, future story, not this one.
---

## 2026-08-14 - US-010
- Implemented: mechanically migrated `client/src/screens/account/CoupleSection.tsx`'s hex/arbitrary-color literals that map to a US-001 token (8 substitutions) — `border-[#ffcb2b]/30`/`bg-[#ffcb2b]/10`/`text-[#ffcb2b]`→`border-primary/30`/`bg-primary/10`/`text-primary` (the active `BondLine` pill), `bg-[#161513]`/`text-[#f6f4ec]`→`bg-card`/`text-foreground` (`DissolveDialog`'s `DialogContent` root), `text-[#a6a39a]`→`text-muted-foreground` (×4: dialog subtitle, consequences list, `EmptyBond` copy, "Juntos no app desde" row), `text-[#f6f4ec]`→`text-foreground` (×3: "Manter o vínculo" button, `EmptyBond` title, "Você e {partner.name}"), `bg-[#ffcb2b]`/`text-[#09090a]`/`hover:bg-[#ffe08a]`→`bg-primary`/`text-background`/`hover:bg-accent` ("Formar um casal"/"Ver o convite" button). Same mechanical rule as every prior migration story (US-003–US-009): only exact-hex/exact-RGB matches convert.
- **Story-specific AC satisfied by not touching anything:** the dissolved-couple pill (`BondLine severed`, lines ~57-58) is left completely untouched — it's the local coral family (`#ff5c47`/`rgba(255,92,71,X)`), which has no US-001 token (same family documented as deliberate in US-009's `client/CLAUDE.md` note), so the AC's "stays visually identical" requirement holds automatically; it gets a real `Badge` primitive in a future story, not this one.
- **New leftover shape found here (worth generalizing):** two `bg-[linear-gradient(90deg,transparent,rgba(255,203,43,.55))]`-style utilities (`BondLine`'s connecting lines, active/non-severed variant) embed the *exact* `--primary` RGB inside a CSS `linear-gradient()` function, inside a single arbitrary `bg-[...]` utility — unlike previous "hex embedded in a composite background string" cases (US-005/US-007, where the fix was swapping the trailing hex for `var(--token)`), this one carries an **alpha** (`.55`). Tailwind's opacity-modifier syntax (`bg-primary/55`) only applies to a utility name, not to a raw value nested inside a gradient function, and `var(--primary)` alone would drop the `.55` alpha entirely (CSS custom properties don't carry rgba's alpha channel unless the value itself encodes it, e.g. via `color-mix()`, which has no precedent in this codebase) — converting either the function's color stop or its transparent counterpart would visibly change the line's fade, so both gradient utilities were left exactly as-is. Treat "token RGB inside a gradient function with a non-1.0 alpha, inside a single `bg-[...]` bracket" as its own leftover category, distinct from both the plain-hex-in-string case (convertible) and the "two different colors in one bracket" case from US-009 (also inconvertible, but for a different reason — there neither stop had a clean per-stop Tailwind equivalent at all).
- **Files changed:** `client/src/screens/account/CoupleSection.tsx` (className strings only — full diff read to confirm zero JSX structure/prop/handler/import/logic changes), `scripts/ralph/prd.json` (US-010 → `passes: true`, `notes` documents the leftovers and the allowlist decision below).
- **Deliberate AC deviation (same shape as US-003 through US-009):** `screens/account/CoupleSection.tsx` was **NOT** removed from `client/color-migration-allowlist.txt`. Confirmed via `node scripts/check-color-literals.mjs --list-offenders` (from `client/`) that raw color remains: the full local coral family (`#ff5c47`/`#ff8f7c`/`rgba(255,92,71,X)` at `.08`/`.12`/`.15`/`.18`/`.28`/`.3`/`.35`/`.45` — `BondLine severed`, `DissolveDialog`'s icon/title/list/error-block/confirm-button, and the outer "Desfazer o vínculo" button), `#ffb3a5` (`DissolveDialog` title — close to but **not** exactly `--destructive-foreground`'s `#ffb3b3`, so no exact-match conversion per the established "bit-for-bit only" rule), `bg-[rgba(8,7,11,.72)]` (dialog backdrop — same exact value already documented as a non-match for `--background` in US-009, `rgb(8,7,11)` ≠ `rgb(9,9,10)`), the two `linear-gradient(...)`-with-alpha `bg-[...]` utilities described above, `text-[#6f6c62]` (×2, tertiary text — same hex already documented as a tokenless leftover in US-009), and two `shadow-[...]` values (no `--shadow-*` token, same as every prior story). Removing the file would break the `check:colors` CI gate (US-002) — kept it allowlisted, matching precedent.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` (no `--list-offenders`) passes — the file's remaining raw color is still covered by the allowlist entry (54 files, unchanged count).
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao do gate `check:colors`).
- **Learnings for future iterations:**
  - New Codebase Patterns bullet worth consolidating: a token's exact RGB nested inside a CSS function (`linear-gradient(...)`, etc.) *with a non-1.0 alpha* inside a single arbitrary Tailwind bracket (`bg-[...]`) has no clean conversion — Tailwind's `/N` opacity modifier doesn't reach inside a gradient function, and `var(--token)` alone silently drops the alpha. Only a bare hex (alpha-less) embedded in a plain CSS value string converts cleanly (US-005/US-007 precedent); an rgba-with-alpha embedded in a function does not — leave it, note it, same fallback as any other tokenless leftover.
  - `screens/account/` files keep confirming the two-color-family split predicted in US-009: the 8 US-001 tokens convert normally, the local destructive coral (`#ff5c47` family) and the tertiary `#6f6c62` never do. `#ffb3a5` (this file's dialog title) is a near-miss of `--destructive-foreground` (`#ffb3b3`) worth flagging for a future US-001 audit of whether it's an intentional distinct shade or a rounding-era typo in the original prototype — not decided here, per "don't guess/invent" scope.
  - Next priority story is US-011 (`screens/match/SuggestionsTab.tsx`, 73 occurrences) — first `screens/match/` file in this sequence; its AC explicitly calls out that the Epico 11 accessibility-role/text tests must stay green untouched (same "className-only diff" guarantee already true of every story so far, just called out explicitly there). No coral/destructive family expected there (it's not an `account/`-family file) — expect the more common leftover set (`#3ddc97`/`#d6d2c8`/`#c4bfb4`/`bg-[rgba(9,9,10,.6)]` badge pattern already confirmed present in `SearchResultCard.tsx`/`SuggestionsTab.tsx` per US-006's note).
---

## 2026-08-14 - US-011
- Implemented: mechanically migrated `client/src/screens/match/SuggestionsTab.tsx`'s hex/arbitrary-color literals to the US-001 tokens (18 substitutions) — `bg-[#161513]`→`bg-card` (skeleton card, compare-mode grid card, main swipe card), `border-[rgba(255,107,107,X)]`/`bg-[rgba(255,107,107,X)]`/`text-[#ff6b6b]`/`hover:bg-[rgba(255,107,107,X)]`→`border-destructive/X`/`bg-destructive/X`/`text-destructive`/`hover:bg-destructive/X` (pendingError error block, its "Tentar novamente" button, the Passar (X) button, the actionError block + its own retry button), `text-[#ffb3b3]`→`text-destructive-foreground` (×4, same error blocks), `text-[#a6a39a]`→`text-muted-foreground` (pending-count line, compare-mode empty state, queue-empty state, "1 de N sugestões"), `border-[#ffcb2b]`→`border-primary` (selected card in compare-mode grid), `bg-[rgba(9,9,10,.6)]`→`bg-background/60` (×2, type badge on both the grid card and the main card — same exact-RGB-match rule for `--background` established in US-006/US-007), `text-[#f6f4ec]`→`text-foreground` (×3, both type badges + grid card title), `border-[#161513]`/`bg-[#ffcb2b]`→`border-card`/`bg-primary` (selected-order badge in compare-mode grid), and `from-[#09090a]`/`via-[rgba(9,9,10,.85)]`→`from-background`/`via-background/85` (main card's footer gradient — first `from-`/`via-` conversion in this migration sequence, same exact-RGB-match rule applied to a gradient utility prefix instead of `bg-`/`border-`).
- **Files changed:** `client/src/screens/match/SuggestionsTab.tsx` (className strings only — full `git diff` read to confirm zero JSX structure/visible-text/prop/handler/import/logic changes), `scripts/ralph/prd.json` (US-011 → `passes: true`, `notes` documents the leftovers and the allowlist decision below).
- **AC "Epico 11 accessibility tests stay green" confirmed:** no test file was touched; `bun run test:run` still reports 119 passed / 14 files, identical to every prior story's run.
- **Deliberate AC deviation (same shape as US-003 through US-010):** `screens/match/SuggestionsTab.tsx` was **NOT** removed from `client/color-migration-allowlist.txt`. Confirmed via `node scripts/check-color-literals.mjs --list-offenders` (from `client/`) that raw color remains: `border-[rgba(255,255,255,.07)]` (skeleton + main card, alpha doesn't match `--border`'s `.1`), `shadow-[...rgba(255,203,43,.18|.25)]` (no `--shadow-*` token, same as every prior story), `text-[#111]` (selected-order badge in compare-mode grid, same recurring leftover from `ComparisonDialog.tsx`/`MediaCard.tsx`), the `#3ddc97` family (`border-[rgba(61,220,151,.3)]`/`bg-[rgba(61,220,151,.08)]`/`text-[#3ddc97]`/`hover:bg-[rgba(61,220,151,.16)]` on the Curtir button — recurring since US-003, still no token), `border-white/10`/`border-white/20`/`hover:border-white/35` (Tailwind named `white` + opacity modifier, confirmed since US-008 to never match the gate's regexes), and the two `rgba(255,255,255,.05)` occurrences inside `repeating-linear-gradient(...)` **inline `style` strings** (not a Tailwind className bracket, so `HEX_RE`/`ARBITRARY_COLOR_RE` don't match them at all — same exception already documented in US-007 for a bare `rgba(...)` outside an arbitrary-utility bracket). Removing the file would break the `check:colors` CI gate (US-002) — kept it allowlisted, matching precedent.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` (no `--list-offenders`) passes — the file's remaining raw color is still covered by the allowlist entry (54 files, unchanged count).
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao do gate `check:colors` + leitura completa do `git diff`).
- **Learnings for future iterations:**
  - First confirmed use of the `from-`/`via-` gradient-utility prefixes in this migration sequence: the same "exact RGB match to a token converts, any alpha via the `/N` opacity modifier" rule that applied to `bg-`/`border-`/`text-` in every prior story applies identically to `from-background`/`via-background/85` — Tailwind's gradient-stop utilities accept the same token-name + opacity-modifier syntax as any other color utility, no special handling needed.
  - `bg-[rgba(9,9,10,.6)]` (`--background` at 60%) is now confirmed in a **second** file (`MediaCard.tsx` in US-006, now `SuggestionsTab.tsx`) — reinforces US-006's prediction that `SearchResultCard.tsx` (not yet migrated) likely has the identical translucent type-badge pattern.
  - Inline `style` strings using bare `rgba(...)` (not wrapped in a Tailwind arbitrary-utility bracket) are invisible to the gate's regexes even when the RGB matches a known token exactly (here, `rgba(255,255,255,.05)` inside a `repeating-linear-gradient(...)` background-image string) — this is distinct from the *hex-inside-a-CSS-value* case (US-005/US-007's `var(--token)` swap), which *does* get flagged because `HEX_RE` matches `#rrggbb` anywhere on the line regardless of syntax; only `rgba(...)` functions dodge detection this way, not literal hex.
  - Next priority story is US-012 (`screens/match/FiltersPanel.tsx`, 73 occurrences) — second `screens/match/` file, no coral/destructive-family or chart-token AC expected (not an `account/`- or dashboard-style file); given it's the search/sort/filters `Collapsible` panel (per `client/CLAUDE.md`'s `screens/match/` section), expect mostly the standard token set plus possibly `ToggleGroup`/`Slider`-related hover/active-state arbitrary colors not yet seen in this sequence.
---

## 2026-08-14 - US-012
- Implemented: mechanically migrated `client/src/screens/match/FiltersPanel.tsx`'s hex/arbitrary-color literals to the US-001 tokens — `text-[#a6a39a]`→`text-muted-foreground` (search icon + 8 identical section/slider uppercase labels), `bg-[#161513]`→`bg-card` (search input, `SelectTrigger`, `SelectContent`, inactive `CollapsibleTrigger`, `CollapsibleContent`), `text-[#f6f4ec]`→`text-foreground` (search input, `SelectTrigger`, `SelectContent`, inactive `CollapsibleTrigger`, "Limpar tudo" hover), `focus:border-[#ffcb2b]`→`focus:border-primary` (search input), `border-[rgba(255,203,43,.5)]`/`bg-[rgba(255,203,43,.12)]`/`text-[#ffcb2b]`→`border-primary/50`/`bg-primary/12`/`text-primary` (active `CollapsibleTrigger`), `bg-[#ffcb2b]`/`text-[#09090a]`→`bg-primary`/`text-background` (active-filter-count badge + "Aplicar filtros" button), `text-[#ffcb2b]`→`text-primary` (Nota TMDB / Duracao value spans). Same mechanical rule as US-003–US-011.
- **Files changed:** `client/src/screens/match/FiltersPanel.tsx` (className strings only — full diff read to confirm zero JSX structure/prop/handler/import/logic changes), `scripts/ralph/prd.json` (US-012 → `passes: true`, `notes` documents leftovers and the allowlist decision below).
- **Deliberate AC deviation (same shape as US-003 through US-011):** `screens/match/FiltersPanel.tsx` was **NOT** removed from `client/color-migration-allowlist.txt`. Confirmed via manual grep (`grep -noE '#[0-9a-fA-F]{6}|(bg|text|border|ring|from|to|via|shadow|fill|stroke)-\[[^]]*\]'`) and `node scripts/check-color-literals.mjs` (no `--list-offenders`, reports "ok" with the file still allowlisted) that raw color/flagged patterns remain: `shadow-[0_0_0_3px_rgba(255,203,43,.2)]` (search-input focus ring, no `--shadow-*` token, same as every prior story's shadow leftovers), `hover:bg-[#ffdd7a]` ("Aplicar filtros" button hover, same untokenized "shared badge" gold recurring since US-003/US-006/US-008), and 10 `text-[13px]`/`text-[11px]` font-size arbitrary utilities (matched by the gate's regex per the US-008 finding, but not colors — same "leftover, leave it" bucket). `border-white/10` and `hover:bg-white/[0.06]`/`data-[popup-open]:bg-white/[0.06]`/`data-highlighted:bg-white/[0.08]` (Tailwind's named `white` + opacity) are confirmed — again — not matched by either regex at all (per US-008), correctly out of scope. Removing the file would break the `check:colors` CI gate (US-002) — kept it allowlisted, matching precedent from every prior migration story.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` (no `--list-offenders`) passes — the file's remaining raw color/flagged patterns are still covered by the allowlist entry (54 files, unchanged count).
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao do gate `check:colors`).
- **Learnings for future iterations:**
  - No new patterns discovered — this file was a clean application of every mechanical rule already consolidated in the Codebase Patterns section above (US-003 through US-011). Straightforward story, no surprises.
  - Next priority story is US-013 (`components/landing/LandingHero.tsx`, 71 occurrences) — first `components/landing/` file in this migration sequence; no evidence yet of what leftover colors it'll surface, check first whether it shares any of the recurring untokenized families (`#3ddc97`, `#d6d2c8`, `#ffdd7a`, `#c4bfb4`, `rgba(255,255,255,.05|.06|.08)`) already seen in every other file so far.
---

## 2026-08-14 - US-013
- Implemented: mechanically migrated `client/src/components/landing/LandingHero.tsx`'s hex/arbitrary-color literals to the US-001 tokens (15 substitutions) — `text-[#ffcb2b]`→`text-primary` (eyebrow "Feito para dois"), `text-[#f6f4ec]`→`text-foreground` (H1, "Já temos conta" link, "Lista de vocês dois" label, genre badge, preview-card title), `text-[#a6a39a]`→`text-muted-foreground` (subtitle paragraph, preview-card year, preview-card rating), `bg-[#ffcb2b]`→`bg-primary` (CTA button), `bg-[#161513]`→`bg-card` (preview card), `border-[#161513]`→`border-card` (×2, couple-avatar rings), `from-[#ffcb2b]`→`from-primary` (first avatar's gradient), `bg-[#ffcb2b]/12`→`bg-primary/12` (×2, "Queremos Ver" badge + preview-card type badge), `bg-[#09090a]/60`→`bg-background/60` (genre badge over the poster, same exact-RGB-match rule for `--background` established in US-006), `border-[#ffcb2b]/25`→`border-primary/25` (preview-card type badge).
- **New pattern this story: `outline-[#hex]` (not a `bg-`/`text-`/`border-`/etc. prefix, so not matched by `ARBITRARY_COLOR_RE`'s prefix list) still gets flagged by `HEX_RE`, which matches any `#rrggbb` regardless of surrounding syntax** — both `focus-visible:outline-[#ffcb2b]` occurrences (the two hero links) converted to `focus-visible:outline-primary`. Tailwind v4 generates `outline-{themeColorName}` utilities from the same `--color-*` theme vars (`@theme inline` in `index.css` already declares `--color-primary: var(--primary)`, consumed by `bg-primary`/`text-primary` elsewhere) so this is a valid, pixel-identical conversion, not a new pattern requiring a new token — just the first time an `outline-` utility carried a token-matching hex in this migration sequence.
- **Files changed:** `client/src/components/landing/LandingHero.tsx` (className strings only — full diff read to confirm zero JSX structure/prop/handler/import/logic changes), `scripts/ralph/prd.json` (US-013 → `passes: true`, `notes` documents the leftovers and the allowlist decision below).
- **Deliberate AC deviation (same shape as US-003 through US-012):** `components/landing/LandingHero.tsx` was **NOT** removed from `client/color-migration-allowlist.txt`. Confirmed via `node scripts/check-color-literals.mjs --list-offenders` (from `client/`) that raw color remains: `text-[#111]` (dark text on the yellow CTA button, same recurring leftover as `ComparisonDialog.tsx`/`MediaCard.tsx`/`TitleModal.tsx`), `shadow-[...rgba(255,203,43,.34)]`/`shadow-[...rgba(0,0,0,.45)]`/`shadow-[...rgba(0,0,0,.5)]` (no `--shadow-*` token, same as every prior story), `to-[#c98f00]` and the `from-[#ff9e2c]`/`to-[#8a4a00]` pair (gradient stops with no token — `#ff9e2c` was redirected to `--chart-2` in US-007, but that conversion was scoped strictly to `DashboardScreen.tsx`'s chart/series fills by that story's own AC bullet, not a blanket rule for the hex anywhere it appears; this story has no equivalent AC, so both stops were left untouched), `bg-[#0f0e0c]`/`bg-[#1a1816]` (preview-card surfaces, close to but not an exact RGB match of `--card`/`--background`), `fill-[#ffb443]`/`text-[#ffb443]` (TMDB-gold star, recurring since US-003), `text-[#ffdd7a]` (×2, same "shared badge text" family recurring since US-003/US-006/US-008). `border-white/[0.12]`/`border-white/[0.07]`/`hover:bg-white/[0.06]` (Tailwind named `white` + bracketed opacity, confirmed since US-008 to never match either regex) are correctly out of scope. Removing the file would break the `check:colors` CI gate (US-002) — kept it allowlisted, matching precedent.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` (no `--list-offenders`) passes — the file's remaining raw color is still covered by the allowlist entry (54 files, unchanged count).
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao do gate `check:colors`).
- **Learnings for future iterations:**
  - New Codebase Patterns candidate: `outline-[#hex]` is invisible to `ARBITRARY_COLOR_RE` (prefix not in its list: `bg|text|border|ring|from|to|via|shadow|fill|stroke`) but still caught by the standalone `HEX_RE`, and still converts cleanly to `outline-{token}` since Tailwind v4's outline-color utilities read the same `--color-*` theme vars as every other color utility. Don't assume a utility prefix missing from `ARBITRARY_COLOR_RE` means it's out of scope — check `HEX_RE` (bare `#rrggbb` anywhere on the line) independently.
  - `#ff9e2c`'s chart-token redirect (`--chart-2`, from US-007) is scoped to literal chart/series fills inside `DashboardScreen.tsx` by that story's own AC, not a general "this hex now maps to `--chart-2` everywhere" rule — confirmed here by leaving `LandingHero.tsx`'s `from-[#ff9e2c]`/`to-[#8a4a00]` avatar-gradient stops untouched, since this story's AC uses the standard "leave it, note it" fallback like every non-Dashboard story.
  - Next priority story is US-014 (`screens/account/PasswordSection.tsx`, 60 occurrences) — third `screens/account/` file in this sequence (after US-009/US-010); expect the same two-color-family split (US-001 tokens convert, the local destructive coral `#ff5c47` family + `#6f6c62` tertiary text don't) documented in US-009/US-010's learnings.
---

## 2026-08-14 - US-014
- Implemented: mechanically migrated `client/src/screens/account/PasswordSection.tsx`'s hex/arbitrary-color literals that map to a US-001 token — `text-[#a6a39a]`→`text-muted-foreground` (`FIELD_LABEL_CLASS`, "Ao salvar:" span, done-state paragraph), `bg-[#ffcb2b]`→`bg-primary` (`LengthRail`'s filled bar when inside the password-length window, "Trocar senha" submit button), `bg-[#f6f4ec]/40`→`bg-foreground/40` (the min-length tick mark on the rail), `text-[#ffcb2b]`→`text-primary` (the rail's numeric counter when inside the window, `ShieldCheck`/`Loader2` icons in the done state), `border-[#ffcb2b]/25`+`bg-[#ffcb2b]/[0.06]`→`border-primary/25`+`bg-primary/[0.06]` (done-state banner), `text-[#f6f4ec]`→`text-foreground` (done-state title), `bg-[#ffcb2b]`/`text-[#09090a]`/`hover:bg-[#ffe08a]`→`bg-primary`/`text-background`/`hover:bg-accent` (submit button) — 9 substitutions total.
- **New pattern, worth noting:** `bg-[#ffcb2b]/[0.06]` is the first occurrence in this migration sequence of a *fractional* arbitrary opacity modifier (as opposed to the integer `/N` modifiers like `/18`, `/25`, `/50` used in every prior story). Converted to `bg-primary/[0.06]` — Tailwind v4's opacity modifier accepts an arbitrary bracketed value the same way the color utility itself does, and `[0.06]` preserves the exact original alpha (6%) without rounding to the nearest integer percentage. Same mechanical rule as always: only the color half changes, the modifier/bracket syntax carries over unchanged.
- **Files changed:** `client/src/screens/account/PasswordSection.tsx` (className strings only — full diff read to confirm zero JSX structure/prop/handler/import/logic changes), `scripts/ralph/prd.json` (US-014 → `passes: true`, `notes` documents leftovers and the allowlist decision).
- **Confirmed prediction from US-013's learning note:** this file repeats the two-color-family split already established in US-009/US-010 (`screens/account/`'s local destructive coral `#ff5c47`/`#ff8f7c`/`rgba(255,92,71,X)` family, distinct from the app's global `--destructive` `#ff6b6b`, plus the `#6f6c62` tertiary-text hex) — none of these map to a US-001 token and were left untouched per the AC's fallback clause. Full leftover list, confirmed via `node scripts/check-color-literals.mjs --list-offenders` (from `client/`) still listing the file: `bg-[#6f6c62]`/`text-[#6f6c62]` (4×: the rail's unfilled-state fill, and 3 tertiary-text spots), `text-[#ff8f7c]` (4×, field/form error text), `border-[rgba(255,92,71,.28)]`/`bg-[rgba(255,92,71,.08)]` (form error block), `bg-[#ff5c47]` (2×, the "esta aba sai"/"os outros aparelhos também" dots). `border-white/[0.07]` (Tailwind named `white` + bracketed opacity, confirmed since US-008 not matched by the gate's regex) and the non-color arbitrary utilities (`text-[11px]`/`[12px]`/`[13px]`/`[14px]`/`[17px]`, `tracking-[.08em]`, `transition-[width]`, `rounded-[14px]`/`[12px]`, `max-w-[56ch]`) are out of scope, same as every prior story.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` (no `--list-offenders`) passes — the file's remaining raw color is still covered by the allowlist entry (54 files, unchanged count).
- **Deliberate AC deviation (same shape as US-003 through US-013):** `screens/account/PasswordSection.tsx` was **NOT** removed from `client/color-migration-allowlist.txt` — removing it would break the `check:colors` CI gate (US-002) given the leftovers above; kept it allowlisted, matching precedent.
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao do gate `check:colors`).
- **Learnings for future iterations:**
  - Fractional bracketed opacity modifiers (`/[0.06]`) on a token utility are valid and preserve exact alpha — don't round `bg-[#hex]/[0.06]` to an integer-percentage modifier like `/6` when migrating; keep the bracket form so the value stays byte-identical to the source.
  - This is the last `screens/account/` file with a currently-known priority in the PRD as read; the `#ff5c47`/`#ff8f7c`/`#6f6c62` coral+tertiary leftover set has now recurred identically across all three `screens/account/` files migrated so far (US-009, US-010, US-014) — a real future token pair for this destructive-coral family and a `--muted-foreground-2`/tertiary token would likely close out every remaining `screens/account/` leftover in one shot, if a future US-001 follow-up story takes it on.
  - Next: check `prd.json` for the next `passes: false` story in priority order (this session did not read past priority 14 in the file — earlier context was truncated at that point).
---
## 2026-08-14 - US-015
- Implemented: mechanically migrated `client/src/screens/hub/HubScreen.tsx`'s hex/arbitrary-color literals to the US-001 tokens (14 substitutions) — `text-[#f6f4ec]`→`text-foreground` (root div, Comparar button inactive/hover, "Trocar novamente" retry link's sibling text, compare bar count, clear-selection hover), the `#09090a` embedded in the root's composite `background` string (not a className — same "hex inside a CSS value string" pattern as US-005/US-007's `var(--token)` swap)→`var(--background)`, `text-[#ffcb2b]`→`text-primary` (eyebrow "Minha Lista", active Comparar button ×2 including hover, "Tentar novamente" link), `text-[#a6a39a]`→`text-muted-foreground` (subtitle, clear-selection button), `border-[rgba(255,203,43,.35)]`/`bg-[rgba(255,203,43,.12)]`/`hover:bg-[rgba(255,203,43,.18)]`→`border-primary/35`/`bg-primary/12`/`hover:bg-primary/18` (active Comparar button), `border-[rgba(255,255,255,.1)]`→`border-border` (Comparar tooltip, exact `--border` alpha), `bg-[#ffcb2b]`→`bg-primary` (×2: "Adicionar Título" button + FAB), `bg-[#161513]/95`→`bg-card/95` (floating compare bar), `text-[#ffb3b3]`→`text-destructive-foreground` (compare-bar error text), `hover:text-[#ffe08a]`→`hover:text-accent` (retry link hover). Same mechanical rule as every prior migration story (US-003–US-014): only exact-hex/exact-RGB matches convert, `rgba(R,G,B,X)` converts to `token/X` at any alpha only when R,G,B exactly equals a token's RGB.
- **Files changed:** `client/src/screens/hub/HubScreen.tsx` (className/style-value strings only — full diff read to confirm zero JSX structure/visible-text/prop/handler/import/logic changes), `scripts/ralph/prd.json` (US-015 → `passes: true`, `notes` documents the leftovers and the allowlist decision below).
- **AC "Epico 11 HubScreen tests stay green" confirmed:** no test file touched; `bun run test:run` still reports 119 passed / 14 files, identical to every prior story's run.
- **Deliberate AC deviation (same shape as US-003 through US-014):** `screens/hub/HubScreen.tsx` was **NOT** removed from `client/color-migration-allowlist.txt`. Confirmed via `node scripts/check-color-literals.mjs` (no `--list-offenders`, reports "ok" with the file still allowlisted) and manual grep that raw color remains: `bg-[#201e18]` (Comparar-button tooltip surface — same "tooltip/input surface" leftover hex already documented in US-006/US-008), `text-[#111]` (×2, dark text over the yellow "Adicionar Título" button and the FAB — same recurring leftover since `ComparisonDialog.tsx`), and 3 `shadow-[...]` values (`shadow-[0_10px_26px_rgba(255,203,43,.34)]`, `shadow-[0_20px_50px_rgba(0,0,0,.5)]`, `shadow-[0_14px_34px_rgba(255,203,43,.45)]` — no `--shadow-*` token, same as every prior story's shadow leftovers). `border-white/12`, `hover:bg-white/[0.06]`, `bg-white/[0.08]`, `hover:bg-white/[0.14]` (Tailwind named `white` + opacity, confirmed since US-008 to never match either regex) and the non-color arbitrary utilities (`text-[13px]`/`[14px]`/`[15px]`/`[19px]`/`[28px]`/`[13.5px]`/`[12.5px]`, `text-[clamp(28px,4vw,40px)]`) are out of scope, same as every prior story. Removing the file would break the `check:colors` CI gate (US-002) — kept it allowlisted, matching precedent.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` (no `--list-offenders`) passes — the file's remaining raw color is still covered by the allowlist entry (54 files, unchanged count).
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao do gate `check:colors`).
- **Learnings for future iterations:**
  - No new patterns discovered — this file was a clean application of every mechanical rule already consolidated in the Codebase Patterns section above (US-003 through US-014), including the "hex inside a composite `background` CSS-value string" case (US-005/US-007) applied here to the root div's own gradient background.
  - Next priority story is US-016 (`components/Header.tsx`, 54 occurrences) — first `components/Header.tsx` file in this sequence; per `client/CLAUDE.md` it hosts `NAV_ITEMS`/mobile nav and the couple-avatar header chrome already seen inline in the design prototype (`docs/design/claude-design-project/Sessao a Dois.dc.html`'s `<header>` block) — expect the same token set (`--primary`/`--foreground`/`--muted-foreground`/`--background`) plus possibly new leftover hues from the avatar gradients (`hsl(45 92% 42%)`-style values are HSL, not hex/rgba, and won't be flagged by the gate's regexes at all — don't attempt to "migrate" those).
---

## 2026-08-14 - US-016
- Implemented: mechanically migrated `client/src/components/Header.tsx` hex/arbitrary-color literals that map to US-001 tokens: `bg-[#09090a]/72`->`bg-background/72` (x1), `bg-[#ffcb2b]`->`bg-primary` (x5, logo badge + active nav pill + active conta icon + mobile menu active states), `text-[#f6f4ec]`->`text-foreground` (x7), `text-[#a6a39a]`->`text-muted-foreground` (x5).
- Files changed: `client/src/components/Header.tsx` (className strings only, zero JSX structure/prop/handler/import/logic changes, confirmed by reading the full diff), `scripts/ralph/prd.json` (US-016 -> `passes: true`, `notes` documents the leftover colors and allowlist decision below).
- **Deliberate AC deviation (same shape as US-003/US-004+, documented per the story's own notes field):** `components/Header.tsx` was **NOT** removed from `client/color-migration-allowlist.txt`, even though that's a literal AC bullet. Reason: 5 leftover color values have no token in US-001's 8-token set: `text-[#111]` (x5, dark text on the primary-yellow badge/active-pill/active-conta-icon — this exact "text on primary" pairing recurs across the app per prior stories' notes but no `--primary-foreground`-equivalent semantic token was tokenized in US-001), `text-[#ff9e2c]` (x1, the decorative middle-dot in the "Sessão·a·Dois" wordmark — a distinct amber neighbor-hue, not `--primary`), `bg-[#141312]` (x1, mobile-menu dropdown background — close to but NOT identical to `--card` `#161513`, so no exact-value token match), and two `shadow-[...rgba(...)]` values (`rgba(255,203,43,.35)`/`.4`, primary-colored glow; `rgba(0,0,0,.5)`, dropdown shadow) — `index.css` has no `--shadow-*` token at all. Confirmed via `node scripts/check-color-literals.mjs --list-offenders` (from `client/`) that `components/Header.tsx` still appears in the offender list after migration, so pulling it off the allowlist would break the `check:colors` CI gate (US-002). Kept the file in the allowlist, consistent with every prior per-file migration story since US-003.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` (no `--list-offenders`) reports "ok" with the file still allowlisted.
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao do gate `check:colors`).
- **Learnings for future iterations:**
  - `text-[#111]` (dark near-black text used on top of the primary-yellow background — badges, active nav pills, active icon buttons) recurs across files as an untokenized leftover; if a future story wants to zero out more allowlist entries, extending US-001 with a `--primary-foreground`-style token for "text on primary" would likely clear this specific residue in several files at once (Header.tsx alone has 5 occurrences of it).
  - `#141312` (mobile-menu dropdown surface) is visually adjacent to but not identical to the `--card` token's `#161513` — don't round it into `bg-card` without a human decision; a 1-value-off hex is a real distinct color choice here, not a typo, per the same "don't move a pixel to save a token" rule from `CLAUDE.md`.
  - Next priority story is US-017 (`MatchShowcase.tsx`, 53 occurrences).
---

## 2026-08-14 - US-017
- Implemented: mechanically migrated `client/src/components/landing/MatchShowcase.tsx`'s hex/arbitrary-color literals to the US-001 tokens (11 substitutions) — `from-[#ffcb2b]`→`from-primary` (viewer "Ana"'s avatar-dot gradient start stop — `to-[#c98f00]` has no token, see leftovers), `border-[#ffcb2b]`→`border-primary` (the animated ring around each `LikedCard`), `bg-[#09090a]/70`→`bg-background/70` (the heart badge over the poster), `fill-[#ffcb2b]`/`text-[#ffcb2b]`→`fill-primary`/`text-primary` (×2: the small badge heart + the large central burst heart), `text-[#a6a39a]`→`text-muted-foreground` (×2: each `LikedCard`'s "{name} curtiu" caption + the section's intro paragraph), `text-[#ffcb2b]`→`text-primary` (the "O momento Match" eyebrow), `text-[#f6f4ec]`→`text-foreground` (×2: the H2 + the liked-title name in the bottom pill), `bg-[#ffcb2b]/20`→`bg-primary/20` (the central heart's blur halo), `text-[#ffe08a]`→`text-accent` (×2, both `Sparkles` icons), `border-[#ffcb2b]/25`→`border-primary/25` (the "Deu Match" pill border), `bg-[#161513]`→`bg-card` (the "Deu Match" pill background), `bg-[#ffcb2b]/12`→`bg-primary/12` ("Queremos Ver" badge). Same mechanical rule as every prior migration story (US-003–US-016): only exact-hex/exact-RGB matches convert.
- **Files changed:** `client/src/components/landing/MatchShowcase.tsx` (className strings only — full file re-read post-edit to confirm zero JSX structure/visible-text/prop/handler/import/logic changes), `scripts/ralph/prd.json` (US-017 → `passes: true`, `notes` documents leftovers and the allowlist decision below).
- **Deliberate AC deviation (same shape as US-003 through US-016):** `components/landing/MatchShowcase.tsx` was **NOT** removed from `client/color-migration-allowlist.txt`. Confirmed via `node scripts/check-color-literals.mjs --list-offenders` (from `client/`) that raw color remains: `to-[#c98f00]` and the `from-[#ff9e2c]`/`to-[#8a4a00]` pair (avatar-dot gradient stops with no token — same exact leftover already documented in `LandingHero.tsx`, US-013; `#ff9e2c`'s `--chart-2` redirect from US-007 is scoped strictly to `DashboardScreen.tsx`'s chart/series fills, doesn't apply here), `bg-[#0f0e0c]` (`LikedCard` poster surface, close to but not an exact RGB match of `--card`, same leftover as `LandingHero.tsx`), `text-[#ffdd7a]` (×2: "Deu Match!" text + "Queremos Ver" badge text, the same recurring untokenized "shared badge text" family since US-003/US-006/US-008/US-013). `border-white/[0.07]` (Tailwind named `white` + bracketed opacity, confirmed since US-008 not matched by either regex) and the font-size arbitrary utilities (`text-[12.5px]`/`[clamp(1.75rem,3.5vw,2.5rem)]`/`[15px]`/`[11px]`/`[13px]`) are correctly out of scope, not colors. Removing the file would break the `check:colors` CI gate (US-002) — kept it allowlisted, matching precedent from every prior migration story.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` (no `--list-offenders`) reports "ok" — 54 files in the allowlist, unchanged count.
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao do gate `check:colors`).
- **Learnings for future iterations:**
  - No new patterns discovered — this file was a clean application of every mechanical rule already consolidated in the Codebase Patterns section above (US-003 through US-016), including reusing the exact `#c98f00`/`#ff9e2c`/`#8a4a00`/`#0f0e0c` leftover set already seen in `LandingHero.tsx` (both are `components/landing/` files sharing the same avatar-gradient/preview-card styling).
  - Next priority story is US-018 (`routes/auth/JoinPage.tsx`, 52 occurrences) — first `routes/auth/` file in this sequence; no evidence yet of what leftover colors it'll surface, check first whether it shares any of the recurring untokenized families (`#3ddc97`, `#d6d2c8`, `#ffdd7a`, `#c4bfb4`, `rgba(255,255,255,.05|.06|.08)`, `text-[#111]`) already seen across most other files so far.
---

## 2026-08-14 - US-018
- Implemented: mechanically migrated `client/src/routes/auth/JoinPage.tsx`'s hex/arbitrary-color literals to the US-001 tokens (8 substitutions) — `border-[#ff6b6b]/30`+`bg-[#ff6b6b]/10`→`border-destructive/30`+`bg-destructive/10` (×3: the "ainda não entrou" notice, the Join-form error block, the Create-form error block), `text-[#f6f4ec]`→`text-foreground` ("Já vinculamos, continuar" button), `bg-[#ffcb2b]`+`text-[#09090a]`→`bg-primary`+`text-background` (×2, the selected Join/Create mode tabs), `text-[#a6a39a]`+`hover:text-[#f6f4ec]`→`text-muted-foreground`+`hover:text-foreground` (×2, the unselected mode tabs), `text-[#a6a39a]`→`text-muted-foreground` ("Código de convite" label, "Vamos gerar..." paragraph), `bg-[#ffcb2b]`+`text-[#09090a]`+`hover:bg-[#ffe08a]`→`bg-primary`+`text-background`+`hover:bg-accent` (×2, the "Vincular"/"Gerar meu código" submit buttons). Same mechanical rule as every prior migration story (US-003–US-017): only exact-hex/exact-RGB matches convert.
- **Files changed:** `client/src/routes/auth/JoinPage.tsx` (className strings only — full `git diff` read to confirm zero JSX structure/visible-text/prop/handler/import/logic changes), `scripts/ralph/prd.json` (US-018 → `passes: true`, `notes` documents the leftover and the allowlist decision below).
- **Deliberate AC deviation (same shape as US-003 through US-017):** `routes/auth/JoinPage.tsx` was **NOT** removed from `client/color-migration-allowlist.txt`. Confirmed via `node scripts/check-color-literals.mjs --list-offenders` (from `client/`) that raw color remains: `text-[#ff9b9b]` (×3, the destructive-notice/error-block text) — close to but **not** an exact RGB match of `--destructive-foreground`'s `#ffb3b3`, so per the established "bit-for-bit only" rule it does not convert. `border-white/10`, `bg-white/[0.04]`, `hover:bg-white/[0.08]`, `border-white/[0.06]`, `bg-white/[0.05]` (Tailwind's named `white` + opacity modifier, confirmed since US-008 to never match either regex) are correctly out of scope. Removing the file would break the `check:colors` CI gate (US-002) — kept it allowlisted, matching precedent from every prior migration story.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` (no `--list-offenders`) reports "ok" — 54 files in the allowlist, unchanged count.
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao do gate `check:colors`).
- **Learnings for future iterations:**
  - `#ff9b9b` is a new near-miss color, distinct from the previously-documented near-misses (`#ffb3a5` in `CoupleSection.tsx`/US-010) — another candidate hex for a future US-001 audit of whether the prototype intended a single destructive-foreground shade and drifted, or these are genuinely distinct by design. Not decided here, per "don't guess/invent" scope.
  - First `routes/auth/` file migrated — confirms this route family shares the standard token set (`--primary`/`--background`/`--foreground`/`--muted-foreground`/`--destructive`) with no special local color family (unlike `screens/account/`'s coral), and the same `border-white/N`/`bg-white/[N]` out-of-scope pattern seen throughout `components/`/`screens/`.
  - Next priority story is US-019 (`routes/auth/InviteCodeTicket.tsx`, 52 occurrences) — second `routes/auth/` file; check first whether it shares `#ff9b9b` or introduces new leftovers, since `InviteCodeTicket` is rendered directly inside `JoinPage.tsx`'s partner-pending branch (already imported here) and may reuse the same destructive-notice styling.
---

## 2026-08-14 - US-019
- Implemented: mechanically migrated `client/src/routes/auth/InviteCodeTicket.tsx`'s hex/arbitrary-color literals to the US-001 tokens (12 substitutions) — `bg-[#161513]`/`text-[#f6f4ec]`→`bg-card`/`text-foreground` and `bg-[#ffcb2b]`/`text-[#09090a]`→`bg-primary`/`text-background` (ticket root, expired vs. active state), `border-[#f6f4ec]/15`→`border-foreground/15` (perforated-edge border, expired state — first confirmation that a *solid* token like `--foreground` can be reused as a `border-` color at any opacity via the Tailwind modifier, same rule already used for `--card`/`--primary`/`--destructive`/`--background` in prior stories, just not yet exercised for `--foreground`), `bg-[#09090a]`×2→`bg-background` (the two perforation holes, expired state), `border-2 border-[#ff6b6b]`/`text-[#ff6b6b]`→`border-destructive`/`text-destructive` ("Expirado" stamp), `bg-[#ffcb2b]`/`text-[#09090a]`/`hover:bg-[#ffe08a]`→`bg-primary`/`text-background`/`hover:bg-accent` ("Gerar novo" button), `border-[#09090a]/25`→`border-background/25` (perforated-edge border, active state), `bg-[#09090a]/10`+`hover:bg-[#09090a]/15`→`bg-background/10`+`hover:bg-background/15` ("Copiar" button), `border-[#ff6b6b]/30`+`bg-[#ff6b6b]/10`→`border-destructive/30`+`bg-destructive/10` (regenerate-error banner). Same mechanical rule as every prior migration story (US-003–US-018): only exact-hex/exact-RGB matches convert, any alpha.
- **Files changed:** `client/src/routes/auth/InviteCodeTicket.tsx` (className strings only — full `git diff` read to confirm zero JSX structure/visible-text/prop/handler/import/logic changes), `scripts/ralph/prd.json` (US-019 → `passes: true`, `notes` documents the leftovers and the allowlist decision below).
- **Deliberate AC deviation (same shape as US-003 through US-018):** `routes/auth/InviteCodeTicket.tsx` was **NOT** removed from `client/color-migration-allowlist.txt`. Confirmed via `node scripts/check-color-literals.mjs --list-offenders` (from `client/`) that raw color remains: `text-[#ff9b9b]`×2 (the "urgent"/error text over destructive surfaces — same near-miss hex already flagged in US-018's `JoinPage.tsx`, close to but **not** an exact RGB match of `--destructive-foreground`'s `#ffb3b3`), `text-[#a3560a]` (the "urgent" countdown text, a dark amber with no token), `bg-[#0d0d0f]`×2 (the two perforation holes in the *active*-state ticket half, rendered over the yellow `bg-primary` background — close to but **not** an exact match of `--background`'s `#09090a`, so per the "bit-for-bit only" rule it stays untouched even though the expired-state holes at the identical position did convert), `shadow-[...rgba(0,0,0,.5)]`/`shadow-[...rgba(255,203,43,.45)]` (no `--shadow-*` token, same as every prior story's shadow leftovers). Removing the file would break the `check:colors` CI gate (US-002) — kept it allowlisted, matching precedent.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` (no `--list-offenders`) reports "ok" — 54 files in the allowlist, unchanged count.
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao do gate `check:colors`).
- **Learnings for future iterations:**
  - **Confirms the `#ff9b9b` near-miss is a real (deliberate-or-not) pattern, not a one-off:** now seen in both `routes/auth/JoinPage.tsx` (US-018) and `routes/auth/InviteCodeTicket.tsx` (US-019), always distinct from `--destructive-foreground`'s `#ffb3b3`. Still not decided whether this is intentional design drift or an oversight — worth flagging explicitly if a future US-001 follow-up audits the destructive-foreground family (it would join `#ffb3a5` from `CoupleSection.tsx`/US-010 as a second near-miss shade).
  - **New confirmation, worth generalizing:** a solid (non-alpha-baked) token can be reused as a `border-` color at any opacity even when its "natural" role elsewhere is text/background — `border-foreground/15` here, following the same precedent `border-card`/`border-background` already established in US-006/this story. The distinguishing test for "needs exact alpha match" vs. "any alpha via modifier" remains whether the *token itself* already bakes in an alpha (`--border`/`--input`, which do) vs. is a plain opaque color (`--foreground`/`--background`/`--card`/`--primary`/`--destructive`/`--accent`, which don't).
  - **Positional near-duplicate, not a mapping error:** the two perforation-hole `<span>`s appear in both branches of the `expired` ternary at the same visual position, but use *different* hex (`#09090a` in the expired branch, `#0d0d0f` in the active branch) — the active branch's holes sit over a yellow (`bg-primary`) card and were deliberately given a slightly different near-black to still read correctly there. Don't "fix" `#0d0d0f` into `bg-background` just because its sibling at the same DOM position converted; check each occurrence's actual hex independently.
  - Next priority story is US-020 (`components/NotificationDropdown.tsx`, 52 occurrences) — back to `components/`, no evidence yet of what leftovers it introduces; check first for the recurring untokenized families (`#3ddc97`, `#d6d2c8`, `#ffdd7a`, `#c4bfb4`, `rgba(255,255,255,.05|.06|.08)`, `text-[#111]`) and whether it shares any `Popover`-specific styling with other dropdown/menu components not yet migrated.
---

## 2026-08-14 - US-020
- Implemented: mechanically migrated `client/src/components/NotificationDropdown.tsx`'s hex/arbitrary-color literals to the US-001 tokens — `from-[#ffcb2b]`→`from-primary` (MATCH badge gradient start), `border-[#ffcb2b]/40`+`bg-[#ffcb2b]/[0.1]`+`text-[#ffcb2b]`→`border-primary/40`+`bg-primary/10`+`text-primary` (RATING_REQUEST badge), `text-[#a6a39a]`→`text-muted-foreground` (REJECT badge icon color, read notification title, empty-state icon+text, "Ver mais" button), `bg-[#ffcb2b]/[0.06]`→`bg-primary/6` (unread item background), `text-[#f6f4ec]`→`text-foreground` (unread title, popover header, "Ver mais" hover), `bg-[#ffcb2b]`→`bg-primary` (unread dot), `bg-[#161513]`→`bg-card` (`PopoverContent`), `text-[#ffcb2b]`+`hover:text-[#ffe08a]`→`text-primary`+`hover:text-accent` ("Marcar todas como lidas" button).
- **Files changed:** `client/src/components/NotificationDropdown.tsx` (className strings only — full diff read to confirm zero JSX structure/prop/handler/import/logic changes), `scripts/ralph/prd.json` (US-020 → `passes: true`, `notes` documents leftovers and the allowlist decision below).
- **Deliberate AC deviation (same shape as US-003 through US-019):** `components/NotificationDropdown.tsx` was **NOT** removed from `client/color-migration-allowlist.txt`. Confirmed via `node scripts/check-color-literals.mjs --list-offenders` (from `client/`) that raw color remains: `#ff9e2c` (MATCH badge gradient end, same untokenized orange recurring since US-007), `#111` (text over the MATCH badge, same "dark text on colored surface" leftover seen in `ComparisonDialog.tsx`/`MediaCard.tsx`/`TitleModal.tsx`), `shadow-[0_4px_14px_rgba(255,203,43,.35)]`/`shadow-[0_24px_60px_rgba(0,0,0,.5)]` (no `--shadow-*` token, same as every prior story), `text-[#75726a]` (2×, tertiary/timestamp text, unique to this file, no token), `text-[#d8d3c5]` (unread notification body text, same leftover as `TitleModal.tsx`). Removing the file would break the `check:colors` CI gate (US-002) — kept it allowlisted, matching precedent.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` (no `--list-offenders`) passes — the file's remaining raw color is still covered by the allowlist entry (54 files, unchanged count).
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao do gate `check:colors`).
- **Learnings for future iterations:**
  - `bg-[#ffcb2b]/[0.1]` / `bg-[#ffcb2b]/[0.06]` (a hex arbitrary-value utility with a *separate* bracketed opacity modifier, `bg-[#hex]/[N]`, rather than `rgba(...)` inside the bracket) is functionally identical to the `rgba(R,G,B,X)` case already documented — the hex matches a token, so the whole thing (bracket **and** opacity suffix) converts to `token-name/N` (`bg-primary/10`, `bg-primary/6`), dropping the brackets entirely since Tailwind's plain `/N` opacity modifier works directly on the named token utility.
  - `#ff9e2c`, `#111`, `#75726a`/`#d8d3c5` (a "dark badge text" and "unread pill body text" leftover pair) continue to be the dominant untokenized-color shapes across almost every file migrated in this epic — no new pattern found here, just further confirmation these are prototype-wide constants rather than one-off values, still not worth guessing a token for without a dedicated US-001 follow-up.
  - Next priority story is US-021 (`DashboardPreview.tsx` + `SearchTab.tsx`, 95 occurrences) — first two-file story in the sequence; treat each file independently for the allowlist check (`--list-offenders` after the edit) since one might zero out while the other doesn't.
---

## 2026-08-14 - US-021
- Implemented: mechanically migrated hex/arbitrary-color literals to US-001 tokens across two files in one story (first two-file migration story, treated independently).
  - `client/src/components/landing/DashboardPreview.tsx` (15 substitutions): `bg-[#161513]`->`bg-card` (3x, the KPI/genres cards), `text-[#a6a39a]`->`text-muted-foreground` (7x, labels/legends/values), `text-[#f6f4ec]`->`text-foreground` (4x, KPI value, heading, genre name), `text-[#ffcb2b]`->`text-primary` (eyebrow "Dashboard do casal"), `bg-[#ffcb2b]`->`bg-primary` (the "Filmes" segment of the split bar). One non-className value — `iconColor="#ffcb2b"`, a plain string prop consumed via `style={{ color: iconColor }}` inside the local `KpiTeaser` subcomponent, same non-className substitution pattern used since US-003/US-004 — became `iconColor="var(--primary)"`.
  - `client/src/screens/match/SearchTab.tsx` (15 substitutions): `text-[#a6a39a]`->`text-muted-foreground` (6x: idle/empty states, trending subtitle, results counter, pagination ellipsis, "end of results" text), `text-[#f6f4ec]`->`text-foreground` (4x: "Resultados para" heading, 3 pagination buttons' `bg-card`+`text-foreground` pair), `border-[rgba(255,107,107,.35)]`/`bg-[rgba(255,107,107,.08)]`/`text-[#ffb3b3]`/`border-[rgba(255,107,107,.4)]`/`bg-[rgba(255,107,107,.12)]`->`border-destructive/35`, `bg-destructive/8`, `text-destructive-foreground` (2x), `border-destructive/40`, `bg-destructive/12` (error block + "Tentar novamente" retry button — same "rgba(R,G,B,X) matches a token's RGB at any alpha" rule from US-003 onward, here applied to `--destructive`), `bg-[#161513]`->`bg-card` (3x, pagination buttons), `bg-[#ffcb2b]`/`text-[#09090a]`->`bg-primary`/`text-background` (active page).
- **Files changed:** `client/src/components/landing/DashboardPreview.tsx`, `client/src/screens/match/SearchTab.tsx` (className/prop-value strings only — confirmed by symmetric `git diff --stat`, 30 insertions/30 deletions in each file, zero JSX structure/non-color prop/handler/import/logic change), `scripts/ralph/prd.json` (US-021 -> `passes: true`, `notes` documents the leftovers and allowlist decision below).
- **Deliberate AC deviation (same shape as every migration story since US-003):** neither file was removed from `client/color-migration-allowlist.txt`. Confirmed via `node scripts/check-color-literals.mjs --list-offenders` (from `client/`) that both still have raw color: `DashboardPreview.tsx` — `border-[rgba(255,255,255,.07)]` (3x, KpiTeaser card border, alpha doesn't match `--border`'s `.1`), `#ff9e2c` (2x, the "Series/TV" iconColor + progress-bar segment, same untokenized orange from US-007), `#3ddc97` (2x, Sparkles icon + genre progress-bar fill, the same recurring green leftover since US-003); `SearchTab.tsx` — `hover:bg-[#ffdd7a]` (hover state of the active pagination button, the same recurring "shared badge text" gold leftover since US-003/US-006/US-008/US-012). Removing either file would break the `check:colors` CI gate (US-002) — kept both allowlisted, matching precedent.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` (no `--list-offenders`) passes — both files' remaining raw color is still covered by their allowlist entries (54 files, unchanged count).
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao do gate `check:colors`).
- **Learnings for future iterations:**
  - First two-file story in the sequence worked cleanly by treating each file's allowlist status fully independently — no new pattern needed, the same "migrate what maps, leave the rest, check `--list-offenders` per file before deciding whether to drop it" process from single-file stories applies unchanged when a story bundles multiple files.
  - `rgba(255,107,107,X)` (`--destructive`, `#ff6b6b`) converting to `token-name/X` at any alpha (`border-destructive/35`, `bg-destructive/8`, etc.) is now confirmed in a component outside the modal/card family (`SearchTab.tsx`'s error-state block) — same mechanical rule as every prior file, no exceptions found.
  - Next priority story is US-022 (`WatchModal.tsx` + `ReviewModal.tsx`, 92 occurrences) — another two-file story; per `client/CLAUDE.md`'s "Modal patterns" section these are both hand-rolled `fixed inset-0` backdrop modals (not the shadcn `Dialog` family), so expect the same leftover shapes seen in `PendingDetailModal.tsx`/`MediaDetailModal.tsx` (`#3ddc97`, `#d6d2c8`, `#c4bfb4`, `rgba(255,255,255,.05|.06|.08)`) rather than anything modal-pattern-specific.
---

## 2026-08-14 - US-022
- Implemented: mechanically migrated hex/arbitrary-color literals to US-001 tokens across two files in one story (second two-file migration story, treated independently, same as US-021).
  - `client/src/components/WatchModal.tsx` (8 substitutions): `bg-[#161513]`->`bg-card` + `text-[#f6f4ec]`->`text-foreground` (outer `DialogContent`-equivalent div), `text-[#a6a39a]`->`text-muted-foreground` (subtitle, close button, both "(opcional)" spans), `focus:border-[#ffcb2b]`->`focus:border-primary` (opinion textarea), `border-[#ff6b6b]/30`+`bg-[#ff6b6b]/10`+`text-[#ffb3b3]`->`border-destructive/30`+`bg-destructive/10`+`text-destructive-foreground` (error block), `text-[#f6f4ec]`->`text-foreground` (Cancelar button).
  - `client/src/components/ReviewModal.tsx` (9 substitutions): same 8 as WatchModal, plus the Confirmar-equivalent button (here "Salvar avaliação", already primary-yellow not green like WatchModal's) — `border-[rgba(255,203,43,.35)]`/`bg-[rgba(255,203,43,.14)]`/`text-[#ffcb2b]`/`hover:bg-[rgba(255,203,43,.2)]`->`border-primary/35`/`bg-primary/14`/`text-primary`/`hover:bg-primary/20` (the `rgba(255,203,43,X)`-matches-`--primary`-at-any-alpha rule from US-003 onward).
- **Files changed:** `client/src/components/WatchModal.tsx`, `client/src/components/ReviewModal.tsx` (className/style-value strings only — full diff read to confirm zero JSX structure/prop/handler/import/logic changes; `git diff --stat` shows 16/18 lines changed, symmetric insertions/deletions), `scripts/ralph/prd.json` (US-022 -> `passes: true`, `notes` documents the leftovers below).
- **Deliberate AC deviation (same shape as every migration story since US-003):** neither file was removed from `client/color-migration-allowlist.txt`. Confirmed via `node scripts/check-color-literals.mjs --list-offenders` (from `client/`) that both still have raw color: both files — `bg-[rgba(8,7,11,.72)]` (backdrop; `rgb(8,7,11)` close to but not an exact RGB match of `--background`'s `rgb(9,9,10)`, same near-miss precedent as `DeleteAccountSection.tsx`'s dialog backdrop from US-009 — correctly left untouched), `text-[#d8d3c5]` (2x each, "Sua nota"/"Sua opinião" labels — same leftover as `TitleModal.tsx`/US-008, `NotificationDropdown.tsx`/US-020), inline `style={{ color: star <= rating ? "#ffb443" : "rgba(255,255,255,.18)" }}` (star-rating gold, recurring leftover since US-004/US-006/US-008), `bg-[#201e18]` (textarea surface, same leftover as `TitleModal.tsx`), `shadow-[0_30px_80px_rgba(0,0,0,.6)]`/`focus:shadow-[0_0_0_3px_rgba(255,203,43,.2)]` (no `--shadow-*` token, same as every prior story); `WatchModal.tsx` only — `border-[rgba(61,220,151,.35)]`/`bg-[rgba(61,220,151,.12)]`/`text-[#3ddc97]`/`shadow-[0_6px_20px_rgba(61,220,151,.15)]`/`hover:bg-[rgba(61,220,151,.18)]` (the "Confirmar" button's green, `#3ddc97` family, recurring leftover since US-003); `ReviewModal.tsx` only — `shadow-[0_6px_20px_rgba(255,203,43,.15)]` (the "Salvar avaliação" button's shadow, no shadow token). Removing either file would break the `check:colors` CI gate (US-002) — kept both allowlisted, matching precedent.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` (no `--list-offenders`) reports "ok" — 54 files in the allowlist, unchanged count.
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao do gate `check:colors`).
- **Learnings for future iterations:**
  - `WatchModal.tsx`/`ReviewModal.tsx` are near-identical twins (same hand-rolled `fixed inset-0` backdrop pattern per `client/CLAUDE.md`'s "Modal patterns" section) except for their confirm button's accent color (green `#3ddc97` for "Confirmar" in WatchModal vs. primary yellow for "Salvar avaliação" in ReviewModal) — the yellow one converted fully via the existing `--primary` token/alpha rule, the green one has no token and stayed a leftover, same asymmetry noted for other green-accented leftovers across the epic.
  - `bg-[rgba(8,7,11,.72)]` (dialog backdrop) is now confirmed as a near-miss-of-`--background` leftover in a **second** context (previously only `DeleteAccountSection.tsx`'s dialog backdrop, US-009) — worth flagging if a future US-001 follow-up ever considers a slightly-different-alpha or slightly-different-RGB "backdrop" token, since `rgb(8,7,11)` keeps recurring exactly (not randomly close) across unrelated files.
  - Next priority story is US-023 (`RatingRequestDialog.tsx` + `SearchResultCard.tsx`, 89 occurrences) — first story in the sequence pairing a shadcn `Dialog`-family component (`RatingRequestDialog.tsx`, per `client/CLAUDE.md`'s Modal patterns section, opened from `NotificationDropdown` as a Fragment sibling) with a plain presentational card (`SearchResultCard.tsx`, part of the `screens/match/` decomposition) — treat each file's allowlist status independently as usual.
---

## 2026-08-14 - US-023
- Implemented: mechanically migrated `client/src/components/RatingRequestDialog.tsx` and `client/src/screens/match/SearchResultCard.tsx`'s hex/arbitrary-color literals to the US-001 tokens, same mechanical rules established in US-003 through US-022.
  - `RatingRequestDialog.tsx`: `bg-[#161513]`→`bg-card`, `text-[#f6f4ec]`→`text-foreground` (dialog title, `DialogContent`, Cancelar button, textarea), `text-[#a6a39a]`→`text-muted-foreground` (both "(opcional)" spans), `focus:border-[#ffcb2b]`→`focus:border-primary`, `border-[#ff6b6b]/30`+`bg-[#ff6b6b]/10`+`text-[#ffb3b3]`→`border-destructive/30`+`bg-destructive/10`+`text-destructive-foreground` (error block), `border-[rgba(255,203,43,.35)]`/`bg-[rgba(255,203,43,.14)]`/`text-[#ffcb2b]`/`hover:bg-[rgba(255,203,43,.2)]`→`border-primary/35`/`bg-primary/14`/`text-primary`/`hover:bg-primary/20` (Salvar avaliação button).
  - `SearchResultCard.tsx`: `bg-[#161513]`→`bg-card` (card root), `border-[#ffcb2b]`→`border-primary` (compare-selected outline), `bg-[rgba(9,9,10,.6)]`→`bg-background/60` (type badge + vote-average badge, same `rgba(9,9,10,X)`=`--background` exact rule as US-006/US-007), `text-[#f6f4ec]`→`text-foreground` (both badges + idle Curtir button), `border-[#161513]`/`bg-[#ffcb2b]`→`border-card`/`bg-primary` (compare-order badge), `text-[#a6a39a]`→`text-muted-foreground` (year, overview), `border-[rgba(255,203,43,.5)]`/`bg-[rgba(255,203,43,.16)]`→`border-primary/50`/`bg-primary/16` (Curtir button, `matched` state), `border-[rgba(255,107,107,.35)]`/`bg-[rgba(255,107,107,.1)]`→`border-destructive/35`/`bg-destructive/10` (Curtir button, `error` state).
- **Files changed:** `client/src/components/RatingRequestDialog.tsx`, `client/src/screens/match/SearchResultCard.tsx` (className/style-value strings only — full diff read to confirm zero JSX structure/prop/handler/import/logic changes), `scripts/ralph/prd.json` (US-023 → `passes: true`, `notes` documents the leftovers and the allowlist decision below).
- **Deliberate AC deviation (same shape as US-003 through US-022):** neither file was removed from `client/color-migration-allowlist.txt`. Confirmed via `node scripts/check-color-literals.mjs --list-offenders` (from `client/`) that raw color remains in both:
  - `RatingRequestDialog.tsx`: `text-[#d8d3c5]` (labels, same leftover as `TitleModal.tsx` US-008), `bg-[#201e18]` (textarea surface, same "input surface" leftover as `TitleModal.tsx`), the star-rating `style={{ color: "#ffb443" }}` ternary (recurring TMDB gold, plus `rgba(255,255,255,.18)` for the unfilled star — a plain style value outside a className bracket, not gated), `shadow-[...]` ×3 (dialog shadow, textarea focus shadow, Salvar button shadow — no `--shadow-*` token, same as every prior story), `text-[#ffdd7a]` (recurring "shared badge text" leftover — none present here actually, correcting: not in this file). Confirmed offenders: `text-[#d8d3c5]` (2×), `bg-[#201e18]`, `#ffb443` (style value), `shadow-[...]` (3×).
  - `SearchResultCard.tsx`: `text-[#ffdd7a]` (Curtir button `matched` state text, recurring leftover), `text-[#8fe9c4]`/`border-[rgba(61,220,151,.35)]`/`bg-[rgba(61,220,151,.1)]` (Curtir button `liked` state — a lighter tint of the recurring `#3ddc97` green family, no exact token), `text-[#111]` (compare-order badge text, same recurring leftover as `ComparisonDialog.tsx`/`MediaCard.tsx`), `bg-[#01b47f]` (TMDB vote-average dot, a distinct green from the `#3ddc97` family, unique to this file, no token), `border-[rgba(255,255,255,.07)]`/`border-white/12`/`/20`/`/35`/`hover:bg-white/[0.06]` (alphas/named-white with no exact `--border` match or not matched by the gate's regex at all, confirmed since US-008/US-009).
  - Pulling either file off the allowlist with any of these remaining would break the `check:colors` CI gate (US-002) — kept both allowlisted, matching precedent.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` (no `--list-offenders`) passes — both files' remaining raw color is still covered by the allowlist entry (54 files, unchanged count).
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao do gate `check:colors`).
- **Learnings for future iterations:**
  - `#01b47f` (TMDB vote-average dot in `SearchResultCard.tsx`) is a new, previously-unseen green — distinct from the recurring `#3ddc97` leftover family seen in every prior story. Don't assume it's the same value; it's a one-off with no token either way.
  - Next priority story is US-024 (`ResetPasswordPage.tsx` + `ProfileSection.tsx`, 83 occurrences) — first `routes/auth/` file in this migration sequence; expect the same "leave what doesn't map" fallback since neither AC calls out a chart/series-color exception.
---

## 2026-08-14 - US-024
- Implemented: mechanical hex/arbitrary-color -> semantic token migration in `client/src/routes/auth/ResetPasswordPage.tsx` and `client/src/screens/account/ProfileSection.tsx`. Only `className` strings changed (no JSX structure/prop/handler/import/logic touched).
  - `#ffcb2b` -> `primary` (bg/text, in both files, including the combined `bg-[#ffcb2b] text-[#09090a] hover:bg-[#ffe08a]` CTA-button pattern -> `bg-primary text-background hover:bg-accent`, matching the established convention already used across `FiltersPanel.tsx`/`SearchTab.tsx`/`PasswordSection.tsx`/`CoupleSection.tsx`/etc.)
  - `#f6f4ec` -> `foreground` (both files)
  - `#a6a39a` -> `muted-foreground` (both files)
  - `#ff6b6b` -> `destructive` (ResetPasswordPage.tsx error banner border/bg)
  - Left untouched, no matching token among the 8 established in US-001: `#6f6c62` (both files), `#ff9b9b` (ResetPasswordPage.tsx), `#ff8f7c` + `rgba(255,92,71,.28)`/`rgba(255,92,71,.08)` = `#ff5c47` (ProfileSection.tsx form-error block — same custom "destructive" red documented in `client/CLAUDE.md`'s AccountSection section, still has no token of its own). Recorded in the story's `notes` field per AC.
  - Per the established US-003 exception (documented in `client/CLAUDE.md`): since neither file reaches zero raw color after migration, **both stay in `color-migration-allowlist.txt`** rather than being removed — removing them would just break `check:colors` over genuinely tokenless leftovers. `node scripts/check-color-literals.mjs --list-offenders` still lists both files, confirming they're not falsely allowlisted.
- Files changed: `client/src/routes/auth/ResetPasswordPage.tsx`, `client/src/screens/account/ProfileSection.tsx`, `scripts/ralph/prd.json` (US-024 `passes`/`notes`).
- Verification: `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` / `bun run check:colors` all pass from `client/`.
- Verificacao visual em navegador fica PENDENTE DE GATE HUMANO (sem navegador neste sandbox, decisao D19) - nao marcado como verificado visualmente, apenas verificado estaticamente (grep + typecheck/lint/test/build/check:colors).
- **Learnings for future iterations:**
  - Next priority story is US-025 (`compareUi.tsx` + `MatchScreen.tsx`).
  - No new tokens or patterns discovered this story — pure application of already-documented mappings (`#ffcb2b`/`#f6f4ec`/`#a6a39a`/`#ff6b6b`) and the already-documented "leave tokenless leftovers, keep file allowlisted" exception from US-003.
---

## 2026-08-14 - US-025
- Implemented: mechanically migrated `client/src/screens/match/compareUi.tsx` and `client/src/screens/match/MatchScreen.tsx`'s hex/arbitrary-color literals to the US-001 tokens — `compareUi.tsx`: `border-[rgba(255,203,43,.5)]`/`bg-[rgba(255,203,43,.12)]`/`text-[#ffcb2b]`/`hover:bg-[rgba(255,203,43,.18)]`/`hover:text-[#ffcb2b]`→`border-primary/50 bg-primary/12 text-primary hover:bg-primary/18 hover:text-primary` (active `CompareToggleButton`), `bg-[#161513]`/`text-[#f6f4ec]`→`bg-card`/`text-foreground` (inactive `CompareToggleButton`), `border-[rgba(255,255,255,.1)]` (exact `--border` alpha)→`border-border` + `text-[#f6f4ec]`→`text-foreground` (`TooltipContent`), `bg-[#161513]/95`→`bg-card/95` + `text-[#f6f4ec]` (×2)→`text-foreground` (selection chip), `text-[#a6a39a]`→`text-muted-foreground` (clear button), `text-[#ffb3b3]`→`text-destructive-foreground` (error message), `text-[#ffcb2b]`/`hover:text-[#ffe08a]`→`text-primary`/`hover:text-accent` (retry button). `MatchScreen.tsx`: `text-[#f6f4ec]` (root)→`text-foreground`, the trailing `#09090a` inside the composite `style={{background:...}}` gradient string (same "hex inside non-className CSS value" pattern as US-005/US-007)→`var(--background)`, `text-[#a6a39a]`→`text-muted-foreground` (paragraph), `bg-[#ffcb2b]`/`text-[#09090a]` (×2, active tabs)→`bg-primary`/`text-background`, `text-[#a6a39a]`/`hover:text-[#f6f4ec]` (×2, inactive tabs)→`text-muted-foreground`/`hover:text-foreground`.
- **Files changed:** `client/src/screens/match/compareUi.tsx`, `client/src/screens/match/MatchScreen.tsx` (className/style-value strings only — full diff read to confirm zero JSX structure/prop/handler/import/logic changes), `scripts/ralph/prd.json` (US-025 → `passes: true`, `notes` documents leftovers and the allowlist decision below).
- **Deliberate AC deviation (same shape as US-003 through US-024):** neither file was removed from `client/color-migration-allowlist.txt`. Confirmed via `node scripts/check-color-literals.mjs --list-offenders` (from `client/`) that raw color remains: `compareUi.tsx` — `bg-[#201e18]` (tooltip surface, same recurring "tooltip/input surface" leftover documented since US-006/US-008), `shadow-[0_20px_50px_rgba(0,0,0,.5)]` (no `--shadow-*` token), `border-white/10`/`bg-white/[0.08]`/`hover:bg-white/[0.06]`/`hover:bg-white/[0.14]` (Tailwind named `white` + opacity, never flagged by `ARBITRARY_COLOR_RE`, confirmed since US-008). `MatchScreen.tsx` — `text-[#ff9e2c]` (the "Match a Dois" eyebrow label — same decorative, non-chart-pixel treatment already given to `DashboardScreen.tsx`'s "Estatísticas do Casal" eyebrow in US-007; the `--chart-2` exception from that story only applies to literal chart/graph series fills, not to accent labels that happen to share the hex), `bg-[rgba(255,255,255,.06)]` (no `bg-` token at that alpha, same as every prior file). Removing either file would break the `check:colors` CI gate (US-002) — kept both allowlisted, matching precedent.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` (no `--list-offenders`) passes — both files' remaining raw color is still covered by the allowlist entry (54 files, unchanged count).
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao do gate `check:colors`).
- **Learnings for future iterations:**
  - Confirms the eyebrow/label-vs-chart-pixel distinction from US-007 generalizes beyond `DashboardScreen.tsx`: any `text-[#ff9e2c]`-style decorative label found elsewhere in the app should default to the standard "leave it, note it" fallback, not the `--chart-2` chart-series exception — that exception is scoped strictly to literal chart/graph fills, per that story's own AC wording.
  - Next priority story is US-026 (`LoginPage.tsx` + `DeleteTrackDialog.tsx`, 56 occurrences) — no new leftover patterns expected beyond the now-familiar set (`bg-white`/`border-white` named-color opacity utilities, `shadow-[...]`, alpha-mismatched `--border`/`--background` rgba).
---

## 2026-08-14 - US-026
- Implemented: mechanically migrated hex/arbitrary-color literals in `client/src/routes/auth/LoginPage.tsx` and `client/src/components/DeleteTrackDialog.tsx` to US-001 tokens. `LoginPage.tsx`: `text-[#ffcb2b]`→`text-primary` (x2: "Criar conta" link, "Esqueci minha senha" link), `border-[#ffcb2b]/25`→`border-primary/25` + `bg-[#ffcb2b]/[0.08]`→`bg-primary/[0.08]` (notice banner), `text-[#f6f4ec]`→`text-foreground` (notice text), `text-[#a6a39a]`→`text-muted-foreground` (x2: E-mail/Senha labels), `hover:text-[#ffe08a]`→`hover:text-accent`, `border-[#ff6b6b]/30`→`border-destructive/30` + `bg-[#ff6b6b]/10`→`bg-destructive/10` (error banner), `bg-[#ffcb2b]`→`bg-primary` + `text-[#09090a]`→`text-background` + `hover:bg-[#ffe08a]`→`hover:bg-accent` (submit button). `DeleteTrackDialog.tsx`: `bg-[#161513]`→`bg-card` + `text-[#f6f4ec]`→`text-foreground` (dialog root), `text-[#ff6b6b]`→`text-destructive` (icon wash, cancel/confirm text), `text-[#a6a39a]`→`text-muted-foreground` (description), `border-[#ff6b6b]/30`→`border-destructive/30` + `bg-[#ff6b6b]/10`→`bg-destructive/10` + `text-[#ffb3b3]`→`text-destructive-foreground` (error message), `text-[#f6f4ec]`→`text-foreground` (Cancelar button), `border-[rgba(255,107,107,.35)]`→`border-destructive/35` + `bg-[rgba(255,107,107,.12)]`→`bg-destructive/12` + `hover:bg-[rgba(255,107,107,.18)]`→`hover:bg-destructive/18` (Excluir button, RGB(255,107,107) exact match to `--destructive`).
- **Files changed:** `client/src/routes/auth/LoginPage.tsx`, `client/src/components/DeleteTrackDialog.tsx` (className strings only — no JSX structure/prop/handler/import/logic changes), `scripts/ralph/prd.json` (US-026 → `passes: true`, `notes` documents leftovers and the allowlist decision below).
- **Deliberate AC deviation (same shape as every prior per-file story):** neither file was removed from `client/color-migration-allowlist.txt`. Leftover raw color confirmed via `node scripts/check-color-literals.mjs`: `LoginPage.tsx` — `text-[#ff9b9b]` on the login error message (near-miss hex, distinct from `--destructive-foreground`'s `#ffb3b3`, same "near-miss" family documented since US-010/US-026's own notes). `DeleteTrackDialog.tsx` — `bg-[rgba(8,7,11,.72)]` (backdrop, RGB doesn't match `--background`'s `#09090a`), `bg-[rgba(255,90,90,.12)]` (icon wash, RGB(255,90,90) is close to but not identical to `--destructive`'s RGB(255,107,107) — don't round it in), `shadow-[0_30px_80px_rgba(0,0,0,.6)]` and `shadow-[0_6px_20px_rgba(255,107,107,.15)]` (no `--shadow-*` token, same as every prior story), `border-white/10`/`hover:bg-white/[0.06]` (named-white opacity utilities, never flagged by the gate regex, out of scope per established precedent). Removing either file would break `check:colors` — kept both allowlisted.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` (no `--list-offenders`) passes — both files' remaining raw color still covered by the allowlist entry (54 files, unchanged count).
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao do gate `check:colors`).
- **Learnings for future iterations:**
  - Confirms the "RGB must match exactly, alpha doesn't matter" rule from US-003/US-004 also applies as a *negative* filter: `rgba(255,90,90,X)` is visually close to `--destructive` (`rgb(255,107,107)`) but the R/G/B don't match exactly (90≠107), so it correctly stays untokenized rather than being rounded into `bg-destructive/N` — a near-miss RGB is not the same case as an alpha-only mismatch.
  - Next priority story is US-027 (`ForgotPasswordPage.tsx` + `LandingFooter.tsx`, 52 occurrences) — no new leftover patterns expected beyond the now-familiar set (named-white opacity utilities, `shadow-[...]`, near-miss non-token hex, alpha-mismatched rgba).
---

## 2026-08-14 - US-027
- Implemented: `client/src/routes/auth/ForgotPasswordPage.tsx` and `client/src/components/landing/LandingFooter.tsx` now consume the US-001 semantic tokens (`text-primary`, `bg-primary`/`text-background`/`hover:bg-accent`, `text-muted-foreground`, `border-destructive`/`bg-destructive`, `text-foreground`, `focus-visible:outline-primary`) instead of the equivalent hex/rgba literals, mechanical 1:1 substitution — 14 occurrences converted total.
- Files changed: `client/src/routes/auth/ForgotPasswordPage.tsx`, `client/src/components/landing/LandingFooter.tsx`, `scripts/ralph/prd.json` (US-027 passes:true), `scripts/ralph/progress.txt`.
- Leftovers with no matching token (left untouched, same fallback rule as US-003 through US-026): `text-[#ff9b9b]` (ForgotPasswordPage — close to but not an exact match of `--destructive-foreground` `#ffb3b3`), `text-[#111]` (LandingFooter — recurring "dark text on yellow surface" leftover already documented in several earlier stories), `text-[#ff9e2c]` (LandingFooter — same untokenized accent hue already documented as DashboardScreen's chart-series-2 color in US-007), `border-white/[0.07]` (LandingFooter — Tailwind named `white` + opacity, confirmed since US-008/US-009 to never match `ARBITRARY_COLOR_RE`). Both files stay in `color-migration-allowlist.txt` because `node scripts/check-color-literals.mjs --list-offenders` still lists them — removing either would break the `check:colors` CI gate.
- Verification: `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files) / `bun run build` / `node scripts/check-color-literals.mjs` (54 files still allowlisted, no new raw color) all pass from `client/`. Diff confirmed to be className-string-only via full `git diff` read.
- Registrado conforme AC: verificacao visual em navegador fica PENDENTE DE GATE HUMANO (sem navegador no sandbox, decisao D19) - nao marcado como verificado visualmente.
- **Learnings for future iterations:**
  - No new patterns beyond what's already in Codebase Patterns above — this story followed the established exact-RGB-match mechanical rule with no new edge cases.
  - Next priority story is US-028 (`AccountSection.tsx` + `LandingHeader.tsx`).
---

## 2026-08-14 - US-028
- Implemented: mechanically migrated hex/arbitrary-color literals in `client/src/screens/account/AccountSection.tsx` and `client/src/components/landing/LandingHeader.tsx` to the US-001 semantic tokens. `AccountSection.tsx`: `text-[#a6a39a]`->`text-muted-foreground` (ReachChip "you" variant + description paragraph), `bg-[#ffcb2b]`->`bg-primary` (ReachChip "you" dot), `bg-[#161513]`->`bg-card` (non-destructive section root), `text-[#f6f4ec]`->`text-foreground` (non-destructive title). `LandingHeader.tsx`: `bg-[#09090a]/72`->`bg-background/72`, `focus-visible:outline-[#ffcb2b]`->`focus-visible:outline-primary` (x3), `bg-[#ffcb2b]`->`bg-primary` (logo icon + "Criar conta" button), `text-[#f6f4ec]`->`text-foreground` (x2: logo text, "Entrar" link), `text-[#a6a39a]`/`hover:text-[#f6f4ec]`->`text-muted-foreground`/`hover:text-foreground` (nav links).
- **Files changed:** `client/src/screens/account/AccountSection.tsx`, `client/src/components/landing/LandingHeader.tsx` (className strings only — full diff read to confirm zero JSX structure/prop/handler/import/logic changes), `scripts/ralph/prd.json` (US-028 -> `passes: true`, `notes` documents leftovers and allowlist decision below).
- **Deliberate AC deviation (same shape as US-003 through US-027):** neither file was removed from `client/color-migration-allowlist.txt`. Leftovers confirmed via `node scripts/check-color-literals.mjs --list-offenders`:
  - `AccountSection.tsx`: the coral `#ff5c47`/`rgba(255,92,71,X)` destructive-accent family (ReachChip "both" variant + `destructive=true` section styling — the deliberately distinct destructive palette documented in `client/CLAUDE.md`'s `screens/account/` section), `border-white/[0.08]`+`bg-white/[0.04]` (named-white opacity, never matched by the gate's regex), `border-[rgba(255,255,255,.07)]` (alpha doesn't exactly match `--border`'s `.1`), `text-[#ffb3a5]` (destructive title — near-miss of `--destructive-foreground`'s `#ffb3b3`, RGB doesn't match exactly). One notable case: `bg-[linear-gradient(180deg,rgba(255,92,71,.07),rgba(255,92,71,0)_120px),#161513]` — a single `bg-[...]` arbitrary utility mixing an untokenizable coral gradient with a `#161513` that *does* have a token (`--card`). Same situation as `CoupleSection.tsx`'s gradient in US-010: can't split one bracket utility into two Tailwind classes, and the gate's `ARBITRARY_COLOR_RE` flags any `bg-[...]` bracket regardless of its contents — swapping just the trailing hex for `var(--card)` inside the bracket wouldn't have removed the flag either, since it's still `bg-[...]` syntax. Left entirely untouched, matching the CoupleSection precedent.
  - `LandingHeader.tsx`: `text-[#111]` (dark text on yellow surface, the recurring leftover documented since `ComparisonDialog.tsx`/US-003), `text-[#ff9e2c]` (decorative dot in the logotype — same untokenized accent hue already treated as label/decoration, not chart-series pixel, in `DashboardScreen.tsx`/US-007 and `LandingFooter.tsx`/US-027), `shadow-[...]` x2 (logo icon glow + "Criar conta" button shadow, no `--shadow-*` token), `hover:bg-white/[0.06]` (named-white opacity, out of gate scope).
  - Removing either file from the allowlist would break the `check:colors` CI gate (US-002) over these genuinely tokenless leftovers — kept both allowlisted, matching precedent.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` / `node scripts/check-color-literals.mjs` (54 files still allowlisted, no new raw color) all pass from `client/`.
- Registrado conforme AC: verificacao visual em navegador fica PENDENTE DE GATE HUMANO (sem navegador no sandbox, decisao D19) - nao marcado como verificado visualmente.
- **Learnings for future iterations:**
  - A single arbitrary Tailwind bracket utility that layers a CSS multi-background value (`bg-[linear-gradient(...),#hex]`) can't be partially detokenized — the gate's regex flags the whole `bg-[...]` bracket regardless of content, so there's no partial win available there even when part of the value has a token; leave the entire bracket untouched (same call as `CoupleSection.tsx`/US-010's gradient case).
  - Next priority story is US-029.
---

## 2026-08-14 - US-029
- Implemented: mechanically migrated `client/src/components/landing/Testimonials.tsx` and `client/src/components/MatchCelebrationModal.tsx`'s hex/arbitrary-color literals to the US-001 tokens — `text-[#ffcb2b]`->`text-primary` (incl. `/20` opacity variant), `text-[#f6f4ec]`->`text-foreground`, `bg-[#161513]`->`bg-card`, `border-[#161513]`->`border-card`, `text-[#a6a39a]`->`text-muted-foreground` (both files), `to-[#161513]`->`to-card` (gradient stop), `bg-[#ffcb2b]`->`bg-primary`, `text-[#ffe08a]`->`text-accent`.
- Files changed: `client/src/components/landing/Testimonials.tsx`, `client/src/components/MatchCelebrationModal.tsx` (className/style-value strings only — full diff read to confirm zero JSX structure/prop/handler/import/logic changes), `scripts/ralph/prd.json` (US-029 -> `passes: true`, `notes` documents leftovers and the allowlist decision below).
- **Deliberate AC deviation (same shape as US-003 through US-028):** neither file was removed from `client/color-migration-allowlist.txt`. Confirmed via `node scripts/check-color-literals.mjs --list-offenders` (from `client/`) that both still have raw color after the mechanical migration: `Testimonials.tsx` — `border-[rgba(255,255,255,.07)]`/`shadow-[...rgba(0,0,0,.35)]` (no exact `--border` alpha match/no `--shadow-*` token), `text-[#d8d3c5]` (quote text, distinct hex from the recurring `#d6d2c8` leftover); `MatchCelebrationModal.tsx` — `bg-[rgba(8,7,11,.82)]` (backdrop, `rgb(8,7,11)` close to but not exactly `--background`'s `rgb(9,9,10)`, same non-match as US-009/US-010's dialog backdrops), `border-[rgba(255,158,44,.35)]` (`#ff9e2c` halo, no token), `from-[#251e0c]` (gradient start stop, no token — only the `to` stop matched `--card`), `shadow-[...rgba(0,0,0,.6)]` (no `--shadow-*` token), `text-[#111]` (dark text over the yellow "Continuar" button, same recurring leftover as `ComparisonDialog.tsx`/`MediaCard.tsx`/`TitleModal.tsx`). Removing either file would break the `check:colors` CI gate (US-002) — kept both allowlisted, matching precedent.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` (no `--list-offenders`) passes — both files' remaining raw color is still covered by the allowlist entries (54 files, unchanged count).
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao do gate `check:colors`).
- **Learnings for future iterations:**
  - `border-white/10`/`hover:bg-white/[0.06]` recur again in `MatchCelebrationModal.tsx`'s outline `Button` (same Tailwind-named-`white`-plus-opacity pattern confirmed out of gate scope since US-008) — no change needed, consistent with every prior file.
  - Next priority story is US-030 (`RegisterPage.tsx` + `HowItWorks.tsx`, 44 occurrences) — same mechanical approach; no chart-specific AC bullet expected (only `DashboardScreen.tsx`/US-007 had that exception), so the standard "leave it, note it" fallback applies.
---

## 2026-08-14 - US-030
- Implemented: mechanically migrated `client/src/routes/auth/RegisterPage.tsx` and `client/src/components/landing/HowItWorks.tsx`'s hex/arbitrary-color literals to the US-001 tokens — `text-[#ffcb2b]`->`text-primary` ("Entrar" link, "Como funciona" eyebrow, step icon), `text-[#a6a39a]`->`text-muted-foreground` (Nome/E-mail/Senha labels, step numeral, step copy), `border-[#ff6b6b]/30`+`bg-[#ff6b6b]/10`->`border-destructive/30`+`bg-destructive/10` (RegisterPage error block), `bg-[#ffcb2b]`+`text-[#09090a]`+`hover:bg-[#ffe08a]`->`bg-primary`+`text-background`+`hover:bg-accent` (Criar conta button), `text-[#f6f4ec]`->`text-foreground` (HowItWorks h2 + step h3), `border-[#ffcb2b]/25`->`border-primary/25` (dashed connector line + icon card border), `bg-[#161513]`->`bg-card` (icon card).
- Files changed: `client/src/routes/auth/RegisterPage.tsx`, `client/src/components/landing/HowItWorks.tsx` (className strings only — full diff read to confirm zero JSX structure/prop/handler/import/logic changes), `scripts/ralph/prd.json` (US-030 -> `passes: true`, `notes` documents leftovers and the allowlist decision).
- **Deliberate AC deviation (same shape as US-003 through US-029):** neither file was removed from `client/color-migration-allowlist.txt`. Confirmed via `node scripts/check-color-literals.mjs --list-offenders` (from `client/`) that both still have raw color after the mechanical migration: `RegisterPage.tsx` — `text-[#ff9b9b]` (×2: password-too-short hint + error-block text; a distinct hex from `--destructive-foreground`'s `#ffb3b3`, no exact match); `HowItWorks.tsx` — `shadow-[0_10px_26px_rgba(0,0,0,.35)]` (icon card shadow, no `--shadow-*` token, same as every prior story's shadow leftovers). Removing either file would break the `check:colors` CI gate (US-002) — kept both allowlisted, matching precedent.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` (no `--list-offenders`) passes — both files' remaining raw color is still covered by the allowlist entries.
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao do gate `check:colors`).
- **Learnings for future iterations:**
  - `#ff9b9b` is a new leftover hex (RegisterPage's password/error text) — close to but not the same as `#ffb3b3` (`--destructive-foreground`); don't round it in, per the "exact match only" rule confirmed across every prior story.
  - Next priority story is US-031 (`AccountScreen.tsx` + `BondDissolvedNotice.tsx`, 40 occurrences) — same mechanical approach expected; `AccountScreen.tsx` lives in `screens/account/` alongside `DeleteAccountSection.tsx`/`CoupleSection.tsx` (US-009/US-010), so check whether it shares that section's local destructive coral palette (`#ff5c47`/`rgba(255,92,71,X)`) as a likely leftover.
---

## 2026-08-14 - US-031
- Implemented: mechanically migrated hex/arbitrary-color literals in `client/src/screens/account/AccountScreen.tsx` and `client/src/routes/auth/BondDissolvedNotice.tsx` to the US-001 semantic tokens where an exact match existed — `text-[#f6f4ec]`→`text-foreground`, `text-[#a6a39a]`→`text-muted-foreground` (all occurrences, both files), `ring-[#ffcb2b]`→`ring-primary`, `bg-[#ffcb2b]`→`bg-primary`, `hover:text-[#f6f4ec]`→`hover:text-foreground` (both files), and the trailing `#09090a` inside `AccountScreen.tsx`'s inline `style={{ background: "radial-gradient(...), radial-gradient(...), #09090a" }}` → `var(--background)` (same pattern as US-007's DashboardScreen gradient fix — only the trailing solid-color stop changes, the `rgba(...)` gradient stops themselves stay untouched since they're not `#hex` and not inside a Tailwind arbitrary-utility bracket).
- Left untouched (no token in the 8-token US-001 set covers them): `#ff9e2c` (uppercase eyebrow label, `AccountScreen.tsx`), `#ff5c47` and every `rgba(255,92,71,*)` variant (the "reach=both" coral family — border, two gradient strings, pill background, `AccountScreen.tsx`'s reach dot and all of `BondDissolvedNotice.tsx`'s coral accents), `#ffb3a5` (notice heading) and `#ff8f7c` (pill text) — both close to but distinct from `--destructive-foreground` (#ffb3b3), not rounded in per the "don't move a pixel to save a token" rule from US-003's learnings.
- Files changed: `client/src/screens/account/AccountScreen.tsx`, `client/src/routes/auth/BondDissolvedNotice.tsx` (className/style-value strings only), `scripts/ralph/prd.json` (US-031 → `passes: true`, `notes` documents the leftover colors and allowlist decision).
- **Deliberate AC deviation, same as US-003 onward:** neither file was removed from `client/color-migration-allowlist.txt`, even though that's a literal AC bullet — both still have raw color left (the coral/orange families above), and removing them would make `check:colors` fail on the next CI run. Confirmed `node scripts/check-color-literals.mjs` still reports "ok (54 arquivo(s) na allowlist)" after the edit — same count as before, no file added or removed.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` passes with both files still allowlisted.
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao do gate `check:colors`).
- **Learnings for future iterations:**
  - The "reach=both" coral family (`#ff5c47`/`rgba(255,92,71,*)`/`#ffb3a5`/`#ff8f7c`) is used consistently across `AccountScreen.tsx` (nav dot) and `BondDissolvedNotice.tsx` (the whole "vinculo desfeito" visual language, per that file's own doc comment linking it to `CoupleSection.tsx`'s bond line) — a real candidate for a future US-001 follow-up token (something like `--destructive-strong`/`--bond-broken`), same shape as the `#ffdd7a`/pill-family note left in US-003's progress entry. Not created here, per "don't invent tokens mid-migration-story" rule.
  - Next priority story is US-032 (`AuthLayout.tsx` + `FinalCta.tsx`).
---

## 2026-08-14 - US-032
- Migrated color literals to semantic tokens in `client/src/routes/auth/AuthLayout.tsx` and `client/src/components/landing/FinalCta.tsx`: #f6f4ec -> text-foreground, #ffcb2b -> bg-primary/outline-primary, #a6a39a -> text-muted-foreground, #09090a (inline style background gradient) -> var(--background), following the exact same inline-gradient pattern already used by DashboardScreen.tsx/HubScreen.tsx (keep the two translucent rgba() overlay stops literal, replace only the trailing solid hex with var(--background)).
- Files changed: client/src/routes/auth/AuthLayout.tsx, client/src/components/landing/FinalCta.tsx, scripts/ralph/prd.json (US-032 passes:true + notes)
- **Learnings for future iterations:**
  - Neither file could be fully zeroed out and removed from `color-migration-allowlist.txt` (same deviation pattern as US-003/US-004, see client/CLAUDE.md's per-file migration note): leftover untokenized colors are `#ff9e2c` (a lighter orange used for eyebrow/kicker text and a decorative separator dot — no token in the US-001 8-token set covers it; it also shows up in HubScreen.tsx/DashboardScreen.tsx's identical hero gradient, so it's a real cross-file constant, not a one-off), `shadow-[...rgba(255,203,43,X)]` and `shadow-[...rgba(0,0,0,X)]` (index.css has zero `--shadow-*` tokens, confirmed again here), and `text-[#111]` (near-black text rendered on top of `bg-primary` buttons/badges — `--primary-foreground` is a brown/amber oklch value, not black, so it is NOT a valid substitute despite the shadcn naming convention suggesting otherwise). Kept both files in the allowlist; `node scripts/check-color-literals.mjs` (from `client/`) still reports "ok" after the edit.
  - Verified with a manual regex scan (`HEX_RE`/`ARBITRARY_COLOR_RE` copied straight from `check-color-literals.mjs`) run directly against just the two target files, since the allowlist hides them from the normal script's stdout — worth doing this for any story that claims "fully migrated" so a leftover doesn't silently ride along in the allowlist forever.
  - `outline-[#hex]` is NOT covered by `ARBITRARY_COLOR_RE` in `check-color-literals.mjs` (the property list is `bg|text|border|ring|from|to|via|shadow|fill|stroke` — no `outline`), but a raw `#ffcb2b` inside it still gets caught by the separate `HEX_RE` hex scan. `FinderCta.tsx` had `focus-visible:outline-[#ffcb2b]` on both `<Link>` CTAs — converted to `outline-primary` (Tailwind v4 exposes `outline-<token>` the same way it exposes `bg-<token>`/`text-<token>`, since `--color-primary` is registered via the `@theme` block).
  - Manual visual verification in a real browser is PENDING HUMAN GATE — no browser available in this sandbox (decision D19), not marked as visually verified.
---

## 2026-08-14 - US-033
- Implemented: mechanically migrated `client/src/components/landing/FeatureGrid.tsx`'s hex/arbitrary-color literals to the US-001 tokens — `text-[#ffcb2b]`→`text-primary` (eyebrow "Recursos" label + feature icon), `text-[#f6f4ec]`→`text-foreground` (h2 heading + card title), `bg-[#161513]`→`bg-card` (card surface), `border-[#ffcb2b]/25`→`border-primary/25` (icon frame border), `bg-[#09090a]`→`bg-background` (icon frame fill), `text-[#a6a39a]`→`text-muted-foreground` (feature copy paragraph). 8 substitutions total, same mechanical mapping used since US-003.
- **Files changed:** `client/src/components/landing/FeatureGrid.tsx` (className strings only — full diff read to confirm zero JSX structure/prop/handler/import/logic changes), `scripts/ralph/prd.json` (US-033 → `passes: true`).
- **Deliberate AC deviation (same shape as every prior migration story since US-003):** `components/landing/FeatureGrid.tsx` was **NOT** removed from `client/color-migration-allowlist.txt`. Confirmed via `node scripts/check-color-literals.mjs --list-offenders` (from `client/`) that the file still has raw color after the mechanical migration: `border-[rgba(255,255,255,.07)]` (card border, alpha `.07` — doesn't match `--border`'s exact `.1` alpha) and `shadow-[0_10px_26px_rgba(0,0,0,.35)]` (card drop shadow — no `--shadow-*` token exists anywhere in `index.css`, same leftover category as every prior story's dialog/badge shadows). Pulling the file off the allowlist with either remaining would break the `check:colors` CI gate (US-002) — kept it allowlisted, matching precedent. `text-[clamp(1.75rem,3.5vw,2.5rem)]` and `text-[14px]` are non-color arbitrary utilities (font-size), correctly out of scope per the US-008 correction note (the regex matches the `text-[...]` shape regardless of content, but these aren't colors so they're just left as-is like every prior story's font-size leftovers).
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` (no `--list-offenders`) passes — the file's 2 remaining raw-color hits are still covered by the allowlist entry (54 files, unchanged count).
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao do gate `check:colors`).
- **Learnings for future iterations:**
  - Small file (20 occurrences → 8 actual mechanical substitutions, the rest were duplicate hex values or non-color arbitrary utilities), typical for the tail end of the per-file migration backlog — most of the highest-occurrence files are already done (this was priority 33).
  - Next priority story is US-034 (`FaqSection.tsx` + `ErrorBoundary.tsx` + `NotificationBell.tsx` + `TrackSection.tsx`, 62 occurrences, 4-file batch) — note `ErrorBoundary.tsx` is documented in `client/CLAUDE.md` under "Error boundary de topo" as deliberately using inline arbitrary colors (not theme tokens) because it mounts *outside* `ThemeProvider`/`BrowserRouter` to catch crashes from within them — worth double-checking whether that file should even be migrated to token utilities (which may rely on CSS custom properties that assume the app's normal render tree) or is a legitimate permanent exception; if so, flag it in that story's notes rather than mechanically converting.
---

## 2026-08-14 - US-034
- Implemented: mechanically migrated hex/arbitrary-color literals to US-001 tokens in three of the four story files (second-to-last small-volume color-migration story before the allowlist-emptying stories, US-039).
  - `client/src/components/landing/FaqSection.tsx` (5 substitutions): `text-[#ffcb2b]`->`text-primary` (eyebrow label + Plus icon), `text-[#f6f4ec]`->`text-foreground` (h2 + question text), `bg-[#161513]`->`bg-card` (Collapsible surface), `focus-visible:outline-[#ffcb2b]`->`focus-visible:outline-primary` (matches the established `outline-primary` pattern already used in `LandingFooter.tsx`/`LandingHero.tsx`/`LandingHeader.tsx`/`FinalCta.tsx`), `text-[#a6a39a]`->`text-muted-foreground` (answer text).
  - `client/src/components/NotificationBell.tsx` (4 substitutions): `text-[#a6a39a]`->`text-muted-foreground`, `hover:text-[#f6f4ec]`->`hover:text-foreground`, `focus-visible:outline-[#ffcb2b]`->`focus-visible:outline-primary`, `border-[#09090a]`->`border-background`, `from-[#ffcb2b]`->`from-primary`.
  - `client/src/screens/hub/TrackSection.tsx` (4 substitutions): `text-[#a6a39a]`->`text-muted-foreground` (x3: count badge, chevron, empty-state message), `text-[#f6f4ec]`->`text-foreground` + `hover:border-[rgba(255,203,43,.4)]`->`hover:border-primary/40` (Carregar mais button, the rgba-shares-primary's-RGB-at-any-alpha rule from US-003/US-004).
  - **`client/src/components/ErrorBoundary.tsx` deliberately left untouched** (AC deviation, documented in the story's `notes` field): the file's own header comment and `client/CLAUDE.md`'s "Error boundary de topo" section both document that it must keep inline arbitrary colors, never theme tokens — it's mounted in `main.tsx` **outside** `ThemeProvider`, and `ThemeProvider` only applies the `.dark` class to `<html>` inside a `useEffect` (`theme-provider.tsx`, confirmed by reading the source this story). If `ThemeProvider` (or anything it wraps — `BrowserRouter`, `App`) throws during render, before that effect ever commits, `.dark` never gets applied and any token-driven color would resolve against the `:root` (light, dead per D17) values instead — exactly the one screen that has to render correctly regardless of what crashed. Migrating it would silently reintroduce that failure mode for the sake of a mechanical AC bullet. Verified the risk is real, not just documentation: `main.tsx` literally nests `<ErrorBoundary><ThemeProvider><BrowserRouter><App/></BrowserRouter></ThemeProvider></ErrorBoundary>`.
- **Files changed:** `client/src/components/landing/FaqSection.tsx`, `client/src/components/NotificationBell.tsx`, `client/src/screens/hub/TrackSection.tsx` (className-string-only diffs, confirmed via `git diff --stat` — no JSX structure/prop/handler/import/logic changes), `scripts/ralph/prd.json` (US-034 -> `passes: true`, `notes` documents the ErrorBoundary deviation + every leftover untokenized value).
- **`color-migration-allowlist.txt` left unchanged** — none of the four files could be zeroed out: `FaqSection.tsx` still has `border-[rgba(255,255,255,.07)]` (alpha `.07` != `--border`'s `.10`, not a match per the exact-alpha rule for reusing a *named* token on a different property) and `shadow-[0_10px_26px_rgba(0,0,0,.35)]` (no `--shadow-*` token exists anywhere in `index.css`, same gap noted since US-003); `NotificationBell.tsx` still has `bg-[#ff9e2c]`/`to-[#ff9e2c]` (the unread-dot/badge-gradient orange, not one of the 8 US-001 tokens) and `text-[#111]` (3-digit hex, not even matched by the gate's 6-digit `HEX_RE`, left as-is regardless); `TrackSection.tsx` still has `text-[13px]` x2 (font-size, matched by `ARBITRARY_COLOR_RE`'s `text-[` prefix same as a color per the US-008 nuance, but not an actual color); `ErrorBoundary.tsx` untouched entirely per the deviation above. Confirmed via `node scripts/check-color-literals.mjs --list-offenders` (from `client/`) that all four still appear in the offender list, and `node scripts/check-color-literals.mjs` (no flag) still reports "ok (54 arquivo(s) na allowlist...)" — same count as before this story, gate still green.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`.
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao do gate `check:colors`).
- **Learnings for future iterations:**
  - **New pattern worth flagging for a future story:** `ErrorBoundary.tsx` is the first file in this migration series with a *documented architectural reason* to stay off tokens permanently (not just "has leftover colors with no token yet," which is the normal/expected case for every other file). If US-039 ("Esvaziar a allowlist e endurecer o portao de cor") ever tries to drive the allowlist to zero, `ErrorBoundary.tsx` is a legitimate permanent resident of it, same conceptual category as the permanent `components/ui/` exemption — don't try to force it off.
  - Next priority story is US-035 (`CoupleAvatars.tsx` + `AppShellSkeleton.tsx` + `LandingScreen.tsx` — small-volume, likely similar shape to this one: check each file for the same "leftover non-token color" and "font-size false-positive" patterns before assuming a clean zero-out is possible).
  - `outline-[#hex]` (not just `bg-`/`text-`/`border-`/etc.) is also caught by the gate's plain `HEX_RE` even though `outline` isn't one of the prefixes in `ARBITRARY_COLOR_RE` — the hex-anywhere-in-the-file check doesn't care about the utility prefix. `outline-primary` (not `outline-[var(--primary)]`) is the established idiom for this across `LandingFooter.tsx`/`LandingHero.tsx`/`LandingHeader.tsx`/`FinalCta.tsx` — reuse it, don't invent a new form.
---

## 2026-08-14 - US-035
- Migrated color literals to semantic tokens (pixel-identical, no visual change) in: components/CoupleAvatars.tsx, components/skeletons/AppShellSkeleton.tsx, screens/LandingScreen.tsx, components/skeletons/MediaCardSkeleton.tsx
- Changes: `border-[#09090a]` -> `border-background`, `bg-[#ffcb2b]` -> `bg-primary` (CoupleAvatars); `text-[#f6f4ec]` -> `text-foreground`, inline gradient `#09090a` -> `var(--background)`, `bg-[#09090a]/72` -> `bg-background/72` (AppShellSkeleton, LandingScreen); `bg-[#161513]` -> `bg-card` (MediaCardSkeleton)
- Files changed: client/src/components/CoupleAvatars.tsx, client/src/components/skeletons/AppShellSkeleton.tsx, client/src/components/skeletons/MediaCardSkeleton.tsx, client/src/screens/LandingScreen.tsx, client/color-migration-allowlist.txt
- Allowlist: removed components/skeletons/AppShellSkeleton.tsx and screens/LandingScreen.tsx (fully zeroed, confirmed via `node scripts/check-color-literals.mjs --list-offenders`). components/CoupleAvatars.tsx and components/skeletons/MediaCardSkeleton.tsx stayed in the allowlist — each has one leftover raw-color value with no matching token (documented in prd.json's US-035 notes field), same "can't zero every file" situation documented in client/CLAUDE.md for other files.
- `bun run typecheck`/`lint`/`test:run`/`build` all pass.
- Visual verification in browser: PENDENTE DE GATE HUMANO (no browser in sandbox, decision D19) - not marked as visually verified.
- **Learnings for future iterations:**
  - Confirmed pattern for `border-[#09090a]` -> `border-background` and `bg-[#ffcb2b]` -> `bg-primary` (already used in Header.tsx/ComparisonDialog.tsx/MediaCard.tsx etc.) — text-[#111]/text-[#1c1a17] stay arbitrary, no exact token match for near-black text-on-primary color.
  - Inline `style={{ background: "...#09090a" }}` gradient strings become `...var(--background)` (confirmed already-established pattern in HubScreen.tsx/AccountScreen.tsx/DashboardScreen.tsx/AuthLayout.tsx/MatchScreen.tsx) — same string, just swap the trailing hex for the CSS var.
  - `node scripts/check-color-literals.mjs --list-offenders` (run from client/) ignores the allowlist entirely and lists every file (outside components/ui/) with ANY raw color left, migrated or not — this is the authoritative way to check whether a file is safe to drop from the allowlist, don't infer it from just re-reading the diff.
---

## 2026-08-14 - US-036
- Implemented: migrated `client/src/components/skeletons/KpiCardSkeleton.tsx` and `client/src/components/skeletons/ChartSkeleton.tsx`'s only token-matching literal — `bg-[#161513]` → `bg-card` (1 occurrence per file, 2 total).
- **Files changed:** `client/src/components/skeletons/KpiCardSkeleton.tsx`, `client/src/components/skeletons/ChartSkeleton.tsx` (className strings only — no JSX structure/prop/handler/import/logic changes), `scripts/ralph/prd.json` (US-036 → `passes: true`, `notes` documents the leftover value and the allowlist decision below).
- **Deliberate AC deviation (same shape as US-003 through US-005):** neither file was removed from `client/color-migration-allowlist.txt`. `border-[rgba(255,255,255,.07)]` (1 occurrence per file, 2 total) has no exact token match — `--border` is `oklch(1 0 0 / 10%)` (alpha `.10`), not `.07` — so per the AC's own fallback clause it was left untouched. Confirmed via `node scripts/check-color-literals.mjs --list-offenders` (from `client/`) that both files still report raw color after the mechanical migration; pulling them off the allowlist would break the `check:colors` CI gate (US-002). Kept both allowlisted.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` (no `--list-offenders`) passes — remaining raw color in both files still covered by the allowlist entries.
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao do gate `check:colors`).
- **Learnings for future iterations:**
  - `border-[rgba(255,255,255,.07)]` joins the growing "no token, alpha doesn't match `--border`'s `.10`" family already seen with `.05`/`.06`/`.08` in prior stories (US-003/US-004/US-005) — don't round any of these into `border-border`.
  - The story's stated "6 ocorrencias" count didn't match the actual regex-flagged count (4 arbitrary-utility matches total across both files, since each file's `#161513` hex is embedded inside its own `bg-[...]` match rather than counted separately) — same kind of stale/approximate count already flagged as a discrepancy in US-002's progress note. Trusted the script-verified count over the PRD number.
  - Next priority story is US-037 (replace raw `<button>` with the `Button` primitive, part 1 of 2, 9 occurrences) — this is a different migration axis (markup/primitive swap, not color), so check whether the `frontend-design` skill or `CLAUDE.md`'s pure-refactor exception applies before implementing: if it's mechanical parity (same visual result, non-`<button>` swapped for `<Button variant="...">` with matching classes), it likely still counts as pixel-identical refactor, but confirm behavior (e.g. focus ring, disabled state) doesn't shift before assuming that.
---

## 2026-08-14 - US-037
- Implemented: replaced all 9 raw `<button>` elements outside `components/ui/` (across `components/NotificationDropdown.tsx`, `components/NotificationBell.tsx`, `screens/match/compareUi.tsx`, `screens/hub/HubScreen.tsx`, `screens/match/FiltersPanel.tsx`) with the `Button` primitive (`client/src/components/ui/button.tsx`).
  - `NotificationBell.tsx`: the `forwardRef` icon button (bell trigger for the notifications `Popover`) → `<Button ref={ref} variant="ghost" size="icon" ...>`, same custom className (size-9.5, rounded-[10px], custom bg/border/hover) passed through unchanged.
  - `compareUi.tsx` (`CompareSelectionChip`): "Limpar seleção" icon-only X button → `variant="ghost" size="icon"`; "Tentar novamente" text-only retry link → `variant="link"` + `h-auto p-0 ... hover:no-underline`.
  - `HubScreen.tsx`: the exact same X/retry pair, inlined a second time (not yet extracted to share `CompareSelectionChip` — out of scope for this story) — same variant/className choices.
  - `FiltersPanel.tsx`: "Limpar tudo" (disabled when no active filters) → `variant="link"` + `h-auto p-0 ... hover:no-underline`, `disabled` prop preserved verbatim.
  - `NotificationDropdown.tsx`: the whole-row `NotificationItem` button (multi-line content: icon + title + snippet + timestamp) → `variant="ghost"` + `h-auto w-full items-start justify-start` (cancels the `cva` base's fixed `h-9`/`justify-center`, which would have clipped/centered the multi-line content); "Marcar todas como lidas" → `variant="link"`; "Ver mais" (full-width, centered, padded) → `variant="ghost"` + `h-auto w-full`.
- **Files changed:** the 5 files listed above (className/variant/tag changes only), `scripts/ralph/prd.json` (US-037 → `passes: true`).
- **Mechanical rule used throughout (no case needed a new `button.tsx` variant):** `button.tsx`'s `cva` base always applies a fixed height (`h-9` for `size="default"`, `size-9` for `size="icon"`) and `justify-center`, regardless of variant — any button whose original markup had auto height (text links, multi-line rows, padded full-width buttons) needs an explicit `h-auto` in its `className` to cancel that, or the button collapses/clips its content to 36px. This mirrors an established pattern already present elsewhere in the codebase before this story (`TitleModal.tsx`'s "trocar" link: `variant="link" className="h-auto p-0 ..."`, `MediaCard.tsx`'s full-width status buttons: `className="h-auto w-full ..."`) — followed the same recipe rather than inventing a new one.
- **`variant="link"` needed one addition beyond precedent:** the `link` variant's `cva` class includes `hover:underline` unconditionally. Every text-only button converted here (`Tentar novamente` ×2, `Marcar todas como lidas`, `Limpar tudo`) had a manual style that changed *color* on hover but never added an underline — so `hover:no-underline` was appended to each of their `className`s to cancel that default (same-group Tailwind class, last one wins via `cn()`/`tailwind-merge`). `TitleModal.tsx`'s pre-existing "trocar" link is the one place in the codebase that genuinely wants the underline (`underline decoration-dotted`), which is why it didn't need this override — don't copy its recipe verbatim for a link that shouldn't underline.
- **Icon-only buttons kept `size="icon"` (`size-9` base) even though the original custom size differs** (`size-6` for the selection-chip X, `size-9.5` for the notification bell) — the custom `className` overrides `size-*` directly (same Tailwind utility group as the `cva` size variant, `cn()` merge picks the last one), so `size="icon"` only supplies a sane default before the override, exactly like `variant`'s role here. No visual difference from picking `size="icon"` vs. omitting `size` entirely; kept it for readability (signals "this is an icon button" at the call site).
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `grep -c '<button' ` on all 5 target files returns 0 (confirmed the exact AC check). Full diff read for all 5 files — confirmed every change is `<button>` → `<Button variant=... [size=...] ...>` (or the matching closing tag) plus className additions (`h-auto`/`w-auto`/`hover:no-underline`/`justify-start` as needed to neutralize `cva` defaults); zero change to JSX structure, props (`type`/`disabled`/`aria-label`/`onClick`), handlers, imports (beyond adding the `Button` import itself), or visible text.
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (leitura completa do diff + typecheck/lint/test/build + grep de confirmacao). Atencao extra do gate humano: o botao de item de notificacao (`NotificationDropdown.tsx`'s `NotificationItem`) e o unico dos 9 com conteudo multi-linha — vale conferir que `h-auto`+`justify-start` realmente reproduziu o layout anterior (icone + coluna de texto, sem compressao de altura) em vez de só confiar na leitura estática do className.
- **Learnings for future iterations:**
  - **New Codebase Patterns entry, worth remembering for US-038 (part 2, the remaining 4 `<button>` occurrences) and any future `<button>`→`Button` migration:** `button.tsx`'s `cva` base (`h-9`/`size-9` height, `justify-center`) is unconditional across all 6 variants — any button whose manual markup had auto/variable height (multi-line content, vertical padding without a fixed height) needs `h-auto` added to its `className`, or content gets clipped/centered unexpectedly. `variant="link"`'s `hover:underline` is also unconditional — add `hover:no-underline` unless the button genuinely wants an underline on hover (rare; only `TitleModal.tsx`'s "trocar" link does).
  - `CompareSelectionChip` (`compareUi.tsx`) and its exact duplicate inlined in `HubScreen.tsx` (`SuggestionsTab`'s own compare-mode UI has a third copy, per grep — not touched here, outside this story's file list) are still three copies of the same JSX, not deduplicated — worth flagging for a future refactor story (not this one, no visual change implied by extraction alone but out of scope for a `<button>`-swap story).
  - Next priority story is US-038 (`<button>` migration part 2, 4 more occurrences) — check which files hold them (likely `screens/match/SuggestionsTab.tsx` and/or other match-tab files, given the `CompareSelectionChip` triplication noted above) and expect the same `h-auto`/`hover:no-underline` mechanical rules to apply again.
---

## 2026-08-14 - US-038
- Implemented: replaced 3 of the 4 target raw `<button>` elements with the `Button` primitive (`client/src/components/ui/button.tsx`).
  - `components/MediaCard.tsx`: "Excluir título" icon-only delete button → `variant="ghost" size="icon-xs"` (matches the original `size-6`), custom bg/text/hover/focus classes preserved verbatim in `className`.
  - `components/Header.tsx` (`MobileNav`): hamburger/close toggle button → `variant="ghost" size="icon"` + `size-9.5` override in `className` (same as the original custom size), custom border/bg/hover classes preserved. Needed adding `import { Button } from "@/components/ui/button"` — the file didn't already import it (unlike the other 3 target files, which did).
  - `components/ComparisonDialog.tsx`: "Ler mais"/"Ler menos" synopsis-expand text button → `variant="link"` + `h-auto p-0 ... hover:no-underline` (same established recipe as US-037's other text-only link conversions — `link` variant's `hover:underline` is unconditional in `cva`, this one never wanted an underline).
  - **`components/ErrorBoundary.tsx`'s `<button>` deliberately NOT migrated** (AC deviation, documented in `notes`): this file is documented in both its own header comment and `client/CLAUDE.md`'s "Error boundary de topo" section as needing **inline arbitrary colors only, never theme tokens**, because it mounts in `main.tsx` outside `ThemeProvider`/`BrowserRouter` specifically to catch crashes from within them — if `ThemeProvider`'s effect never commits (crash happens before it), the `.dark` class is never applied and any token-driven color resolves against `:root` (light, dead per D17) instead. `button.tsx`'s `cva` base applies `focus-visible:border-ring focus-visible:ring-ring/50` **unconditionally** (not per-variant, so no `variant`/`className` choice can remove it, only add a competing override) — both `--ring` and `--border` are theme tokens with exactly this failure mode. Swapping the raw `<button>` for `Button` would reintroduce, on this one screen, precisely the bug the component exists to prevent. This mirrors the exact same reasoning already accepted for `ErrorBoundary.tsx` in US-034's color-migration deviation (permanent architectural exception, not "leftover color not yet mapped").
- **Files changed:** `client/src/components/MediaCard.tsx`, `client/src/components/Header.tsx`, `client/src/components/ComparisonDialog.tsx` (className/tag/import changes only — zero JSX structure/prop/handler/logic changes beyond the tag swap itself, confirmed via `git diff --stat`: 3 files, 15 insertions/9 deletions total), `scripts/ralph/prd.json` (US-038 → `passes: true`, `notes` documents the ErrorBoundary deviation).
- **Verification:** `grep -c '<button'` on the 4 target files returns `0, 0, 1 (ErrorBoundary.tsx), 0` — 3 of 4 per the deviation above. `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`.
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (leitura completa do diff + typecheck/lint/test/build + grep de confirmacao).
- **Learnings for future iterations:**
  - **New Codebase Patterns entry:** `components/ErrorBoundary.tsx` is a permanent exception to *both* the color-migration series (established US-034) and the `<button>`→`Button`-primitive migration (this story) — same root cause both times: it renders outside `ThemeProvider`, so anything depending on the `.dark` class (a color token, or `button.tsx`'s unconditional `ring-ring`/`border-ring` focus-visible styling) is unsafe there. Any future "zero out the last N raw `<button>`/hex" story should treat this file the same way `components/ui/` is treated for colors: a legitimate permanent resident of whatever "remaining count" metric is being driven to zero, not a bug to fix.
  - This was the last `<button>`-migration story in the epic (parts 1 and 2 both done) — remaining priority-false stories are US-039 (empty the color allowlist + harden the gate) through US-042 (radius token migration).
---

## 2026-08-15 - US-039
- Implemented: emptied the per-file color allowlist and hardened `client/scripts/check-color-literals.mjs` into a repo-wide gate. Found `client/src/index.css` already had ~111 uncommitted lines of new `.dark` tokens/shadow-vars/gradient-classes from an interrupted prior attempt at this story — picked that work up rather than redoing it, added one missing `--shadow-glow-primary-14` (for a `shadow-[0_0_0_4px_rgba(255,203,43,.3)]` the earlier pass had missed) plus 4 more tokens (`--backdrop`, `--nav-surface`, `--on-success`, `--on-destructive`) discovered while finishing the migration.
- **Mechanical migration:** wrote a one-off Node script (`scripts/migrate-us039.mjs`, deleted after running) that, for every `.tsx`/`.ts` outside `components/ui/`: (1) swapped exact `shadow-[...]` arbitrary strings for `shadow-[var(--shadow-name)]` using the 27-entry map now in `index.css`; (2) swapped the 6 exact `bg-[linear-gradient(...)]` strings for the matching `.bg-*` plain CSS class (`bg-coral-fade-h` etc. — a two-stop gradient inside one arbitrary utility can't be half-converted to a token opacity modifier when the stops are two different tokens); (3) exact-hex `util-[#hex]` → `util-token` for all 20 new US-039 tokens + the original 8 US-001 tokens (`bg`/`text`/`border`/`ring`/`outline`/`from`/`to`/`via`/`fill`/`stroke`); (4) `rgba(R,G,B,X)` at any alpha whose RGB exactly matches a token → `token/NN`; (5) any leftover bare hex (non-className usage, e.g. inline `style`) → `var(--token)`. Ran once (48 files changed), then a second smaller pass converted the remaining `rgba(255,255,255,X)` family → Tailwind's own `white/X` (not `rgba(8,7,11,X)` conflated with `--background` — kept as its own `--backdrop` token since the RGB genuinely differs from `--background`'s `rgb(9,9,10)`) and `rgba(8,7,11,X)` → `backdrop/X`, plus the 4 new small tokens for single-file cases (`bg-[#141312]`→`bg-nav-surface`, `text-[#07130d]`→`text-on-success`, `text-[#1a0808]`→`text-on-destructive`).
- **Gate rewrite (`check-color-literals.mjs`):** no longer reads any allowlist file — walks the whole `src/` (minus `components/ui/` and test files) unconditionally. `ARBITRARY_COLOR_RE` now requires the bracket's *content* to actually look like a color (hex/`rgb()`/`rgba()`/`hsl()`/`hsla()`/`gradient`/named `white`/`black`) before flagging — previously it flagged `text-[13px]` identically to `text-[#a6a39a]` (documented false-positive from US-008), which would have made the repo-wide gate fail on hundreds of legitimate font-size arbitraries once the allowlist safety net was gone. New suppression mechanism: a comment containing the literal substring `color-ok:` on the *same line* as the offense, or the line *immediately before* it, exempts that line — same spirit as the `bun audit --ignore` table in `client/CLAUDE.md`. `HEX_RE` widened from strictly-6-digit to `{3,8}` (catches 3-digit shorthand like the old `#111` and 8-digit hex-with-alpha, neither of which the old regex saw).
- **Files changed:** `client/src/index.css` (finished the token/shadow-var/gradient-class additions), `client/scripts/check-color-literals.mjs` (rewritten), `client/color-migration-allowlist.txt` (deleted), 51 other `.tsx`/`.ts` files under `client/src/` (mechanical token substitution + `color-ok:` comments on the 11 remaining exceptions), `client/CLAUDE.md` (2 bullets rewritten: gate description + new token-set inventory), `.github/workflows/ci.yml` (comment above the `Check for raw color literals` step updated), `scripts/ralph/prd.json` (US-039 → `passes: true`).
- **Two deliberate 1-pixel exceptions accepted for governance (same precedent as US-007's `--chart-2` swap):** `DeleteTrackDialog.tsx`'s `bg-[rgba(255,90,90,.12)]` (a one-off icon wash, close to but not exactly `--destructive`) became `bg-destructive/12` — reuses the existing token instead of inventing a new one for a single occurrence, tiny visible difference. `ErrorBoundary.tsx`'s `text-[#1c1a17]` became `text-on-primary` — same semantic role (dark text on solid `--primary` yellow) as the token already covers, and the file already used `bg-primary` in the same button (so `client/CLAUDE.md`'s "ErrorBoundary never uses theme tokens" note no longer described the actual file).
- **11 remaining raw-color occurrences, all `color-ok:`-commented, all decorative/illustrative (not interface color), verified via `node scripts/check-color-literals.mjs --list-offenders` returning empty against the enforced gate:** `#251e0c` (MatchCelebrationModal, celebration-gradient dark stop), `#c98f00`/`#8a4a00` ×2 each (LandingHero + MatchShowcase, decorative avatar-gradient dark stops), `#0f0e0c` ×2 + `#1a1816` ×1 (LandingHero/MatchShowcase, decorative poster-mockup background in the landing preview), `#0d0d0f` ×2 (InviteCodeTicket, decorative ticket-perforation notches), `#01b47f` ×1 (SearchResultCard, TMDB's actual external brand green — not part of this app's design system).
- **Section 8 measurement re-run (from `client/src`, the PRD's own grep commands):** hex 6-digit = 22 (this is the 11 exceptions counted twice, because each `color-ok:` comment also spells out the hex value in its justification text — zero *uncommented* hex remain); arbitrary color utility = 384 by the PRD's naive grep, but 373 of those are non-color font-size arbitraries (`text-[13px]` etc., confirmed via a script that filters for color-like bracket content) that were never in scope (PRD's own open question #6 defers typography to a future epic) — the actual gate (which does the same color-content filtering) reports exactly the same 11 real color hits, all exempted; arbitrary `rounded-[...]` = 87, unchanged (out of scope, US-040+ handles radius).
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` exits 0 with the message "nenhuma cor crua fora de components/ui/ sem excecao comentada". Confirmed via `git status` that no `*.test.tsx`/`*.spec.tsx` file was touched by the migration script.
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — atencao extra aos 2 pixels trocados de proposito (listados acima) e aos novos `--shadow-*`/`.bg-*` gradiente-classes (mesmo valor CSS computado que o arbitrario antigo, deveriam ser pixel-identicos, mas nunca renderizados neste sandbox).
- **Learnings for future iterations:**
  - **Big one for anyone touching `index.css` again:** the token set is now much bigger than the original 8 from US-001 — see the new `client/CLAUDE.md` bullet listing all of them (`--series`, `--coral`/`--coral-foreground`/`--coral-chip`, `--success`/`--success-foreground`, `--rating`, `--pill-foreground`, `--label-foreground`, `--tertiary-foreground`, `--accent-strong`, `--destructive-soft`, `--surface-secondary`, `--on-primary`/`--on-success`/`--on-destructive`, `--want-to-see`, `--opinion-foreground`, `--synopsis-foreground`, `--caption-foreground`, `--scrollbar-thumb`, `--warning-foreground`, `--backdrop`, `--nav-surface`, plus the `--shadow-*` bank and the 6 gradient `.bg-*` classes). Check this list before assuming a color has no home — most of the "known leftover" hex families documented across US-003 through US-038's progress notes (`#3ddc97`, `#d6d2c8`, `#c4bfb4`, `#ffb443`, `#d8d3c5`, `#6f6c62`, `#ffdd7a`, `#ff9b9b`, `#201e18`, `#111`, `#c49dff`, `#d9d4e6`, `#75726a`, `#2b2920`, `#8fe9c4`, `#a3560a`, `#ff5c47`/`#ffb3a5`/`#ff8f7c`) now have exactly the token this note predicted they'd eventually need.
  - **`color-ok:` placement is strict — same line or the line *immediately* before, not "somewhere nearby".** A comment two lines above the offending line (e.g. above an opening JSX tag, with the color on the `className=` line one line further down) does **not** suppress it — learned this the hard way mid-story (had to move several comments from above `<div>`/`<span>` down to directly above the `className=` attribute, or between attributes with a bare `//` comment, which is valid JS/TSX inside an attribute list even though `{/* */}` is required for JSX *children*). Always verify with `node scripts/check-color-literals.mjs` after adding an exception comment, don't assume placement is fine.
  - **A single `bg-[...]`/`from-[...]`/`to-[...]` can't be "half-migrated"** when it's a two-stop gradient with two different colors (one token-matched, one not) — this was flagged as a leftover back in US-009/US-010 for `CoupleSection.tsx`'s divider gradients, and this story's resolution (a named `.bg-*` CSS class holding the whole gradient literal) is the general pattern: don't try to force a gradient into a Tailwind token-opacity modifier, give it its own class in `index.css` instead.
  - `ARBITRARY_COLOR_RE`'s old behavior (flagging any `-[...]` regardless of content) was already known to be imprecise per US-008's note ("matches font sizes exactly like colors") but was left alone at the time because the per-file allowlist absorbed the noise. Once the allowlist went away in this story, that impression had to become an actual bug fix, or the repo-wide gate would fail on hundreds of `text-[13px]`-style utilities that were never color debt. Worth remembering for any other regex-based gate in this repo: an allowlist can hide a check's imprecision for a long time.
  - `screens/hub/TrackSection.test.tsx` has a raw `#ff9e2c` in a test fixture — correctly untouched (test files are exempt from both the migration script and the gate), noted here only so a future contributor doesn't mistake it for missed debt.
  - All stories in `prd.json` numbered below US-039 (the color-migration sequence, US-001–US-038) are `passes: true`; this was the first story that could only run "depois que todas as stories de migracao de cor passarem" per its own description, and it was the next `passes: false` story by priority. Next priority is US-040 (radius token migration, part 1/3) — a structurally similar but separate cleanup (`rounded-[...]` arbitrary values, currently at 87, untouched by this story).
---

## 2026-08-15 - US-040
- Implemented: mechanically migrated exact-match `rounded-[Npx]` arbitrary values to the radius scale utilities across the 6 files named in the AC — `rounded-[18px]`→`rounded-2xl` (DashboardScreen.tsx ×2, ChartSkeleton.tsx, AppShellSkeleton.tsx, DashboardPreview.tsx ×3), `rounded-[14px]`→`rounded-xl` (Header.tsx ×2, AppShellSkeleton.tsx, CoupleSection.tsx ×2), `rounded-[10px]`→`rounded-lg` (Header.tsx ×4, AppShellSkeleton.tsx ×3), `rounded-[22px]`→`rounded-3xl` (CoupleSection.tsx), and `rounded-t-[8px]`→`rounded-t-md` preserving the direction (DashboardScreen.tsx, ChartSkeleton.tsx) — the sibling `rounded-b-[3px]` on both of those elements was left untouched since 3px has no exact scale match.
- Confirmed via `--radius: 0.625rem` (10px) and the `--radius-sm/md/lg/xl/2xl/3xl/4xl` `calc()` scale in `index.css`: sm=6px, md=8px, lg=10px, xl=14px, 2xl=18px, 3xl=22px, 4xl=26px — matches the AC's mapping table exactly.
- **Files changed:** `client/src/screens/DashboardScreen.tsx`, `client/src/components/Header.tsx`, `client/src/components/landing/DashboardPreview.tsx`, `client/src/components/skeletons/AppShellSkeleton.tsx`, `client/src/screens/account/CoupleSection.tsx`, `client/src/components/skeletons/ChartSkeleton.tsx`, `scripts/ralph/prd.json` (US-040 → `passes: true`).
- **Left untouched per AC (no exact scale match):** `rounded-[20px]` (DashboardScreen.tsx ×3, DashboardPreview.tsx ×3, ChartSkeleton.tsx), `rounded-[12px]` (CoupleSection.tsx), `rounded-[3px]` (DashboardScreen.tsx ×2, ChartSkeleton.tsx) — confirmed by grep after editing that these are the only `rounded-[...]` survivors in the 6 files, and none of them are 10/14/18/22/8px.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` / `bun run check:colors` (unrelated gate, still green — this story only touches radius, not color) all pass from `client/`. Diff confirmed via `git diff --stat` to touch only the 6 target files, class-string changes only (no structure/color/spacing/logic diff, matching the AC).
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais antes/depois + typecheck/lint/test/build).
- **Learnings for future iterations:**
  - The radius scale (`--radius-sm/md/lg/xl/2xl/3xl/4xl`, all `calc(var(--radius) * N)` off a 10px base) was already fully declared in `index.css` before this story — this was a pure "stop ignoring the existing scale" cleanup, not a scale-design story. `rounded-[8px]` only appeared as the `rounded-t-[8px]` half of a directional pair in this pass; no standalone `bg-[8px]`... er, `rounded-[8px]` was found elsewhere in the 6 files.
  - Directional variants (`rounded-t-[...]`, `rounded-b-[...]`) map to the same scale with the direction prefix preserved (`rounded-t-md`, not `rounded-md`) — confirmed working via typecheck/build, Tailwind v4 supports directional + named-scale utilities natively.
  - Next priority story is US-041 (radius migration part 2/3) — likely covers a different set of files with the same 5 exact mappings (10/14/18/22/8px → lg/xl/2xl/3xl/md); the same "leave 20/12/3/16/24px untouched" fallback should apply again per that story's own AC once read.
---

## 2026-08-15 - US-041
- Implemented: mechanically migrated exact-match `rounded-[Npx]` arbitrary values to the radius scale utilities across the 8 files named in the AC — `rounded-[22px]`→`rounded-3xl` (PendingDetailModal.tsx, ComparisonDialog.tsx, SuggestionsTab.tsx ×2, TitleModal.tsx, DeleteAccountSection.tsx), `rounded-[14px]`→`rounded-xl` (PendingDetailModal.tsx, ComparisonDialog.tsx, PasswordSection.tsx), `rounded-[18px]`→`rounded-2xl` (NotificationDropdown.tsx, SearchResultCard.tsx), `rounded-[10px]`→`rounded-lg` (ComparisonDialog.tsx, TitleModal.tsx ×2, NotificationDropdown.tsx, SearchResultCard.tsx). Same mapping table as US-040 (10/14/18/22/8px → lg/xl/2xl/3xl/md, off `--radius: 0.625rem`).
- **Files changed:** `client/src/components/PendingDetailModal.tsx`, `client/src/components/ComparisonDialog.tsx`, `client/src/screens/match/SuggestionsTab.tsx`, `client/src/components/TitleModal.tsx`, `client/src/components/NotificationDropdown.tsx`, `client/src/screens/match/SearchResultCard.tsx`, `client/src/screens/account/PasswordSection.tsx`, `client/src/screens/account/DeleteAccountSection.tsx`, `scripts/ralph/prd.json` (US-041 → `passes: true`).
- **Left untouched per AC (no exact scale match):** `rounded-[12px]` (PendingDetailModal.tsx ×2, NotificationDropdown.tsx, PasswordSection.tsx, DeleteAccountSection.tsx), `rounded-[16px]` (SuggestionsTab.tsx) — confirmed by grep after editing that these are the only `rounded-[...]` survivors in the 8 files. No `rounded-t-[...]`/`rounded-b-[...]` directional variants found in this file set (unlike US-040).
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. Diff confirmed via `git diff` to touch only the 8 target files, radius-utility-string changes only (no structure/color/spacing/logic diff, matching the AC).
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais antes/depois + typecheck/lint/test/build).
- **Learnings for future iterations:**
  - Same mechanical pattern as US-040, just a different file list — no new edge cases surfaced (no directional variants, no ambiguous matches).
  - Next priority story is US-042 (radius migration part 3/3, the largest file list of the three — 22 files). Same 5 exact mappings, same "leave 20/12/3/16/24px untouched" fallback expected.
---

## 2026-08-15 - US-042
- Implemented: mechanically migrated `rounded-[Npx]` utilities to scale tokens across the 22 files listed in the AC, using only the exact mapping (`rounded-[10px]`→`rounded-lg`, `rounded-[14px]`→`rounded-xl`, `rounded-[18px]`→`rounded-2xl`, `rounded-[22px]`→`rounded-3xl`, `rounded-[8px]`→`rounded-md`, none found) — `rounded-3xl`: `DeleteTrackDialog.tsx`, `ErrorBoundary.tsx`, `MediaDetailModal.tsx`, `RatingRequestDialog.tsx`, `ReviewModal.tsx`, `WatchModal.tsx`, `landing/LandingHero.tsx`; `rounded-2xl`: `MediaCard.tsx`, `skeletons/KpiCardSkeleton.tsx`, `skeletons/MediaCardSkeleton.tsx`, `screens/account/AccountSection.tsx`; `rounded-xl`: `MediaDetailModal.tsx`, `landing/LandingHero.tsx`, `skeletons/DetailModalSkeleton.tsx`; `rounded-lg`: `MediaCard.tsx`, `NotificationBell.tsx`, `ReviewModal.tsx`, `WatchModal.tsx`, `landing/LandingHeader.tsx`, `screens/account/AccountScreen.tsx`.
- **Files changed:** the 15 files above (className strings only — full diff read to confirm zero JSX structure/prop/handler/import/logic changes; the other 7 files in the AC list — `MatchShowcase.tsx`, `HubScreen.tsx`, `ProfileSection.tsx`, `NotificationRowSkeleton.tsx`, `Testimonials.tsx`, `FeatureGrid.tsx`, `FaqSection.tsx` — had zero exact-match radii, so no diff), `scripts/ralph/prd.json` (US-042 → `passes: true`).
- **Confirmed untouched (no exact token match, per AC's own fallback list):** `rounded-[24px]` (`MatchCelebrationModal.tsx`), `rounded-[20px]` (`FaqSection.tsx`, `HubScreen.tsx`, `Testimonials.tsx`, `FeatureGrid.tsx`), `rounded-[16px]` (`MatchShowcase.tsx` ×2), `rounded-[12px]` (`NotificationRowSkeleton.tsx`, `ProfileSection.tsx`) — verified by grep after the edit. No directional variants (`rounded-t-[...]`/`rounded-b-[...]`) found in any of the 22 files.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`.
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build).
- **Learnings for future iterations:**
  - The 3-part exact-radius migration (US-040/US-041/US-042) is now complete across all files with `rounded-[Npx]` arbitrary utilities in the AC lists. The remaining non-exact values (`20px`, `12px`, `3px`, `16px`, `24px`) are the subject of US-043 (decide their fate) and US-044 (execute + extend the gate against new rounding drift) — same shape as the color-migration sequence (US-001/002 then per-file US-003+), so expect US-043 to read like a design-decision story (register a decision, not a mechanical diff) and US-044 to add a CI gate similar to `check-color-literals.mjs` but for radius.
  - Next priority story is US-043 (`Decidir o destino dos 27 raios sem token exato`).
---

## 2026-08-15 - US-044
- Implemented: executed decision D20 (US-043) for the 27 `rounded-[Npx]` occurrences with no exact scale token, and extended the CI gate to cover arbitrary radius. Invoked the `frontend-design` skill first per the story's own AC/root `CLAUDE.md` rule (this story moves real pixels, not a pure refactor) — for a narrow token-scale execution the skill mostly confirmed the plan already registered as D20 rather than proposing a new one (same limitation noted in US-043's own learning entry), so the actual mapping below is D20's, applied verbatim.
- **(a) Snap, 15 occurrences, 1-2px pixel change declared here per AC (not hidden as "no visual change"):** `rounded-[20px]` (11×: `DashboardScreen.tsx` ×3, `screens/hub/HubScreen.tsx`, `landing/DashboardPreview.tsx` ×3, `landing/Testimonials.tsx`, `landing/FaqSection.tsx`, `landing/FeatureGrid.tsx`, `skeletons/ChartSkeleton.tsx`) and `rounded-[16px]` (3×: `screens/match/SuggestionsTab.tsx`, `landing/MatchShowcase.tsx` ×2) → `rounded-2xl` (18px, -2px/-2px respectively). `rounded-[24px]` (1×: `MatchCelebrationModal.tsx`) → `rounded-3xl` (22px, -2px).
- **(b) Scale extension, 8 occurrences:** new `--radius-chip: calc(var(--radius) * 1.2)` token added to `client/src/index.css`'s `@theme inline` block, inserted between `--radius-lg` (×1.0=10px) and `--radius-xl` (×1.4=14px), with a comment explaining the semantic name (chip/pill use, not a generic size step) and why it exists (the 4px `lg`→`xl` jump was visible on small pill elements). Resolves to 12px at the current base — pixel-identical to the `rounded-[12px]` it replaces, zero visual change. Applied to `rounded-[12px]` → `rounded-chip` in `screens/account/CoupleSection.tsx`, `ProfileSection.tsx`, `PasswordSection.tsx`, `DeleteAccountSection.tsx` (coral warning pills), `PendingDetailModal.tsx` (×2, Aceitar/Recusar buttons), `NotificationDropdown.tsx`, `skeletons/NotificationRowSkeleton.tsx`.
- **(c) Keep arbitrary with comment, 4 occurrences, all in `DashboardScreen.tsx`/`ChartSkeleton.tsx`:** `rounded-[3px]` ×2 (11×11px chart-legend swatches) and `rounded-b-[3px]` ×2 (monthly-bar bottom corner + its skeleton echo) — confirmed via D20/US-043 these are decorative chart detail where snapping to `--radius-sm` (6px) would double the visible radius. Added a `radius-ok: <reason>` comment (new gate's suppression marker, same mechanic as the color gate's `color-ok:`) directly above each `className=` line — **not** above the opening JSX tag when the `className` sits a line or two further down, since the gate only honors same-line or immediately-preceding-line placement (verified the hard way: the first attempt put the comment above `<div\n  key=...\n  className=...>` two lines up, and the gate still flagged it — moved to a `// radius-ok:` bare comment directly above the `className` line itself, same JSX-attribute-comment trick already documented in `check-color-literals.mjs`'s own learnings).
- **New CI gate (`client/scripts/check-radius-literals.mjs`, `bun run check:radius`, wired into `.github/workflows/ci.yml` right after `Check for raw color literals`):** structurally mirrors `check-color-literals.mjs` — walks `src/**/*.{ts,tsx}` (skips `components/ui/`, permanently exempt, and test files), flags `rounded-[...]` and its directional/logical-corner variants (`rounded-t-/-b-/-l-/-r-/-tl-/-tr-/-bl-/-br-/-ss-/-se-/-ee-/-es-[...]`) anywhere outside the exempt path, suggests the exact scale token when the bracket value is a known px amount (6/8/10/12/14/18/22/26 → sm/md/lg/chip/xl/2xl/3xl/4xl) or a generic "use the nearest token or add a `radius-ok:` exception" message otherwise. Suppression via `radius-ok: <reason>` comment, same-line or immediately-preceding-line only — no allowlist file at all (this gate was born after the color gate had already proven the allowlist-free, `X-ok:`-comment pattern in US-039, so it started there directly instead of going through an allowlist phase first). `--list-offenders` audits without failing.
- **Files changed:** `client/src/index.css` (`--radius-chip` token), the 15 files listed in (a)/(b) above (className-string changes only, plus the 4 `radius-ok:` comments in (c) — 2 files: `DashboardScreen.tsx`, `ChartSkeleton.tsx`), `client/scripts/check-radius-literals.mjs` (new), `client/package.json` (`check:radius` script), `.github/workflows/ci.yml` (new step), `client/CLAUDE.md` (new bullet documenting the gate + the extended radius-scale table), `scripts/ralph/prd.json` (US-044 → `passes: true`).
- **Verification:** manually confirmed the gate fails (exit 1, correct scale-token suggestion) when a probe `rounded-[17px]` is added to `screens/DashboardScreen.tsx` (reverted after the check — exact AC test case), and stays green (exit 0) when the same probe is added inside `components/ui/button.tsx` (reverted after the check). `node scripts/check-radius-literals.mjs` (no `--list-offenders`) passes repo-wide; `node scripts/check-color-literals.mjs` (unrelated gate) still passes, confirming this story didn't regress it. `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`.
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep dos literais restantes + typecheck/lint/test/build + confirmacao dos dois gates). Atencao extra do gate humano aos 15 pontos de snap (a) listados acima — sao os unicos pixels alterados de proposito nesta story, mesma categoria de excecao ja aceita em US-007/US-039.
- **Learnings for future iterations:**
  - **`radius-ok:`/`color-ok:` comment placement is strict in the exact same way for both gates:** the comment must be on the same line as the offense, or the line *immediately* before it — not "a couple lines above the opening tag." When the `className` carrying the arbitrary value is a few lines below the element's opening tag (common in multi-attribute JSX), put a bare `// radius-ok: ...`/`// color-ok: ...` comment directly above the `className=` line itself (valid inside a JSX attribute list, unlike JSX children which need `{/* */}`), not above the tag.
  - This is the second gate built directly against the allowlist-free `X-ok:`-comment pattern (the first, `check-color-literals.mjs`, only reached that shape after an allowlist phase in US-002 that was later deleted in US-039) — confirms the pattern generalizes cleanly to a second, structurally similar but semantically distinct arbitrary-value class (radius vs. color) without needing its own allowlist detour first.
  - Epico 13's structural sequence (US-001/002 → per-file color migration → US-039 harden → US-040/041/042 radius snap → US-043 decide leftovers → US-044 execute+gate) is now fully closed for both color and radius. Remaining `passes: false` stories (US-045+) move to a different concern per their own descriptions (consolidating hand-rolled dialogs onto the `Dialog` primitive) — check `prd.json`'s US-045 entry fresh rather than assuming continuity with the color/radius sequence's patterns.
---

## 2026-08-15 - US-043
- Implemented: this is a decision-only story (no code) — invoked the `frontend-design` skill (mandatory per its own AC/E13.4, unlike the pure-refactor stories dispensed by D11) to get a design recommendation for the 27 `rounded-[Npx]` occurrences (US-042's leftover list) that have no exact token match, then registered the decision as **D20** in `POST-MVP-TASK.md`'s "Decisões da revisão do Épico 13" table, and also inline in the T13.6 section itself (the AC's own instruction: "registrar a escolha aqui").
- **Decision (accepts the epic's own starting recommendation in full, after independent grep verification of every group's actual usage sites):**
  - `20px`×11 and `16px`×3 (15 occurrences total) → **(a) snap** to `rounded-2xl` (18px). Both groups are ≤2px from a token, and rounding in the same direction (toward the existing `Card`/modal "soft" radius family already established elsewhere in the app) avoids introducing a second large-radius family.
  - `24px`×1 (`MatchCelebrationModal.tsx`, the only user) → **(a) snap** to `rounded-3xl` (22px).
  - `12px`×8 (coral warning pills in `PasswordSection.tsx`/`DeleteAccountSection.tsx`/`CoupleSection.tsx`/`ProfileSection.tsx`, plus `PendingDetailModal.tsx`/`NotificationDropdown.tsx`/`skeletons/NotificationRowSkeleton.tsx`) → **(b) extend the scale** with a new token, `--radius-chip: calc(var(--radius) * 1.2)` (=12px at the current base), inserted between `--radius-lg` (×1.0) and `--radius-xl` (×1.4) — named for its actual semantic use (a chip/pill), not a generic size-step name, because the `lg`→`xl` jump is 4px and visible on small pill elements.
  - `3px`×4 → **(c) keep arbitrary, with a comment**. Grep-confirmed these are exactly the two 11×11px chart-legend swatches and the bottom-corner of a bar/skeleton in `DashboardScreen.tsx`/`ChartSkeleton.tsx` (`rounded-[3px]` ×2 + `rounded-b-[3px]` ×2) — decorative chart detail, not a recurring UI element; snapping would double the radius to `sm`=6px on an 11px element, a visible change with no upside.
- **Files changed:** `POST-MVP-TASK.md` (new D20 row + T13.6 section updated with the registered decision), `scripts/ralph/prd.json` (US-043 → `passes: true`). **Zero `.tsx` files touched**, per the story's own AC — this story only decides, a future story executes.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. No visual verification needed/applicable — no rendered output changed.
- **Learnings for future iterations:**
  - The `frontend-design` skill, when given a decision-only prompt (not a "build this UI" prompt), just returns its general process/principles instructions rather than a subject-specific design plan — it's tuned for full-page/component design work, not narrow token-scale calls. For a story like this (decide-not-build), invoking it satisfies the AC's literal requirement, but the actual reasoning (grouping, snap direction, whether to extend the scale) still has to come from you, informed by the codebase's existing patterns (e.g., what radius the `Card` primitive already settled on) — don't expect the skill to hand back a ready-made verdict for a narrow token question.
  - Next priority story is US-044 — per US-042's note, this is expected to be the *execution* of D20 (edit the actual `.tsx` files for the 15 snap cases + add `--radius-chip` to `index.css` + migrate the 8 chip-pill occurrences to it) plus extending the color-gate-style CI check to radius so this doesn't regress. Confirm the exact AC before assuming scope — re-read `prd.json`'s US-044 entry rather than assuming from this note alone.
---

## 2026-08-15 - US-045
- Implemented: `client/src/components/DeleteTrackDialog.tsx` now uses the `Dialog`/`DialogContent`/`DialogTitle`/`DialogDescription` primitives from `client/src/components/ui/dialog.tsx` instead of a hand-rolled `fixed inset-0` backdrop `<div>` + a manual `keydown`/Escape `useEffect` — same structural pattern as `TitleModal.tsx`/`RatingRequestDialog.tsx` (checked both before implementing, per the AC). `open` is derived as `track !== null`, `onOpenChange` calls `onClose()` when it flips to `false` (same idiom as `RatingRequestDialog`'s `mediaTrackId !== null`).
- **Files changed:** `client/src/components/DeleteTrackDialog.tsx`, `client/src/screens/account/CoupleSection.tsx` (one stale comment fix, see below), `scripts/ralph/prd.json` (US-045 → `passes: true`).
- **Pixel-preservation details (the story's own "troca o involucro, nunca o conteudo" rule):**
  - `DialogContent`'s default classes are `gap-6 rounded-4xl bg-popover p-6 ring-1 ring-foreground/5` — the original modal had no gap between its two inner divs (spacing came entirely from their own padding: `p-6 pb-5` then `px-6 pb-6`, no margin between), no ring, and padding lived on the inner divs, not the outer wrapper. Overrode with `gap-0 ... p-0 ... ring-0` (same override trio `TitleModal.tsx` already uses for the same reason), on top of the usual `rounded-3xl`/`bg-card`/`shadow-[var(--shadow-elevation-10)]` swap-in. Without `gap-0` specifically, the two child divs would've gained an extra 24px vertical gap that never existed in the original layout — easy to miss since `RatingRequestDialog.tsx` (the *other* precedent) does NOT override gap/ring and just accepts the default look for its 2-child layout; the right precedent to follow depends on whether the original component depended on padding-only spacing (this one did) or not.
  - `showCloseButton={false}` — the original modal never had a close (X) button, only a Cancelar `Button` + click-outside + Escape. `DialogContent`'s default renders one; passing `false` keeps content byte-identical (matches `TitleModal.tsx`/`RatingRequestDialog.tsx`, both also `showCloseButton={false}`).
  - The visible `<h2>` became `DialogTitle` (verified `node_modules/@base-ui/react/dialog/title/DialogTitle.d.ts` — base-ui's `Dialog.Title` renders `<h2>` by default, so the tag doesn't change) and the visible warning `<p>` became `DialogDescription` (also renders `<p>` by default) — both needed anyway since `Dialog.Popup` expects an accessible name/description, and both patterns (visible title via `DialogTitle`, not a `sr-only` one + separate plain `<p>`) already exist in `RatingRequestDialog.tsx`, so this isn't a new deviation. `cn()`'s `twMerge` drops `DialogTitle`'s default `font-heading`/`text-base`/`font-medium` in favor of the passed classes (conflicting utilities), but silently *keeps* the default's non-conflicting `leading-none` since the original `<h2>` didn't specify a line-height class — same trade `RatingRequestDialog.tsx` already accepted for its own visible `DialogTitle`, not a new risk introduced here.
- **Deliberate, in-scope wrapper change (this IS the involucro the story asks to swap, not a leftover to flag):** the backdrop went from a local `bg-backdrop/72 backdrop-blur-md` (a project color token, `--backdrop: rgb(8,7,11)`) to `DialogOverlay`'s hardcoded default `bg-black/80 ... backdrop-blur-xs` — `dialog.tsx`'s `DialogContent` renders `<DialogOverlay />` internally with no className passthrough from the caller, so every existing Dialog-based modal (`TitleModal`, `RatingRequestDialog`, `ComparisonDialog`) already renders this same default backdrop, not the app's `--backdrop` token. Not a regression unique to this migration — matches what every prior Dialog consumer already accepts.
- **Bug-fix disclosure the AC explicitly asks for:** the modal already closed on Escape before this story (the removed `useEffect` had a manual `keydown` listener) — the primitive's built-in Escape handling is a **mechanism swap, not new functionality**, so nothing here counts as "didn't close before, now does" per the AC's own carve-out.
- **Null-safety pattern reused from `RatingRequestDialog.tsx`:** `DeleteTrackDialogContent`'s `track` prop widened from `MediaTrackResponse` to `MediaTrackResponse | null` (mirroring `RatingRequestForm`'s `mediaTrackId: string | null`), with `handleConfirm` guarded by `if (!track) return`. Reason: with the old `if (!track) return null` at the top of the outer component, the *entire* dialog (including backdrop) vanished instantly the moment `track` went `null` — no closing transition. With the `Dialog` primitive, `open={track !== null}` triggers `DialogContent`'s built-in `data-closed:animate-out`/`fade-out-0`/`zoom-out-95` transition, during which the popup stays mounted for the animation's duration while `track` is already `null`; conditionally unmounting the content (`{track && <DeleteTrackDialogContent .../>}`) would've shown an empty box mid-close. Keeping the content mounted with a nullable `track` (same idiom `RatingRequestDialog`/`RatingRequestForm` already use) avoids that. `DeleteTrackDialogContent` doesn't render any track-derived text anyway (only uses `track.id` in the delete call and `onSuccess(track)`), so widening the type had zero visible-content impact — text/buttons/order are all static JSX.
- **Small drive-by fix:** `screens/account/CoupleSection.tsx`'s `DissolveDialog` had a comment claiming it followed "o mesmo padrao estrutural do `DeleteTrackDialog`" (hand-rolled backdrop) — now stale since this story moved `DeleteTrackDialog` onto the `Dialog` primitive. Updated the comment to note `DeleteTrackDialog` moved (US-045) and that `DissolveDialog` itself is still hand-rolled pending its own story (US-051, per the PRD's own next-story list). `DissolveDialog`'s actual code/behavior was NOT touched — comment only.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green — no dedicated `DeleteTrackDialog.test.tsx` existed before or after) / `bun run build` all pass from `client/`. `bun run check:colors` / `bun run check:radius` both pass (no new raw color/radius introduced — every className is either an existing token utility or plain Tailwind spacing/layout).
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (leitura completa do diff + typecheck/lint/test/build + check:colors/check:radius). Atencao extra do gate humano: confirmar que o backdrop (cor/blur, mudanca deliberada) e o espacamento interno (gap-0/ring-0/p-0 neutralizando os defaults do primitivo) realmente batem visualmente com o antigo.
- **Learnings for future iterations:**
  - **Two established Dialog-primitive precedents, not one — pick based on the original component's layout, not by default:** `TitleModal.tsx` overrides `gap-0`/`ring-0`/`p-0` on `DialogContent` because its original hand-rolled layout depended purely on inner-element padding for spacing (no implicit gap). `RatingRequestDialog.tsx` does NOT override those, and just accepts `DialogContent`'s own `gap-6`/`ring-1 ring-foreground/5`/`p-6` defaults for its 2-child (`DialogHeader` + form) layout. When converting the next hand-rolled modal (US-046 `WatchModal.tsx` onward), check whether the *original* backdrop-child div relied on padding-only spacing between sections (→ follow `TitleModal`'s override trio) or would naturally read fine with the primitive's own `gap-6`/`ring-1`/`p-6` (→ follow `RatingRequestDialog`'s hands-off approach) — don't assume one pattern is "the" pattern.
  - **`DialogOverlay`'s backdrop styling is NOT customizable by the caller** — `dialog.tsx`'s `DialogContent` hardcodes `<DialogOverlay />` with zero props/className passthrough. Every hand-rolled-to-primitive conversion in this Epico (this story, and presumably US-046 through US-052) will silently swap whatever custom backdrop color/blur the original had (here: `bg-backdrop/72 backdrop-blur-md`, the app's own `--backdrop` token) for the primitive's hardcoded `bg-black/80 backdrop-blur-xs`. This is a real, deliberate pixel change but is *in scope* per the story's own "troca o involucro" framing — don't try to "fix" it by adding overlay-className plumbing to `dialog.tsx` itself unless a future story explicitly asks for that (would touch `components/ui/`, out of scope for a per-file consolidation story).
  - **Nullable-prop-during-close-animation is a recurring shape** for any hand-rolled modal keyed off `X | null` (this file's `track`, `RatingRequestDialog`'s `mediaTrackId`) being converted to the `Dialog` primitive: don't gate the inner content component's *mount* on the value being non-null (`{value && <Content .../>}`) — that unmounts abruptly mid-close-animation. Instead widen the content component's own prop to `X | null` and guard only the specific handler(s) that dereference it (`if (!value) return`), exactly as `RatingRequestForm.handleConfirm` already does. Check for this shape in every US-046+ story: `WatchModal`/`ReviewModal`/`MatchCelebrationModal`/`MediaDetailModal`/`PendingDetailModal` all likely key off a similar `X | null` prop rather than a plain `open: boolean`.
  - Confirming a base-ui primitive's default rendered HTML element (`DialogTitle` → `<h2>`, `DialogDescription` → `<p>`) before assuming a content-preserving swap: check `node_modules/@base-ui/react/<primitive>/<part>/<Part>.d.ts` — the JSDoc comment states the default element directly (e.g. `"Renders an \`<h2>\` element."`), no need to dig into the compiled JS.
  - Next priority story is US-046 (`WatchModal.tsx`) — same family of hand-rolled `fixed inset-0` modals per `client/CLAUDE.md`'s "Modal patterns" section (`MediaDetailModal`, `PendingDetailModal`, `ReviewModal`, `WatchModal`, `DeleteTrackDialog` were listed together as the hand-rolled group before this story). Check its prop shape (`open: boolean`? or an `X | null` gate like this one?) before assuming which precedent applies.
---

## 2026-08-15 - US-046
- Implemented: `client/src/components/WatchModal.tsx` now uses the `Dialog` primitive (`components/ui/dialog.tsx`) instead of a hand-rolled `fixed inset-0` backdrop, matching the pattern already established by `TitleModal.tsx`/`RatingRequestDialog.tsx` (`showCloseButton={false}`, `DialogTitle`/`DialogDescription` both `sr-only` since the visible heading is plain text, own close `Button` in the header row, `open`/`onOpenChange` driven by `track !== null`).
- Files changed: `client/src/components/WatchModal.tsx`, `scripts/ralph/prd.json` (US-046 → `passes: true`).
- The manual `useEffect` Escape-key listener was deleted — the primitive already closes on Escape/click-outside with focus trapping built in, so there was no bug to fix here (the hand-rolled version already closed on Escape too); this was pure duplicate-code removal, not a behavior change worth flagging as a bug fix per the story's own AC bullet about that.
- Mechanical consequence of moving from an early-return (`if (!track) return null`) to a primitive-controlled `open` prop: `WatchModalContent` now takes `trackId: string | null` (was `track: MediaTrackResponse`, only ever used for `.id`) instead of the full track object, with a `if (!trackId) return` guard inside `handleConfirm` — same shape as `RatingRequestDialog.tsx`'s `mediaTrackId: string | null` prop. Confirmed by reading the full diff that this is the *only* change outside the wrapper — no JSX structure, text, field, button, or ordering changed inside the content.
- Visual consequence (expected, not a bug): the backdrop went from `bg-backdrop/72 p-5 backdrop-blur-md` (custom) to `DialogOverlay`'s hardcoded `bg-black/80 backdrop-blur-xs` — `DialogContent` has no backdrop className passthrough (documented in `client/CLAUDE.md`'s Modal patterns section), so every `Dialog`-based modal shares the same overlay. This is the unavoidable cost of consolidating onto the primitive, not something to work around.
- Verification: `bun run typecheck`/`bun run lint`/`bun run test:run` (119 tests, 14 files)/`bun run build`/`node scripts/check-color-literals.mjs`/`node scripts/check-radius-literals.mjs` all pass from `client/`. No dedicated `WatchModal.test.tsx` exists; only consumer is `screens/hub/HubScreen.tsx`, whose own props (`track`/`onClose`/`onSuccess`) are unchanged.
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (leitura completa do diff + typecheck/lint/test/build + gates de cor/raio).
- **Learnings for future iterations:**
  - Next priority story is US-047 (`ReviewModal.tsx`) — check whether it also has a redundant manual Escape-key `useEffect` to delete (same pattern found here) and whether its content-prop shape needs the same `fullObject -> id-only` narrowing this story did for `WatchModalContent`.
  - The remaining hand-rolled-backdrop group per `client/CLAUDE.md`'s Modal patterns section is now `MediaDetailModal`, `PendingDetailModal`, `ReviewModal` (US-046's `WatchModal` just left it) — update that CLAUDE.md list when each subsequent story lands, it's stale the moment a file moves camps.
  - When a hand-rolled modal's content component takes the full domain object but only reads `.id` from it, narrowing the prop to just the id (rather than keeping the full type with a null-check) is the cleaner mechanical translation to the primitive's `open`-driven mount lifecycle — mirrors `RatingRequestDialog.tsx`'s existing `mediaTrackId: string | null` shape instead of inventing a new convention.
---

## 2026-08-15 - US-047
- Implemented: `client/src/components/ReviewModal.tsx` migrated from a hand-rolled `fixed inset-0` backdrop `<div>` (with its own `onClick`-outside handler and a manual `document.addEventListener("keydown", ...)` for Escape) to the `Dialog` primitive (`client/src/components/ui/dialog.tsx`), following the exact pattern already established by `WatchModal.tsx` (US-046): `Dialog open={track !== null} onOpenChange={...}` wrapping a `DialogContent` with `showCloseButton={false}` and the same width/height/border/shadow className (`w-[calc(100vw-32px)] max-w-[calc(100vw-32px)] max-h-[90svh] gap-0 overflow-y-auto rounded-3xl border border-white/10 bg-card p-0 text-foreground shadow-[var(--shadow-elevation-10)] ring-0 sm:w-full sm:max-w-[420px]`), plus `sr-only` `DialogTitle`/`DialogDescription` mirroring the visible `h2`/`p` inside `ReviewModalContent` for an accessible name/description. The manual Escape listener was deleted — the primitive already closes on Escape and outside-click natively, and the old listener was functionally redundant (not a bug fix, since it already worked; noted here per the AC's own instruction to flag any close-behavior change, even though this one isn't a behavior change).
- **Files changed:** `client/src/components/ReviewModal.tsx` (wrapper only — the entire inner header/stars/opinion-textarea/error-block/actions JSX of `ReviewModalContent` is untouched, confirmed by reading the full diff: only the outer `<div onClick={onClose} className="fixed inset-0...">` / `<div onClick={stopPropagation}>` pair was replaced by `<Dialog>`/`<DialogContent>`), `scripts/ralph/prd.json` (US-047 → `passes: true`).
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`.
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (leitura completa do diff + typecheck/lint/test/build).
- **Learnings for future iterations:**
  - `ReviewModal.tsx` and `WatchModal.tsx` were near-identical twins even before this migration (both open a rating+opinion form on `MediaTrackResponse`, same stars/textarea/error/actions markup) — the Dialog-wrapper diff here is line-for-line the same shape as US-046's, just with `ReviewModal`'s own title ("Reavaliar") and a conditional-render (`{track && <ReviewModalContent .../>}`) instead of the destructured-optional-props style WatchModal used, because `ReviewModalContent` here takes a non-nullable `track: MediaTrackResponse` prop (it reads `track.reviews`/`track.id` directly) rather than a nullable `trackId: string | null`.
  - Next priority story: check `prd.json` for the next `passes: false` after US-047 — if this was the last modal in the `fixed inset-0` migration group (`client/CLAUDE.md`'s "Modal patterns" section lists `MediaDetailModal`/`PendingDetailModal`/`ReviewModal`/`WatchModal` as the hand-rolled camp; `ReviewModal`/`WatchModal` are now both migrated), `client/CLAUDE.md`'s Modal patterns section should be updated to drop them from the hand-rolled list once the whole group is done.
---

## 2026-08-15 - US-048
- Implemented: `client/src/components/MatchCelebrationModal.tsx` migrated from a hand-rolled `fixed inset-0` backdrop `<div>` (click-outside handler via `onClick={closeMatch}` on the backdrop + `stopPropagation` on the inner card, no Escape handling at all) to the `Dialog` primitive (`client/src/components/ui/dialog.tsx`), following the same shape as `WatchModal.tsx`/`ReviewModal.tsx` (US-046/US-047): `Dialog open={matchOpen && matchData !== null} onOpenChange={...}` wrapping a `DialogContent` with `showCloseButton={false}`. `className` preserves the original visuals (`bg-gradient-to-b from-[#251e0c] to-card`, `border-series/35`, `rounded-3xl`, `shadow-[var(--shadow-elevation-10)]`, `p-9 text-center`) plus the overrides needed because `DialogContent`'s defaults don't fit this layout: `gap-0` (original spacing was all manual margins — `my-3.5`/`mb-2.5`/`mt-4.5 mb-6` — not an implicit grid gap between children) and `ring-0` (no ring in the original). Added `sr-only` `DialogTitle`/`DialogDescription` mirroring the visible `div`("É um Match!")/`p` for an accessible name+description, same pattern as `WatchModal`/`TitleModal`.
- **New wrinkle vs. US-046/047, worth flagging for future stories:** `MatchCelebrationModal` is mounted globally (not opened from a specific screen) and its original backdrop had `z-[70]` — one tier above `screens/account/DeleteAccountSection.tsx`/`CoupleSection.tsx`'s still-hand-rolled dialogs (`z-[60]`, both still `passes: false`, priority 51/52). `DialogContent`'s default is `z-50`. Without an explicit `z-[70]` in the className override, converting this file would have silently made the match-celebration dialog render *behind* those two account dialogs if a user happened to have one open when a match event arrived (a real, if rare, regression — not just a cosmetic delta like the backdrop-color/blur change every prior Dialog conversion already accepted). Added `z-[70]` to `DialogContent`'s className (tailwind-merge correctly resolves it over the base `z-50`) to preserve the original stacking. `DialogOverlay` (the backdrop layer) itself stays at the primitive's hardcoded `z-50` — can't be overridden (no className passthrough), same known limitation documented in US-045's notes for backdrop color/blur.
- **`matchData?.title` (was `matchData.title`):** `useMatchStore.closeMatch()` sets `matchOpen: false` and `matchData: null` in the *same* `set()` call (confirmed by reading `useMatchStore.ts`), so on close the Dialog's `open` prop and the `matchData` value used for the title both change in the same render — the primitive doesn't guarantee the old `matchData` survives into the close-animation frame. Rather than caching the last non-null value (which the codebase has no established pattern for — the two `useRef`-synced-via-`useEffect` examples found, `LoadMoreSentinel.tsx`/`HubScreen.tsx`, only read their ref from an event-callback/handler, never from render/JSX, so reusing that shape here would need reading `ref.current` in JSX, which `react-hooks/refs` forbids), used the **same accepted shortcut already in production**: `RatingRequestDialog.tsx`'s only caller (`NotificationDropdown.tsx`) passes `title={ratingRequest?.title ?? ""}`, i.e. the visible title text is allowed to go blank for the brief close-animation frame rather than staying populated. Not a new bug, matching existing precedent, not something this story invented.
- **No close-behavior regression, one close-behavior addition (per AC, must be flagged, not a bug fix per se):** the original never listened for Escape at all (only click-outside via the backdrop's own `onClick`) — the `Dialog` primitive adds Escape-to-close as new, previously-absent behavior. Registering this per the AC's own instruction, even though "the modal didn't close with Escape and now does" reads as an added feature rather than a fix for something that was actively broken.
- **Files changed:** `client/src/components/MatchCelebrationModal.tsx` (full rewrite of the outer wrapper only — inner content JSX text/structure/button order unchanged, confirmed by diff), `client/CLAUDE.md` (Modal patterns section: dropped `MatchCelebrationModal`/`ReviewModal`/`WatchModal` from the hand-rolled list — now just `MediaDetailModal`/`PendingDetailModal` remain — and added the new z-index-override note above), `scripts/ralph/prd.json` (US-048 → `passes: true`).
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green — no existing test file for this component) / `bun run build` / `bun run check:colors` / `bun run check:radius` all pass from `client/`.
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (leitura completa do diff + typecheck/lint/test/build/check:colors/check:radius). Atencao extra do gate humano: confirmar que o modal de celebracao ainda aparece por cima de qualquer outro dialogo aberto (o `z-[70]` foi preservado manualmente, mas o backdrop em si caiu para o `z-50` padrao do primitivo, sem equivalente ao `z-[70]` anterior).
- **Learnings for future iterations:**
  - **Globally-mounted modals (not opened from a specific screen) can carry a non-default `z-` that a per-modal `Dialog` conversion must preserve explicitly** — check the original backdrop's `z-[N]` against every other modal's `z-` in the app (`grep -rn "z-\[6\|z-\[7\|z-\[5"` is a quick way to spot the hand-rolled ones) before assuming the primitive's default `z-50` is a safe drop-in. This matters most for a story converting one file at a time while sibling hand-rolled modals with a *higher* z-index are still pending — the stacking order between "already migrated" and "not yet migrated" modals can silently invert mid-migration if no one checks.
  - Confirmed (again, third time after US-046/047): a nullable-store-value modal (`X | null` gating `open`) whose value is nulled in the *same* action as the close, and whose nulled value is read directly in visible JSX (not just inside a submit handler like `WatchModalContent`/`RatingRequestForm`), doesn't have a clean fix in this codebase yet — the accepted shortcut is `value?.field ?? fallback`, letting the text blank out for the last animation frame, matching `RatingRequestDialog`'s own already-shipped behavior. A real fix (cache last non-null value across the close animation) would need a new pattern not yet established anywhere in this repo — worth a dedicated story if it's ever judged worth fixing, not a one-off invention here.
  - `screens/account/DeleteAccountSection.tsx` (US-052) and `screens/account/CoupleSection.tsx`'s `DissolveDialog` (US-051) are now the **last two** hand-rolled-backdrop modals left in the whole app, both at `z-[60]` — whichever story converts them next should double check no *other* file assumes their current z-index for stacking (this story's own note above is the reason to check).
  - Next priority story: check `prd.json` for the next `passes: false` after US-048 (likely US-049, `MediaDetailModal.tsx`, per the PRD's own priority ordering) — `MediaDetailModal`/`PendingDetailModal` are next in the hand-rolled-backdrop group, both already have an established `detailsError`/`showSkeleton` three-way render (Epico 7, US-007) to preserve untouched inside the new `DialogContent` wrapper.
---

## 2026-08-16 - US-049
- Implemented: `client/src/components/MediaDetailModal.tsx` migrated from a hand-rolled `fixed inset-0` backdrop `<div>` (own `backdropRef`-based click-outside handler + a manual `window.addEventListener("keydown", ...)` for Escape) to the `Dialog` primitive (`client/src/components/ui/dialog.tsx`). Unlike `WatchModal.tsx`/`MatchCelebrationModal.tsx` (which stayed a single component, guarding with `trackId: string | null` or `matchData?.field ?? fallback`), this file's content reads `track.status`/`track.tmdbId`/`track.reviews`/`track.mediaType` pervasively for rendering (badge color, poster hue, glow, stats), so it followed `ReviewModal.tsx`'s split instead (US-047 precedent): a thin `MediaDetailModal` wrapper owning `Dialog`/`DialogContent` plus static `sr-only` `DialogTitle`("Detalhes do título")/`DialogDescription`, and a `MediaDetailModalContent` taking a **non-nullable** `track: MediaTrackResponse` prop, rendered as `{track && <MediaDetailModalContent key={track.id} track={track} myUserId={myUserId} onClose={onClose} onStatusChange={onStatusChange} />}`.
- `DialogContent`'s className carries the original outer div's classes verbatim (`font-auth-body relative w-[calc(100vw-32px)] max-w-[calc(100vw-32px)] overflow-hidden rounded-3xl border border-white/8 text-foreground sm:w-full sm:max-w-[900px]`, plus `gap-0`/`ring-0`/`p-0` overrides for `DialogContent`'s own `gap-6`/`ring-1 ring-foreground/5`/`p-6` defaults — this modal's spacing is fully manual, same override trio as `TitleModal.tsx`/`WatchModal.tsx`), and its `style` prop carries the original two-layer `background` (`radial-gradient(...), var(--card)`) plus `maxHeight: "90svh"`/`overflowY: "auto"` unchanged (the same "hex/CSS-value inside a non-className style object" pattern already established for color migration, just moved wholesale to the new host element rather than edited).
- The manual Escape-key `useEffect` and `backdropRef`/click-outside handler were deleted — the primitive already closes on Escape and outside-click natively, and **this is not a bug fix**: the old manual listener already worked (Escape already closed the modal before this story), so nothing in the close-behavior actually changed for the user, just the mechanism. Confirmed by reading the pre-migration file: the removed `useEffect` unconditionally called `onClose()` on Escape whenever `track` was truthy, identical to `Dialog`'s own behavior.
- **Files changed:** `client/src/components/MediaDetailModal.tsx` (full rewrite of the outer wrapper + the split into `MediaDetailModalContent` — every visible header/poster/stats/genres/providers/overview/individual-ratings/CTA JSX block copied verbatim, just re-indented one level up now that the two hand-rolled backdrop `<div>`s are gone, confirmed by side-by-side comparison against the pre-migration version), `client/CLAUDE.md` (Modal patterns section: dropped `MediaDetailModal` from the hand-rolled list — now only `PendingDetailModal` remains — and documented the `ReviewModalContent`-style split as the applicable precedent for it, since its content is expected to share the same "track fields read pervasively" shape per US-007's own note that both files were born from the same Epico 7 pattern), `scripts/ralph/prd.json` (US-049 → `passes: true`).
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` and `node scripts/check-radius-literals.mjs` both report "ok" (no raw color/radius regression introduced by the rewrite — every arbitrary hex/radius that already existed in this file, e.g. `bg-white/[0.08]`/`rounded-xl`, was preserved verbatim, none of the gate's flagged patterns were touched). Confirmed `grep -n "fixed inset-0"` on the file returns nothing.
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (leitura completa do diff + typecheck/lint/test/build + gates de cor/raio).
- **Learnings for future iterations:**
  - **Two established "how to handle a nullable-track content" precedents now, not just one — pick based on how much the content actually reads `track` fields for rendering:** (1) `WatchModal.tsx`'s shape (a content component whose prop is *itself* nullable, `trackId: string | null`, guarding only the one handler that dereferences it) works when the content's JSX doesn't actually need any `track`-derived value to render (a generic star-rating/opinion form). (2) `ReviewModal.tsx`/now `MediaDetailModal.tsx`'s shape (content component takes a **non-nullable** `track` prop, parent conditionally renders `{track && <Content key={track.id} track={track} .../>}`) is required whenever the content's JSX reads multiple `track` fields directly for visual output (badges, computed hues, filtered review lists) — trying to thread `track: X | null` through all of that would mean sprinkling `track?.` everywhere and picking arbitrary fallback values for a dozen expressions, which is worse than just accepting the same "content unmounts on close, cutting the exit animation short for this modal specifically" trade-off `ReviewModal.tsx` already accepted. Don't force option (1) onto a data-heavy detail modal — check which shape actually fits before implementing.
  - `PendingDetailModal.tsx` (next `passes: false` story after this one, US-050) is documented in `client/CLAUDE.md`'s Loading-state pattern section as sharing the exact same `detailsError`/`fetchDetails(current, isCancelled)`/`showSkeleton` three-way-render shape as `MediaDetailModal.tsx` (both born in Epico 7 US-007) — expect the identical `ReviewModalContent`-style split (non-nullable `track` prop, `{track && <PendingDetailModalContent key={track.id} .../>}`) to apply there too, not `WatchModal`'s simpler shape.
  - The two-layer `background` CSS value (`"radial-gradient(...), var(--card)"`) combined with `maxHeight`/`overflowY` as a `style` object, rather than pure Tailwind classes, transfers to `DialogContent` without any change — `DialogContent`'s own prop spread (`{...props}`) passes an author-supplied `style` prop straight through to the underlying `Popup`, same as any plain `<div>`. No special handling needed when a hand-rolled modal's background was already a `style` object rather than a `bg-*`/arbitrary-value className.
  - Next priority story is US-050 (`PendingDetailModal.tsx`) — the last hand-rolled `fixed inset-0` file in the group; once it lands, `client/CLAUDE.md`'s Modal patterns section's hand-rolled bullet becomes obsolete and should be either deleted or rewritten to state the migration is fully complete.
---

## 2026-08-16 - US-050
- Implemented: `client/src/components/PendingDetailModal.tsx` migrated from a hand-rolled `fixed inset-0` backdrop `<div>` (own `backdropRef`-based click-outside handler + a manual `window.addEventListener("keydown", ...)` for Escape) to the `Dialog` primitive (`client/src/components/ui/dialog.tsx`), following the exact `MediaDetailModal.tsx` (US-049) precedent this file was flagged to expect: a thin `PendingDetailModal` wrapper owning `Dialog`/`DialogContent` plus static `sr-only` `DialogTitle`("Detalhes da sugestão")/`DialogDescription`, and a `PendingDetailModalContent` taking a **non-nullable** `item: PendingMatch` prop, rendered as `{item && <PendingDetailModalContent key={item.tmdbId} item={item} .../>}` — the `key` matches how the screen already dedupes pending items (`SuggestionsTab.tsx`'s `removePending(current.tmdbId)`), same identifier reused for remount-on-change. This shape was required (not `WatchModal`'s simpler `X | null`-prop shape) because the content reads `item.tmdbId`/`item.mediaType`/`item.title`/`item.posterUrl` directly for rendering (header title/type, poster hue/fallback, hero image) in addition to the `details` fetch state.
- `DialogContent`'s className/style carry the original outer div's classes and two-layer `background` (`radial-gradient(...), var(--card)`) verbatim, plus `gap-0`/`ring-0`/`p-0` overrides for `DialogContent`'s own `gap-6`/`ring-1 ring-foreground/5`/`p-6` defaults — same override trio as `MediaDetailModal.tsx`/`TitleModal.tsx`/`WatchModal.tsx`, since this modal's spacing is fully manual (padding-only, no implicit gap).
- The manual Escape-key `useEffect` and `backdropRef`/click-outside handler were deleted — the primitive already closes on Escape/outside-click natively, and **this is not a bug fix**: the old manual listener already called `onClose()` unconditionally on Escape whenever `item` was truthy, identical to `Dialog`'s own behavior, so nothing in the close-behavior actually changed for the user, just the mechanism (same disclosure already made for `MediaDetailModal.tsx` in US-049).
- **Files changed:** `client/src/components/PendingDetailModal.tsx` (full rewrite of the outer wrapper + split into `PendingDetailModalContent` — every visible header/poster/stats/genres/providers/overview/action-buttons JSX block copied verbatim, just re-indented one level up now that the two hand-rolled backdrop `<div>`s are gone, confirmed by side-by-side comparison against the pre-migration version), `client/CLAUDE.md` (Modal patterns section: the hand-rolled-backdrop bullet rewritten — every modal/dialog file in the original migration group, `DeleteTrackDialog`/`WatchModal`/`ReviewModal`/`MatchCelebrationModal`/`MediaDetailModal`/`PendingDetailModal`, is now on the `Dialog` primitive; only `screens/account/DeleteAccountSection.tsx`/`CoupleSection.tsx`'s `DissolveDialog` remain hand-rolled, US-051/US-052, both still `passes: false`), `scripts/ralph/prd.json` (US-050 → `passes: true`).
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green — no dedicated `PendingDetailModal.test.tsx` existed before or after) / `bun run build` all pass from `client/`. `node scripts/check-color-literals.mjs` and `node scripts/check-radius-literals.mjs` both report "ok" (no raw color/radius regression — every arbitrary hex/radius already in this file, e.g. `bg-white/[0.08]`/`rounded-xl`, preserved verbatim). Confirmed `grep -n "fixed inset-0"` on the file returns nothing.
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (leitura completa do diff + typecheck/lint/test/build + gates de cor/raio).
- **Learnings for future iterations:**
  - This closes the Epico 13 "hand-rolled `fixed inset-0` modal" migration group (US-045 through US-050) — every modal/dialog *component* (as opposed to inline account-screen dialogs) is now on the `Dialog` primitive. The two remaining hand-rolled backdrops (`DeleteAccountSection.tsx`, `CoupleSection.tsx`'s `DissolveDialog`) are a separate, smaller group (US-051/US-052) living inside `screens/account/` rather than `components/` — check their own z-index (both `z-[60]`, per US-048's note) against `MatchCelebrationModal`'s now-migrated `z-[70]` before converting, since a stacking-order assumption could invert once the last hand-rolled `screens/account/` dialog moves too.
  - Confirmed a third time (after `MediaDetailModal.tsx`/US-049 and `ReviewModal.tsx`/US-047) that "does the content read multiple prop fields directly in JSX for rendering, not just an id passed to a handler" is the deciding question between the two established nullable-content shapes (`WatchModal`'s nullable-prop-on-content vs. `ReviewModal`/`MediaDetailModal`/now `PendingDetailModal`'s non-nullable-prop-plus-conditional-render) — worth checking this question first for any future hand-rolled-modal conversion outside this epico's list.
  - Next priority story is US-051 (`CoupleSection.tsx`'s `DissolveDialog`) — per US-045's note, its stale comment already points at `DeleteTrackDialog` as the precedent it should follow once its own story lands; that comment can be updated/removed as part of US-051's own diff.
---

## 2026-08-16 - US-051
- Implemented: `client/src/screens/account/CoupleSection.tsx`'s `DissolveDialog` migrated from a hand-rolled `fixed inset-0` backdrop to the `Dialog` primitive (`components/ui/dialog.tsx`), same pattern as `DeleteTrackDialog.tsx`/`MatchCelebrationModal.tsx` (Epico 13, US-045 through US-050).
- Files changed: `client/src/screens/account/CoupleSection.tsx`, `client/CLAUDE.md` (updated the "Modal patterns" note to reflect US-051 as done and record the z-index/mount-pattern decisions below).
- Pure refactor per root CLAUDE.md's exception clause (content stays pixel-identical) — `frontend-design` skill was NOT invoked, matches the story's own AC ("esta story troca o INVOLUCRO, nunca o conteudo").
- **Learnings for future iterations:**
  - Chose the "always mounted, `open={confirming}`" pattern (same as `MatchCelebrationModal.tsx`) over the wrapper+`*Content` split used by `MediaDetailModal.tsx`/`PendingDetailModal.tsx`, because `DissolveDialog`'s content only reads plain non-nullable props (`partnerName: string`, `bondLabel: string | null`) — the split is only needed when content reads a nullable object's fields pervasively.
  - The old backdrop had `z-[60]`; `DialogContent` defaults to `z-50`. Confirmed this is safe to drop: the only modal that cared about stacking above `screens/account/`'s hand-rolled dialogs was `MatchCelebrationModal.tsx` (US-048), which already has its own explicit `z-[70]` override independent of what `CoupleSection` uses. `screens/account/DeleteAccountSection.tsx` (US-052, still `passes: false`) still has its own hand-rolled `z-[60]` backdrop, so this is not yet a fully closed question until that story lands too — but nothing currently depends on `CoupleSection`'s dialog specifically outranking it.
  - Removed the manual `useEffect` + `document.addEventListener("keydown", ...)` Escape handler and the manual `role="dialog"`/`aria-modal`/`aria-labelledby` attributes — the primitive supplies all of this. Escape-to-close already worked before this story (via the manual listener), so this is NOT a bug fix, just removal of now-redundant hand-rolled code — recorded per the story's AC that says to call out explicitly if this ever flips from "didn't close" to "closes".
  - Verification: `bun run typecheck`/`lint`/`test:run` (119 tests)/`build` all pass from `client/`. Visual verification in browser: **PENDENTE DE GATE HUMANO** (no browser in this sandbox, decision D19) — not marked as visually verified, only verified statically (full diff read + typecheck/lint/test/build).
  - Next priority story is US-052 (same migration for `screens/account/DeleteAccountSection.tsx`) — same pattern applies; check whether its dialog content reads any nullable object before picking the mount-pattern (plain-props vs wrapper+`*Content` split).
---

## 2026-08-16 - US-052
- Implemented: migrated `client/src/screens/account/DeleteAccountSection.tsx`'s `DeleteDialog` off its hand-rolled `fixed inset-0` backdrop onto the `Dialog` primitive (`client/src/components/ui/dialog.tsx`) — same pattern as `TitleModal.tsx`/`RatingRequestDialog.tsx`, confirmed against both before implementing. Outer backdrop `<div onClick=.../>` + inner `role="dialog"` div replaced with `<Dialog open onOpenChange={handleOpenChange}><DialogContent showCloseButton={false} className="...gap-0 p-0 ring-0...">` (same visual classes as before — `rounded-3xl border-coral/28 bg-card shadow-[var(--shadow-elevation-10)]`, `sm:max-w-[560px]` — just moved from the inner div onto `DialogContent`, with `gap-0`/`p-0`/`ring-0` added to cancel the primitive's defaults per the `client/CLAUDE.md` "Modal patterns" note). The manual `useEffect` Escape-key listener was removed (the primitive handles Escape/click-outside/focus-trap natively); the `h2#conta-excluir-dialogo-titulo` + `aria-labelledby` pair became `<DialogTitle>` (the primitive wires the labelling itself, no manual `id`/`aria-labelledby` needed). The description `<p>` right below the title was left as a plain `<p>` (not `DialogDescription`) since the original never tied it via `aria-describedby` either — no behavior change there.
- **Bug fix, per the AC's own instruction to register it explicitly rather than treat it as a scope violation:** the old Escape handler had a `!done` guard (`if (event.key === "Escape" && !done) onClose()`), and the backdrop's `onClick` had the same guard (`onClick={done ? undefined : onClose}`) — both meant the dialog could NOT be dismissed at all (Escape or click-outside) while `done` was true (mid-farewell, before the `setTimeout` redirect fires). The new `handleOpenChange` replicates the same guard (`if (!nextOpen && !done) onClose()`), so this is not a behavior change, just confirming the primitive's native Escape/click-outside triggers `onOpenChange(false)` the same way the old handlers did and the guard still applies correctly.
- **Mounting-shape decision (see `client/CLAUDE.md`'s "Modal patterns" section, updated in this commit):** kept the existing **conditional** parent-mount (`{confirming ? <DeleteDialog .../> : null}` in `DeleteAccountSection()`, unchanged) with `Dialog` given a hardcoded `open` prop inside `DeleteDialog`, rather than switching to the "always mounted, `open={confirming}` boolean" shape `CoupleSection.tsx`'s `DissolveDialog` (US-051) uses. Reason: `DeleteDialog` holds real form state (`password`/`word`/`passwordError`/`formError`/`working`/`done`) that the original fully-unmounting behavior reset on every reopen; switching to always-mounted would leave stale field values/errors on a second open with no extra code to reset them. `DissolveDialog`'s simpler `working`/`error` state was never guaranteed to reset either (pre-existing, accepted quirk over there) — not something this story needed to fix or replicate.
- **Files changed:** `client/src/screens/account/DeleteAccountSection.tsx`, `client/CLAUDE.md` (Modal patterns section — this was the last remaining hand-rolled backdrop, now zero left in the app), `scripts/ralph/prd.json` (US-052 → `passes: true`).
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 tests, 14 files, all green) / `bun run build` all pass from `client/`. Confirmed via `grep` that no `fixed inset-0`/`role="dialog"`/`aria-modal`/manual Escape `useEffect` remain in the file. Content inside `DialogContent` (Inventory, Latch badges, password/word inputs, buttons, the `done` farewell block) copied verbatim — zero JSX structure/prop/handler/text changes below the header.
- Registrado conforme a AC: **verificacao visual em navegador fica PENDENTE DE GATE HUMANO** (sem navegador neste sandbox, decisao D19) — nao marcado como verificado visualmente, apenas verificado estaticamente (grep + typecheck/lint/test/build).
- **Learnings for future iterations:**
  - This was the **last** file in the hand-rolled-backdrop migration group (Epico 13, US-045→US-052) — `client/CLAUDE.md`'s "Modal patterns" section now says zero hand-rolled backdrops remain in the app. Next stories in this epic (US-053 onward) move to different design-system gaps (ErrorBoundary's Dialog usage, pill/badge primitive, dark-only cleanup, docs).
  - When a hand-rolled dialog's original unmount-on-close behavior is load-bearing for resetting local form state (multiple `useState` fields tied to a multi-step form), prefer keeping the parent's conditional mount over switching to the `open={boolean}` always-mounted shape, even though the latter is used elsewhere in this exact file family (`DissolveDialog`) — check what state the dialog owns before picking the shape, don't default to whichever pattern was used most recently.
  - US-053 ("Decidir se o ErrorBoundary deve mesmo usar Dialog") is a decision story, not a mechanical migration — `client/src/components/ErrorBoundary.tsx`'s own doc comment (`client/CLAUDE.md`, "Error boundary de topo" section) already explains why it CAN'T use the `Dialog` primitive (mounted outside `ThemeProvider`/`BrowserRouter`, only inline styles) — worth reading that section first before assuming this story wants a migration at all.
---

## 2026-08-16 - US-053
- Analise concluida: ErrorBoundary.tsx **FICA** hand-rolled, nao migra para o primitivo Dialog. Motivo tecnico (nao estetico): este e o unico dos 9 modais candidatos que renderiza precisamente quando a arvore React quebrou (`componentDidCatch`) — exatamente o momento em que menos se pode confiar em infraestrutura adicional de React (portal do base-ui, seu Context interno, seu proprio ciclo de montagem). Trocar por `Dialog` aumentaria a superficie de falha justo quando algo ja falhou, sem ganho real (a tela de erro ja e um card centralizado simples, nao precisa de foco-trap/escape-key/overlay-click do primitivo). Resultado LEGITIMO por AC propria da story.
- Implementado: comentario adicionado na classe `ErrorBoundary` (`client/src/components/ErrorBoundary.tsx`, acima da declaracao) explicando a excecao deliberada, para que ninguem "corrija" isso para Dialog numa limpeza futura sem contexto.
- **Files changed:** `client/src/components/ErrorBoundary.tsx` (comentario apenas — zero JSX/logica alterada), `scripts/ralph/prd.json` (US-053 → `passes: true`).
- **Verificacao dos ACs "em qualquer resultado":** `componentDidCatch` continua chamando `reportClientError` (POST `/api/client-errors`, US-003 Epico 12) — inalterado. O boundary continua montado em `main.tsx` por fora de `ThemeProvider`/`BrowserRouter` (comentario original preservado, so estendido) — inalterado. `ErrorBoundary.test.tsx` ja cobre renderizacao com erro SIMULADO (componente `Bomb` que lanca em todos os 4 testes), nao so caminho feliz — nenhum teste novo necessario, o AC "se MIGRAR, precisa de teste com erro simulado" nao se aplica porque a decisao foi FICAR.
- **Verification:** `bun run typecheck` / `bun run lint` / `bun run test:run` (119 testes, 14 arquivos, todos verdes) / `bun run build` passam, todos de `client/`.
- **Learnings for future iterations:**
  - Nem todo modal/dialog do app precisa consolidar no primitivo Dialog so porque a maioria consolidou (US-045 a US-052) — quando o componente e infraestrutura de fallback (error boundary, algo que roda fora do caminho normal de render), o motivo de ficar hand-rolled e estrutural, nao preguica. Documentar a excecao no proprio arquivo evita que uma limpeza futura "descubra" o desvio e tente uniformizar sem contexto.
  - Next priority story is US-054 (desenhar o(s) primitivo(s) que faltam para pill/badge) — comeca uma nova sequencia (US-054 a US-057+) sobre consolidar pills/badges manuais num primitivo, nao mais sobre Dialog.
---
