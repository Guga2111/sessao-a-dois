{
  "project": "Sessao a Dois",
  "branchName": "ralph/responsividade-mobile",
  "description": "Responsividade mobile completa: menu hamburguer no header, carrossel com paginação automática no Hub, e ajustes de layout em modais, telas de auth, Match e Dashboard.",
  "userStories": [
    {
      "id": "US-001",
      "title": "Hook useIsMobile + Header com menu hamburguer",
      "description": "As a mobile user, I want a hamburger menu instead of an inline nav bar so the header fits on small screens without overflowing.",
      "acceptanceCriteria": [
        "Criar `src/lib/useIsMobile.ts` — hook que retorna `boolean` usando `window.matchMedia('(max-width: 767px)')` com listener de mudança e inicialização síncrona (sem flash)",
        "Em telas < 768px, o `<nav>` inline do Header fica oculto (`hidden md:flex`) e um botão hamburguer (ícone `Menu` do lucide-react) aparece no canto direito",
        "Ao clicar no hamburguer, um dropdown absoluto exibe os 3 itens de nav ('Hub Principal', 'Match ♥', 'Dashboard') com o item ativo destacado em `#ffcb2b`",
        "Ao clicar em um item do menu, o menu fecha e a navegação ocorre normalmente",
        "Ao navegar para outra rota, o menu fecha automaticamente (useEffect observando a rota)",
        "Em telas >= 768px, o comportamento atual (nav inline) permanece idêntico",
        "O dropdown usa `z-index` acima do header (z-50 ou superior) para sobrepor o conteúdo",
        "Avatares do casal permanecem visíveis no header mobile; bloco de texto de nome/dias pode ser oculto em telas muito pequenas",
        "Typecheck passa (`tsc --noEmit`)",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 1,
      "passes": true,
      "notes": "Hamburger menu implemented in Header.tsx with MobileNav subcomponent; useIsMobile hook created but not consumed by Header (CSS-only responsive switch via hidden/md:flex covers all requirements)."
    },
    {
      "id": "US-002",
      "title": "Carrossel horizontal nas seções do Hub (mobile)",
      "description": "As a mobile user, I want to swipe horizontally through movie/series cards in each Hub section so I can browse comfortably on a small screen.",
      "acceptanceCriteria": [
        "Em telas < 768px, cada seção do HubScreen exibe os cards em um `<div>` com `display:flex`, `overflow-x:auto`, `scroll-snap-type: x mandatory` e `overscroll-behavior-x: contain`",
        "Cada card no carrossel tem `min-width: 72vw` e `max-width: 72vw` e `scroll-snap-align: start`, criando o efeito peek do próximo card (~28vw visível)",
        "O container tem `padding-right` de pelo menos `20vw` para que o próximo card apareça sem ser totalmente clicável",
        "A barra de scroll horizontal fica invisível (`scrollbar-width: none` / `::-webkit-scrollbar { display: none }` — adicionar ao `index.css` ou inline)",
        "Em telas >= 768px, o grid atual (`grid-cols-[repeat(auto-fill,minmax(250px,1fr))]`) permanece sem alteração",
        "O estado collapsible (recolher/expandir seção) funciona normalmente no mobile",
        "O skeleton de carregamento (`SkeletonCard`) aparece alinhado horizontalmente no mobile (flex) e em grid no desktop",
        "O estado vazio (sem títulos) continua exibido normalmente sem layout quebrado",
        "Typecheck passa",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 2,
      "passes": true,
      "notes": "Carousel implemented with a single set of Tailwind classes toggled via `md:` breakpoint (flex+snap+overflow-x-auto on mobile, grid on desktop) rather than a separate JS-driven layout — matches the acceptance criteria's CSS values exactly (72vw card width, 20vw right padding, hidden scrollbar via new `.no-scrollbar` utility in index.css)."
    },
    {
      "id": "US-003",
      "title": "Paginação automática no carrossel mobile",
      "description": "As a mobile user, I want the carousel to load the next 20 titles automatically when I reach the last card so I can keep browsing without tapping a button.",
      "acceptanceCriteria": [
        "Um elemento sentinela (`<div>` invisível com `width: 1px; flex-shrink: 0`) é renderizado como último filho do carrossel mobile, após o último card",
        "Um `IntersectionObserver` (threshold: 0.5, root: null) monitora o sentinela de cada seção; quando ele entra no viewport e `hasMore === true` e `!loadingMore`, `handleLoadMore` é chamado automaticamente",
        "O observer só é ativado no mobile — verificar via `useIsMobile()` hook (criado no US-001); no desktop, o observer não é registrado",
        "Um skeleton card extra é renderizado no final do carrossel enquanto `loadingMore === true`, dando feedback visual de carregamento",
        "Ao receber os novos cards, eles são concatenados ao state sem resetar a posição de scroll (já garantido pela lógica existente em `fetchSectionPage`)",
        "Se `items.length >= total`, o observer não dispara nova requisição (proteção contra loop infinito)",
        "O botão 'Carregar mais' só é renderizado em telas >= 768px (ocultar com classe `hidden md:flex` ou condicional via `useIsMobile()`)",
        "Typecheck passa",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 3,
      "passes": true,
      "notes": "Added LoadMoreSentinel component (IntersectionObserver, threshold 0.5) rendered as last flex child only when `useIsMobile()` is true and `hasMore`; flags read from a ref synced in an effect so the observer (created once per section, keyed by status) always sees fresh hasMore/loadingMore without re-registering. Extra SkeletonCard renders at carousel end while loadingMore (shared by both mobile auto-load and desktop button). 'Carregar mais' button now hidden on mobile via `hidden md:flex`. handleLoadMore made a stable useCallback (reads current page via a sectionsRef synced in an effect) so the sentinel's observer isn't torn down/recreated every render."
    },
    {
      "id": "US-004",
      "title": "Modais responsivos para mobile",
      "description": "As a mobile user, I want modals and dialogs to fill most of the screen so they are readable and scrollable on small devices.",
      "acceptanceCriteria": [
        "`TitleModal`: em mobile, o `DialogContent` ocupa `w-[calc(100vw-32px)] max-w-[calc(100vw-32px)] max-h-[90svh] overflow-y-auto`; em desktop mantém largura atual",
        "`MediaDetailModal`: mesma regra de largura/altura mobile acima; conteúdo interno tem scroll vertical se ultrapassar 90svh",
        "`ReviewModal`: mesma regra mobile; campos de formulário empilham verticalmente se necessário",
        "`WatchModal`: mesma regra mobile",
        "`DeleteTrackDialog`: mesma regra mobile (dialog de confirmação simples, verificar que botões não ficam cortados)",
        "`PendingDetailModal`: mesma regra mobile (incluído por precaução)",
        "Nenhum modal exibe scroll horizontal indesejado em viewport de 375px",
        "Typecheck passa",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 4,
      "passes": true,
      "notes": "Applied mobile width/height rules to all 6 modals. TitleModal (shadcn Dialog): DialogContent className changed to `w-[calc(100vw-32px)] max-w-[calc(100vw-32px)] max-h-[90svh]` on mobile, `sm:w-full sm:max-w-[520px]` on desktop, kept `overflow-visible` (not overflow-y-auto) on the outer element because the search-results dropdown is absolutely positioned within it and the existing inner body div already scrolls independently via `max-h-[calc(90vh-280px)] overflow-y-auto`. MediaDetailModal/PendingDetailModal (custom backdrop divs, no DialogContent): changed inner div from `w-full max-w-[900px]` to `w-[calc(100vw-32px)] max-w-[calc(100vw-32px)] sm:w-full sm:max-w-[900px]`, and switched inline style `maxHeight` from `90dvh` to `90svh` per acceptance criteria (kept existing `overflowY: auto`). ReviewModal/WatchModal/DeleteTrackDialog (custom, max-w-[420px]): same calc(100vw-32px) mobile width plus added `max-h-[90svh] overflow-y-auto` directly since they had no height cap before."
    },
    {
      "id": "US-005",
      "title": "HubScreen — padding e botão 'Adicionar Título' responsivos",
      "description": "As a mobile user, I want the Hub page header and add button to use appropriate spacing and sizing so the page layout is clean on small screens.",
      "acceptanceCriteria": [
        "O `<main>` do HubScreen tem padding lateral `px-4` em mobile e `sm:px-8` em telas maiores (atualmente só tem `px-5 sm:px-8` — verificar e ajustar se necessário)",
        "O botão 'Adicionar Título' no topo da página tem `w-full sm:w-auto` para ocupar largura total em telas muito pequenas",
        "O bloco de título/subtítulo (`h1`, `p`) não transborda horizontalmente em 375px",
        "O FAB fixo (botão ＋ no canto inferior direito) está posicionado corretamente no mobile (`right-4 sm:right-11`) sem sobrepor conteúdo importante",
        "Typecheck passa",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 5,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-006",
      "title": "MatchScreen responsiva",
      "description": "As a mobile user, I want the Match screen to fit my screen so I can swipe and interact with cards comfortably.",
      "acceptanceCriteria": [
        "Inspecionar `src/screens/MatchScreen.tsx` e identificar elementos com largura fixa ou padding excessivo que causam overflow em 375px",
        "Cards de swipe centalizados e com largura máxima de `calc(100vw - 32px)` em mobile",
        "Botões de ação ('Passar' e 'Curtir') com tamanho e espaçamento adequados para toque em mobile",
        "Nenhum scroll horizontal em viewport de 375px",
        "Typecheck passa",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 6,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-007",
      "title": "DashboardScreen responsiva",
      "description": "As a mobile user, I want the Dashboard to stack KPI cards and charts vertically so I can read statistics without horizontal scrolling.",
      "acceptanceCriteria": [
        "Inspecionar `src/screens/DashboardScreen.tsx` e identificar grids ou layouts que não colapsam em mobile",
        "Cards de KPI empilham em coluna única em telas < 768px (alterar grid para `grid-cols-1 sm:grid-cols-2 md:grid-cols-3` ou equivalente)",
        "Gráficos e seções de estatísticas ocupam 100% da largura disponível em mobile sem overflow",
        "Nenhum scroll horizontal em viewport de 375px",
        "Typecheck passa",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 7,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-008",
      "title": "Telas de autenticação responsivas",
      "description": "As a mobile user, I want the login, register, and join pages to display correctly on small screens so I can authenticate without layout issues.",
      "acceptanceCriteria": [
        "Inspecionar `src/routes/auth/AuthLayout.tsx`, `LoginPage.tsx`, `RegisterPage.tsx`, `JoinPage.tsx` e `InviteCodeTicket.tsx`",
        "Formulários centralizam corretamente e usam `w-full max-w-sm` ou similar em vez de larguras fixas que transbordam em 375px",
        "Padding/margin não causa overflow horizontal em nenhuma tela de auth",
        "`InviteCodeTicket` (ticket de convite) tem layout adaptado para mobile (verificar)",
        "Nenhum scroll horizontal em viewport de 375px",
        "Typecheck passa",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 8,
      "passes": false,
      "notes": ""
    }
  ]
}
